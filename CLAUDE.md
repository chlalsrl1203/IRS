## 버전 이력 참고사항 (2026-07-25 정리)

- v3.9까지는 검증된 원본 문서(investment_research_system_v3_9_integrated.docx
  부록 B)로부터 코드를 복원함. 실행 검증 완료(부록 C 로그와 출력 일치 확인).
- **v3.10, v3.11, v3.11.1, v3.12는 결번 처리**. 이 저장소 어디에도 해당 버전의
  코드/CHANGELOG/근거가 존재하지 않아 복원 불가능하다고 판단. 특히 v3.12는
  과거 세션에서 존재하지 않는 함수명(core_fcf_from_series 등)과 함께 언급된
  적이 있어 신뢰할 수 없는 버전으로 확인됨(가짜 버전 사건).
- v3.13부터는 실제 변경 내용이 확인되어 순서대로 반영. 이 파일 하단
  CHANGELOG에 각 버전 변경사항을 실제 코드 diff와 함께 기록할 것.
- v3.17, v3.18은 이 저장소에 **코드/CHANGELOG가 없다**. 다만 2026-07-25
  감사에서 Notion 트래커 노트에 두 버전의 **내용 단서**가 남아있음을 확인했다
  (v3.12 같은 완전한 가짜 버전과는 성격이 다름):
  - v3.17 = self_check_v2의 **음수 Implied Growth 파싱 버그** 수정
    (EVO 분석이 계기, ADBE에서 타종목 재확인)
  - v3.18 = 함수연쇄 엣지케이스 처리(FY25적자→FY24대체→재차플로어링, AXON에서
    실전검증) + 얇은 마진 스트레스테스트의 음수배수 문제(ELV에서 최초 발견)
  트래커에는 이 라벨로 분석된 종목이 10건 이상 존재한다(DECK/CRM/AXON/ADBE/
  ELV/ULTA/EVO/MNST긴급재검증 등). 따라서 **"존재하지 않는 버전"이 아니라
  "코드가 이 저장소에 없는 버전"으로 취급할 것.**
  - 참고: v3.17의 음수 파싱 이슈는 v3.15에서 복원한 self_check_v2가 이미
    `(-?\d+\.?\d*)` 정규식으로 처리하고 있어 실질적으로 반영된 상태다.
  - **새 버전을 만들 때 3.17/3.18 번호를 재사용하지 말 것**(같은 번호에 다른
    내용이 들어가면 v3.12 때와 같은 혼란이 재발). 2026-07-25 이후 신규 작업은
    v3.19부터 이어간다.

## 단위 규약 (v3.19에서 명문화 — 어기면 조용히 100배 틀림)

- `expected_return()`, `scenario_return_from_growth()`, `implied_growth_*()`,
  `realistic_growth_estimate()` → 전부 **소수**(0.1234 = 12.34%)
- `rar()`의 첫 인자만 예외적으로 **퍼센트 숫자**(-22.39). 트래커에 축적된 과거
  RAR 값 전체가 이 컨벤션이라 바꾸지 않는다.
- 따라서 `rar(expected_return(...), drs)`는 **틀린 호출**이다.
  반드시 `rar_from_decimal_return(er, drs)`를 쓸 것(v3.19 가드가 잡아주긴 하나,
  애초에 올바른 경로를 쓸 것).
- `market_cap`: `implied_growth_*()`/`scenario_return_from_growth()`는 **원화
  단위 그대로**(예: 92_952_756_000), `structural_discount_rate()`와
  `classify_lynch_type()`은 **10억 단위**(예: 92.95)를 받는다. 혼동 주의.

## 종목 분석 시작 전 claim/lock 프로토콜 (v3.16)

VRSN, ROP 종목에서 서로 다른 세션이 같은 종목을 중복 분석하는 사고가
있었음(원인: 큐에 "완료" 표시만 있고 "지금 작업중" 표시가 없었음).

**v3.19에서 실제 필드로 구현됨.** 그전까지는 이 규칙이 문서에만 있고 트래커에
잠금 필드 자체가 없어 지켜질 수단이 없었다(2026-07-25 감사: 🔒 표시가 붙은 행이
단 하나도 없었음). 이제 트래커에 다음 3개 컬럼이 있다:
- `작업상태`: 🔒 진행중 / 완료 / 보류
- `잠금시각`: 잠금을 건 시각
- `Ledger`: 재현용 ledger JSON 경로

새 종목 분석을 시작하기 전 반드시:
1. 트래커에서 해당 종목의 `작업상태`가 🔒 진행중인지 확인
2. 🔒 진행중이고 `잠금시각`이 2시간 이내면 → 다른 세션이 작업 중이므로
   건너뛰고 다음 종목으로
3. 없거나 2시간 초과면 → `작업상태`=🔒 진행중, `잠금시각`=현재시각으로 설정 후 시작
4. 분석 완료 후 `작업상태`=완료로 바꾸고 결과와 `Ledger` 경로를 채울 것

## 분석 실행 절차 (v3.19)

**엔진 함수를 손으로 배선하지 말 것.** `engine/pipeline.py`의 `run_analysis()`를
쓴다. 손배선이 실제로 만든 사고: RAR 100배 오류(4종목), 큐22 입력값 전량 유실,
PH 모델선택 실수(판정이 뒤집힐 뻔함).

```python
from engine.pipeline import AnalysisInputs, run_analysis, save_ledger
result = run_analysis(AnalysisInputs(...))   # 단위 처리·모델비교·한계기록 전부 내장
path = save_ledger(result)                    # ledger/<TICKER>_<날짜>.json
```

파이프라인이 강제하는 것:
- `model_choice_reason` 없으면 실행 거부(모델 선택은 판정을 뒤집을 수 있다)
- `subjective_input_basis` 없으면 실행 거부(경쟁강도·수요민감도는 [추정치])
- 두 모델을 항상 같이 계산해 3%p 이상 벌어지면 `data_limitations`에 경고 기록
- 10년치 데이터 부족 시 5년으로 대체한 사실을 `data_limitations`에 명시
  (예전에는 조용히 대체해서 DRS가 실제보다 관대하게 나왔다)
- CAGR 시작값이 음수면 예외(가드 없으면 파이썬이 복소수를 조용히 반환한다)

메모 발행 전 `self_check_v2.run_self_check_v2(memo_text, ctx)`로 메모 원문과
계산값을 대조할 것. "돌렸다"고 적는 것과 실제로 돌리는 것은 다르다.

**강건성점검(`expectation_gap_sensitivity_check`)은 이제 Section 5와 같은
모델로 판정한다(v3.19 근본수정, 2026-07-26).** 이전에는 엔진 원본 구현상
항상 two_stage로만 판정해서, Section 5가 `model_used="single_stage"`를 쓰고
두 모델 괴리가 크면 "강건성점검 flip"이 DRS 민감도가 아니라 단순 모델
불일치 때문에 발생했다. `model_used` 파라미터를 추가해 `pipeline.py`가
`inputs.model_used`를 그대로 넘기도록 고쳤다(기본값은 `"two_stage"`로 과거
호출 하위호환 유지 — 새 코드는 반드시 `model_used`를 명시할 것).

실측 영향: WCN/WM/IDXX(전부 single_stage + 모델괴리 6.5~7.5%p) 3종목이 이
가짜 flip의 실제 피해자였다 — 근본수정 후 셋 다 판정flip→통과(robust)로
바뀌고 Confidence가 15점씩 올랐다(59→74, 64→79, 59→74). RAR·Gap·최종판정은
불변(sensitivity_check와 무관한 경로였으므로). 9종목 전체를 저장된 ledger
입력값으로 재실행해 이 영향 범위를 직접 확인했다(상세는 CHANGELOG).

## 코드 변경 원칙 - Simplicity First (2026-07-31 도입)

이 CLAUDE.md는 이미 "Think Before Coding"(model_choice_reason·subjective_
input_basis 필수화), "Surgical Changes"(git 안전수칙, update_content vs
replace_content 교훈), "Goal-Driven Execution"(run_analysis()/save_ledger()
강제, CI)을 각각 사고 이후 구체적 가드로 갖추고 있다. 빠져 있던 한 가지를
명문화한다 - **engine/pipeline.py·engine/expectation_gap_engine.py에 새
기능을 배선할 때는 그 시점까지 실증된 필요만큼만 넣는다.**

이미 이 방식으로 해온 선례가 있다(원칙이 아니라 관행이었던 걸 규칙으로
승격하는 것):
- `capex_intensity_from_series`/`fcf_conservatism_adjustment`(v3.6/v3.7
  때 만들어졌으나): "실증사례가 없어 서둘러 넣기보다 필요한 사례가 실제로
  나왔을 때 구체적 요구사항을 갖고 배선하는 쪽을 택했다" - NVO 스크리닝이
  실사례가 되고서야 v3.20에서 배선.
- 보험업 P/B 임계값: ACGL·PGR 단 2건으로 "정상범위" 컷오프를 고정하지
  않고, 숫자만 제공해 분석자가 해석하게 함(v3.22).
- GEN의 M&A 성장왜곡: 세그먼트 분리를 표준 입력 스키마로 만들지 않고,
  실증사례가 더 쌓일 때까지 수작업 대조를 요구하는 주석으로만 남김.

앞으로 새 기능을 pipeline.py에 추가하기 전 자문할 것:
1. 지금 분석 중인 종목이 실제로 이 기능을 요구하는가, 아니면 "나중에
   필요할 것 같아서"인가? 후자면 넣지 않는다.
2. 기존 필드(capex_classification/cagr_base_year_override/is_insurer
   패턴처럼) 값이 없을 때는 기존 동작을 그대로 유지하는 opt-in으로
   설계할 수 있는가? 강제 분기보다 opt-in을 우선한다.
3. 100줄이면 될 걸 300줄로 짜고 있지 않은가 - "시니어 엔지니어가 보면
   과하다고 할까?"가 기준이다.
4. 기존 계산 로직(예: `realistic_growth_estimate`의 가중평균)을 새
   기능을 위해 중복 재구현하고 있지 않은가 - 중복 자체가 두 계산이
   미묘하게 어긋나는 새로운 버그를 만든다(capex_classification 배선 시
   `revenue_weighted_cagr`을 별도 재계산하지 말라고 명시한 이유와 동일).

이 원칙은 종목 분석 스크립트(scripts/analyze_*.py)에는 적용하지 않는다 -
티커별 스크립트는 반복돼도 재현성·감사가능성이 더 중요하다(이미 CLAUDE.md
다른 곳에서 확립된 관행). 대상은 engine/ 아래 공유 로직뿐이다.

## 과거 기록이 있는 종목을 재검증할 때 (v3.19)

이 프로젝트가 발견한 사고 대부분은 **과거 기록과 대조하다가** 잡혔다
(PH 모델선택, RAR 100배). 그래서 대조를 습관이 아니라 코드로 만들었다.

```python
from engine.pipeline import cross_check_prior_record
warnings = cross_check_prior_record(result, {
    "rar": -0.456, "gap": -0.0958, "drs": 43.6, "implied_growth": 0.159,
})
```

잡아주는 것: RAR 스케일 100배 차이 / RAR 부호 반전 / Gap 3%p 이상 괴리
(모델선택 차이가 주원인) / 과거 내재성장률이 다른 모델에 더 가까운 경우 /
DRS 10점 이상 차이(주관적 입력 차이).

**소수 규약 의심 7종목 — 2026-07-26 전건 처리 완료**(2026-07-25 전수감사,
상세는 CHANGELOG):
- **5건 완전 재검증**(SEC EDGAR 원본 데이터로 전면 재수행, ledger 확보):
  VRSN(큐18) · WCN(큐26) · WM(큐27) · IDXX(큐25) · BRO(큐28) — 전부 100배
  스케일 오류로 확정, 정정.
- **2건은 ledger 부재로 기계적 스케일 정정만 적용**(RAR×100, 다른 지표
  독립 재현검증은 불가능 — 정직하게 그 사실을 기록해뒀다):
  - ZTS(v3.15판): +0.0022→+0.22. 같은 종목의 완전한 v3.19 재분석이 이미
    트래커에 있으므로(RAR +0.1425, ledger/ZTS_2026-07-25.json) 이 행은
    사실상 폐기 권장 — 향후 ZTS 참조 시 v3.19판을 쓸 것.
  - DVN(프로포르마): +0.0007→+0.07. 애초에 프로포르마/Confidence
    25(최저권)라 투자판단 근거로 쓰면 안 되는 예비 스크리닝이었고, 그
    성격은 이번 정정으로도 바뀌지 않는다. 결합 10-Q(2026-08-04 예정)
    발표 후 전면 재검증 예정.

판별법: `RAR × DRS`가 함의하는 기대수익률이 |1%| 미만이면 소수 규약이다
(정상군 59건은 1.7~110%, 중앙값 19%). 7건 전체가 이 판별법으로 확정 후
정정까지 완료됐으므로, 이 항목은 향후 신규 분석에는 적용되지 않는다(v3.19
`rar()` 가드가 이미 신규 발생을 막고 있음) — 과거 트래커를 다시 감사할 때
참고용으로만 남긴다.

**⚠️ "과거엔 이 데이터가 없었을 것"이라고 추측하지 말 것 — 제출일을 확인하라.**
WM/WCN 재검증(2026-07-25)에서 FCF CAGR이 과거 기록과 크게 달라(WM 2.60%→8.03%,
WCN 5.04%→9.01~10.4%) "FY2025 10-K가 새로 나와서 그렇다"고 즉시 설명했으나
**틀렸다.** 두 종목의 FY2025 10-K는 각각 2026-02-09/2026-02-12에 제출되어
과거 분석일(2026-07-18)보다 5개월 이상 앞선다 — 그 시점에도 이미 있었어야 할
데이터다. "그럴듯한 원인"을 먼저 떠올리고 검증 없이 기록하면, 검증하지 않은
서술형 근사와 다를 바 없다(이 프로젝트가 계속 경계해온 바로 그 실수).
**결론이 안 나면 "원인불명"으로 정직하게 남길 것** — WCN은 5개 조회창을
전부 시도해도 과거값(5.04%)이 재현되지 않아 원인 미상으로 남아있다. WM도
동일 패턴으로 재정정됨(두 종목 다 과거 ledger가 없어 대조검증 불가).

## 알려진 한계 / 향후 과제 (v3.19 전면점검, 2026-07-26)

**`pipeline.py`가 엔진 함수를 전부 배선하지는 않는다 — 아래는 여전히
미배선 상태다:**

- `capex_intensity_from_series()` / `validate_growth_investment_claim()` /
  `fcf_conservatism_adjustment()`(v3.6/v3.7): capex 급증이 "성장투자"인지
  "마진훼손"인지 분류해 FCF CAGR을 조정하는 기능. `pipeline.py`는 항상
  `realistic_growth_estimate()`의 기본 동작(FCF CAGR < Revenue 가중평균이면
  FCF CAGR을 그대로 채택)만 쓰고, 이 세 함수는 import조차 되지 않는다.

  **의도적으로 지금 배선하지 않았다.** `check_deceleration_double_count`와
  달리, 이 기능을 쓰려면 `classification`("growth_investment" vs
  "margin_erosion")이라는 새로운 주관적 입력이 필요하고, `fcf_conservatism_
  adjustment()`가 요구하는 `revenue_weighted_cagr`을 얻으려면
  `realistic_growth_estimate()` 내부의 가중평균 로직(3y/5y/10y, 0.5/0.3/0.2)을
  `pipeline.py`에서 중복 구현해야 한다 - 그 중복 자체가 두 계산이 미묘하게
  어긋나는 새로운 버그를 만들 위험이 있다. 지금까지 분석한 9종목 중 이
  기능이 실제로 필요했던(capex 급증+growth_investment 주장이 있는) 사례가
  없어, 실증되지 않은 상태로 서둘러 넣기보다 필요한 사례가 실제로 나왔을 때
  구체적 요구사항을 갖고 배선하는 쪽을 택했다. 필요해지면: (1) `AnalysisInputs`에
  `capex_classification: str = None` 추가, (2) 값이 있을 때만
  `capex_intensity_from_series` + `fcf_conservatism_adjustment` 경로를 타도록
  분기, (3) `revenue_weighted_cagr`은 `realistic_growth_estimate`가 이미
  계산하는 `base_growth_before_fcf_check`를 재사용하도록 순서를 조정할 것
  (지금처럼 별도로 재계산하지 말 것).

- `operating_margin_from_series()`: `pipeline.py`가 같은 계산을 인라인으로
  직접 하고 있어(`op_margins = [...]`) 기능은 동일하지만 헬퍼를 재사용하지
  않는다. 결과에 차이는 없어 우선순위 낮음.

**v3.20/v3.21에서 배선·추가 완료** (위 미배선 목록 중 일부 해소):
- `capex_intensity_from_series()` + `fcf_conservatism_adjustment()`는
  **v3.20에서 배선했다**(NVO 스크리닝이 실증 사례가 됨 - 매출 CAGR 21.7%인데
  FCF CAGR 4.92%, capex가 5년새 9.5배 폭증). `AnalysisInputs.capex_classification`
  (+ `_basis` 필수)로 값을 넣을 때만 경로를 탄다. 위 지침대로
  `revenue_weighted_cagr`은 재계산하지 않고 `realistic_growth_estimate`가
  내놓은 `base_growth_before_fcf_check`를 재사용한다.
- **v3.21: `cagr_base_year_override`** (+ `_reason` 필수). BKNG 정식분석에서
  필요해졌다 - 코로나가 5년 룩백 자리에 걸리면 (a)그 해 FCF가 음수면 CAGR이
  정의되지 않아 실행이 거부되고 (b)매출 저점이 기준이 되어 '회복 반등'을
  '성장'으로 착각한다. 매출·FCF 양쪽에 동일 기준연도를 적용한다.
  **기준연도는 저점이 아니라 고점을 고를 것** - BKNG은 FY2020(저점)이 아니라
  FY2019(코로나 이전 고점)를 기준으로 삼아 성장률을 부풀리지 않았다.

**⚠️ 스크리너 조회창 한계 (2026-07-28 BKNG에서 실측 확인)**:
`engine/screener.py`는 보통 확보하기 쉬운 5개년만 쓰는데, 그 창 밖에 폭락기가
있으면 DRS가 과소추정되고 성장률이 과대추정된다. BKNG 실측 오차:
Gap +18.99%p(추정) vs +5.98%p(실제), DRS 34.6 vs 56.0, FCF CAGR 37.86% vs 12.44%.
판정 **방향**은 맞았으나 크기는 신뢰할 수 없었다. 스크리닝 Gap으로 후보
순위를 매길 때 조회창 길이를 함께 볼 것 - 여행·항공·소재 등 경기민감 업종은
10년 이상 창으로 재확인 필요.

**⚠️ 스크리너 한계 2건째 - competition_intensity placeholder (2026-07-28 PDD에서
실측 확인)**: BKNG과는 원인이 다른 별개의 한계다. PDD는 조회창 원자료 자체가
SEC 실측치와 정확히 일치했음에도(원인이 아님을 확인) Gap이 크게 벌어졌다:
+40.96%p(추정) vs +29.16%p(실제), DRS 30.6 vs 45.4, 현실적성장률 38.41% vs
25.00%. 원인은 `engine/screener.py`가 DRS 5개 구성요소 중 competition_intensity를
상수 placeholder(12.0)로 고정한다는 점 - 스크리닝 단계에서는 경쟁사 위협도·
시장점유율 추세 같은 정성적 입력이 없어 구조적으로 불가피하다. PDD는
Alibaba/Douyin/JD 3파전 + 美 de minimis 통관특례 폐지 + EU DSA 과징금(2억유로)
까지 겹쳐 competition_intensity가 실제로는 20.00(만점)까지 나왔고, 이게 DRS와
structural_discount_pct를 동시에 끌어올려 Gap을 눌렀다. 판정 **방향**은 맞았으나
(둘 다 저평가) 크기는 이번에도 신뢰할 수 없었다. 규제·경쟁 이슈가 뚜렷한
업종(플랫폼, 중국기업, 반독점 노출 종목)은 스크리닝 Gap을 그대로 신뢰하지 말고
정성적 경쟁강도를 감안해 보수적으로 할인해서 볼 것.

**CI 추가됨(2026-07-26)**: `.github/workflows/tests.yml`이 모든 push에서
`pytest tests/`를 자동 실행한다. 이 프로젝트의 사고 대부분(RAR 100배,
sensitivity_check 모델불일치)이 "테스트는 있었지만 실행을 깜빡함"이 아니라
애초에 테스트가 없어서 생겼지만, 앞으로 회귀를 막으려면 매 커밋마다 자동
실행이 최소한의 안전망이다.

**보험업 FCF-DCF 방법론 - v3.22에서 배선 완료(2026-07-28)**: `pipeline.py`는
원래 보험사의 OCF가 플로트(float, 보험료 선수취-보험금 후지급) 성장으로
회계이익보다 구조적으로 부풀려진다는 점을 보정하는 로직이 없었다. 성장기
보험사에 FCF-DCF를 그대로 적용하면 내재가치가 체계적으로 과대평가될 수
있다(ACGL v3.13 핵심노트: "Gap+31.44%p/RAR3.003은 FCF-DCF를 자본집약적
보험업에 적용한 데서 오는 과장일 가능성 높음"). ACGL·PGR 두 종목에서 종목별로
ROE x 유보율(지속가능성장률)과 P/B를 수작업 교차검증하는 방식으로 우회했다가
(ACGL: 지속가능성장률 10.1%/P/B 1.46x '정상범위' vs Gap 31.44%p - 방향은
맞으나 크기 과장. PGR: 지속가능성장률 18.88%가 오히려 엔진 Realistic
Growth(16.87%)와 근접해 성장추정 자체는 비교적 정합적이었으나, P/B 4.14x는
이미 상당히 높아 - ACGL과 정반대로 - 시장이 구조적 우위를 이미 재평가했을
가능성이 있어 판정 신뢰도를 낮춰야 했다), 실증사례 2건이 쌓여 v3.22에서
`AnalysisInputs.is_insurer: bool`(+ `net_income_by_year`/
`shareholders_equity_by_year`/`dividends_paid_by_year` 필수)로 배선했다.
`is_insurer=True`면 `run_analysis()`가 최근 최대 5개년 평균 ROE와 최근 최대
3개년 합산 배당성향으로 지속가능성장률을 자동 계산해 Realistic Growth와
대조하고(괴리 5%p 이상이면 경고), P/B도 함께 결과의 `insurer_cross_check`에
담는다. PGR을 이 경로로 재실행해 손계산과 정확히 일치함을 확인했다(ledger
갱신 완료). P/B 절대수준 자체는 하드코딩된 임계값 없이 숫자만 제공한다 -
ACGL(1.46x)/PGR(4.14x) 단 2건으로 "정상범위" 컷오프를 고정하는 건 시기상조
라고 판단했기 때문이다(capex_classification/cagr_base_year_override와
동일하게, 근거 없는 자동판정보다 숫자를 드러내고 분석자가 해석하게 하는 쪽을
택함). 반면 보험사 준비금 증가분을 OCF에서 차감하는 등 더 정교한 FCF 자체의
조정은 여전히 미해결 - 업계 표준 조정 관행에 대한 컨센서스가 이 프로젝트에서
아직 충분히 실증되지 않았다.

**SBC(주식보상비용) 병기 교차검증 - v3.23에서 배선 완료(2026-08-01, 방법론
전면감사 Critical-1)**: `FCF = OCF - capex` 정의는 SBC를 현금흐름표의 비현금
비용 가산 항목 그대로 따라가므로, 회계적으로만 비현금일 뿐 지분 희석을 통한
실질적 가치이전인 SBC를 암묵적으로 주주 귀속 현금처럼 취급해왔다. 2026-08-01
방법론 감사에서 ledger 보유 종목 전수의 SEC 원자료 SBC를 직접 대조한 결과:

| 종목 | SBC/FCF | Gap(기존) | Gap(SBC차감) |
|---|---|---|---|
| TTD | 62% | +17.56%p | +6.07%p |
| WDAY | 59% | +11.56%p | **+1.26%p (판정 뒤집힘)** |
| MNDY | 57% | +23.28%p | +13.11%p |
| DUOL | 37% | +19.45%p | +13.96%p |
| UBER | 19% | +11.78%p | +9.28%p |
| ROP | 7% | +7.74%p | +6.89%p |
| ACGL | 2% | +24.38%p | +24.03%p |

**WDAY는 SBC를 실제 비용으로 차감하면 저평가 가능성 -> 적정가/경계선으로
판정 자체가 뒤집힌다.** SBC 강도가 업종별로 2~62%까지 극심하게 갈려 상대
순위도 재편된다(SBC 의존도 높은 SaaS를 현행 방식이 구조적으로 유리하게
평가). is_insurer와 동일한 "병기, 자동판정 안 함" 원칙으로
`AnalysisInputs.sbc_by_year: dict`(opt-in, None이면 기존 동작 그대로)를
배선했다. 값을 넣으면 최근연도 SBC를 FCF0에서 차감한 대안 Implied Growth/
Gap/판정을 **같은 model_used로** 계산해 `sbc_cross_check`에 병기하고,
①판정이 뒤집히면 `data_limitations`에 명시적 경고, ②안 뒤집혀도 SBC가
FCF의 30% 이상이면 여유폭 주의 경고, ③SBC가 FCF보다 크면(차감 시 FCF
음수) `[Model Not Applicable]`로 처리한다. **어느 쪽이 "맞는 FCF"인지는
자동 결정하지 않는다** - SBC 전액차감/전액가산 둘 다 업계에서 논쟁적이라
분석자가 매번 의식적으로 판단하게 하는 쪽을 택했다(P/B 임계값을 고정하지
않은 보험업 사례와 동일 원칙). ledger 보유 7종목(TTD/MNDY/WDAY/DUOL/UBER/
ROP/ACGL) 전부 이 경로로 갱신 완료 - 판정 자체가 바뀐 건 WDAY의 SBC차감
시나리오뿐이며, 각 종목의 공식 판정(SBC 미차감 기준)은 그대로다.
반면 희석(SBC로 인한 주식수 증가) 자체를 DCF에 반영하는 것은 여전히
미해결 - FCF 조정과 희석 조정을 동시에 적용하면 이중계상 위험이 있어
별도 실증사례가 더 필요하다고 판단했다.

**반증조건·분석시점 주가/통화 병기 - v3.24에서 배선 완료(2026-08-01, 방법론
전면감사 권고 #2/#3)**: SBC(Critical-1)를 고친 뒤에도 감사가 지적한 더 근본적인
비대칭은 남아 있었다 - 이 프로젝트의 모든 통제가 "계산이 스스로와 일치하는가"
(내부 일관성)에만 걸려 있고, "계산이 현실과 일치하는가"(외부 타당성)에는 아무
장치가 없다는 점이다(High-1). 지금 당장 백테스트를 만들 수는 없지만(분석
이력이 3주뿐), 그 전제조건 두 가지는 지금 시작할 수 있다:

- `AnalysisInputs.falsification_conditions: str = None`(opt-in) - "이런
  실적이 나오면 이 판정은 틀린 것"을 분석 시점에 미리 적어두는 반증조건.
  사후합리화를 막는 유일한 실질적 장치(감사 원문 그대로)라 새 분석부터
  채워나간다. **과거 분석에 소급 작성하지 않는다** - 소급 작성 자체가
  사후합리화이므로 그렇게 채우면 허위 기록이 된다.
- `AnalysisInputs.price_at_analysis: float = None` / `currency: str = "USD"`
  (opt-in, currency 기본값은 기존 관행과 동일) - 향후 실현수익률 검증(백테스트)의
  전제조건은 "그때 얼마였는지"를 기록해두는 것이다. currency는 M-6(통화 규약
  미문서화, PDD가 CNY 기준인데 ledger에 아무 표시가 없었던 문제)도 함께
  해소한다 - PDD를 이 필드로 재실행해 `currency="CNY"`를 명시했다(핵심수치는
  전부 동일 - 감사 시점 기록: Gap +29.16%p/DRS 45.4/Realistic Growth 25.00%,
  ledger/PDD_2026-08-01.json).
- **screener.py 판별력 주장에 "엔진 자기 라벨 대비"임을 명시**(권고 #4) -
  기존 docstring의 "저평가군 11/13, 과대평가군 0/18" 판별력 수치는 실제
  주가 움직임이 아니라 `run_analysis()`의 판정을 정답지로 삼아 측정한
  것이다. 스크리너가 엔진을 잘 근사한다는 뜻일 뿐 엔진이 시장을 이긴다는
  근거가 아닌데, 문서에 이 구분이 없어 오독 여지가 있었다 - 명시적 경고문
  추가로 해소.

이 셋 다 계산 경로에는 관여하지 않는 순수 기록 필드다(`falsification_conditions`/
`price_at_analysis`/`currency` 모두 결과의 `meta`에 그대로 병기될 뿐 Gap/RAR/
판정 어디에도 영향을 주지 않음 - 골든테스트로 확인).

**demand_sensitivity 업종 앵커표 - v3.24에서 문서화 완료(2026-08-01, 방법론
전면감사 권고 #5)**: High-2에서 확인된 시간 드리프트(전반부 평균 0.167 ->
후반부 0.279, +67%)의 원인을 구분할 방법이 없다는 게 문제였다. 코드로
강제하지는 않는다 - `demand_sensitivity_pct`는 여전히 자유값이고
`subjective_input_basis` 필수화만으로 근거는 남지만, 그 근거가 "이번에
왜 이 숫자를 골랐는가"만 설명할 뿐 "지난번과 왜 다른가"는 설명하지
못했다. 그래서 지금까지 축적된 34종목의 실제값을 업종군으로 묶어 **시작점**
표를 만든다(2026-08-01 ledger 전수 집계, `ROP=0.18`처럼 정확히 앵커에
걸치는 값도 있고 `WM=0.20` vs `WCN=0.25`처럼 같은 업종인데 갈리는 경우도
있다 - 후자가 정확히 그 드리프트의 증거이므로 표에 그대로 남겨둔다):

| 업종군 | 앵커 | 관측범위 | 관측 종목 |
|---|---|---|---|
| 필수/의무 지출(정부SW·인터넷 인프라·의약품유통) | 0.08 | 0.05~0.08 | TYL·VRSN·COR·MCK |
| 헬스케어·필수소비재 반복매출 | 0.12 | 0.10~0.15 | IDXX·ZTS·MNST·RMD |
| 기업용 필수 SW·전문서비스(계약기반, 전환비용 높음) | 0.20 | 0.15~0.25 | CDNS·DSGX·PGR·ROP·BRO·BSY·GWRE·PTC |
| 폐기물·필수 인프라 서비스 | 0.22 | 0.20~0.25 | WM·WCN(⚠️ 같은 업종 내 드리프트 사례) |
| 소비자 구독/플랫폼(재량소비, 대체재 존재) | 0.30 | 0.20~0.38 | MNDY·DUOL·WDAY·GEN·SE·UBER·KLAC·KEYS |
| 광고·전자상거래·경기민감 산업재 | 0.35 | 0.35 | PDD·TTD·PH |
| 자본재/데이터센터 인프라(설비투자 사이클) | 0.45 | 0.45 | VRT |
| 여행·레저(재량소비 최상단) | 0.55 | 0.55~0.60 | TCOM·BKNG |

**자동 적용하지 않는다** - 새 분석 시 해당 업종군 앵커에서 출발해 조정하고,
앵커에서 벗어나면 그 이탈 사유를 `subjective_input_basis`에 명시할 것(기존
필수화 규칙과 동일 지점에 그냥 습관을 하나 더 얹는 것). 표 자체를 코드에
강제로 배선하지 않은 이유는 Simplicity First 원칙과 동일하다 - 8개 버킷
경계가 아직 34종목 관측치로 조정 중이라, 지금 하드코딩하면 다음 종목이
경계선에 걸릴 때마다 또 임의로 재조정해야 한다.

**fast_grower 25% 성장상한 - v3.24에서 문서화·경고 배선 완료(2026-08-01,
방법론 전면감사 M-1, 권고 #6)**: `LYNCH_TYPE_CAPS`의 상하한값(fast_grower
25%/stalwart 12%/slow_grower 5%/cyclical 20%/turnaround 30%/asset_play
10%)은 이 코드베이스가 자체 검증한 수치가 아니다 - 근거가 어디에도 없다.
상한이 실제로 바인딩되면 Realistic Growth가 매출·FCF CAGR 계산과 무관하게
상한값 그대로 고정돼, 그 종목의 Gap은 사실상 `상한 - Implied Growth`만
남고 성장분석 자체는 결과에 기여하지 않는다(감사 시점 DUOL·MNDY·PDD·SE
4종목이 정확히 25.00%로 이 상한에 걸려 있었다). **하향 조정은 하지
않기로 결정했다** - 근거 없이 유지하던 걸 근거 없는 다른 숫자로 바꾸는
것은 개선이 아니다(P/B 임계값을 2건만으로 고정하지 않은 것과 동일 판단).
대신 `run_analysis()`가 상한이 바인딩될 때마다 `[성장상한 바인딩]` 경고를
`data_limitations`에 명시적으로 남기도록 배선했다 - 캡이 조용히 순위를
결정하는 상황을 최소한 눈에 보이게는 만든다(테스트:
`test_growth_cap_binding_warns_when_upper_cap_overrides_calculated_growth`).

**structural_discount_rate 10년 대체값 버그수정 - v3.25(2026-08-01, 방법론
전면감사 M-3)**: 이건 설계 판단이 아니라 **코드와 경고문이 서로 다른 말을
하고 있던 순수 버그**였다. 10년 CAGR이 없을 때 `data_limitations`는 이미
"structural_discount_rate에 5년 CAGR을 대체 입력했다"고 명시하고 있었는데,
실제 `pipeline.py`는 `rev_cagr_3y`를 넣고 있었다. `rev_cagr_3y`를 자기
자신과 비교하면 `trend_delta`가 **언제나 정확히 0**이 되어 구조적 할인율이
아무 신호 없이 `base_discount(10%) + 시총가산`만 반환한다(M-1과 별개로
M-3가 지적한 "36종목 중 16개가 정확히 10.00%"의 실제 원인). 경고문이 이미
약속한 대로 `rev_cagr_5y`로 바로잡았다 - 18개 ledger(10년 데이터 없는
종목)로 재계산해보니 편차는 GWRE −1.91%p ~ SE +8.35%p였다(테스트:
`test_structural_discount_fallback_uses_5y_not_3y`). **18개 전부 소급
재검증 완료(2026-08-02)** - SE·MNDY·KLAC·TTD·KEYS·DUOL·BSY·WDAY·PTC·ROP·
COR·PDD·UBER·RMD·VRT·MCK·GWRE·GEN. **판정이 뒤집힌 종목은 0건**이었다 -
DUOL/MNDY/ROP/PDD/GEN/UBER 등은 Lynch 유형 성장상한이 이미 바인딩돼 있어
할인율 변화가 애초에 Realistic Growth에 도달하지 못했고(위 M-1 절 참고),
나머지도 Gap이 판정 경계(±5%p)에서 충분히 멀어 이번 폭의 변화로는
넘어가지 않았다. BSY는 스크립트 자체가 저장소에 없던 상태였는데(2026-07-26
당시 세션 내 직접 배선으로 실행, 재현용 파일 미보관) 이번에 ledger의
원본 입력값을 그대로 옮겨 `scripts/analyze_bsy_2026_08_02.py`로 정식
등록했다. 상세 종목별 편차·재검증 결과는 CHANGELOG 참고.

**n_requested 기본값 이탈 시 사유 필수화 - v3.25(2026-08-01, 방법론 전면감사
M-4)**: `capped_n()`은 8~15년을 허용해 해자 강도에 따라 성장지속기간을
차등화하도록 설계됐으나, 지금까지 분석한 36종목 ledger 전부가 기본값 12를
그대로 썼다 - 코드 버그는 아니고 단지 아무도 이 유연성을 쓴 적이 없을
뿐이었다. `lynch_type_override`/`cagr_base_year_override`와 동일한 패턴으로
`n_requested_reason: str = None`을 추가해, 기본값(12)에서 벗어날 때만 근거를
요구한다. **기존 36개 ledger는 전부 기본값 12 경로라 영향 없음**(zero
blast radius) - `capped_n()`을 실제로 차등화할지는 여전히 분석자 판단이고,
언제 차등화해야 하는지에 대한 코드화된 기준은 아직 없다(demand_sensitivity
앵커표와 달리 관측 데이터가 전무해 표를 만들 근거 자체가 없다).

**RAR 방향성 경고 - v3.26(2026-08-02, 방법론 전면감사 M-2)**: 감사 원문의
"RAR vs DRS 순위상관 −0.063(사실상 무상관)"은 실제로는 더 구체적인 결함의
평균이었다. `rar_from_decimal_return(ER, DRS)`은 `ER/DRS`인데, **ER이
음수일 때 이 공식은 의도와 반대로 움직인다** - 음수를 더 큰 DRS로 나누면
0에 가까워져(=덜 나빠 보여) 위험이 클수록 오히려 개선된 것처럼 보인다.
CDNS 원자료로 다른 값은 전부 고정하고 DRS만 20→80으로 올려 기계적으로
검증: RAR이 −0.9523→−0.3551로 "개선"됐다(같은 종목, 같은 ER 악화
방향인데도). ER≥0에서는 반대로 정상 작동한다(PDD로 검증: DRS 20→80일 때
RAR +6.43→+1.17로 정상 감소).

⚠️ **이 항목의 상관계수 수치는 2026-08-05에 정정됐다(중복 ledger 집계 오류).**
원 서술은 "ledger 전수(36종목) 재확인: RAR<0인 13종목 ... 상관 +0.291(방향
반전) vs RAR≥0인 23종목 +0.042"였는데, 당시 `ledger/`에는 WM·WCN·IDXX의
구 파일(2026-07-25, v3.19 sensitivity 근본수정 **이전** 값)이 지워지지 않고
남아 있어 **파일을 그냥 세면 33종목이 36건으로 잡혔다**. 나열된 티커가 10개인데
개수만 13으로 적혀 있었던 게 그 증거다 - 하필 그 3종목이 전부 RAR<0 집단이라
RAR<0 하위집단만 10건→13건으로 부풀려졌다. 중복을 제거하고 재계산한 값:

| 구분 | 정정 전(파일 36건) | 정정 후(티커 33종목) |
|---|---|---|
| 전체 RAR-DRS 순위상관 | −0.036 | −0.001 |
| RAR<0 하위집단 | +0.285 (n=13) | **+0.067 (n=10)** |
| RAR≥0 하위집단 | +0.104 (n=23) | +0.104 (n=23) |

즉 **"RAR<0 집단에서 상관 부호가 반전된다"는 통계적 근거는 중복계상의
산물이었고, 중복을 빼면 사실상 사라진다(+0.067).** 다만 **v3.26 경고 자체는
유효하다** - 그 근거는 이 상관계수가 아니라 CDNS 원자료로 다른 값을 전부
고정하고 DRS만 20→80으로 올린 **기계적 검증**(RAR −0.9523→−0.3551)이고,
그건 공식이 ER<0에서 어떻게 움직이는지를 직접 보여주는 결정적 증거라 표본
구성과 무관하다. 상관계수는 어디까지나 보조근거였고, 그 보조근거가 과장돼
있었다는 사실만 정정한다. 경고 배선·문구는 그대로 유지한다.

**공식 자체는 바꾸지 않았다** - RAR은 트래커 전체가 의존하는
컨벤션이라(단위규약과 동일한 이유로 v3.19에서 고정) 바꾸면 축적된 모든
과거 RAR 값과 비교 불가능해진다. 대신 `run_analysis()`가 ER<0일 때마다
`[RAR 방향성 경고]`를 `data_limitations`에 남기도록 배선해 - 해당 종목은
RAR 절대값이 아니라 Expectation Gap을 우선 참고하라고 명시적으로 안내한다
(테스트: `test_rar_direction_warning_fires_when_er_negative`).

**판정 세분화(Judgment Grade) - v3.27(2026-08-02, 사용자 요청)**: 기존
3단계 판정(저평가/적정가/과대평가, ±5%p 경계)은 "저평가 가능성" 한 칸에
33종목 중 17개(52%)를 몰아넣어 PDD(+29.16%p)와 BSY(+5.70%p)를 구분하지
못했다(High-3가 지적한 높은 기저율과 같은 증상). `engine/expectation_gap_
engine.py`의 `judgment_grade_from_gap()`이 **기존 3단계 경계값(±5%p)을
그대로 유지한 채** 6단계(S/A/B/C/D/F)로 세분화한다 - S(≥+15%p)/A(+7~15%p)/
B(+5~7%p)는 전부 "저평가 가능성"의 부분집합, D(-15~-5%p)/F(≤-15%p)는 전부
"과대평가 가능성"의 부분집합인 **엄밀한 상위호환**이라 기존 `judgment`
필드는 하나도 바뀌지 않는다. 경계값(±15%p 추가경계, ±7%p)은 2026-08-02
33종목 실측 분포의 자연 단절점에서 잡았다(TTD +17.01%p와 GEN +12.18%p
사이 4.83%p 간극이 +15%p 경계, BRO +7.43%p와 BKNG +5.98%p 사이 1.45%p
간극이 +7%p 경계) - demand_sensitivity 앵커표와 동일하게 관측 기반
시작점으로 취급하며 F등급 종목이 없어(현재 최저는 KEYS -14.36%p) 매도
쪽 경계는 아직 실증되지 않았다. 순수 함수(Gap만 입력)라 `judgment_grade`
필드를 `run_analysis()`에 추가로 얹은 것과 별개로, `scripts/rank_
portfolio_2026_08_02.py`가 엔진 재실행 없이 기존 33종목 ledger의 저장된
Gap 값만으로 소급 재순위한다(등급분포: S7·A6·B4·C14·D2·F0,
`reports/portfolio_ranking_2026-08-02.json`).

**S등급 캡바인딩 3종목 심층조사 - 2026-08-02(사용자 요청 "엔진에 없었던
부분까지 철저하게 조사")**: S등급 7종목 중 fast_grower 25% 상한이 실제로
바인딩된(=Realistic Growth가 원시 계산값이 아니라 상한값 그대로인) 3종목
PDD·MNDY·DUOL을 대상으로, DCF 엔진이 구조적으로 볼 수 없는 영역(자본배분
품질·회계품질·거버넌스·내부자 매매·희석 추이·경쟁환경 최신동향)을
WebSearch 기반으로 조사했다. 판정을 뒤집을 근거는 셋 다 없었으나, 세
종목 모두 **Confidence 89를 액면 그대로 신뢰하기엔 부담이 있는 정성적
리스크**가 새로 발견됐다:

- **PDD**: 희석은 문제없음(SBC 6~8%, 트래커 하위권 - 최초로 교차검증
  수행). 다만 2026-01 SAMR(중국 시장감독총국)과의 물리적 충돌·임원
  체포로 확대된 사기·탈세 조사, 2026-04 RMB15.1억 벌금 - competition_
  intensity(이미 20점 만점)와는 성격이 다른 거버넌스·컴플라이언스
  리스크.
- **MNDY**: 가장 우려스러운 케이스. 2026-02-09 실적발표에서 FY2027
  매출목표($1.8B) 철회 + 주가 하루 -21% 급락 사건이 원분석에 누락돼
  있었고, 이를 근거로 성장서사 과장 의혹 증권소송이 진행 중(S.D.N.Y.).
  회사 자체 FY2026 조정FCF 가이던스도 전년대비 감소 제시 - 엔진이
  노이즈로 간주해 미채택한 FCF CAGR 230%가 실제로 지속불가능했음이
  회사 스스로에 의해 확인됨.
- **DUOL**: 주가 66~80% 하락 구간 내내 내부자 순매도만 확인되고 매수는
  전혀 없었으며, 성장서사 과장 의혹 증권소송 진행 중(초기단계). OpenAI
  자체 스타트업펀드가 투자한 AI네이티브 경쟁사 Speak(ARR $100M)가
  급성장 중이나, DAU는 여전히 +21%YoY로 "AI가 실제로 사용자를 대체
  중"이라는 직접 증거는 미확인(서사 vs 실측이 갈리는 미해결 지점).

**공식 판정·Gap·RAR·Confidence 수치는 전혀 건드리지 않았다** - is_insurer/
sbc_cross_check와 동일한 "병기, 자동판정 안 함" 원칙(엔진이 계산하지 않는
정성적 판단을 자동으로 수치화해 끼워넣지 않는다). 대신 두 가지 순수
기록 경로로만 반영했다:
1. 세 종목의 `falsification_conditions`를 이번 조사에서 나온 구체적이고
   날짜가 명시된 반증조건으로 채움(예: DUOL "2026-08-05 Q2 실적에서
   유료구독자 순증이 재차 감소하면 재검토") - 전부 아직 결과가 나오지
   않은 미래 이벤트 기준이라 사후합리화가 아니다(과거 분석에 소급
   작성하는 것과는 다름 - 이 항목은 "결과를 이미 알고 나서 끼워맞추는
   것"이 아니라 "결과를 모르는 시점에 검증 가능한 조건을 미리 박아두는
   것"이라 falsification_conditions 필드의 원래 취지 그대로 사용한
   사례임).
2. MNDY·DUOL은 `price_at_analysis`/`currency`도 함께 채움(원 스크립트에
   이미 기록돼 있던 원분석 시점 종가를 그대로 옮김 - 오늘 조사 시점의
   근사치가 아니라 원분석일 실측치라는 점이 중요, PDD는 원 스크립트에
   주당가가 기록돼 있지 않아 추측 없이 비워둠).

ledger 3건(`PDD/MNDY/DUOL_2026-08-02.json`)은 이 두 필드만 추가되고
Gap/RAR/판정은 정확히 동일함을 재실행 후 확인했다. Notion 핵심노트에도
조사 결과를 상세 반영.

**정성 심층조사 절차(재사용 가능한 프로세스로 기록, 2026-08-02 사용자
요청 "이 과정과 단계 잘 기억해놔")**: "실제 매수리스트를 만들려면 어떻게
해야 하냐"는 질문에서 나온 3단계 계획 중 1단계에 해당한다.

1. **트리거 조건**: 캡바인딩 종목(Realistic Growth가 Lynch 상한 그대로라
   성장분석이 결과에 기여 안 함, M-1 참고) 또는 Confidence를 액면가로
   신뢰하기 부담스러운 종목. S/A등급이라고 전부 하는 게 아니라 이 조건에
   해당하는 것부터 우선순위를 매긴다.
2. **종목당 리서치 에이전트 1개, 병렬 실행**. 브리핑에는 반드시 (a) 엔진이
   이미 계산한 값(Gap/DRS/RAR/Confidence/Lynch유형/캡바인딩 여부)과 원
   `subjective_input_basis`를 그대로 넘겨 중복 조사를 막고, (b) "DCF
   엔진이 구조적으로 볼 수 없는 영역"을 명시적으로 나열한다. 공통 5축:
   자본배분 품질(buyback/M&A 가격규율) · 회계품질(FCF의 질, 일회성 항목) ·
   거버넌스(내부자 매매 패턴, 이중주식구조, 진행중 소송) · 희석 추이
   (SBC/FCF, 실제 주식수 증감) · 경쟁환경 최신동향(엔진의 주관적
   competitor_threat_weights가 놓친 신규 위협). **업종별로 5축을
   변형할 것** - 보험사(ACGL/PGR)는 준비금적정성·카타스트로프리스크·
   언더라이팅규율로, 복합기업(SE)은 세그먼트별 자본배분·계열사관계
   (SE-Tencent 등)로 대체.
3. **에이전트 산출물 형식 고정**: 축별 섹션 + 마지막에 반드시 "이 정보가
   판정을 강화/약화/불변시키는가, Confidence가 정당한가"를 명시적으로
   답하게 한다. 불확실한 항목은 "확인 못함"이라고 정직하게 쓰게 지시
   (추측 금지 - 이 프로젝트 전체의 원칙).
4. **결과 반영은 "병기, 자동판정 안 함" 원칙**(is_insurer/sbc_cross_check와
   동일): Gap/RAR/Confidence/판정 수치는 절대 건드리지 않는다. 대신
   (a) `falsification_conditions`에 조사에서 나온 미래 시점 검증조건을
   날짜와 함께 채우고(과거 분석 소급작성 금지 원칙과 충돌 안 함 - 결과가
   아직 안 나온 이벤트 기준이라 사후합리화가 아님), (b) 원 스크립트에
   이미 있던 분석시점 주가가 있으면 `price_at_analysis`에 반영(없으면
   추측하지 말고 비워둘 것), (c) Notion 핵심노트에 발견사항 상세 기록.
5. **재실행 후 반드시 diff 확인**: Gap/RAR/판정이 그대로인지 확인하고
   ledger를 같은 날짜 파일에 덮어쓴다(새 파일 생성 아님).

**⚠️ 진행 중 알게 된 것 - 백그라운드 에이전트는 세션 사용량 한도에
걸릴 수 있다**: 첫 시도에서 ACGL·PGR·SE·TTD(S등급 나머지 4종목) 조사가
4개 전부 "session limit" 에러로 중간에 실패했다(리셋 시각 09:20 UTC로
안내됨) - 조사 방법론의 문제가 아니라 API 쿼터 문제였고, 재시도는
그대로 재실행해 전부 성공했다(아래 참고).

**S등급 나머지 4종목(ACGL·PGR·SE·TTD) 심층조사 완료(2026-08-03)**:
PDD·MNDY·DUOL과 동일 절차로 완료 - S등급 7종목 전부 심층조사를 마쳤다.
판정을 뒤집을 근거는 4종목 모두 없었으나, 전부 Confidence를 액면가로
신뢰하기 부담스러운 발견이 나왔다:

- **ACGL**: 준비금 적정성·자본배분(BVPS +22.6%)·거버넌스 전부 양호(신용
  등급 오히려 상향). 다만 재보험 ex-cat 컴바인드레이쇼가 2분기 연속
  소폭 악화(+130~160bp YoY) 중이고 2025년 LA산불급 대재해리스크는 DCF의
  평활화된 FCF로 전혀 반영 안 됨. Confidence 80대 초중반 권고.
- **PGR**: 지속가능성장률이 Realistic Growth와 가장 근접(2%p 이내)해
  성장추정 자체는 정합적이나, 텔레매틱스 데이터우위(Snapshot)가 실제
  손해율 우위로 안 이어짐(2025 컴바인드레이쇼 GEICO~84/Allstate~85.0 <
  PGR~88 - 점유율 1위, 손해율은 3위). 신규계약 증가율 11%→8%YoY 둔화,
  상업용차 라인 소폭 역발전준비금($140M). Confidence 80대 중후반 권고.
- **SE**: 세그먼트 분리가 핵심 발견 - Shopee EBITDA가 사상최대 GMV에도
  전년比 감소(TikTok Shop 방어비용), 그룹 이익성장은 사실상 Garena
  단독부담. TikTok Shop 점유율 잠식 계속 진행(베트남 52%→41%로 원분석
  보다 심함), 과거 인도네시아 규제제동 재발 안 됨. SBC/희석은 양호
  (FCF의 14%, 최초 교차검증)하나 희석주식수는 여전히 +5.5%. Confidence
  70대 후반~80대 초반 권고.
- **TTD**: 이 7종목 중 가장 심각 - 원분석에 없던 **연방 증권사기
  집단소송이 기각동의 기각(2026-03-17) 후 본안 진행 중**이며 CEO Jeff
  Green을 내부자거래 혐의로 직접 지목. CFO 14개월새 4명 교체, CRO 7개월
  만에 이탈 후 미충원. Amazon DSP 점유율 15개월새 10%→20%로 배증,
  TTD는 "시장평균 속도로만 성장"(점유율 방어 실패) 확인. 다만 CEO의
  2026-03-04 $148M 개인자금 직접매수는 진성 강세신호. Confidence 70대
  권고.

처리 방식은 PDD/MNDY/DUOL과 동일(병기·falsification_conditions·
price_at_analysis만 반영, 공식 수치 불변 - 재실행 후 4종목 전부 Gap/
RAR/판정 동일함 확인). ledger를 전부 `_2026-08-03.json`으로 통합.

**실제 매수리스트 - 2·3단계(팩터진단+사이징) 실행 완료(2026-08-03)**:
"S/A등급만 투자하는 건 비추천"(집중도·미검증확신도·사이징부재) ->
1단계(S등급 7종목 정성조사) 완료 후 이어서 실행. `scripts/build_
buylist_2026_08_03.py`:

- **2단계 진단**: S/A등급 13종목을 4개 팩터버킷으로 분류 - growth_platform
  (PDD·MNDY·DUOL·SE·TTD·UBER·WDAY, "폭락발굴 고베타 성장주" 공통점)이
  종목수 기준 7/13=54%를 차지, 나머지는 insurance(ACGL·PGR·BRO)·
  industrial_stalwart(GEN·ROP)·travel(TCOM). Lynch유형만으로는 이 몰빵을
  못 잡는다(fast_grower가 growth_platform 7개 중 6개+insurance 2개에도
  걸쳐 있어 유형 자체는 분산 신호로 못 씀).
- **3단계 사이징**: 공분산 최적화가 아니라 투명한 규칙기반 배분(수익률
  상관행렬 자체가 이 프로젝트에 없음 - 알려진 스코프 갭). 버킷 목표비중
  growth_platform 40%/insurance 30%/industrial_stalwart 20%/travel 10%로
  강제 분산(동일가중 시 54%였던 걸 40%로 캡). 버킷 내부는 quality_score
  = Gap%p x (Confidence_adj/100)로 배분하되 캡바인딩 종목 x0.85, TTD
  (증권사기소송+CEO 내부자거래혐의)에 추가 x0.85. 종목당 상한 12%.
  **Confidence_adj는 S등급 7종목만 이번 정성조사 결과를 반영**(MNDY 65/
  TTD 72/PDD 75/DUOL 78/SE 79/ACGL 83/PGR 87 - MNDY·DUOL은 이 스크립트에서
  처음 구체적 숫자로 확정, CLAUDE.md에 범위만 있던 걸 결정함). A등급
  6종목(GEN/UBER/WDAY/ROP/TCOM/BRO)은 심층조사 전이라 엔진 원값을
  그대로 쓰고 `conf_status="미검증"`으로 명시 - 액면가로 믿으라는 뜻이
  아니라 아직 확인 안 했다는 뜻임을 결과표에 남긴다.
- **결과**: ACGL·PGR·GEN이 상한 12%로 나란히 최상위(보험업 2종목은
  정성조사에서 상대적으로 양호했던 게, GEN은 상한바인딩 종목 특성상
  quality_score 계산에서 유리하게 나온 결과 - 후자는 조사가 아니라
  공식의 산물이므로 과신 금지). TTD가 최하위(4.54%) - 거버넌스 적신호
  이중할인이 실제로 작동함을 보여주는 사례.
- `reports/buylist_2026-08-03.json`에 전체 계산 과정과 근거 저장.

**⚠️ 미해결 항목 2건째 - M&A/사업다각화로 인한 CAGR 왜곡이 통계적으로
안 보일 수 있다 (2026-07-28 GEN에서 발견)**: BRO 때부터 알려진 "M&A주도
성장은 유기적 성장과 분리검증 필요"라는 원칙이 있었지만, GEN은 그 원칙만으론
부족한 사례였다. 기준연도(3y CAGR의 경우 FY2023)와 종료연도(FY2026) **양쪽
모두**에 서로 다른 인수(Avast/MoneyLion) 관련 단계상승이 끼어있으면, 두
왜곡이 통계적으로 상쇄돼 CAGR 수치 자체가 정상범위처럼 보인다(GEN의 3y/5y
매출 CAGR은 14.4%대로 TCOM의 46%처럼 눈에 띄는 이상치가 아니었다) - 즉
"숫자가 튀지 않는다"는 사실 자체가 안전하다는 근거가 되지 못한다. 세그먼트
공시를 직접 대조해서야 핵심사업(Cyber Safety, 유기적 성장에 가까움)이
+5.13%에 불과하고 연결성장 27%의 대부분이 완전히 다른 산업(소비자대출・
핀테크)으로의 사업다각화 연결효과임이 드러났다. 회사 자체 익년도 가이던스
(+6.5~8.5%)도 과거 CAGR(14%대)보다 훨씬 낮아 회사 스스로 이 성장률이
지속불가능함을 사실상 인정하고 있었다. **의도적으로 아직 배선하지
않았다** - 세그먼트 매출 분리는 회사마다 공시 방식이 달라 표준화된 입력
스키마를 만들기 어렵고, "핵심사업 vs 인수사업"의 경계 자체가 주관적 판단이라
capex_classification처럼 근거 필수화된 필드로 만들려면 실증사례가 더
필요하다. 지금은 M&A가 최근 5년 CAGR 구간(기준연도~종료연도) 양끝 중
하나라도 걸리는 종목은 **정량 CAGR이 정상범위로 보이더라도** 반드시
세그먼트 공시・회사 가이던스를 별도로 대조할 것 - "숫자가 안 튀니 괜찮다"는
가정 자체가 이번 사고의 원인이었다.

**A등급 나머지 6종목(GEN·UBER·WDAY·ROP·TCOM·BRO) 심층조사 완료(2026-08-04)**:
S등급 7종목(PDD/MNDY/DUOL/ACGL/PGR/SE/TTD) 정성 심층조사에 이어, 같은 절차를
A등급 나머지 6종목에 적용해 S/A등급 13종목 전부가 "정성 심층조사 완료" 상태가
됐다. 판정을 뒤집을 근거는 6종목 모두 없었으나, 전부 Confidence를 액면가로
신뢰하기 부담스러운 발견이 나왔다:

- **GEN**: ROIC가 9.15%(5y평균 20.74%대비 급락, ainvest.com 단일출처로 10-K
  대조 미실시)로 추정되고, Trust-Based Solutions(MoneyLion) 세그먼트 마진이
  30%로 Cyber Safety(61%)의 절반. 시장은 27%매출성장에도 -9%로 반응했고
  (멀티플압축), 대형 매도데스크 2곳(Barclays/Morgan Stanley)이 beat-and-raise
  분기 이후에도 중립등급 유지 - 세그먼트/ROIC 시야를 가진 전문투자자들이 이미
  이 우려를 가격에 반영 중일 가능성. 반면 SBC/FCF 15.6%(트래커 최하위권, 최초
  교차검증)는 긍정적. Confidence 89→70 권고.
- **UBER**: Prop 22가 CA대법원에서 만장일치 합헌 확정(2024-07)되며 최대
  과거리스크가 durably 해소, MA/NY/MN은 임금하한 합의로 유한화. 반대로 **Waymo가
  Phoenix 철수완료(2026-06)+Atlanta/Austin 배타권 종료 발표(2026-07-24, Waymo
  자체앱 2028-01 예정)로 "파트너" 프레이밍이 최초분석 대비 약화** - Uber
  스스로 85%인간운전자의무 법안을 로비 중이라는 점이 플랫폼모트 자신감 부족의
  신호. EU Platform Work Directive(전환시한 2026-12-02) 미해결. FCF가 GAAP
  순이익보다 깨끗한 지표(Grab/Didi 마크투마켓 변동성이 GAAP만 왜곡)라는 점은
  엔진 방법론에 긍정적. Confidence 94→83 권고.
- **WDAY**: 자사주매입이 SBC 2배속 상회($2.9B vs $1.5B, 최근 순주식수 감소
  전환)는 긍정적 신규발견. 반대로 **Mobley v. Workday가 2026-03 기각argument
  패소+수정소장으로 청구범위 확대**, "AI벤더=고용주 에이전트" 이론이 Eightfold
  등 유사소송과 함께 업계 파급력 있는 판례로 부상 중. AI네이티브 경쟁
  (Ramp ARR$1B+/YoY+110%, Rippling ARR$1B+/YoY+78%)이 서사에서 실측 트랙션으로
  전환 확인 - competitor_threat_weight(0.15) 과소 가능성. WDAY는 이 트래커에서
  유일하게 SBC차감 시나리오가 판정을 뒤집는 종목이라 이 조사결과와 결합해 특히
  주의 필요. Confidence 94→81 권고.
- **ROP**: **가장 비중있는 발견 - "M&A 유기적/비유기적 성장 분리 미실시"로
  기록돼 있던 알려진 한계를 이번에 실제로 채웠고, 결과가 불리하다.** 회사공시
  오가닉성장률이 5-6%(3년연속 감속: 8%→6%→5-6%)로 엔진이 사용한 12%
  Lynch캡(stalwart 상한, 실제로는 원시계산이 아니라 캡 그 자체)과 큰 괴리.
  유기적성장 기준으로 재계산하면 Gap이 +7.74%p→약+1~2%p, 연결성장(8-9%)
  기준으로도 약+4~5%p로 축소돼 "적정가/경계선" 근접·진입 가능 - **다음
  세션에서 유기적성장 입력으로 재실행 검증 강력 권고**. 레버리지도
  2.9x(FY25)→3.4x(Q2'26)로 상승(자사주매입이 원인, Moody's 업그레이드
  트리거 3x 상회), M&A배수도 14x→18x→22-23x로 상승추세(실현ROIC는 ~5%로 낮음).
  Confidence 89→77 권고.
- **TCOM**: SEC CORRESP로 확인된 中규제리스크 공시완화 이력(진행중인 증권소송
  De Wilde v. Trip.com과 직결되는 거버넌스/공시품질 문제)이 신규 발견. **회사
  자체 Q2 2026 가이던스(+3~8%YoY)가 Realistic Growth(12.39%)를 크게 하회** -
  MNDY류 자체가이던스 하향 패턴과 유사. DRS 73.6(트래커 역대 최고)의 타당성은
  재확인됨(다만 근거는 competition_intensity가 아니라 own-conduct 규제리스크
  실현이었음 - 레버리지는 오히려 순현금이라 낮춰야 할 요인). 자본배분(디스플린,
  $5B자사주+MakeMyTrip지분정리)·희석(SBC 낮음, 최초교차검증)은 양호.
  Confidence 94→80 권고.
- **BRO**: **ROP와 동일 유형의 발견** - lynch_type_override 사유에 이미 있던
  "Q1'26 오가닉 0%" 반전신호가 이후 실제로 더 악화(7분기 연속 감속, Q4'25
  -2.8%/Q2'26 -0.7%로 2회 마이너스)됨을 확인, stalwart 오버라이드 판단이
  사후적으로 뒷받침됨. 시장은 이미 EBITDA멀티플을 17.8x→12.9x로 재평가
  완료(Insurance Journal 분석, BRO 스스로가 원인). Accession 인수($9.8B,
  ~12-16x)는 경쟁사 Gallagher AssuredPartners(14.5x)와 비슷한 수준으로 특별히
  비합리적이진 않음. 레버리지 2.77x(피어평균 1.39x대비 높음), M&A주식대가로
  인한 별도 다일루션(~19% 주식수 증가, SBC와 무관 - 표준 SBC교차검증이
  놓치는 채널)도 신규 확인. Confidence 89→70 권고.

처리 방식은 S등급과 완전히 동일(병기·falsification_conditions·ledger 경로만
반영, 공식 Gap/RAR/판정 수치 불변 - 6종목 전부 재실행 후 동일함을 diff로
확인). **BRO는 재현용 스크립트가 저장소에 없던 상태였다**(BSY와 동일한 패턴 -
과거 세션 내 직접 배선으로 실행되고 재현용 파일 미보관)- ledger의 원본
입력값을 그대로 옮겨 `scripts/analyze_bro_2026_07_25.py`로 정식 등록했다.
ledger는 전부 `_2026-08-04.json`으로 통합(구 파일 git rm, 신규 파일 git add -
git이 rename으로 인식). `scripts/build_buylist_2026_08_03.py`의
`CONFIDENCE_ADJ`가 이제 S/A등급 13종목 전부 "검증" 상태로 갱신됐고(A등급
6종목은 이번 조사 결과 반영), 재실행해 최종 매수리스트를 갱신했다(버킷목표
전부 정확히 달성: growth_platform 40.00%/insurance 30.00%/
industrial_stalwart 20.00%/travel 10.00%). ROP가 상한(12%) 아래로 내려간
유일한 캡바인딩 종목이 됐다(quality_score가 Confidence 하향으로 낮아진 결과).

**ROP 유기적성장률 교차검증 - 2026-08-04(위 falsification_conditions의 즉시
검증)**: 정성심층조사가 "다음 세션에서 반드시 재실행 검증할 것"이라 명시한
항목을 `scripts/rop_organic_growth_crosscheck_2026_08_04.py`로 실행했다.
공식 재분석이 아니라 `is_insurer`/`sbc_cross_check`와 동일한 "병기, 자동판정
안 함" 원칙의 교차검증이다 - engine/pipeline.py의 `AnalysisInputs`에는
"유기적성장 오버라이드" 필드가 없다(세그먼트 공시 방식이 회사마다 달라
표준 입력 스키마를 만들기엔 아직 실증사례 부족, 위 GEN 항목과 동일 판단).
대신 ledger/ROP_2026-08-04.json에 이미 저장된 DRS·할인율(r)·Implied
Growth(growth 입력과 무관하게 고정)를 그대로 가져와 Realistic Growth
자리에만 회사 공시 성장률을 대입해 Gap·RAR·판정을 재계산했다.

**결과가 정성조사의 예상("근접 또는 진입 가능")보다 더 뚜렷하다 - 판정이
실제로 뒤집힌다.** 유기적성장 기준(5.5%, Q1'26/Q2'26 실측 5%와 FY26
가이던스 상단 6%의 중간값)에서 Gap +7.74%p→**+1.24%p**, 연결성장 기준
(8.5%, FY26 가이던스 "north of 8%")에서도 Gap +7.74%p→**+4.24%p** - 둘 다
±5%p 경계 안쪽이라 판정이 "저평가 가능성"에서 "적정가/경계선"으로
바뀐다(RAR도 +0.6928→+0.17~+0.48로 크게 축소). 즉 ROP의 공식 A등급
판정(+7.74%p)이 서 있는 근거는 사실상 stalwart 성장상한(12%) 그
자체이며, 회사가 실제로 공시한 성장률로 대체하면 저평가 근거가 사실상
사라진다. **공식 ledger의 Gap/RAR/판정/Confidence는 변경하지 않았다** -
`reports/rop_organic_growth_crosscheck_2026-08-04.json`에 결과를
병기해뒀을 뿐, 이를 공식 판정으로 승격할지(예: `lynch_type_override`로
캡 자체를 낮추거나 별도 입력 스키마를 신설)는 다음 세션에서 사용자와
함께 결정할 사안으로 남겨둔다 - Simplicity First 원칙상 실증사례 1건만으로
새 필드를 서둘러 만들지 않는다.

**ROP 유기적성장률 - 공식판정 승격, `realistic_growth_override` 신규 배선
(v3.28, 2026-08-04, 같은 세션 사용자 명시적 승인)**: 위 교차검증 직후
사용자에게 "병기만 할지 공식판정으로 승격할지" 범위를 명확히 물었고,
"ROP 공식판정 승격 + 매수리스트 재실행"으로 답을 받아 실행했다. 이 프로젝트가
지금까지 일관되게 지켜온 "병기, 자동판정 안 함" 원칙(is_insurer/sbc_
cross_check/S·A등급 정성조사 전부)의 첫 예외다 - 예외로 둔 이유는 (1)
크로스체크가 이미 두 시나리오(유기적 5.5%/연결 8.5%) 모두 판정이 뒤집힘을
사전에 명확히 보여줬고, (2) 사용자가 그 사실을 알고도 명시적으로 승격을
지시했기 때문이다. `AnalysisInputs`에 `realistic_growth_override`(+
`_reason` 필수) 필드를 신규 추가했다 - CAGR 계산과 Lynch 캡을 모두 우회하고
지정값을 Realistic Growth로 직접 사용한다. Simplicity First 관점에서
"실증사례 1건만으로 새 필드를 서둘러 만들지 않는다"는 관행과 충돌하는
것처럼 보이지만, capex_classification/cagr_base_year_override와 동일하게
**opt-in이라 기존 36개+ ledger에는 영향이 전혀 없고**(None이면 기존 동작
그대로), 이 필드가 해소하는 문제(M-1 성장상한 바인딩이 회사 실제 공시
성장률과 크게 괴리될 수 있음)는 GEN·BRO도 같은 패턴으로 이미 플래그된
상태라 재사용 가능성이 낮지 않다고 판단했다.

ROP를 `scripts/analyze_rop_2026_08_01.py`에 `realistic_growth_override=0.055`
(오가닉 성장 기준, M&A 배수상승·낮은 실현ROIC를 감안해 두 크로스체크
시나리오 중 더 보수적인 쪽 채택)로 재실행 - 크로스체크와 완전히 동일한
결과(Gap +7.74%p→+1.24%p, RAR +0.6928→+0.1716, 판정 저평가 가능성→적정가/
경계선)가 공식 ledger(ledger/ROP_2026-08-04.json, 같은 날짜라 in-place
갱신)에 반영됐다. **결과: ROP가 A등급→C등급으로 이탈**, S/A 유니버스가
13종목→12종목으로 줄었다.

이 이탈이 매수리스트에 연쇄효과를 낳았다: `industrial_stalwart` 버킷이
GEN 단독으로 남아 목표비중(20%)을 채울 수 없게 됐고(GEN 혼자 12%캡에
막힘), 이 과정에서 **버킷별 캡 재분배 이후의 전역 정규화가 이미 12%로
캡된 종목(ACGL/PGR/GEN)까지 13.04%로 재상승시키는 버그**를 발견해 즉시
고쳤다(`scripts/build_buylist_2026_08_03.py`에 정규화 후 2차 전역 캡
강제 패스 추가 - 버킷 목표 미달은 그대로 두되 종목당 12% 상한만큼은
전역적으로 지킨다). 최종 배분: ACGL/PGR/GEN 12.00%씩, TCOM 11.43%,
PDD 9.56%, SE 7.79%, BRO 6.86%, DUOL 6.63%, MNDY 6.61%, TTD 5.35%,
UBER 5.03%, WDAY 4.75%(합계 100%, industrial_stalwart는 목표 20% 대비
실제 12.00%로 구조적 미달 - GEN 단독 버킷이라 이 사이징 규칙 안에서는
해소 불가). `industrial_stalwart` 버킷을 다른 버킷과 합칠지 등 구조
재설계는 다음 세션 판단으로 남긴다.

테스트: `tests/test_pipeline.py`에 `realistic_growth_override` 배선
검증 3건 추가(CDNS 골든케이스 재사용 - 기본값 None이면 결과 불변,
사유 없이 쓰면 거부, 오버라이드 값이 CAGR/캡과 무관하게 그대로 쓰여
판정까지 뒤집는지 확인). 122개 전부 통과.

**매수리스트 industrial_stalwart 버킷 재설계 - 목표비중 동적 상한
(v3.29, 2026-08-04, 사용자 요청)**: ROP 이탈 직후 사용자가 "industrial_
stalwart 버킷 구조 재설계"를 명시적으로 요청했다. 검토한 대안:
(1) GEN과 TCOM을 억지로 한 버킷에 합치기(둘 다 1종목 버킷이라는 이유만
있을 뿐 공통 리스크축이 없음 - M&A롤업 지주회사 vs 中여행 규제리스크는
전혀 다른 위험요인이라 스크립트 자체의 "실제 사업모델·리스크요인
군집화" 원칙에 위배됨), (2) industrial_stalwart 목표비중을 12%로
하드코딩 - ROP류 종목이 다시 A/S등급에 진입하면 또 손으로 고쳐야 함.
둘 다 기각하고 **일반 규칙**을 택했다: `effective_bucket_targets()` -
버킷 목표가 `종목수 x 종목당상한(12%)`을 넘으면 그 상한까지 깎고,
초과분을 캡에 안 걸린 나머지 버킷들에 **명목 목표비중 비율대로**
재분배한다. 종목수가 늘거나 줄 때마다 자동 재계산되므로 버킷 하나를
예외처리하는 게 아니라 미래의 유사 사례(어느 버킷이든 종목이 줄어드는
경우)에 전부 적용된다.

결과: growth_platform 40%→44%, insurance 30%→33%, industrial_stalwart
20%→12%(GEN 1종목 x 12%캡이 상한), travel 10%→11%(합계 100%). 이전에
추가했던 "정규화 후 2차 전역 캡 강제 패스"는 이제 주된 방어선이 아니라
버킷 내부에서 quality_score 편차가 극단적일 때의 안전망으로 격하됐지만
그대로 유지한다(방어적 이중화). 재실행 결과 종목당 상한(12%)이 어디서도
깨지지 않고, 버킷별 실제 배분이 재계산된 목표치와 정확히 일치함을
확인했다(ACGL/PGR/GEN 각 12.00%, TCOM 11.00%, 나머지 growth_platform
7종목이 44% 안에서 quality_score 비례 배분). `industrial_stalwart`가
GEN 단독으로 남는 것 자체(버킷 다양화 효과가 그만큼 약해짐)는 여전히
구조적 한계이지만, 최소한 목표-실제 괴리와 캡 위반이라는 두 가지
구체적 결함은 해소됐다.

**매수리스트 버킷 목표 최소 달성률 바닥 - v3.30(2026-08-04, 사용자 요청
"모두 실사용목표 90%까지 올려")**: v3.29는 달성 불가능한 목표를 "달성
가능한 데까지" 깎기만 해서 industrial_stalwart가 명목 20% 대비 12%
(달성률 60%)까지 떨어지는 걸 그대로 뒀다. `MIN_BUCKET_TARGET_ACHIEVEMENT
= 0.90`을 도입해 **어떤 버킷도 명목목표의 90% 밑으로 내려가지 않게**
바닥을 깔았다.

**⚠️ 이 바닥은 종목당 상한(12%)과 직접 충돌하며, 이번 규칙은 의도적으로
"버킷 목표 달성"을 "단일종목 과다집중 방지"보다 우선한다.** 1종목뿐인
버킷은 12% 상한 때문에 최대 12%인데 바닥은 18%를 요구하므로 둘 다
지킬 수 없다 - 사용자 요청대로 바닥을 지키고, **그 버킷에 한해** 종목당
상한을 필요한 만큼(12%→18%) 완화한다. 전역 상한을 올리는 게 아니라
`effective_bucket_targets()`가 (목표, **버킷별 상한**) 튜플을 반환하도록
바꿔, 완화가 필요 없는 버킷(growth_platform/insurance/travel)은 12%를
그대로 유지한다.

실측 영향: **GEN이 12%→18%로 포트폴리오 최대 보유종목**이 됐다(명목
달성률: industrial_stalwart 60%→90%, 나머지 셋은 미달분 흡수가 줄어
110%→102.5%). 최종 배분은 GEN 18.00%, ACGL/PGR 각 12.00%, TCOM 10.25%,
PDD 8.57%, SE 6.99%, BRO 6.75%, DUOL 5.95%, MNDY 5.93%, TTD 4.80%,
UBER 4.51%, WDAY 4.26%(합계 100%). **GEN은 Confidence 70으로 최하위권
(BRO와 공동)이고 M&A 연결효과로 성장상한이 바인딩된 종목**이라 이
집중이 의도한 것인지 매번 확인해야 한다 - 스크립트가 실행 시 완화된
상한·해당 종목 비중·이 주의사항을 경고문으로 명시 출력하고, 저장되는
JSON의 각 행에도 `bucket_cap_applied`/`bucket_target_effective`를 남겨
"이 종목이 왜 12%를 넘겼나"를 리포트만으로 추적할 수 있게 했다.

**B등급 라벨 정정 - v3.31(2026-08-04, 사용자 지적)**: B/C/D등급 20종목
정성 심층조사 1차 배치(PH/KEYS/BKNG/RMD/BSY/PTC) 결과를 보고하는 도중
사용자가 "저평가면 매수하라는거야 말라는거야"라고 직접 물었다. 확인해보니
`JUDGMENT_GRADE_LABELS`의 B등급 라벨이 "관심(약매수)"였는데,
`scripts/build_buylist_2026_08_03.py`의 실제 유니버스는
`grade in ("S", "A")`로 B를 완전히 배제한다 - 라벨은 "조금 사라"고
말하고 코드는 "안 산다"로 동작하는 자기모순이었다. 두 가지 해결책
(B를 실제 매수리스트에 소액 편입 vs B 라벨을 정직하게 수정)을 물었고
**후자**로 답을 받았다. `engine/expectation_gap_engine.py`의
`JUDGMENT_GRADE_LABELS["B"]`를 "관심(약매수)"→"관찰대상(매수리스트
미포함)"으로 변경 - 코드 동작은 그대로, 라벨만 실제 의미에 맞게
정정했다. `scripts/rank_portfolio_2026_08_02.py`의 등급분포 출력
컬럼폭도 늘어난 라벨에 맞게 조정. 122개 테스트 전부 통과(하드코딩된
라벨 문자열을 assert하는 테스트가 없어 영향 없음).

**B/D등급 6종목 가벼운 정성검증 1차 배치 완료(2026-08-04) - KEYS
성장률 교차검증 필요성 발견**: B/C/D등급 20종목 확인 작업의 우선순위
배치(D등급 PH·KEYS + B등급 BKNG·RMD·BSY·PTC)를 "가벼운 검증"(정식
falsification_conditions/ledger/Notion 기록 없이 채팅 요약 + Confidence
재평가만) 수준으로 실행했다. 판정을 뒤집을 근거가 나온 건 KEYS
하나였지만, 그 하나가 트래커 전체에서 가장 극적인 발견이었다:

- **PH**: "과대평가" 판정을 대체로 뒷받침(동종업계 대비 프리미엄
  멀티플, Wolfe Research가 밸류에이션 근거로 다운그레이드). Confidence
  94→75-85 권고.
- **KEYS(핵심 발견)**: 트래커 최고 과대평가 신호(-14.36%p)의 근거인
  Realistic Growth 1.47%(trailing CAGR 기반)를 회사 자체 FY2026
  가이던스(20%대 후반, AI 데이터센터 테스트 수요 급증)가 정면 반박.
  Spirent 인수도 후퇴가 아니라 확장(Viavi 매각은 반독점 조건 충족용
  부수거래).
- **BKNG**: 방향 유지. Google AI Mode 여행예약 파트너십 이후 시장이
  디스인터미디에이션 우려로 -5.5% 반응, YTD -20%+ 하락의 상당부분이
  이 우려 - 미해소. Confidence 94→75-85 권고.
- **RMD**: "저평가" 강화. GLP-1 수요파괴 우려가 자체 데이터(CPAP+GLP-1
  병용환자 195만명, 병용시 오히려 CPAP 도입률 상승)로 계속 반박됨.
  Philips 미국 재진입 리스크는 여전히 미해결(날짜 불명). Confidence
  94→중상위80대 권고.
- **BSY**: 방향 유지, 강화는 아님. SBC/FCF 14%(최초검증, 양호)는
  긍정적이나 이중주식구조(Bentley 가문 Class B 과반) 거버넌스 제약
  신규확인, 매도데스크는 최근 오히려 신중해짐(ARR 감속 근거 다운그레이드
  - 다만 이 감속치가 엔진 Realistic Growth 10.54%와 정합적이라 입력값
  자체는 타당). Confidence 94→75-82 권고.
- **PTC**: "저평가" 강화. ARR 가속(9.1%YoY, 가이던스 상향), 자사주매입이
  SBC 6-7배 상회(anti-dilutive, 최초검증), Kepware/ThingWorx 매각으로
  핵심 CAD/PLM 집중도 상승. 다만 분석시점 주가가 실적발표 후 +21%
  랠리구간에 걸려있어 Gap이 stale일 가능성 - 재확인 필요. Confidence
  94→80대 권고.

**KEYS 성장률 교차검증(`scripts/keys_growth_crosscheck_2026_08_04.py`)**:
ROP와 성격이 다르다는 점을 명시하고 접근했다 - ROP는 회사가 **여러 분기
실제로 실현한** 다년 오가닉 성장률(검증된 과거실적)을 근거로 승격했지만,
KEYS는 회사의 **향후 1개년 가이던스**(AI 데이터센터 투자 슈퍼사이클이라는
특수국면에 의존, 아직 실현 안 됨)만 있다 - 사이클이 꺾이면 빠르게
되돌아갈 수 있는 예측치를 다년 성장률 개념인 Realistic Growth에
끼워맞추는 것이라 신중해야 한다. 그래서 이번엔 ROP처럼 공식판정
승격을 하지 않고 두 시나리오만 병기했다:
- A) cyclical Lynch 상한(20%, trailing CAGR을 완전히 무시하고 이
  프로젝트가 cyclical 유형에 허용하는 최댓값) 시나리오조차 판정을
  '과대평가 가능성'→**'적정가/경계선'**으로 끌어올린다(Gap -14.36%p→
  +4.16%p).
- B) FY26 가이던스(28%, Q3 가이던스 중간값 근사) 시나리오는 '저평가
  가능성'(A등급, Gap +12.16%p)까지 완전히 뒤집는다.

**권고: 재실행(공식판정 승격)은 보류, FY26 Q4 실적(가이던스 이행 여부
확인 가능한 다음 분기)이 나온 뒤 재검토.** 가장 보수적인 시나리오조차
방향이 바뀐다는 사실 자체는 이 종목의 "트래커 최고 과대평가" 라벨이
매우 취약한 근거(trailing CAGR이 AI 수요 인플렉션을 구조적으로 못
봄) 위에 서 있음을 보여주지만, 검증 안 된 1개년 가이던스만으로
`realistic_growth_override`를 쓰는 것은 ROP 사례가 확립한 기준(다년
실적 필요)에 못 미친다고 판단했다. 결과는
`reports/keys_growth_crosscheck_2026-08-04.json`에 저장, 공식 ledger는
불변.

**형식상 특이사항**: 이번 배치는 "가벼운 검증" 방침(falsification_
conditions/ledger/Notion 정식 기록 생략)에 따라 6종목 모두 채팅 요약만
남겼다 - S/A등급 13종목과 달리 ledger나 Notion 페이지에 이 발견들이
아직 반영돼 있지 않다. KEYS 크로스체크 스크립트/리포트만 예외적으로
저장소에 남겨뒀다(판정 취약성이 구체적 수치로 확인된 유일한 사례라
재현성이 특히 중요하다고 판단).

**C등급 7종목 가벼운 정성검증 2차 배치 완료(2026-08-04)**: DSGX·CDNS·
GWRE·COR·TYL·VRT·WCN 조사(1차 배치와 동일하게 채팅요약만, 정식 ledger/
Notion 기록 생략). 판정을 뒤집을 근거는 없었으나 방향성 있는 발견들:

- **DSGX**: 방향 유지. 성장세는 안정적(+15%YoY 지속)이나 project44
  경쟁강도 데이터공백, SBC 미확인 - Confidence 79대 유지 권고.
- **CDNS**: 저평가 쪽 - Synopsys가 Ansys 인수로 커졌음에도 CDNS는
  백로그 사상최대·가이던스 2회 상향, Hexagon 인수($3.16B)로 멀티피직스
  영역 반격 중. DOJ/BIS 수출규제 건도 후속악재 없음(오히려 中라이선스
  요건 완화). SBC/OCF 26-27%(최초검증). Confidence 69(트래커 최저권)는
  오히려 낮게 잡혔을 가능성 - 중70대 상향 권고.
- **GWRE**: 신중 쪽 - Duck Creek 위협은 데이터상 미실현(GWRE 압도적
  우위 유지). 다만 **SBC/FCF 52.5%**(MNDY·DUOL급, 최초검증)+딜타이밍
  지연·영업총괄 교체+광범위 인사이더매도(38매도/0매수, 대부분
  10b5-1). Confidence 74→60-68대 권고.
- **COR**: 저평가 쪽 - 아편유사제 소송이 개방형 리스크에서 예정된
  상환일정(13년 분할)으로 전환, SBC/FCF 14-15%(양호, 최초검증),
  자사주매입 확대. PBM 수직계열화 위협은 아직 핵심유통 잠식 안 함
  (오히려 COR가 역통합 성공). Confidence 79 대체로 적정.
- **TYL**: 과대평가 쪽 - 펀더멘털 자체는 견조하나 **SBC/FCF ≈ 62%**
  (TTD·WDAY급, 최초검증) - SBC차감 기준 재계산시 Gap이 과대평가
  경계를 넘을 가능성. Confidence 94→고70/저80대 권고, sbc_cross_check
  정식 실행 필요.
- **VRT**: 사실상 동전던지기 - 이미 초박빙(+1.72%p)이던 Gap인데 Q2
  매출미스(수요문제 아닌 공급망지연)에도 시장이 -17~20% 반응, 자사주
  매입 중에도 주식수 오히려 증가(순희석, 최초확인). Confidence
  94→75-85대 권고.
- **WCN**: 저평가 쪽 - **ROP/BRO 패턴(M&A주도성장) 부재 확인** -
  진짜 가격주도 오가닉성장(M&A는 매출의 1.8%뿐), 자사주매입이 SBC의
  3배(최초검증). 다만 과거부터 미해결이던 FCF CAGR 불일치(~5% vs
  ~9-10%, 원인불명)는 여전히 안 풀림 - Confidence 74 대체로 적정.

**공통 테마**: TYL·GWRE 둘 다 SBC를 이번에 처음 확인했는데 트래커
최고 수준(52-62%)으로 나와, SBC차감 시 판정이 흔들릴 후보로 새로
식별됐다(WDAY가 유일하게 실제로 뒤집힌 선례).

**C등급 7종목 가벼운 정성검증 3차 배치 완료(2026-08-04/05) - B/C/D등급
20종목 전체 완료**: ZTS·KLAC·IDXX·WM·MCK·MNST·VRSN 조사(동일하게
채팅요약만). 이 배치로 2026-08-04에 시작한 B/C/D등급 20종목 확인
작업이 전부 마무리됐다(1차 6종목+2차 7종목+3차 7종목):

- **KLAC(핵심 발견)**: 저평가 쪽 - **KEYS와 완전히 동일한 구조적
  패턴**이 반도체장비 섹터에서 재확인됨. 가이던스된 공정제어 성장률
  (20%+)이 trailing CAGR 기반 Realistic Growth(10.33%)를 크게 상회.
  경쟁력도 오히려 강화(공정제어 점유율 50%→60-63%로 확대, Applied
  Materials는 13%→8%로 하락). SBC 7%(양호, 최초검증). Confidence
  79는 오히려 낮게 잡혔을 가능성 - 상향 권고.
- **ZTS**: 과대평가 쪽 - Q1 2026(5월 발표)에서 미국 반려동물부문
  -11%, 핵심 피부과 프랜차이즈(Apoquel/Cytopoint) -11%로 Elanco
  잠식이 실제 침식으로 확인(엔진 가중치 0.6이 오히려 정당함을
  뒷받침). 증권소송은 "경영진이 바로 이 리스크를 은폐했다"는 혐의라
  DRS가 못 보는 별도 거버넌스 리스크. 부채로 자사주매입 자금조달
  ($1.75B 전환사채)도 우려요인. Confidence 94→75-82대 권고.
- **IDXX**: 저평가 쪽 소폭 - 조사 전날(8/4) Q2실적이 9-10% 오가닉
  성장+가이던스 상향으로 엔진 추정치 상회. SBC 한자릿수 후반%(양호,
  최초검증). 미국 임상방문 16분기 연속감소는 여전한 구조적 리스크.
  Confidence 74 대체로 적정.
- **WM**: 진짜 동전던지기 확인 - 가격주도 성장은 견조하나 물량감소+
  가이던스 소폭하향. 신규발견: RNG(재생천연가스) 사업이 2026년
  목표($500M+ EBITDA) 대비 크게 지연(~$150M 수준). SBC 10-13%(양호,
  최초검증). Confidence 79 대체로 적정.
- **MCK**: 확인 - 아편유사제 소송은 COR와 마찬가지로 확정 상환일정
  으로 정리. PBM 수직계열화 위협은 실재·확대 중(0.25 가중치 정당함
  뒷받침). CFO·CSO 교체가 MMS분사 작업과 동시진행 중 실행리스크.
  조사 당일(8/5) 장마감 후 Q1 FY2027 실적발표 예정. Confidence 79
  대체로 적정.
- **MNST**: 확인/강화 - 코카콜라 유통망 활용 국제확장(中본토 물량
  +45%)은 강력한 상쇄요인이나, **Celsius 통합점유율(Alani Nu·
  Rockstar 인수후)이 엔진가정(~10%)의 약 2배(~21%)**로 확인 -
  competitor_intensity 입력값 재검토 필요. SBC 낮음(양호, 최초검증).
  조사 다음날(8/6) Q2실적 예정. Confidence 89→75-82대 권고.
- **VRSN**: 방향 유지 - 도메인 등록 사상최대+가이던스 상향, ICANN
  계약상 2027-2030 가격인상권(최대 연7%, 계약으로 이미 확정)은
  엔진이 못 보는 상방요인. 자사주매입이 SBC 상회(순희석 아님, 양호).
  다만 **버크셔 해서웨이 락업이 조사 6일 전(7/29) 만료**됐는데 후속
  매도 데이터 아직 없음 - 미해소 리스크. Confidence 89→80-85대 권고.

**공통 테마**: KLAC가 KEYS와 동일한 "trailing CAGR이 AI 수요
인플렉션을 못 봄" 문제를 재확인시켜 이 패턴이 반도체장비 섹터
전반의 구조적 이슈일 가능성을 시사한다. 반대로 ZTS·MNST 두 종목은
엔진의 경쟁강도 가정이 오히려 실제보다 관대했음이 드러난 사례 -
이 프로젝트가 "정성조사는 항상 판정을 강화하거나 신중하게 만든다"는
한쪽 방향 편향 없이 양방향으로 작동함을 보여준다.

**20종목 전체 마무리 - 형식상 특이사항**: 처음부터 끝까지 "가벼운
검증" 방침(falsification_conditions/ledger/Notion 정식 기록 생략)을
일관되게 유지했다 - S/A등급 13종목과 달리 이 20종목은 채팅 요약으로만
남아있다. 판정을 실제로 뒤집을 만큼 강한 근거가 나온 건 KEYS 하나뿐
이었고(크로스체크 스크립트로 별도 검증), 나머지는 전부 Confidence
재평가 수준에 그쳤다 - 공식 ledger/판정은 KEYS를 포함해 전부 불변.

## ⚠️ 알려진 한계 3건째 - 반도체/AI장비 섹터의 trailing CAGR 성장과소추정
(2026-08-05, KEYS/KLAC 2건 관측 기반 문서화)

BKNG/PDD가 "조회창이 과거 폭락기를 놓쳐 성장률을 과대추정"하는 한계였다면,
KEYS·KLAC는 정반대 방향의 별개 한계다 - **trailing CAGR 기반 Realistic
Growth가 현재 진행 중인 AI 수요 인플렉션을 구조적으로 과소추정**한다.
두 종목 모두 반도체 공정제어·테스트&측정 장비 업종이고, 둘 다 같은 패턴을
보였다:

- **KEYS**(2026-08-04 크로스체크, `scripts/keys_growth_crosscheck_2026_08_04.py`):
  trailing CAGR 기반 Realistic Growth 1.47% vs FY2026 자체 가이던스
  20%대 후반(AI 데이터센터 광통신/인터커넥트 테스트 수요, Q2 주문
  +56%YoY). 가장 보수적인 대체 시나리오(cyclical Lynch 상한 20%,
  trailing CAGR을 완전히 무시)조차 판정을 과대평가→적정가/경계선으로
  끌어올렸고, 가이던스 시나리오는 저평가 가능성까지 완전히 뒤집었다.
- **KLAC**(2026-08-05 가벼운 정성검증, 정식 크로스체크는 미실시):
  엔진 Realistic Growth 10.33% vs 가이던스된 공정제어 성장률 20%+
  (AI향 웨이퍼 공정제어 수요). 경쟁력도 오히려 강화 중(점유율
  50%→60-63%, Applied Materials는 13%→8%로 하락) - 즉 competition_intensity
  가정이 실제보다 관대했던 ZTS/MNST 패턴과는 반대로, 이쪽은 순수하게
  성장률 입력값 자체가 문제다.

**원인**: `realistic_growth_estimate()`가 매출·FCF의 과거 3y/5y/10y CAGR
가중평균에만 의존하는데, AI 데이터센터 투자 슈퍼사이클처럼 최근 1~2개
분기에서야 뚜렷해진 수요 인플렉션은 5년 CAGR 계산에 few-quarter 영향만
줘 희석된다. BKNG/PDD 한계(조회창이 짧아 과거 저점을 놓침)와는 원인이
반대다 - 저점이 아니라 **최근 변곡점을 못 본다**는 것이 문제.

**의도적으로 아직 코드 대응을 하지 않는다** - Simplicity First 원칙과
동일 판단: 관측 사례가 2건(KEYS/KLAC)뿐이고, 둘 다 반도체/AI장비
업종이라는 공통점은 있지만 "1개년 가이던스를 성장률에 어떻게 반영할
것인가"(가중치? 상한? 별도 필드?)에 대한 구체적 요구사항이 아직
불충분하다. KEYS 크로스체크가 이미 명시했듯, 검증 안 된 1개년 가이던스를
`realistic_growth_override`로 곧바로 승격하는 것은 ROP(다년 실현
오가닉실적)가 확립한 기준에 못 미친다.

**향후 신규 분석 시 지침**: 반도체 장비·AI 인프라 관련 종목에서
Realistic Growth가 trailing CAGR로 낮게 나오고 동시에 회사가 AI 수요를
근거로 큰 폭의 가이던스 상향을 제시하고 있다면, 판정을 확정하기 전에
가이던스 기반 대체 시나리오를 KEYS 크로스체크와 같은 방식으로 반드시
병기해서 볼 것(`data_limitations`에 자동 경고가 없으므로 분석자가
직접 챙겨야 한다). 최소 1~2개 분기의 실적으로 가이던스 이행이 확인된
뒤에야 공식판정 승격을 고려한다.

## TYL·GWRE 공식 SBC 크로스체크 - SEC 원자료 정식 검증(2026-08-05,
경량검증 추정치의 정확도 확인)

2026-08-04 C등급 2차 배치 경량검증에서 TYL·GWRE 둘 다 "SBC/FCF가
트래커 최고권"이라는 신규 발견이 나왔으나(TYL ≈62%, GWRE ≈52.5%),
둘 다 웹서치 기반 추정치였고 재현용 스크립트 자체가 저장소에 없었다
(BRO/BSY와 같은 패턴 - 과거 직접배선, 파일 미보관). `sbc_by_year`가
이미 엔진에 배선돼 있는 opt-in 필드(v3.23)라, 추정치를 그대로 믿기보다
SEC EDGAR XBRL companyconcept API(`us-gaap:ShareBasedCompensation`)
원자료로 정식 크로스체크하기로 했다.

- **GWRE**: `scripts/analyze_gwre_2026_07_28.py`에 FY2025(2024-08-01~
  2025-07-31) 실측 SBC $161,556K 배선. 결과 SBC/FCF **57.6%**(추정
  52.5%와 근접 - 경량검증 추정이 대체로 타당했음을 확인). SBC차감 시
  Gap +3.15%p→+1.85%p, 판정 불변(둘 다 중립밴드 안). ledger/GWRE_2026-08-05.json
  (구 GWRE_2026-08-02.json git rm 후 rename 처리).
- **TYL**: 재현용 스크립트가 아예 없어 `scripts/analyze_tyl_2026_07_26.py`
  신규 등록(ledger/TYL_2026-07-26.json 원본 입력값 그대로 전사, BRO
  선례와 동일 패턴) - FY2025(calendar 2025) 실측 SBC $151,276K 배선.
  **결과 SBC/FCF 24.4% - 경량검증 추정치(≈62%)의 약 1/3에 불과, 실질적
  정정.** 원인 추적: 경량검증 리서치 에이전트가 "FY2025 FCF $236.9M"을
  인용했으나, ledger에 이미 확정돼 있던 실제 FCF0는 $620.75M(OCF
  $653.5M − capex $32.8M) - 리서치 에이전트가 웹서치 2차 출처에서 잘못된
  FCF 수치를 인용한 것으로 보인다. SBC차감 시 Gap +2.77%p→-0.48%p, 판정
  불변(둘 다 중립밴드 안 - 정정에도 불구하고 판정에는 영향 없음). ledger/
  TYL_2026-08-05.json(구 TYL_2026-07-26.json git rm 후 rename 처리).

**교훈**: 경량검증(웹서치 기반 정성조사)의 정확도가 종목별로 편차가 크다
- GWRE는 근접했지만 TYL은 3배 가까이 틀렸다. 원인은 방법론 자체의
  체계적 편향이 아니라 개별 리서치 에이전트가 인용한 2차 출처 수치
  오류였다(같은 배치의 다른 종목들에서 유사 오류가 재현되는지는
  미확인). **SBC/FCF가 "트래커 최고권"처럼 판정에 영향을 줄 수 있는
  수치가 경량검증에서 새로 나왔을 때는, 이번처럼 SEC 1차자료로 정식
  검증하는 것이 안전하다** - 특히 판정 자체를 흔들 수 있는 결론(SBC차감
  시 밴드 경계 근접 등)을 채팅요약에만 남기고 넘어가지 않을 것.

두 종목 모두 공식 Gap/RAR/판정은 SBC 미차감 기준으로 그대로 유지되며
(`sbc_cross_check`는 병기, 자동판정 안 함 원칙), 재실행 후 diff로 SBC
필드 추가 외 다른 계산값 불변을 확인했다.

## v3.32 코드 감사 — 기록 무결성 4건 정정(2026-08-05, 사용자 요청 "개혁 필요
부분 스스로 철저히 찾아 완벽히 개혁")

지금까지의 감사는 전부 **방법론**(무엇을 계산하는가)을 봤다. 이번엔 **기록
무결성**(계산 결과가 저장소에 정직하게 남는가)을 봤고, 넷이 나왔다. 넷 다
"코드는 맞는데 남는 기록이 틀린" 유형이라 계산값은 하나도 안 바뀌었다 -
**33종목 전부 저장된 ledger 입력값으로 재실행해 Gap/RAR/DRS/Realistic
Growth/Implied Growth/판정/Confidence/강건성flip 8개 지표가 전건 완전 동일함을
확인했다**(v3.19 sensitivity 근본수정 때와 같은 검증 방식).

**① 버전 스탬프가 거짓말을 하고 있었다 (가장 심각)**
`run_analysis()` 본문에 `"engine_version": "v3.27"`이 **문자열 리터럴로**
박혀 있었다. v3.28에서 `realistic_growth_override`(계산 결과를 실제로 바꾸는
기능)를 배선하면서 아무도 그 리터럴을 올리지 않았고, v3.29~v3.31도 마찬가지였다.
결과: **`ledger/ROP_2026-08-04.json`은 v3.27로 스탬프돼 있으면서 v3.27에는
존재하지도 않는 필드(`realistic_growth_override_applied`)를 담고 있다.**
이 프로젝트가 v3.12 "가짜 버전 사건"에서 배운 게 정확히 이것인데(버전 라벨과
실제 코드가 어긋나면 재현 시 어느 코드로 계산된 값인지 특정 불가), 같은 일이
스탬프 쪽에서 반복되고 있었다. 리터럴을 함수 본문 800줄 안쪽에 두는 한 재발이
보장되므로 `engine/expectation_gap_engine.py` 최상단 `ENGINE_VERSION` 상수로
올리고 pipeline이 그것만 참조하게 했다(테스트:
`test_engine_version_comes_from_single_constant`). **새 기능을 배선하면 이
상수를 반드시 올릴 것** - CHANGELOG에 버전을 쓰면서 여기를 그대로 두면
ledger 전체가 다시 거짓말을 시작한다.
과거 ledger의 스탬프는 소급 수정하지 않는다(재실행 없이 스탬프만 고치면
그게 또 다른 허위기록이다) - 대신 **v3.27로 스탬프된 15건은 실제로는
v3.27~v3.31 사이 코드로 계산된 것**이라는 사실을 여기 기록해둔다.

**② 판정 규칙이 네 곳에 복사돼 있었고, 이미 어긋나 있었다**
±5%p 3단계 판정이 `run_analysis()`, pipeline의 SBC 병기 블록,
`expectation_gap_sensitivity_check()` 내부 `_judge()`, 크로스체크 스크립트
2개 - 총 4곳(스크립트 포함 5곳)에 독립 구현돼 있었다. 경계값은 같았지만
**중립 라벨이 이미 갈라져 있었다**: 셋은 "적정가/경계선"인데 sensitivity만
"적정가". 그래서 저장된 ledger 36건 중 **13건이 한 파일 안에서
`judgment="적정가/경계선"`과 `sensitivity_check.judgment_with_drs="적정가"`를
동시에** 들고 있었다. CLAUDE.md의 Simplicity First가 경고한 상황이 실제로
일어난 것이다("중복 자체가 두 계산이 미묘하게 어긋나는 새로운 버그를 만든다").
지금은 라벨만 어긋났지만, 다음에 경계값을 조정하는 사람이 넷 중 셋만 고치면
`judgment_flipped`(두 `_judge()` 출력의 != 비교)가 조용히 무의미해진다.
`judgment_from_gap()` 하나로 통합하고 나머지를 전부 호출부로 바꿨다 -
경계값·라벨은 하나도 바꾸지 않았다(다수파이자 ledger 최상위 필드가 쓰던
"적정가/경계선"으로 통일).

**③ 오버라이드가 캡 플래그를 지우지 않아 감점 근거가 틀려 있었다**
v3.28에서 `realistic_growth_override`를 배선할 때, 오버라이드로 캡을
우회한 뒤에도 `growth_breakdown["cap_applied"]`에 **이미 우회된 캡의 문구가
그대로 남아** 있었다. ROP가 실제 피해자다 - Realistic Growth는 5.5%인데
ledger의 `cap_applied`는 "상한 캡 적용(12.0%)"이라고 적혀 있고(자기모순),
그 값이 `confidence_score(lynch_type_cap_applied=...)`로 흘러가 **걸리지도
않은 캡을 근거로 -5점**이 붙어 있었다. 캡 플래그를 사실대로 `None`으로
되돌리되(원 문구는 `pre_override_cap_note`에 보존), 감점이 그냥 사라지면
Confidence가 근거 없이 오르므로 **동일 크기(-5)의 `realistic_growth_overridden`
항목을 신설**해 실제 원인이 감점을 이어받게 했다 - 오버라이드는 분석자가 직접
넣은 주관적 입력이라 신뢰도를 낮출 사유가 맞다. 결과적으로 **Confidence
총점은 불변**(ROP 89 유지)이고 감점 근거만 정정된다.

**④ 정정 후 폐기했어야 할 구 ledger 3건이 남아 저장소를 오염시키고 있었다**
"구 ledger는 git rm하고 새 파일로 통합한다"는 관행은 2026-07-28 이후 매번
지켜졌지만(GWRE/TYL/ROP/BRO 등), 그 관행이 자리잡기 **전**에 정정된
WM·WCN·IDXX 3종목은 구 파일(2026-07-25, v3.19 sensitivity 근본수정 **이전**
값: flip=True, Confidence 59/59/64)이 지워지지 않았다. 그래서 `ledger/*.json`을
그냥 세면 **33종목이 36건**으로 잡혔다.

이게 이미 실제 피해를 냈다 - **위 v3.26 항목의 RAR-DRS 상관 통계가 바로 이
중복으로 산출된 값이다**(하필 3종목 전부 RAR<0 집단이라 그 하위집단만
10건→13건으로 부풀려짐). 정정 내역은 v3.26 항목에 표로 추가해뒀다: RAR<0
하위집단 상관 **+0.285 → +0.067**로, "부호 반전"이라는 통계적 근거가 사실상
사라진다(경고 자체는 CDNS 기계적 검증이 뒷받침하므로 유지).
`rank_portfolio_2026_08_02.py`가 티커별 최신만 고르도록 방어적으로 짜여
최종 산출물은 무사했지만, 그건 그 스크립트 하나가 우연히 그랬을 뿐이다.
구 파일 3건을 git rm하고, **관행을 테스트로 고정했다**
(`tests/test_ledger_integrity.py`): 종목당 ledger 1건 / 파일명과 meta의
티커·날짜 일치 / 판정 라벨이 알려진 어휘인지. 문서로만 둔 규칙이 지켜지지
않는다는 걸 이 프로젝트는 이미 세 번 겪었다(run_self_check·confidence_score·
claim/lock) - 네 번째를 만들지 않는다.

**부수 정정**: 이분탐색 로그의 `implied_cap`에 시가총액 대신 성장률 추정치
(`g_guess`와 동일한 값)가 들어가던 복붙 버그를 고쳤다. 로그 전용이라 계산에는
영향이 없었으나, 수렴이 이상할 때 원인을 추적하는 유일한 단서가 통째로 없는
셈이었다. ROP 크로스체크 스크립트가 출력하던 "공식 기록: Realistic Growth
12.00%"도 v3.28 승격 이후 사실이 아니게 된 하드코딩이라 ledger에서 읽도록
고쳤다.

**이번 감사에서 확인했지만 고치지 않은 것**: `cross_check_prior_record()`가
25개 분석 스크립트 중 **1개(BKNG)에서만** 호출된다. CLAUDE.md는 "과거 기록이
있는 종목을 재검증할 때" 쓰라고 규정하지만 사실상 미사용 경로다. 다만 이걸
자동화하려면 `run_analysis()`나 `save_ledger()`가 디스크의 다른 ledger를 읽는
부작용을 갖게 되는데, 그러면 골든테스트가 실제 ledger 유무에 따라 흔들릴 수
있다(테스트가 저장소 상태에 의존하게 됨). 실증된 요구(재검증 시 대조 누락으로
실제 사고가 난 사례)가 아직 없어 Simplicity First 원칙대로 배선하지 않고
기록만 남긴다 - 필요해지면 `save_ledger()`가 같은 티커의 직전 ledger를 찾아
`cross_check_prior_record()`에 위임하고 결과를 저장 JSON에 병기하는 방향이
가장 부작용이 작다(모든 스크립트가 이미 `save_ledger()`를 부르므로 배선
누락이 구조적으로 불가능하다).

## ETF 자체 밸류에이션 비교 - 새 분석 유형 신설(2026-08-06, 사용자 요청
"voo같은 미국에 상장된 etf 조사하라고, 개별 종목이 아니라")

지금까지의 모든 스크리닝/분석은 개별 기업(10-K 재무제표 보유)을 대상으로
`engine/pipeline.py`의 `run_analysis()`를 돌리는 구조였다. ETF는 자체
재무제표(revenue/OCF/capex)를 내지 않으므로(NAV·보수율·기초자산 집계만
공시) 이 엔진을 ETF 티커에 직접 적용할 수 없다 - 사용자가 ETF 구성종목이
아니라 **ETF 인스트루먼트 자체**의 저평가/고평가를 물었을 때 이 구조적
한계가 처음 드러났다.

대응: `run_analysis()`를 우회하지 않고, 지수 수준 집계 밸류에이션 지표
(P/E·배당수익률·최근 수익률)로 여러 ETF를 상대비교하는 **별개의 분석
유형**을 신설했다 - `reports/etf_valuation_comparison_2026-08-06.json`.
이건 screen()/run_analysis()의 대체가 아니라 이 프로젝트가 다루지 못하던
질문("ETF 자체가 싼가")에 대한 첫 시도다.

**핵심 발견 - IWM(러셀2000) 밸류에이션 논쟁, 단일 지표 함정의 실전 사례**:
stockanalysis.com 기준 IWM은 이번 조사 3대 광범위지수 ETF(VOO/QQQ/IWM) 중
P/E가 가장 낮으면서(20.07x) 1년 수익률은 가장 높다(+37.84%) - 통계적으로
이상적인 "싸면서 오르는" 조합처럼 보인다. 그러나 다른 출처(Goldman Sachs)는
정반대로 "Russell2000이 forward 26x로 오히려 Nasdaq100(24x)·S&P500(20x)보다
비싸다"고 본다. 원인은 Russell2000 구성종목 중 무이익/저이익 기업 비중이
커서 P/E 집계방식(트레일링 단순평균 vs forward 애널리스트추정 가중)에 따라
결론이 통째로 뒤집히는 지수 고유의 계산 왜곡 - 실제로 "valuation trap" 경고가
나와 있는 상태다. **한쪽 출처의 숫자만 보고 결론냈다면 정반대로 틀렸을 것** -
PDD의 competition_intensity 상수 문제, BKNG의 조회창 문제와 같은 계열의
교훈("단일 지표를 검증 없이 확정하지 말 것")이 ETF 레벨에서도 그대로
재현됐다.

**다른 발견**: S&P500(VOO) 자체는 forward P/E 19.5~19.6x로 5년/10년 평균과
거의 같아 지수 전체 차원에서는 뚜렷한 기회도 위험신호도 없다(개별종목
스크리닝에서 ADBE/CRM 같은 뚜렷한 저평가가 나온 것과 대비됨 - 시장 평균은
평범한데 그 안의 개별 종목 misprising은 여전히 존재한다는 뜻). 8개 ETF
표본 중 XLF(금융)가 P/E 최저(17.85x)면서 동시에 1년 수익률도 최저(+13.47%) -
시장이 아직 재평가 안 했거나 정당한 할인일 수 있어 추가 검증 대상.

**이 분석 유형의 한계**: (1) DIA의 P/E·배당수익률 미확보, (2) XLU 수익률
부진의 원인(금리민감도 vs 펀더멘털) 미확인, (3) 국제/신흥국·채권/리츠 ETF는
아직 다루지 않음(주식형 미국상장 ETF만), (4) engine 코드에 배선된 게 아니라
WebSearch 기반 스냅샷이라 재현 가능성이 회사 단위 분석(SEC EDGAR/ledger)보다
낮다 - 다음에 이 유형을 다시 쓸 때는 이번처럼 매번 새로 조사할지, 아니면
전용 스크립트로 고정할지 실증사례가 더 쌓인 뒤 결정할 것(Simplicity First
원칙과 동일 판단).

## v3.33 — ETF 분석 엔진 신설(2026-08-06, 사용자 요청 "etf 스크리닝과 분석을
위한 코드 작성. IRS수준만큼 전문적으로")

바로 위 항목(ETF 자체 밸류에이션 비교)은 WebSearch 스냅샷을 JSON 리포트로만
남긴 1회성 조사였고, "engine 코드에 배선된 게 아니라 재현 가능성이 회사 단위
분석보다 낮다"는 한계를 스스로 적어뒀었다. 이번에 그 한계를 코드로 해소했다.

**구조**: `engine/etf_engine.py`(순수함수) + `engine/etf_pipeline.py`
(오케스트레이션·ledger) + `tests/test_etf_engine.py`(28건) +
`scripts/analyze_etfs_2026_08_06.py`(실측 7종목). 회사 엔진의
expectation_gap_engine/pipeline 2단 구조를 그대로 따랐다.

**왜 회사 pipeline에 안 끼워넣었나**: `AnalysisInputs`는 revenue/OCF/capex
시계열을 필수로 요구하는데 ETF에는 그게 존재하지 않는다. 억지로 합치면 절반이
항상 None인 필드가 되고 `run_analysis()` 본문이 "ETF면/회사면"으로 갈라진다 -
Simplicity First의 "강제 분기보다 opt-in" 원칙에 정면으로 어긋난다. 대신
**공유 원시함수는 전부 재사용**했다: `implied_growth_from_fcf_yield`(Gordon
역산은 분자가 FCF든 이익이든 동일), `erp_from_drs`(ERS를 DRS와 같은 0~100
스케일로 설계해 그대로 사용), `judgment_from_gap`(v3.32에서 단일화한 유일한
판정 규칙 - ETF라고 사본을 만들지 않았고, 테스트로 고정했다).

**핵심 설계 근거 - IWM 사건을 코드로 만든 것**: P/E를 단일 스칼라가 아니라
`{출처명: P/E}` 딕셔너리로 받는다. 2026-08-06 실측에서 같은 IWM의 P/E가
stockanalysis 20.07x(트레일링)와 Goldman Sachs 26x(forward)로 갈렸고 그 차이가
"3대 지수 중 가장 쌈"과 "가장 비쌈"이라는 정반대 결론을 만들었기 때문이다.
엔진은 출처별로 Gap·판정을 각각 계산해 `judgment_flipped_across_sources`를
최상위로 보고한다(sbc_cross_check의 병기 원칙과 동일 구조).

**⚠️ 구현 중 테스트가 잡아낸 중요한 성질 - `flipped=False`를 안전으로 읽지 말 것**:
처음엔 "IWM은 판정이 갈린다"를 상수처럼 단정하고 테스트를 썼는데 실패했다.
확인해보니 **P/E 괴리는 Gap에 일정한 폭(IWM의 경우 약 1.16%p)을 만들 뿐,
그 폭이 ±5%p 판정 경계를 가로지르는지는 r·성장률이 어디 놓이느냐에 달려
있었다** - 같은 IWM 데이터가 r=0.09에서는 갈리고 r=0.1081에서는 안 갈렸다.
즉 판정 일치는 데이터 신뢰도가 확보됐다는 뜻이 아니라 우연히 같은 구간에
들어왔다는 뜻일 수 있다. 그래서 괴리가 임계값(20%)을 넘는데 판정만 일치하는
경우에도 `[판정 우연 일치 주의]` 경고를 별도로 남기도록 배선했다(테스트:
`test_high_divergence_without_flip_still_warns`). 가설을 실측이 기각한
사례라 그대로 기록해둔다.

**ERS(ETF Risk Score)**: DRS와 같은 0~100 스케일, 5개 항목(집중도/보유종목수/
보수율/P/E출처괴리/무이익기업비중) 평균 x5. 구간 임계값은 **검증된 값이
아니다** - 8종목 관측치 기반 시작점이며 LYNCH_TYPE_CAPS·demand_sensitivity
앵커표와 동일하게 취급할 것. `pct_unprofitable_constituents`를 모르면 그
항목을 빼되 제외 사실을 `data_limitations`에 남긴다(조용히 관대해지지 않게).

**의도적으로 넣지 않은 것들**: (1) 배당성향 반영 Gordon - 재투자수익률(ROE)
가정이 주관적 입력으로 하나 더 늘어나 실증사례 없이는 과설계. 대신 "이익
전액 귀속 가정"이라는 한계를 docstring에 명시. (2) Fed모델 스프레드는
계산해서 병기만 하고 **판정에 쓰지 않는다**(논쟁적 지표 - is_insurer의 P/B를
임계값 없이 숫자만 주는 것과 동일 처리). (3) ETF간 보유종목 중복(VOO+QQQ+XLK를
같이 사면 메가캡 기술주 3중 계상) 측정 - 구성종목 전체 데이터가 필요한데
FMP ETF holdings API가 상위플랜 전용이라 확보 못함, 실증 필요시 배선.

**ledger는 `ledger_etf/`에 분리 저장**한다. 회사 `ledger/`에 섞으면
`tests/test_ledger_integrity.py`가 회사 스키마(judgment/sensitivity_check 키)를
가정하고 전수 파싱하다 깨진다 - 스키마가 다른 두 기록을 한 폴더에 섞지 않는다
(테스트로 고정: `test_ledger_dir_is_separate_from_company_ledgers`).

**실측 결과(7종목)**: 전부 "적정가/경계선" 밴드 안이고 IWM만 출처간 판정
불일치. 보수적 Gap(가장 비싼 P/E 기준) 기준 XLK +3.57%p > QQQ +2.69%p >
XLF +1.64%p > VOO +1.13%p > XLU -1.74%p > XLE -2.33%p 순.
**섹터 ETF 4종(XLK/XLE/XLF/XLU)은 P/E 출처가 1개뿐이라 IWM 같은 집계방식
왜곡을 검증할 수단 자체가 없다** - 다음 조사 시 forward P/E를 추가 확보할 것
(엔진이 `[단일 출처 경고]`로 자동 표시한다).

## v3.34 — ETF 엔진 자체 진단 후 구조 수정(2026-08-06, 사용자 요청 "테스트해보고
보완 및 수정 필요한 부분 진행")

v3.33을 만든 직후 스스로 테스트해 **치명적 결함 1건**을 찾아 고쳤다.

**결함: 주관적 성장률 입력이 결과를 사실상 단독 결정하고 있었다.**
Gap = `expected_earnings_growth - implied_growth`인데 `implied_growth`는
`expected_earnings_growth`와 **완전히 독립**이다. 따라서 Gap의 성장률 민감도는
정확히 1:1이고, 판정 밴드가 ±5%p뿐이라 분석자가 성장률을 2%p만 달리 잡아도
판정이 뒤집힌다. 실측 진단:
  - ERS가 만드는 r 스프레드 0.84%p vs 성장률 가정 스프레드 8.00%p → **9.5배**
  - **7개 ETF의 성장률 가정을 전부 8%로 통일했더니 순위가 거의 정반대로
    뒤집혔다**(XLK 2위→7위, XLE 7위→2위). 즉 v3.33의 순위는 엔진이 계산한 게
    아니라 내가 타이핑한 값을 되비춘 것이었다.
CLAUDE.md M-1(성장상한 바인딩)과 같은 계열이되, 고정 규칙조차 없어 더 나빴다.

**검토한 방향과 기각 사유**(냉철한 비교 결과):
| 방향 | 판단 |
|---|---|
| A. 지수 EPS 실적 CAGR로 앵커(회사 엔진처럼) | 원리적 최선이나 **데이터 접근 불가** - S&P500은 되지만 섹터SPDR·러셀2000 지수 EPS 시계열은 유료. 일부만 앵커하면 ETF간 비교 불가능해져 오히려 악화. **목표 종착점으로만 기록** |
| **B. 프레이밍 역전(채택)** | 신규 데이터 0, 이미 계산 중인 값 활용, 결함을 정면 해소 |
| C. 성장률 제거·내재성장률 단독 순위 | 섹터간 비교가 무의미해짐(기술주가 에너지보다 빨리 크는 건 정당) - 기각 |
| D. 섹터별 성장률 앵커표 | demand_sensitivity 앵커표는 34종목 실측 기반인데 나는 관측치 0 - **"근거 없이 유지하던 걸 근거 없는 다른 숫자로 바꾸는 것은 개선이 아니다"** 원칙에 위배, 기각 |
| E. 문서화만 | 요청 수준 미달 |

**B의 구현**: `required_growth_thresholds()` 신설 - "내가 X% 성장을 본다 →
따라서 싸다"가 아니라 **"시장은 이미 몇 %를 요구하고 있고, 저평가라 부르려면
몇 %를 믿어야 하는가"**를 내놓는다. `breakeven`(=내재성장률)은 P/E와 r에서만
나오므로 분석자 주관이 개입할 수 없는 **유일한 객관적 비교축**이다.
`growth_sensitivity()`로 ±2%p 밴드에서 판정이 유지되는지 보고, 취약하면
`[성장률 가정 취약]` 경고 + `compare_etfs`에서 후순위로 밀어낸다(IWM의
출처갈림 처리와 같은 구조). 비교표도 객관값(시장요구성장)을 앞에, 주관 오염값
(Gap)을 뒤에 두도록 열 순서를 바꿨다.

**부수 정정 - 보수율을 위험점수에서 성장률 차감으로 이동**: v3.33은 보수율을
ERS(위험점수)의 한 항목으로 넣고 정작 Gap에서는 빼지 않았다. 둘 다 틀렸다 -
보수율은 **불확실성이 아니라 계약으로 확정된 비용**이라 위험 프리미엄의 입력이
될 수 없고(ERS→ERP 경로), 동시에 투자자 수익에 직접 미치는 경로에는 반영이
안 돼 VOO(0.03%)와 액티브 ETF(0.95%)가 Gap 상에서 동일 취급됐다. 이제
`net_expected_growth()`가 성장률에서 직접 차감하고 ERS에서는 제거했다
(`check_deceleration_double_count`가 경고하는 이중반영 회피).

**엣지케이스 점검 결과 이상 없음**: P/E 극단값(5x~1000x)에서도 Gordon 역산이
발산 없이 수렴하고, 내재성장률이 r에 점근할 뿐 초과하지 않음을 확인.

**회사 엔진 영향 없음**: 33종목 전건 재실행해 8개 핵심지표 완전 동일 확인
(공유 함수는 읽기만 했고 수정하지 않았다). 테스트 159 → 165개.

**남은 한계(정직하게 기록)**: 이 수정은 성장률 지배 문제를 **없앤 게 아니라
드러낸 것**이다. Gap과 순위는 여전히 주관적 성장률에 1:1로 좌우되며, 근본
해결은 방향 A(지수 EPS 실적 앵커)뿐인데 데이터 접근이 막혀 있다. 그때까지는
`required_growth.breakeven`을 비교축으로 쓰고, 그 값이 각 지수에서 실현
가능한지는 **사람이 판단해야 한다**.

## v3.35 — ETF 엔진 보완: 판정 밴드 중복 제거 + 성장률 실적 앵커 배선
(2026-08-06, 사용자 요청 "보완점 및 한계 수정 진행")

v3.34가 "남은 한계"로 적어둔 항목들을 실제로 고쳤다.

**① 판정 경계값이 또 중복돼 있었다(v3.32 죄의 3회차)**
v3.32에서 판정 *규칙*은 `judgment_from_gap()` 하나로 단일화했지만 **경계값
숫자(0.05)는 여전히 함수 안 리터럴**이었고, v3.34에서 ETF 엔진의
`required_growth_thresholds(band=0.05)`가 같은 값을 독립적으로 또 하드코딩하며
중복이 되살아났다. "저평가가 되려면 필요한 성장률"은 정의상 판정 경계와 **같아야**
하는데 두 곳에 따로 적혀 있으면, 한쪽만 바꿨을 때 엔진이 "저평가라 부르려면 X%
필요"라고 안내해놓고 정작 그 X%에서 저평가 판정을 안 내리는 자기모순이 생긴다.
`JUDGMENT_BAND` 상수로 뽑아 양쪽이 같은 값을 쓰게 하고, **안내값을 그대로 믿으면
실제로 그 판정이 나오는지**를 검증하는 테스트로 고정했다
(`test_judgment_band_is_single_source`).

**② 방향 A(지수 EPS 실적 앵커) 배선 - v3.34가 "근본 해결은 이것뿐"이라 한 항목**
v3.34는 데이터 접근 불가를 이유로 방향 A를 보류했는데, 그건 **데이터와 기능을
혼동한 판단**이었다. 기능이 없으면 데이터를 구해도 쓸 곳이 없다. 그래서 계산부를
먼저 배선했다 - 이 프로젝트가 `capex_intensity_from_series`(v3.6에 만들고 v3.20에
배선)·`cagr_base_year_override`에서 이미 쓴 패턴 그대로다.
  - `realized_eps_growth()`: 지수 EPS 시계열 → CAGR. **회사 엔진의 `_cagr`을
    재사용**해 시작값 음수일 때 복소수가 조용히 반환되는 v3.19 함정을 그대로 막는다.
  - `growth_anchor_cross_check()`: 분석자 가정을 실적 CAGR과 대조해 3%p 이상
    벌어지면 경고. **자동으로 덮어쓰지 않는다** - `insurer_cross_check`와 같은
    "병기, 자동판정 안 함" 원칙이다(지수 EPS 실적 CAGR도 미래의 정답은 아니고
    경기 사이클·구성종목 교체·기저효과가 섞여 있기 때문).
  - `growth.basis_type`: `"observed_anchored"` vs `"analyst_estimate"`를 결과에
    남긴다. **두 유형을 섞어 순위를 매기면 사과와 오렌지를 비교하는 셈**이라,
    앵커가 없는 ETF에는 `[성장률 앵커 없음]` 경고가 붙는다.
현재 7종목 전부 `analyst_estimate`다 - S&P500·나스닥100 지수 EPS부터 확보되는
대로 채우면 즉시 앵커가 작동한다.

**③ ETF ledger 무결성 테스트 신설**
회사 ledger에서 겪은 "구 파일이 안 지워져 33종목이 36건으로 집계되고 그 중복이
CLAUDE.md 통계까지 오염시킨" 사고(v3.32)를 ETF 쪽에서 반복하지 않도록 같은
규칙을 걸었다(종목당 1건 / 파일명-meta 일치 / 회사 디렉터리 혼입 금지).
**실제로 이번 작업 중 UTC 날짜가 바뀌며 같은 ETF 파일이 두 날짜로 생길 뻔했다** -
규칙이 없으면 정확히 같은 일이 벌어진다는 게 바로 확인된 셈이다.

**테스트 165 → 174개. 회사 엔진 33종목 전건 재실행해 8개 핵심지표 완전 동일
확인.**

**v3.35 후반부에서 위 한계 중 2건을 실제 데이터로 해소**(같은 세션, 검색
쿼터 회복 후):

- **섹터 ETF 4종 2차 P/E 출처 확보** - allinvestview.com(2026-08-06 기준)에서
  트레일링 P/E를 추가로 받아 XLK/XLE/XLF/XLU 전부 `[단일 출처 경고]`를
  해소했다. 실측 괴리: XLF 1.0%(17.85 vs 18.03) < XLE 5.5% < XLK 6.9% <
  **XLU 11.6%(22.88 vs 20.50)**. 같은 "트레일링"끼리도 10%대 차이가 나는
  경우가 있다는 게 확인된 셈이라, IWM의 트레일링 vs forward 괴리(29.5%)만
  특수한 게 아니었다. XLU는 이 괴리 때문에 ERS가 70.0 -> 83.3으로 올랐다.
- **DIA 추가** - v3.34까지 P/E 미확보로 빠져 있던 종목을 채워 8종목이 됐다
  (P/E 25.57x, 보수율 0.16%, 31종목).

- **⭐ VOO에 지수 EPS 실적 앵커 실제 배선 - 방향 A가 처음으로 작동했다.**
  multpl.com의 S&P500 EPS(2014~2025)를 넣었더니 실적 CAGR **4.97%(실질)**,
  인플레 2.5% 가정으로 환산해 **명목 7.59%**가 나왔고, 내가 시장 통념으로
  타이핑했던 8.00%와 **0.41%p밖에 차이가 나지 않았다**(허용폭 3%p 이내라
  경고 없음). 즉 VOO의 성장률 가정은 이제 "통념"이 아니라 **관측으로
  뒷받침된 값**이고, `basis_type="observed_anchored"`로 라벨이 붙는다.
  나머지 7종목은 여전히 `analyst_estimate`.

  ⚠️ **이 과정에서 단위 함정을 하나 밟을 뻔했다**: multpl.com의 EPS는
  **실질(constant June 2026 dollars)** 기준인데 기대성장률은 명목이다.
  그대로 비교했으면 인플레이션율(연 2~3%p)만큼 조용히 어긋나 ±5%p 판정
  밴드를 넘길 수 있었다 - RAR 100배 사고와 같은 계열의 단위 사고다. 그래서
  `realized_eps_basis`("nominal"|"real")를 **필수**로 받고, "real"이면
  `inflation_for_conversion`까지 요구해 `to_nominal_growth()`로 환산하도록
  코드가 막는다(테스트: `test_real_vs_nominal_eps_unit_trap_is_guarded`).
  또한 기준연도를 2020(코로나 저점 EPS 120.68)으로 잡으면 실질 CAGR이
  15%대로 튀어 - BKNG에서 배운 저점 기저효과 그대로 - 저점을 피해 전체
  구간(2014~2025)을 썼다.

**여전히 남은 한계**: (1) 지수 EPS 앵커는 VOO 1건뿐 - 섹터SPDR·러셀2000·
나스닥100 지수 EPS 시계열은 여전히 유료라 미확보. (2) ETF간 보유종목
중복(VOO+QQQ+XLK의 메가캡 3중 계상)은 미측정 - 구성종목 전체 데이터 필요.
(3) ERS 임계값은 8종목 관측 기반 시작점으로 검증되지 않았다. (4) 확보한
섹터 2차 출처가 전부 트레일링이라, IWM을 갈라놓은 **트레일링 vs forward**
축은 섹터 ETF에서 아직 검증되지 않았다.

## v3.36 — ETF간 중복노출 측정 신설(2026-08-07, "계속해서 진행")

v3.35가 남긴 한계 중 **"ETF간 보유종목 중복 미측정"**을 해소했다. 이건 단순한
보완이 아니라 **엔진이 구조적으로 볼 수 없던 위험**이었다 - `run_etf_analysis()`는
ETF를 하나씩만 평가하므로, 여러 개를 함께 살 때 생기는 문제를 원리적으로
드러낼 수 없다.

**실측 결과(2026-08-07 stockanalysis.com top10)**:

| 조합 | 공통종목 | 겹침(하한) |
|---|---|---|
| QQQ + VOO | 9종목 | **34.5%p** |
| QQQ + XLK | 6종목 | **31.9%p** |
| VOO + XLK | 5종목 | **23.1%p** |

**NVDA·AAPL·MSFT는 세 ETF 모두에 들어 있다.** 즉 "시장 전체(VOO) + 성장주(QQQ)
+ 기술섹터(XLK)"로 분산했다고 믿는 조합이 실제로는 같은 메가캡 몇 개를 세 번
사는 것에 가깝다. 개별 판정은 셋 다 "적정가/경계선"이라 이 위험이 전혀 드러나지
않는다 - 정확히 그래서 별도 측정이 필요했다.

**측정 방식**: 공통 종목별 `min(비중_A, 비중_B)`의 합 = 두 펀드가 공유하는 최소
공통 노출. **이 값은 실제 겹침의 하한(lower bound)이다** - top10만 봤기 때문이며,
나머지 구성종목도 겹치므로 진짜 겹침은 더 크다. 함수 자체는 top10에 한정되지
않으므로 전체 구성종목 데이터가 확보되면 그대로 넣어 정확한 값을 낼 수 있다.

**설계 원칙**: 겹침은 개별 ETF 판정에 **영향을 주지 않는다**(테스트로 고정:
`test_overlap_does_not_affect_individual_judgment`). 겹침은 본질적으로 "여러 개를
같이 살 때"의 문제이지 특정 ETF가 나쁘다는 뜻이 아니기 때문이다 - 회사 엔진의
"병기, 자동판정 안 함" 원칙과 같은 계열이다. `top10_holdings`가 없는 ETF는
조용히 건너뛰지 않고 `skipped_no_holdings`에 남긴다(**데이터 없음을 '겹침 없음'
으로 오독하면 안 된다**).

**이번에 의도적으로 하지 않은 것 - 섹터 forward P/E**: v3.35가 "트레일링 vs
forward 축이 섹터에서 미검증"이라 남겼기에 시도했으나, 확보 가능한 섹터 forward
P/E가 FactSet 2026-04 자료(4개월 stale)뿐이었다. **8월 트레일링과 4월 forward를
나란히 넣으면 시점차가 집계방식차로 둔갑해 가짜 괴리 신호를 만든다** - 엔진이
이미 `[단일 출처 경고]`로 정직하게 표시하고 있는 편이 낫다고 판단해 넣지 않았다
(TYL에서 2차 출처 수치를 검증 없이 인용해 SBC/FCF를 3배 틀렸던 교훈과 같은
판단). 현재 시점의 섹터 forward P/E가 확보되면 그때 넣을 것.

**테스트 176 → 181개.**

**여전히 남은 한계(v3.37에서 갱신)**: (1) 겹침은 top10 기준 하한 - 전체
구성종목 데이터 필요. (2) 지수 EPS 앵커는 여전히 VOO 1건. (3) 섹터 트레일링
vs forward 축 미검증. (4) ERS·겹침 임계값 모두 관측 기반 시작점으로
검증되지 않았다.

## v3.37 — `holdings_overlap()` 해석 함정 정정 + 나머지 5종목 top10 확보
(2026-08-07, "계속 보완")

v3.36을 커밋한 직후 8종목 전체 조합으로 겹침 매트릭스를 처음 돌려봤을 때
**결함을 하나 더 발견했다** - "XLF+VOO = 0.0%p"가 그대로 출력됐는데, 이건
명백히 틀린 결론이다. XLF(금융 섹터 SPDR)의 구성종목(JPM/BRK.B/V/MA/BAC…)은
**정의상 전부 S&P500 소속**이라 VOO가 이미 그 전부를 담고 있다 - 0.0%p는
"안 겹친다"가 아니라 "top10 표본끼리 우연히 안 겹쳤다"는 뜻이다. 이 상태로
나갔다면 "섹터 SPDR을 광범위지수 ETF와 같이 담아도 분산 효과가 있다"는
**정반대의 결론**을 유도했을 것이다 - v3.36이 막으려던 바로 그 착시(같은
메가캡을 여러 번 사는 걸 분산으로 착각)를 다른 경로로 재발시키는 셈이었다.

**원인**: `holdings_overlap()`이 `shared_weight = 0`과 "실측 결과 겹침이
0"을 구분하지 않았다. n_common=0이면 계산상 shared_weight도 기계적으로
0이 되는데, 두 경우가 결과 dict 상에서 똑같이 보였다.

**수정**: `holdings_overlap()`에 `informative`(bool)와 `interpretation`
(설명 문자열)을 추가해 "실측된 낮은 겹침"과 "표본이 안 겹쳐 측정 불가"를
구분한다. `portfolio_overlap_report()`는 이제 쌍을 `pairs`(실측, 정렬·경고
대상)와 `uninformative_pairs`(측정 불가, 정렬·경고에서 제외)로 나눠
반환하고, `format_overlap_table()`은 uninformative 쌍을 "0.0%p"가 아니라
"측정 불가(모름)"로 렌더링한다. 회사 엔진의 `is_insurer`/`sbc_cross_check`와
같은 "데이터 없음을 유리한 값으로 오독하지 않는다" 원칙을 겹침 측정에도
그대로 적용한 것이다. 스크립트 출력에도 이 함정을 명시하는 경고문을 추가했다.

**부수 작업 - 나머지 5종목(DIA/XLF/XLE/XLU/IWM) top10 보유종목 확보**:
v3.36 시점엔 VOO/QQQ/XLK 3종만 top10_holdings가 있었다. 이번에 stockanalysis.com
에서 나머지 5종도 확보해 **8종목 전부**가 겹침 측정 대상이 됐다(28개 쌍 중
7개만 실측 가능 - 나머지 21개는 top10 표본 비교로는 측정 불가로 정직하게
남는다). 실측 겹침(내림차순): QQQ+VOO 34.5%p > QQQ+XLK 31.9%p > VOO+XLK
23.1%p > XLF+DIA 14.3%p > QQQ+DIA/DIA+XLK/VOO+DIA 각 4~5%p대.

**회사 엔진 영향 없음**(로직 변경이 ETF 전용 파일에만 있었고, 공유 함수는
읽기만 함). `ENGINE_VERSION`을 v3.37로 올렸다(v3.32 규칙: 배선 변경 시 반드시
상수를 올릴 것). 테스트 181 → 184개.

## v3.38 — KRX 래퍼 ETF 엔진 신설(2026-08-07, 사용자 요청 "한국 isa 계좌에서
살 etf를 찾기 위해 만든 것 - 국내 상장 미국 ETF에 중점을 맞춰서 진행")

**경위 - 이 ETF 엔진 전체의 실제 목적이 뒤늦게 드러났다.** v3.33~v3.37에서
VOO/QQQ/IWM/XLK/XLE/XLF/XLU/DIA 등 **미국 상장** ETF를 분석해왔는데, 사용자가
이걸 만든 진짜 이유는 한국 ISA 계좌에서 살 ETF를 찾는 것이었다. **ISA 계좌는
국내 상장 ETF만 매수 가능**하므로 지금까지 분석한 미국 원본 자체는 매수
대상이 아니고, 실제 매수 대상은 그 지수를 추종하는 **국내 상장 ETF**(TIGER/
KODEX/ACE 미국S&P500 등)다.

**핵심 설계 판단 - 밸류에이션을 다시 계산하지 않는다.** 국내 상장 추종형 ETF는
미국 원본과 같은 주식 바스켓을 담는다 - P/E와 이익성장률은 화폐 표시와 무관한
값이라(원화 환산이 분자·분모에 동일 배율로 적용돼 P/E 자체는 불변), 같은
지수를 추종하는 국내 ETF는 이미 계산된 미국 원본의 P/E·성장률·위험점수를
**그대로 재사용**할 수 있다. `engine/krx_etf_pipeline.py::run_krx_wrapper_analysis()`
는 `ledger_etf/`의 저장된 미국 원본 결과에서 `ETFInputs`를 재구성해
`run_etf_analysis()`를 다시 호출하는 방식으로 이를 구현했다 - 새 밸류에이션
로직을 단 한 줄도 추가하지 않았다(Simplicity First가 반복 경고한 "중복 계산이
두 계산을 미묘하게 어긋나게 만든다"는 함정을 원천 차단). 바뀌는 입력은 딱
하나, `expense_ratio`(국내 상장분 자체의 총보수)뿐이다.

**재사용하지 않는 것 - 국내 래퍼 고유의 비용/구조**(`engine/krx_etf_engine.py`):
  - **총보수차**: `expense_ratio_delta()` - 국내 상장분은 미국 원본(VOO
    0.03%)보다 대체로 높다(0.06~0.5%대). 확정 비용이라 Gap에 그대로 반영된다.
  - **환헤지 비용**: `hedge_cost_warning()` - 헤지형(상품명 "(H)")은 한미
    금리차만큼의 캐리비용이 별도로 발생하는데, 이건 총보수처럼 고정값이
    아니라 금리차에 따라 변동하므로 **자동으로 Gap에 반영하지 않는다** -
    `is_insurer`/`sbc_cross_check`와 동일한 "병기, 자동판정 안 함" 원칙. 값을
    추정치로 넣으면 병기하고, 안 넣으면 "[환헤지 비용 미반영]" 경고를 남긴다.
  - **순자산/유동성**: 스프레드·상장폐지 리스크에 영향을 주지만 밸류에이션과는
    무관하므로 비교표에 병기만 한다.

**의도적으로 다루지 않는 것 - 레버리지/인버스/커버드콜/액티브 상품**: 이들은
기초지수를 그대로 복제하지 않아(레버리지는 일별 수익률 배수 복제로 장기
수익률이 지수 배수와 어긋나고, 커버드콜은 상방을 프리미엄과 맞바꾼다) "미국
원본 재사용" 전제 자체가 깨진다. `KRXWrapperInputs.product_type`이 `"plain"`이
아니면 실행을 거부한다.

**⚠️ 재사용의 전제조건 - 정말 같은 지수인지 확인**: 조사 중 실제로 함정을
하나 발견했다 - "TIGER 미국배당다우존스"는 이름이 비슷해 보이지만 다우존스30
(DIA)이 아니라 전혀 다른 지수(Dow Jones U.S. Dividend 100 Index, SCHD와 동일
계열)를 추종한다. 그대로 DIA 래퍼로 썼다면 완전히 틀린 밸류에이션을 재사용할
뻔했다. 그래서 `KRXWrapperInputs.tracks_same_index_as`를 **필수 필드**로 만들어
어떤 지수를 추종하는지 근거(공식 상품페이지에서 기초지수명 확인)를 명시하게
했다(`expected_earnings_growth_basis`와 동일한 패턴). DIA의 실제 국내 래퍼는
TIGER 미국다우존스30(245340, 공식페이지에서 "Dow Jones Industrial Average"
직접 확인)이다.

**실측 결과(2026-08-07, `scripts/analyze_krx_etfs_2026_08_07.py`, 11종목)**:
같은 지수를 추종하는 국내 ETF끼리는 밸류에이션(Gap)이 완전히 동일하고,
차이는 오직 총보수·환헤지·순자산뿐이라는 게 실측으로 확인됐다:

| 추종 지수(미국 원본) | 국내 ETF | 총보수 | 순자산(억원) | 비고 |
|---|---|---|---|---|
| S&P500(VOO), Gap +0.87~+2.37%p | TIGER 미국S&P500(360750) | 0.06% | 204,640 | 최저보수+최대AUM |
| 〃 | KODEX 미국S&P500(379800) | 0.07% | 100,701 | |
| 〃 | ACE 미국S&P500(360200) | 0.07% | 40,405 | |
| 〃 | KODEX 미국S&P500(H)(449180) | 0.11% | 9,937 | 환헤지 |
| 나스닥100(QQQ), Gap +2.48~+3.67%p | TIGER 미국나스닥100(133690) | 0.09% | 114,540 | 최저보수+최대AUM |
| 〃 | ACE 미국나스닥100(367380) | 0.10% | 35,067 | |
| 〃 | KODEX 미국나스닥100(H)(449190) | 0.10% | 5,605 | 환헤지 |
| 〃 | KODEX 미국나스닥100(379810) | 0.12% | 90,579 | |
| 다우30(DIA), Gap -0.76%p | TIGER 미국다우존스30(245340) | 0.35% | 1,491 | 유일 후보, 보수 높음 |
| 에너지(XLE), Gap -3.32~-3.06%p | KODEX 미국S&P500에너지(합성)(218420) | 0.25% | 265 | 유일 후보, 지수 계열 확정 못함 |
| 러셀2000(IWM), Gap +3.70~+4.86%p | KODEX 미국러셀2000(H)(280930) | 0.45% | 266 | 유일 후보, **헤지형만 존재** |

S&P500·나스닥100은 저비용·대형 상품이 3~4개씩 경쟁 중이라 선택지가 넓은
반면, 다우30·에너지·러셀2000은 **후보가 하나뿐**이고 그 하나도 총보수가
0.25~0.45%로 훨씬 비싸다 - 지수가 대중적일수록 국내 래퍼 시장도 경쟁이
치열해져 보수가 낮아진다는 패턴이 드러난다. 러셀2000은 특히 **국내에 환노출형
자체가 없어** ISA 계좌로는 소형주 익스포저를 헤지형으로만 담을 수 있다는
구조적 제약이 있다.

**실제 작업 중 잡은 실수 2건(둘 다 이 프로젝트가 반복 경계해온 유형)**:
1. 테스트 골든데이터에 TIGER 미국S&P500의 상장일을 "2021-04-09"로 넣었는데,
   이는 실제로는 TIGER 미국테크TOP10 INDXX의 상장일이었다 - 검증 없이
   패턴매칭으로 다른 상품 날짜를 재사용한 것(TYL SBC 3배 오류와 같은 유형의
   실수). 실제 검색으로 재확인해 "2020-08-07"로 정정했다. 밸류에이션에는
   영향 없는 정보성 필드였지만, 이 프로젝트의 원칙(추측 금지, 확인 못하면
   "미확인"으로 정직하게 남길 것)에 위배되는 상태였다.
2. `us_result`를 `ledger_etf/*.json`에서 `json.load()`로 불러오면 dict 키가
   전부 문자열로 바뀐다(JSON 객체 키는 문자열만 허용) - `realized_eps_by_year`
   는 연도(int) 뺄셈을 하는 `realized_eps_growth()`에 그대로 들어가 "2014"-
   "2015" TypeError를 냈다. `run_krx_wrapper_analysis()`에서 정수로 되돌리는
   변환을 추가하고 회귀 테스트로 고정했다.

상장일 일부(KODEX/ACE 계열 다수)는 검색으로 확인하지 못해 **추측하지 않고
"미확인"으로 남겼다** - 밸류에이션에 영향을 주지 않는 정보성 필드라 다음
조사 때 채워도 무방하다.

**ledger는 `ledger_krx/`에 분리 저장**한다(회사 `ledger/`, 미국ETF
`ledger_etf/`와 스키마가 다르므로 - 셋을 섞으면 `tests/test_ledger_integrity.py`
가 스키마를 가정하고 전수 파싱하다 깨진다, v3.32/v3.35와 동일 원칙). KRX
종목코드는 6자리 숫자라 회사/미국ETF 티커용 정규식(`[A-Z.]+`)과 매칭이 안 돼
`KRX_FNAME_RE`를 별도로 뒀다.

**의도적으로 넣지 않은 것들**: (1) 헤지 캐리비용 자동 추정 - 한미 금리차
데이터를 확보하지 않아 opt-in 필드로만 열어두고 기본은 경고만 남긴다.
(2) 국내 래퍼의 "종합 품질 점수"(보수·유동성·추적오차를 가중합한 단일 지표) -
demand_sensitivity 앵커표·P/B 임계값과 동일 판단으로, 관측 사례가 11건뿐이라
임의 가중치를 고정하지 않고 원자료(보수·AUM)를 그대로 노출해 분석자가 판단하게
한다. (3) 추적오차·괴리율(NAV 대비 시장가 괴리) - 이번 조사에서 원자료를
확보하지 못했다.

**회사 엔진 영향 없음**(신규 파일만 추가, 기존 파일은 import만 함).
`ENGINE_VERSION`을 v3.38로 올렸다. 테스트 184 → 205개(krx 엔진 17건 +
ledger 무결성 4건).

## v3.39 — KRX 래퍼 섹터 커버리지 확장(2026-08-07, 사용자 요청 "섹터 다양한
방면으로")

v3.38에서 국내 래퍼는 5개 지수(S&P500/나스닥100/다우30/에너지/러셀2000)만
커버했는데, 미국 원본은 이미 8개(XLF·XLK 포함)가 있었다는 걸 다시 확인해
XLF(금융)·XLU(유틸리티)를 먼저 채우고, 이어서 미국 원본조차 없던 4개 섹터
(산업재 XLI·헬스케어 XLV·커뮤니케이션서비스 XLC·필수소비재 XLP)를
`scripts/analyze_etfs_sectors_2026_08_07.py`로 신규 분석한 뒤 국내 래퍼까지
연결했다.

**KODEX가 GICS 섹터별 "미국S&P500{섹터}" 라인업을 사실상 전 섹터에 걸쳐
운영 중임을 확인** - 조사한 7개(금융/유틸리티/산업재/헬스케어/커뮤니케이션/
필수소비재/테크놀로지) 전부 총보수 0.25%(운용보수 0.229%)로 통일돼 있다.
**테크놀로지(463680)만 이번 배치에서 제외했다** - 다른 형제 상품 전부가
0.25%로 확인됐다는 패턴상 같은 값일 가능성이 높지만, 여러 경로로 검색해도
공식 확인을 못해 추측으로 채우지 않기로 했다(이 프로젝트가 TYL SBC 오류에서
얻은 교훈과 동일한 판단) - 확인되는 대로 추가할 것.

**설계 완화 - `aum_krw`를 Optional로 바꿨다.** v3.38 원안은 순자산을 필수
필드로 요구했는데, KODEX 미국S&P500유틸리티(463640)의 AUM을 여러 경로로도
확인하지 못하면서 "검증 안 된 정보성 필드 하나 때문에 섹터 전체를 빼야 하나"
라는 실제 갈등이 생겼다. `listed_date`가 이미 "미확인" 문자열을 허용하는
선례가 있었으므로 동일 원칙을 적용해 `aum_krw=None`을 허용하도록
`KRXWrapperInputs`를 고쳤다. **부호 함정에 주의해서 처리했다** -
`compare_krx_wrappers()`의 정렬 키에서 `None`을 `0`으로 취급하면 "-None"이
TypeError를 내거나(막았지만) 잘못 처리하면 최하위가 아니라 최상위로 잘못
밀릴 수 있어, `(expense_ratio, aum_is_none, -aum_or_0)` 3중 키로 명시적으로
최하위 고정했다. `format_krx_comparison_table()`은 "0"이 아니라 "미확인"으로
렌더링한다(회귀 테스트: `test_unconfirmed_aum_sorts_last_and_renders_as_unconfirmed`).

**실측 결과(6개 신규 국내 래퍼, 미국 원본 재사용 Gap)**: XLF(금융) +1.10~
+1.16%p, XLU(유틸리티) -2.67~-2.15%p, XLI(산업재) -0.71%p, XLV(헬스케어)
-0.49%p, XLC(커뮤니케이션) +3.96%p, XLP(필수소비재) -2.22%p - 전부 ±5%p
중립밴드 안(적정가/경계선)이라 뚜렷한 저평가/고평가 신호는 없다. 신규 미국
원본 4종은 전부 P/E 출처가 1개뿐이라(`[단일 출처 경고]`) 기존 XLE/XLU처럼
2차 출처 보강이 남은 과제다.

**국내 래퍼 총 17종목(회사 엔진과 무관, 신규 파일만 추가)**. `ENGINE_VERSION`
v3.38 → v3.39. 테스트 205 → 206개.

## v3.39 후속 — 임의소비재(XLY) 추가 + 소재·리츠 공백 확정(2026-08-08, "계속해서
섹터 확장")

같은 v3.39 코드로 데이터만 추가한 순수 배치라 `ENGINE_VERSION`은 올리지 않았다
(엔진 코드 변경 없음 - `ledger_etf/XLY_2026-08-08.json`·`ledger_krx/453660_
2026-08-08.json` 둘 다 "v3.39"로 정확히 스탬프됨, v3.32가 세운 "코드가 안
바뀌면 버전도 안 바뀐다" 원칙 그대로).

- **KODEX 미국S&P500경기소비재(453660, XLY 래퍼) 추가** - `scripts/
  analyze_etfs_sectors_2026_08_08.py`로 XLY 원본 신규 분석(P/E 28.17x, 단일
  출처) 후 국내 래퍼 연결. Gap +1.87%p, 적정가/경계선. 상위비중이 Amazon
  23.18%+Tesla 15.66%로 39%에 달해 사실상 두 종목이 섹터 성장률을 좌우한다는
  점을 성장률 근거에 명시했다.
- **소재(Materials/XLB)는 KODEX·TIGER·ACE 어디서도 국내 상장 상품을 찾지
  못했다** - GICS 11섹터 중 유일하게 국내 래퍼 후보 자체가 없는 섹터로 확정.
  추측하지 않고 정직한 공백으로 남긴다.
- **부동산(리츠/XLRE)은 국내 상품(KODEX 미국부동산리츠(H), 352560)이 있지만
  기초지수가 다르다** - 삼성자산운용 공식 안내문이 "Dow Jones US Real Estate
  Index"이며 "Real Estate Select Sector와는 다른 지수"라고 **스스로 명시**하고
  있다. 이 프로젝트의 재사용 전제(`tracks_same_index_as`가 요구하는 "정말
  같은 지수")가 성립하지 않아 제외했다 - 이름이 비슷해 보이는 상품을 지수
  확인 없이 재사용하면 안 된다는 v3.38의 다우존스30/배당다우존스 교훈이 여기서
  또 한 번 실증됐다(이번엔 운용사가 스스로 다른 지수라고 밝혀 판단이 더 쉬웠다).
- 테크놀로지(463680) 총보수는 이번에도 재시도했으나 확인하지 못해 계속 제외.

**국내 래퍼 총 18종목(GICS 11섹터 중 9섹터 커버 - 소재·부동산만 미해결).**
테스트 수 불변(206개, 엔진 코드 변경 없어 새 테스트 없음).

## v3.39 후속 2차 — 반도체(SMH)·리츠(IYR) 추가, GICS 밖 테마 확장 시작
(2026-08-08, 사용자가 실제 거래앱 "지금 뜨고 있는 카테고리" 스크린샷 제공하며
"종류/섹터는 이 이미지를 보고 확장해" 요청)

지금까지의 섹터 확장은 전부 GICS 11섹터(S&P Select Sector 지수 계열) 틀
안에서만 진행됐다. 사용자가 제공한 실제 거래앱 트렌드 카테고리 목록(GICS보다
훨씬 세분화된 산업/테마 분류, 예: 반도체·반도체장비·반도체파운드리·
반도체부품소재·반도체팹리스·스마트폰부품·리츠·바이오·우주항공·로봇·클라우드·
은행 등 50개 이상)을 보면 **상위 11개 트렌드 카테고리 중 6개가 반도체
밸류체인**이었다 - 이 프로젝트가 GICS 틀에 갇혀 정작 한국 투자자가 가장 많이
찾는 테마(반도체)를 다루지 않고 있었다는 뜻이라 우선순위를 그쪽으로 옮겼다.
이번 배치로 **GICS 밖 테마/산업 ETF도 같은 "미국 원본 재사용" 엔진으로 다룰
수 있음을 확인**했다 - `run_krx_wrapper_analysis()`는 애초에 GICS 여부를
따지지 않고 "정말 같은 지수를 추종하는가"만 검증하므로 코드 변경이 전혀
필요 없었다.

- **반도체(KODEX 미국반도체 390390 ↔ SMH) 추가 - 지수 매칭 함정을 하나 더
  잡았다.** 처음엔 TIGER 미국필라델피아반도체나스닥(PHLX Semiconductor Sector
  Index 추종)에 맞춰 SOXX(iShares Semiconductor ETF, 예전 이름이 정확히
  "iShares **PHLX** Semiconductor ETF")를 원본으로 쓰려 했다 - 이름과 과거
  상품명까지 정확히 일치해 보여 가장 안전한 매칭처럼 느껴졌다. 그런데 확인해보니
  **SOXX는 2021-06-21에 추종지수를 PHLX Semiconductor Sector Index에서 NYSE
  Semiconductor Index로 바꿨다**(공식 프로스펙터스로 확인) - 상품명(iShares
  PHLX Semiconductor ETF)에는 옛 지수명이 그대로 남아있어 이름만 보면 지금도
  PHLX를 추종하는 것처럼 보인다. **이름·과거 상품명이 아무리 정확히 일치해도
  운용사가 지수를 바꿨을 수 있다**는, 다우존스30/배당다우존스·리츠 사례보다도
  한 단계 더 미묘한 함정이다. 대신 KODEX 미국반도체가 추종하는 "MVIS US Listed
  Semiconductor 25 Index"를 **똑같이** 추종하는 SMH(VanEck Semiconductor ETF)
  로 원본을 교체했다 - TIGER 미국필라델피아반도체나스닥은 지수가 진짜 일치하는
  미국 상장 ETF를 찾지 못해 이번 배치에서 보류(정직한 공백). Gap +2.64%p,
  적정가/경계선. 성장률(12%)은 AI 데이터센터 수혜(NVIDIA 21.70%+TSM 9.51%+
  Broadcom 6.73%가 상위3)를 XLK와 동일 수준으로 반영하되, 반도체 특유의 극심한
  boom/bust 사이클 리스크를 이유로 그 이상 올리지 않았다.
- **리츠(KODEX 미국부동산리츠(H) 352560 ↔ IYR) 추가 - v3.39 공백 해소.**
  v3.39에서는 XLRE(Real Estate Select Sector Index)와 지수가 달라 제외했던
  종목인데, **IYR(iShares U.S. Real Estate ETF)이 정확히 KODEX가 추종하는
  "Dow Jones US Real Estate Index" 계열("Capped" 버전)을 추종**한다는 걸 찾아
  다시 살렸다. ⚠️ "Capped"(집중도 상한) 적용 여부까지 완전히 일치하는지는
  확정하지 못해 XLE/KODEX에너지와 동일 수준의 "같은 계열이되 완전동일 미확정"
  으로 취급한다. Gap -2.73%p, 적정가/경계선. 환헤지형(H)만 존재(러셀2000과
  같은 패턴).
- 소재(Materials/XLB)는 이번에도 국내 상품을 찾지 못해 공백 유지. 테크놀로지
  (463680) 총보수도 여전히 미확인.

**국내 래퍼 총 20종목(GICS 9섹터 + GICS 밖 테마 2종: 반도체·리츠).** 엔진
코드 변경 없음(순수 데이터 배치), `ENGINE_VERSION` v3.39 그대로, 테스트
206개 불변.

## v3.39 후속 3차 — 배터리(LIT)·로봇(ROBO) 추가, 매칭 실패/방법론 한계 사례
전수 기록(2026-08-08, 사용자 "모두 진행시켜")

사용자가 스크린샷의 카테고리를 "전부 진행"하라고 요청. 53개 카테고리를
기계적으로 다 시도하는 대신, **"같은 지수를 추종하는 미국 원본이 실제로
존재하는가"를 먼저 걸러내는 방식**으로 접근했다 - 이 프로젝트의 재사용
전제(`tracks_same_index_as`)를 지키려면 어차피 이 검증 없이는 아무것도
만들 수 없기 때문이다. 조사한 9개 후보 중 **2건만 통과**하고 나머지는 전부
구체적인 사유와 함께 기각했다 - 이 기각 사유들 자체가 이 방법론의 적용
범위를 보여주는 유용한 기록이라 전부 남긴다.

**통과 2건**:
- **배터리/리튬(TIGER 글로벌리튬&2차전지SOLACTIVE(합성) 394670 ↔ LIT)**:
  둘 다 "Solactive Global Lithium Index"를 추종한다는 걸 확인. Gap +0.31%p.
  성장률(8%)은 "배터리 '기술' 테마"라는 이름과 달리 실질은 리튬 채굴·정제
  기업 비중이 커 커머디티 가격 사이클(2022~2024 리튬가 폭락 전례)에 더
  가깝다고 보고 XLE(에너지, 4%)에 가깝게 낮춰 잡았다.
- **로봇/자동화(KODEX 글로벌로봇(합성) 276990 ↔ ROBO)**: "ROBO Global
  Robotics & Automation" 지수 계열(UCITS/PR vs 본토/TR 표기 차이, IYR의
  Capped 사례와 동일 수준으로 취급) 확인. Gap +1.66%p. 91개 종목으로 상위10
  비중이 17.59%뿐이라(집중도 낮음) XLI(산업재)보다는 높이고 순수 테크섹터
  보다는 낮춰 9%로 잡았다.

**기각 7건 - 사유별로 분류**:
1. **지수는 일치하나 원리적으로 밸류에이션 불가(바이오)**: KODEX 미국S&P바이오
   (합성)(185680)가 XBI(SPDR S&P Biotech ETF)와 완전히 동일한 지수("S&P
   Biotechnology Select Industry Index")를 추종한다는 것까지 확인했으나,
   **XBI 자체가 적자 임상단계 바이오텍 비중이 너무 높아 유의미한 P/E가
   존재하지 않는다**(stockanalysis.com "n/a" 표시). 이 엔진은 P/E 기반
   Gordon 역산이 핵심이라 P/E가 없는 섹터는 데이터를 더 모아도 다룰 수
   없다 - 데이터 공백이 아니라 **방법론 자체의 적용범위 한계**로 별도
   기록해둔다(다른 P/E 소스나 forward P/E를 시도해볼 여지는 남아있음).
2. **총보수 확인 실패(로봇/AI)**: TIGER 글로벌AI&로보틱스INDXX(464310)가
   BOTZ와 완전히 동일한 지수("Indxx Global Robotics & Artificial
   Intelligence Thematic Index")를 추종한다는 것까지 확인했으나 총보수를
   여러 경로로도 찾지 못해 보류(테크놀로지 463680과 같은 유형의 공백).
3. **커스텀/독점 지수라 매칭 원본이 원천적으로 없음(우주항공·방산·클라우드)**:
   TIGER 미국우주테크(Akros U.S. Space Tech Index)·TIGER 미국방산TOP10
   (Mirae Asset US Defense Top10 Index)·KODEX 미국우주항공(iSelect 미국우주항공
   지수)·TIGER 글로벌클라우드컴퓨팅INDXX(Indxx Global Cloud Computing Index)
   전부 운용사 자체 커스텀/독점 지수를 써서, 이 엔진이 다루는 미국 상장 ETF
   (ITA/XAR/WCLD/SKYY 등) 어느 것과도 지수가 일치하지 않는다. **이 프로젝트의
   재사용 원칙상 "이름이 비슷하다"·"테마가 같다"만으로는 재사용 근거가 될 수
   없다**(다우존스30/배당다우존스 교훈의 반복 적용).
4. **국내 상품이 미국 대신 국내 종목을 추종(은행·게임)**: KODEX 은행/TIGER
   은행은 신한지주·하나금융지주 등 "KRX 은행" 지수(국내 은행주), KODEX
   게임산업/TIGER K게임도 FnGuide/WISE의 "국내 게임산업" 지수를 추종한다 -
   애초에 "미국 은행"·"미국 게임" 익스포저를 주는 국내 상장 상품 자체가
   존재하지 않았다.

**국내 래퍼 총 22종목(GICS 9섹터 + GICS 밖 테마 4종: 반도체·리츠·배터리·
로봇).** 엔진 코드 변경 없음, `ENGINE_VERSION` v3.39 그대로, 테스트 206개
불변(순수 데이터 배치).

## v3.39 후속 4차 — 보류/기각 항목 재시도, 3건을 "다른 방향"으로 해소
(2026-08-08, 사용자 "지금까지 보류/기각한거 다시 진행. 안되면 다른방향으로라고
진행해")

3차에서 기각한 7건(테크놀로지 보수·소재·바이오·로봇AI 보수·우주항공/방산·
은행·클라우드·게임)을 전부 재시도했다. **3건은 다른 방향으로 뚫렸고, 4건은
재시도에도 뚫리지 않았다** - "다른 방향" 지시를 문자 그대로 따른 결과다.

**해소 3건**:
- **테크놀로지(463680) 총보수 최종 확인** - cbonds.com에서 0.25%를 확인했다
  (다른 형제 상품 7개 전부 0.25%였던 패턴과 정확히 일치, AUM 110억원·상장일
  2023-08-01도 함께 확보). XLK는 이미 `ledger_etf/`에 있어(v3.39, 2026-08-07)
  국내 래퍼만 새로 연결 - 미국 원본 재분석 불필요. Gap +2.85~+3.05%p.
- **방산/우주항공 - "다른 방향"의 실제 사례**: TIGER 미국우주테크·TIGER
  미국방산TOP10·KODEX 미국우주항공은 여전히 커스텀 지수라 매칭 불가지만,
  검색을 넓혀 **WON 미국우주항공방산(440910, 우리자산운용)**을 찾았다 -
  "S&P Aerospace & Defense Select Industry Index (PR)"를 추종해 **XAR(SPDR
  S&P Aerospace & Defense ETF)**과 정확히 일치한다. 처음 조사에서 TIGER/
  KODEX 두 대형 운용사만 확인하고 "매칭 원본 없음"으로 단정한 게 성급했다 -
  중소형 운용사(우리자산운용)까지 넓혀야 했다. XAR 신규 분석 후 연결,
  Gap +0.07%p.
- **클라우드 - 같은 유형의 "다른 방향"**: 처음엔 WCLD(BVP Nasdaq Emerging
  Cloud Index)·SKYY(ISE CTA Cloud Computing Index) 두 곳만 확인하고 TIGER
  글로벌클라우드컴퓨팅INDXX(371450, Indxx Global Cloud Computing Index)와
  매칭 실패로 결론냈었다. 재조사에서 **CLOU(Global X Cloud Computing ETF)**가
  정확히 같은 "Indxx Global Cloud Computing Index"를 추종한다는 걸 확인 -
  후보 미국 ETF가 여럿일 때(클라우드 테마에는 최소 3개 경쟁 상품이 있다)
  전부 확인하지 않고 처음 두 개만 보고 실패로 단정하면 안 된다는 게, 반도체의
  SOXX/SMH 사례에 이어 두 번째로 확인됐다. Gap +1.83%p.

**재시도했으나 여전히 못 뚫은 4건**:
- **로봇/AI(TIGER 글로벌AI&로보틱스INDXX ↔ BOTZ)**: 지수 일치는 확인됐으나
  (Indxx Global Robotics & Artificial Intelligence Thematic Index) 총보수를
  검색 경로를 바꿔가며 여러 번 재시도해도 확인하지 못했다. 공식 PDF도
  바이너리 손상으로 파싱 불가(테크놀로지 463680은 결국 cbonds.com에서 확인됐지만
  이건 같은 방법이 통하지 않았다).
- **은행**: KBWB(KBW Nasdaq Bank Index)와 매칭되는 국내 상품을 SOL/RISE/
  KIWOOM까지 넓혀 재검색했으나 못 찾았다 - 국내에 "미국은행" 익스포저를 주는
  상장 ETF 자체가 존재하지 않는 것으로 보인다(KODEX/TIGER 은행은 전부 국내
  은행주 추종).
- **게임**: 동일 패턴 - RISE 게임테마·KODEX 게임산업 전부 국내(FnGuide/WISE)
  게임산업 지수 추종, 미국 게임/e스포츠 익스포저 국내 상품 부재.
- **소재(XLB)**: 3차에 이어 재확인, 여전히 후보 없음.
- **바이오(XBI)**: 지수 일치(S&P Biotechnology Select Industry Index)는
  이미 3차에서 확인했으나, IBB(NASDAQ Biotechnology Index) 등 P/E가 있는
  대안 미국 ETF로 바꿔서 매칭되는 국내 상품을 찾아봤지만 실패 - 국내 상품은
  전부 XBI와 같은(P/E 없는) 지수 계열만 존재해 방법론적 한계가 그대로
  남는다.

**국내 래퍼 총 25종목.** 엔진 코드 변경 없음, `ENGINE_VERSION` v3.39 그대로,
테스트 206개 불변.

## v3.39 후속 5차 — 판별력 복구(시장요구성장 순위표) + 자산군 경계 명문화
(2026-08-08, 사용자 "너가 보기에 이정도면 충분해? 스스로 전문적인 관점에서
워크플로 계획하고 실행해")

사용자가 종목 수를 더 늘리는 것보다 근본적인 질문을 던졌다 - 지금 담은
25종목 전부가 **±5%p 판정 밴드 안(적정가/경계선)에 들어와 있다.** 이건
우연이 아니라 v3.34가 이미 자체진단한 구조적 결함(Gap이 분석자가 타이핑한
가정성장률에 1:1로 좌우된다)이 그대로 재현된 것이다 - 섹터마다 "통념상
합리적인" 성장률을 골랐기 때문에, 결과적으로 전부 판정 경계 안쪽으로
스스로 수렴했다. **종목을 더 담아봐야 "적정가" 라벨만 늘어날 뿐 판별력은
안 생긴다.** 이걸 먼저 고치는 게 세부 ETF를 더 찾는 것보다 우선한다고
판단해 작업 순서를 바꿨다.

**① `scripts/rank_krx_etfs_2026_08_08.py` 신설 - v3.34가 이미 처방한 해법을
실제로 적용.** Gap이 아니라 `required_growth.breakeven`(시장이 이미 요구하고
있는 성장률 - P/E와 r만으로 결정되어 분석자 주관이 개입할 수 없는 유일한
객관적 비교축)으로 25종목 전체를 재순위했다. 여러 P/E 출처가 있는 종목은
가장 보수적인(가장 비싼) 쪽을 채택한다 - `format_comparison_table()`이 미국
원본 비교에서 쓰는 것과 동일한 원칙. 새 밸류에이션 로직은 0줄 추가 - ledger에
이미 저장된 값만 재조합한다.

실측 결과: 판정 분포는 여전히 "적정가/경계선" 25/25(예상대로, 판정 자체는
Gap 기반이라 안 바뀜)지만, **시장요구성장은 4.79%(커뮤니케이션서비스)~
9.27%(반도체)로 4.5%p 넘게 벌어진다** - 이게 진짜 비교 가능한 신호다. 하위권
(커뮤니케이션서비스 4.79%, 금융 5.65%, 러셀2000 6.85%)은 "이 정도만 커도
저평가"라는 뜻이고, 상위권(반도체 9.27%, 테크놀로지 8.90%, 방산 8.58%)은
"시장이 이미 상당한 낙관을 가격에 반영했다"는 뜻이다. **이 순위를 "낮을수록
사라"로 곧바로 읽으면 안 된다** - breakeven이 낮은 것과 그 섹터가 실제로 그
성장조차 낼 수 있는지는 별개 질문이다(원자재·경기민감 섹터가 특히 위험).

25종목 중 16종목에 `주의` 플래그가 붙었다(단일출처 13 / 성장가정취약 2 /
출처갈림 0 / 환헤지비용미반영 4, 일부 중복). 이 절반 이상이 **아직
검증되지 않은 채로 순위에 들어와 있다는 뜻** - 순위표를 그대로 투자판단에
쓰지 말고 반드시 `주의` 열을 함께 볼 것.

**② 자산군 경계 명문화 - "빠뜨린 것"이 아니라 "이 엔진이 원리적으로 다룰
수 없는 것"으로 못박는다.** 지금까지 채권·금·원자재 ETF(TLT/AGG/LQD/GLD/
SLV/USO 등)를 조용히 안 다뤄왔는데, 이건 데이터 공백(소재·바이오처럼 언젠가
채울 수 있는 것)이 아니라 **이 엔진의 핵심 수식(P/E 기반 Gordon 역산)이
구조적으로 적용 불가능한 영역**이다:
  - **채권 ETF**: 이익(E)이라는 개념 자체가 없다 - 채권은 배당이 아니라
    쿠폰이고, 밸류에이션은 P/E가 아니라 수익률곡선·듀레이션·신용스프레드로
    결정된다. 완전히 다른 프레임워크(예: 만기수익률 대비 정책금리 스프레드)가
    필요하다.
  - **금/귀금속 ETF**: 이익도 배당도 없는 순수 가격추종 자산이다 - Gordon
    성장모형의 분자(이익) 자체가 정의되지 않는다.
  - **원자재/원유 선물 ETF**: 배당·이익 없이 선물 곡선(콘탱고/백워데이션)에
    수익률이 좌우된다 - 이미 분석한 XLE/에너지 섹터(정유·탐사기업 **주식**)와
    원자재 자체(원유 **선물**)는 전혀 다른 자산군이다.
  이 셋은 이 프로젝트의 다른 방법론적 한계(바이오/XBI처럼 "지수는 맞는데 P/E가
  없는" 경우)와도 다르다 - 지수 매칭 문제가 아니라 애초에 이 엔진이 답할 수
  있는 질문("이 이익흐름의 시장가가 정당한가")이 성립하지 않는 자산군이다.
  향후 이 엔진을 확장해도 이 셋은 별도의 완전히 다른 계산 프레임워크
  없이는 다룰 수 없다 - 필요해지면 `engine/etf_engine.py`를 손대는 게 아니라
  독립된 새 모듈(예: `engine/bond_etf_engine.py`)로 시작해야 한다.

**의도적으로 하지 않은 것 - 세부 테마 ETF 추가 확대**: 사용자가 "각 분야별
세부 ETF"를 언급했으나, 이번엔 위 ①·②를 먼저 처리했다. 세션 도중 WebSearch/
WebFetch가 사용량 한도에 걸려(리셋 04:00 UTC) SCHD(미국배당다우존스)·동일가중
(RSP)·사이버보안 등 신규 데이터 수집이 필요한 확장은 이번 배치에서 진행하지
못했다 - 다음 세션에서 이어갈 것.

테스트 206개 불변(신규 로직 없는 순수 재조합 스크립트 + 문서화).

## v3.39 후속 6차 — SCHD(미국배당다우존스) 추가, 동일가중·사이버보안 보수
미확인으로 확정(2026-08-08, 한도 리셋 후 재개)

WebSearch/WebFetch 한도가 리셋돼 5차에서 미룬 세 항목을 마저 조사했다.

**SCHD(미국배당다우존스) 추가 - VOO/QQQ와 같은 "3파전" 구조**: TIGER(458730)·
SOL(446720)·ACE(402970) 미국배당다우존스 전부 "Dow Jones U.S. Dividend 100
Index"를 추종한다는 걸 확인했다 - 이 데이터는 사실 2026-08-06 최초 ETF
조사 때 이미 확보돼 있었는데(TIGER 미국배당다우존스가 다우존스30이 아니라는
**반면교사** 사례로만 CLAUDE.md에 남기고, 정작 SCHD 자체를 분석하지는
않았다). SCHD 원본을 신규 분석해 셋을 전부 연결하니 총보수 0.06~0.07%,
AUM 9,478억~41,759억원으로 VOO 그룹과 맞먹는 규모의 3파전임이 드러났다.
시장요구성장 5.14%로 **28종목 중 2위**(1위는 커뮤니케이션서비스 4.79%) -
`rank_krx_etfs_2026_08_08.py` 재실행 결과 SCHD가 상위권에 오른 게 이번
확장에서 가장 눈에 띄는 신규 신호다.

**동일가중(RSP)·사이버보안(BUG) - 지수는 확인, 보수는 끝내 미확인**:
TIGER 미국S&P500동일가중(488500)이 RSP와 동일한 "S&P 500 Equal Weight
Index(PR)"를, TIGER 글로벌AI사이버보안(418670)이 BUG(Global X Cybersecurity
ETF)와 동일한 "Indxx Cybersecurity Index"를 추종한다는 것까지는 확인했다.
**사이버보안 매칭은 클라우드(CLOU)와 완전히 같은 패턴이다** - 처음엔
HACK/CIBR 두 곳만 보고 다른 지수라 매칭 실패로 결론 날 뻔했으나, Global X의
BUG가 정확히 같은 Indxx 지수를 쓴다는 걸 확인했다(Global X가 Indxx 지수의
주요 미국 파트너라는 패턴이 클라우드·사이버보안 2건에서 재확인됨). 다만 두
종목 다 **총보수를 4회 이상 재시도해도 찾지 못해** 이번 배치에서는 제외한다
- 로봇/AI(BOTZ)와 같은 유형의 공백으로, 새로 만든 게 아니라 기존 공백
목록에 추가한다.

**국내 래퍼 총 28종목.** 엔진 코드 변경 없음, `ENGINE_VERSION` v3.39 그대로,
테스트 206개 불변.

## v3.40 — KRX 래퍼끼리의 실제 중복노출 측정 (2026-08-08, 사용자 "그대로
순서대로 실행해. 전문적인 수준으로" — 3단계 워크플로 중 우선순위 1번)

지금까지 겹침(overlap)은 v3.36/v3.37에서 **미국 원본끼리만** 측정했다
(VOO+QQQ+XLK를 같이 담으면 같은 메가캡을 여러 번 사는 문제). 그런데 한국
투자자는 미국 원본을 못 사고 국내 래퍼를 산다 - "TIGER 미국S&P500 +
KODEX 미국반도체 + WON 미국우주항공방산을 같이 담으면 실제로 얼마나
겹치나"는 지금까지 전혀 답할 수 없는 질문이었다.

**전제조건 작업 - 12개 신규 미국 원본에 top10_holdings 추가.** 겹침을
측정하려면 종목별 보유비중이 필요한데, v3.39에서 확장한 12개 미국 원본
(XLI/XLV/XLC/XLP/XLY/SMH/IYR/LIT/ROBO/XAR/CLOU/SCHD)은 집계 비중(%)만 있고
종목별 명세가 없었다. stockanalysis.com/etf/{ticker}/holdings에서 12종
전부 확보해 기존 스크립트에 추가하고 재실행했다. XLI/XLV/XLC/XLP는 원래
2026-08-07 날짜였는데 오늘 재실행하며 08-08로 날짜가 바뀌었다 - **재계산
결과가 timestamp와 top10_holdings 필드 추가를 빼면 완전히 동일함을 diff로
확인한 뒤** 구 08-07 파일을 git rm하고 신규 파일로 교체했다(v3.32 관행).

**핵심 함수 - `krx_holdings_overlap_report()`(신규, `engine/krx_etf_pipeline.py`)**:
새 계산 로직을 추가하지 않았다 - 이 엔진의 재사용 설계 원칙(국내 래퍼는
미국 원본과 동일한 주식 바스켓을 담는다)이 여기서도 그대로 성립하므로,
KRX 래퍼 A·B의 겹침은 각각의 `us_reference_ticker` 원본이 가진
top10_holdings로 근사할 수 있다. v3.36/v3.37에서 이미 만들고 검증한
`holdings_overlap()`을 그대로 재사용했다(Simplicity First - 중복 계산 금지
원칙 재확인).

**같은 미국 원본을 재사용하는 쌍은 별도 카테고리로 분리했다.** TIGER/KODEX/
ACE 미국S&P500은 셋 다 VOO를 재사용하므로 정의상 거의 완전히 겹친다 - 이건
버그가 아니라 사실이지만, "서로 다른 지수인데 우연히 겹친다"(v3.36의 원래
경고 대상)와 성격이 달라 `same_index_pairs`로 분리하고 `[동일지수 경고]`
라는 별개 문구를 붙였다("같이 담아도 분산 효과가 전혀 없다 - 총보수·유동성
만 비교해 하나만 고를 것").

**실측 결과(28종목 전체, 378쌍 중)**: 동일지수 쌍 15개(전부 VOO/QQQ/SCHD
그룹) / 실측 가능 쌍 85개 / 측정불가(top10 표본이 우연히 안 겹침) 278개 /
데이터없음 0개(12종 전부 확보한 덕분에 스킵 없음). 가장 눈에 띄는 신규
발견 - **SMH(반도체)와 XLK(기술) 겹침이 29.9%p**(공통 5종목: AMAT/AMD/
AVGO/MU/NVDA, 리포트 JSON에서 직접 확인) - "반도체 ETF + 기술섹터 ETF"를
분산으로 착각하기 쉬운 조합인데, 사실상 QQQ+XLK(31.9%p)에 버금가는
중복이다. 그 다음은 QQQ+XLK 계열(31.9%p, 4개 나스닥100 래퍼×XLK 조합) -
v3.36에서 이미 확인된 패턴이 KRX 계층에서도 그대로 재현됨을 실측으로
검증했다.

`scripts/krx_overlap_report_2026_08_08.py`가 `ledger_krx/` 28건 +
`ledger_etf/` 최신 20건을 로드해 리포트를 생성한다(`reports/
krx_overlap_2026-08-08.json`).

**`ENGINE_VERSION`을 v3.40으로 올렸다** - 새 함수(`krx_holdings_overlap_
report`)가 추가됐으므로 v3.32 규칙에 따라 상수를 올렸다. 기존 ledger는
소급 재실행하지 않는다(v3.32가 확립한 "과거 스탬프는 그 시점 코드를
정확히 반영한다" 원칙 유지) - top10_holdings가 추가된 12개 원본은 이미
이번 세션에서 재실행됐으므로 v3.39로 스탬프돼 있고, 이는 정확하다(그
시점엔 아직 krx_holdings_overlap_report가 없었다).

테스트 211개(206 + 신규 5건: 실측 겹침 / 동일지수 분리 / 데이터없음 스킵 /
개별 밸류에이션 불변 / 표 포맷팅).

**남은 한계**: (1) top10 표본 기준 하한 - 전체 구성종목 데이터 필요.
(2) LIT/ROBO는 비-미국 상장 종목(심천·도쿄·홍콩·한국거래소)이 섞여있어
다른 순수 미국주식 중심 ETF와는 자연히 안 겹칠 가능성이 높다 - 이 자체가
분산 신호일 수도, 표본 부족일 수도 있어 해석 주의.

## v3.41 — 환헤지 캐리비용 실제 반영 + 결과 dict 버그수정 (2026-08-08,
"그대로 순서대로 실행해" 워크플로 2번째 단계)

v3.38부터 `estimated_hedge_carry`가 opt-in 필드로 열려 있었지만 실제 값을
넣은 적이 없어 헤지형 4종목(KODEX 미국S&P500(H)/미국나스닥100(H)/미국러셀
2000(H)/미국부동산리츠(H)) 전부 "[환헤지 비용 미반영]" 경고만 달려 있었다.
한국은행 기준금리(2.75%, 2026-07-16 인상)와 미국 연준 정책금리(3.50~3.75%,
중간값 3.625%)의 차이 0.875%p를 캐리비용 추정치로 병기했다.
`scripts/apply_hedge_carry_2026_08_08.py`가 헤지형 4종목만 재실행한다 -
전체 17종목 스크립트를 다시 돌리면 헤지형 아닌 13종목까지 불필요하게
오늘 날짜로 재저장되는 낭비가 생겨(v3.32/v3.35/v3.36과 같은 계열의 사고)
의도적으로 4종목만 분리했다.

⚠️ 이 값은 **자동으로 Gap에 반영되지 않는다** - is_insurer/sbc_cross_check와
동일한 "병기, 자동판정 안 함" 원칙. 스팟 정책금리 차이는 실제 FX 스왑레이트
근사치일 뿐 정확한 헤지비용이 아니라는 점도 명시했다.

**버그 발견·수정**: `estimated_hedge_carry`를 `hedge_cost_warning()` 문구를
만드는 데만 쓰고 정작 `result["wrapper"]` dict에는 저장하지 않고 있었다 -
`scripts/rank_krx_etfs_2026_08_08.py`가 "환헤지비용미반영" 플래그를 실제
반영 여부와 무관하게 `hedged=True`인 모든 종목에 무조건 붙이고 있었다는
뜻이다(캐리비용을 넣어도 플래그가 안 바뀜). `run_krx_wrapper_analysis()`의
`result["wrapper"]`에 `estimated_hedge_carry` 필드를 추가해 다운스트림
스크립트가 경고 문자열을 파싱하지 않고도 실제 반영 여부를 확인할 수 있게
했고, 순위 스크립트도 "환헤지비용참고반영"(반영됨, 그래도 자동판정은 안
됨을 뜻하는 라벨)으로 정정했다. 회귀 테스트 추가
(`test_hedged_wrapper_stores_estimated_hedge_carry_in_result`).

`ENGINE_VERSION` v3.40 → v3.41(코드 변경 - result dict 구조 수정). 헤지형
4종목 ledger만 재실행(3종목은 08-07→08-08 날짜 이동, diff로 캐리비용 문구
추가 외 완전 동일 확인 후 구 파일 교체). 테스트 211 → 212개.

## v3.41 후반 — 단일 P/E 출처 13종목 전건 해소 ("그대로 순서대로 실행해"
워크플로 3번째 단계, 2026-08-08)

`rank_krx_etfs_2026_08_08.py`가 매기던 "단일출처" 주의 플래그가 28종목 중
13개(미국 원본 기준)에 붙어있었다 - 반도체(SMH)·리츠(IYR)·배터리(LIT)·
로봇(ROBO)·방산(XAR)·클라우드(CLOU)·배당다우존스(SCHD)·경기소비재(XLY)
·산업재(XLI)·헬스케어(XLV)·커뮤니케이션(XLC)·필수소비재(XLP)·다우30(DIA)
전부. allinvestview.com(섹터·테마 12종)과 worldperatio.com(DIA)에서 2차
트레일링 P/E를 확보해 **13종목 전부 해소했다**(엔진 코드 변경 없음, 순수
데이터 보강).

**괴리 폭 실측**: 대체로 5~10%대였으나 **XLV(헬스케어)가 19.0%로 유독
컸다**(24.53x vs 29.19x) - IWM(29.5%)만큼 극단적이진 않지만 이 프로젝트가
경계하는 "단일 출처 신뢰 금지" 원칙이 유효함을 다시 보여준다. SCHD도
2차 출처(19.50x)가 1차(17.60x)보다 10.8% 높게 나와, breakeven이 5.14%→
6.07%로 오르고 Gap이 +0.80%p→약 -0.13%p로 **판정 방향이 사실상 뒤집혔다**
(둘 다 중립밴드 안이라 3단계 judgment 자체는 "적정가"로 안 바뀌었지만,
저평가 신호가 사실상 사라졌다) - `rank_krx_etfs_2026_08_08.py`가 이미
객관축(breakeven)으로 순위를 매기고 있었기에 이 변화가 표에 그대로
드러났다.

**작업 방식 - collateral 재실행 최소화**: 13종목이 3개 스크립트
(`analyze_etfs_2026_08_06.py`/`analyze_etfs_sectors_2026_08_07.py`/
`analyze_etfs_sectors_2026_08_08.py`)에 흩어져 있었는데, DIA만 속한
`analyze_etfs_2026_08_06.py`를 통째로 재실행하면 이미 2개 이상 출처를
가진 나머지 7종목(VOO/QQQ/IWM/XLK/XLE/XLF/XLU)까지 불필요하게 오늘
날짜로 재저장된다 - `scripts/patch_dia_second_source_2026_08_08.py`로
DIA만 분리해서 재실행했다. XLI/XLV/XLC/XLP·XLY/SMH/IYR/LIT/ROBO/XAR/
CLOU/SCHD는 각자 소속된 스크립트 자체가 그 섹터/테마 전용이라(다른
종목과 안 섞임) 안전하게 전체 재실행했다.

**KRX 래퍼 재연결 - 놓칠 뻔한 연쇄 효과**: 미국 원본이 갱신되면 그걸
재사용하는 국내 래퍼도 다시 연결해야 하는데, 처음엔 이걸 놓쳐서 원본은
2개 출처인데 국내 래퍼는 여전히 옛 1개 출처를 참조하는 불일치가 생겼다.
DIA→TIGER 미국다우존스30(`patch_dia_krx_wrapper_2026_08_08.py`),
XLI/XLV/XLC/XLP→4개 KODEX 섹터 래퍼(`patch_sector_krx_second_source_
2026_08_08.py`)를 별도 패치 스크립트로 재연결했고, XLY/SMH/IYR/LIT/ROBO/
XAR/CLOU/SCHD 관련 11개 래퍼는 전부 `analyze_krx_etfs_2026_08_08.py`
안에 있어(이미 오늘 날짜라 재실행해도 파일명 충돌 없음) 그 스크립트를
통째로 재실행해 한 번에 해소했다. **재실행 후 ETF 28건·KRX 20건 전부에서
`len(valuation.by_source) == 1`인 항목이 0건임을 코드로 직접 확인했다** -
"아마 다 됐겠지"로 넘기지 않고 전수 검증했다.

국내 래퍼 총 28종목(변경 없음), 미국 원본 20종목(변경 없음) - 이번은 데이터
보강뿐이라 `ENGINE_VERSION`은 그대로 v3.41. 테스트 212개 불변(엔진 코드
변경 없어 신규 테스트 없음).

## v3.39 후속 7차 — RSP(동일가중)·BOTZ(로봇AI)·BUG(사이버보안) 3종목 마무리
(2026-08-08, "계속" — v3.39 후속 6차가 보수 미확인으로 보류한 항목 해소)

v3.39 후속 6차는 TIGER 미국S&P500동일가중(488500)·TIGER 글로벌AI&로보틱스
INDXX(464310)·TIGER 글로벌AI사이버보안(418670) 3종 전부 **지수 일치는
확인했지만 총보수를 찾지 못해 보류**했었다(로봇AI·사이버보안은 이전 배치인
3차/4차부터 이어진 공백). 미래에셋 공식 상품페이지(investments.miraeasset.com)
에서 3종 전부 확인해 마무리했다: 488500=0.20%, 464310=0.49%, 418670=0.49%.

**미국 원본 RSP/BOTZ/BUG를 이 엔진에서 처음으로 신규 분석**했다
(`scripts/analyze_etfs_rsp_botz_bug_2026_08_08.py`) - 둘 다 이전에는
"지수 일치만 확인, 밸류에이션 미실시" 상태였다. 2차 P/E 출처는
investing.com(트레일링)에서 확보(stockanalysis 대비 17~28% 낮게 나옴 -
IWM/XLV급 극단은 아니지만 여전히 "단일 출처 신뢰 금지" 원칙이 유효함을
재확인).

- **RSP(동일가중)**: top10 비중이 **2.57%**로 이 트래커의 다른 모든 미국
  ETF(대부분 VOO 34%~BOTZ 60%대)와 자릿수가 다르다 - 동일가중 설계가
  메가캡 집중을 구조적으로 배제한다는 뜻이 코드 산출물로 그대로 드러난
  사례. **겹침 리포트에서 RSP(488500)는 다른 어떤 종목과도 실측 겹침이
  0건**이었다(전부 "측정불가"로 분류, top10 표본이 우연히 안 겹침) - 이건
  데이터 공백이 아니라 동일가중 설계상 top10에 메가캡이 아예 안 들어가기
  때문일 가능성이 높다(V3.37이 확립한 "측정불가 ≠ 겹침 없음" 원칙과 미묘하게
  다른 결이라 그대로 기록해둔다 - 이 경우엔 상당히 신빙성 있는 "진짜 안
  겹침" 신호로 읽을 수 있다). 성장률은 VOO의 v3.35 관측앵커(명목 7.59%)를
  출발점 삼아 메가캡 배제를 이유로 7%로 소폭 낮춰 잡았다.
- **BOTZ(로봇·AI)**: v3.39 3차에서 이미 매칭한 ROBO(KODEX 글로벌로봇)와는
  **다른 지수**(Indxx Global Robotics & AI Thematic Index vs ROBO Global
  Robotics & Automation Index)를 추종하는 별개 종목 - 상위비중이 산업용
  로봇(ABB/Keyence/Fanuc/SMC/Daifuku)과 AI(NVIDIA/Intuitive Surgical)로
  갈려 ROBO보다 집중도가 3배 이상 높다(top10 60.19% vs 17.59%). 겹침
  리포트에서 **SMH(반도체)·XLK(기술)와 각 9.6%p**(공통종목 NVDA 1개),
  QQQ 계열과 7.9%p, VOO 계열과 7.5%p - NVDA 비중(9.55%)이 이 겹침의 유일한
  원인이다.
- **BUG(사이버보안)**: Fortinet/Palo Alto/CrowdStrike/Okta 등 고성장 SaaS형
  보안기업 중심(top10 60.87%). 겹침 리포트에서 **CLOU(클라우드)와 8.2%p**
  (공통 2종목) - 보안·클라우드 테마가 실제로 종목을 공유함을 수치로 확인.

**국내 래퍼 3종 연결**(`scripts/analyze_krx_etfs_rsp_botz_bug_2026_08_08.py`):
Gap은 전부 중립밴드 안(488500 +1.12~+2.16%p, 464310 +0.29~+1.09%p, 418670
+1.92~+3.01%p). `rank_krx_etfs_2026_08_08.py`·`krx_overlap_report_2026_08_08.py`
둘 다 자동으로 `ledger_krx/`·`ledger_etf/` 전체를 다시 읽는 구조라 재실행만으로
반영됐다(코드 수정 불필요) - 순위표는 25→**31종목**, 겹침 리포트는 대상
28→**31종목**(실측 겹침 쌍 85→**96쌍**, 데이터없음 0건 유지 - 신규 3종 전부
top10_holdings를 처음부터 갖춘 채 추가했기 때문).

**부수 확인 - 418670 상품명 변경 이력**: 검색 중 KRX 공시에서 "TIGER
글로벌사이버보안INDXX"라는 구 상품명으로 된 2025-06-27 "ETF변경등록" 공시를
발견했다 - 418670이 그 사이 개명된 것으로 보인다(기초지수는 Indxx
Cybersecurity 지수로 동일하게 유지). 밸류에이션에는 영향 없는 정보성
사실이라 참고로만 남긴다.

국내 래퍼 총 **31종목**, 미국 원본 총 **22종목**. `ENGINE_VERSION`은 그대로
v3.41(순수 데이터 추가, 엔진 코드 변경 없음 - RSP/BOTZ/BUG도 기존
`ETFInputs`/`KRXWrapperInputs` 스키마를 그대로 썼다). 테스트 212개 불변.

**남은 공백(v3.39 계열 누적)**: 소재(XLB)·바이오(XBI, P/E 없음 - 방법론
한계)·은행·게임은 국내 래퍼 후보 자체가 없어 여전히 공백. 섹터 forward
P/E(트레일링 vs forward 축) 검증도 여전히 미해결.

## v3.46 — Phase 0 전수감사 체계 도입 + P0 결함 4건 수정 (2026-08-15,
사용자 제시 System Development Contract 이행)

**이번 작업의 성격이 기존과 다르다.** 지금까지의 감사는 전부 "방법론이
타당한가"(2026-08-01) 또는 "기록이 정직한가"(v3.32)를 봤다. 이번엔 사용자가
제시한 개발 계약서에 따라 **코드를 고치기 전에 저장소 전체를 감사하고, 발견을
우선순위로 분류하고, 현재 실제로 보장되는 불변조건을 문서화한 뒤**, 그중 P0만
최소 변경으로 고쳤다. 산출물 3건이 `docs/`에 상설로 남는다:

- **`docs/system_audit.md`** — 실제 import 관계·data flow·failure mode를
  추측이 아니라 코드 실행으로 재구성. 엔진 수정 전 필독.
- **`docs/change_plan.md`** — 결함 14건을 P0~P3 + REQUIRED_NOW/DEFERRED/
  NOT_JUSTIFIED로 분류하고 각 사유를 기록.
- **`docs/invariants.md`** — **현재 강제되는 조건(A절)**과 **아직 보장하지
  않는 조건(C절)**을 엄격히 분리. C절이 특히 중요하다 - 보장되지 않는 것을
  보장되는 것처럼 쓰지 않기 위한 장치다.

### 새로 발견한 P0 결함 4건 (전부 실행 재현, 과거 결과 영향 0건)

**① EBITDA≤0에서 레버리지 위험이 정반대로 산출됨 (가장 위험한 유형)**
`net_debt/EBITDA`에 도메인 가드가 없었다. EBITDA가 적자면 비율이 음수가 되고
`leverage_score()`는 음수를 "순현금"으로 해석해 **최저 위험점수(2.0)**를 준다.

| 순부채 +$30억 동일 | net_debt/EBITDA | leverage | DRS | 경고 |
|---|---|---|---|---|
| EBITDA −$5억(적자) | −6.00 | **2.0(최저위험)** | **22.4** | 없음 |
| EBITDA +$5억(흑자) | +6.00 | 20.0(최고위험) | 40.4 | 없음 |

**부실한 쪽이 DRS 18점 더 안전하게 나온다.** DRS는 ERP→할인율→Implied
Growth→Gap→판정과 시나리오확률·Confidence·RAR 전부를 타고 흐르므로 조용히
판정을 뒤집을 수 있다 - capex 부호 사고(v3.19)·RAR 100배 사고와 같은
"단위/부호가 조용히 반대로 작동하는" 계열이다. EBITDA=0이면 원인 설명 없는
ZeroDivisionError가 났다. `__post_init__`에 가드를 추가했고, **순현금
(net_debt<0, EBITDA>0) 정상경로 13종목은 건드리지 않는다**(분모만 검사).

**② `save_ledger()`가 같은 날짜 분석을 경고 없이 덮어씀**
파일명이 `<TICKER>_<날짜>.json`이라 같은 날 재실행하면 경로가 충돌한다.
실측 재현: Gap +0.10 저장 → Gap −0.30 저장 시 **파일 1개만 남고 1차 결과가
흔적 없이 사라졌다**(경고·예외·백업 없음). ledger는 이 프로젝트의 유일한
재현 근거라 조용한 소실이 가장 나쁜 실패다.

무조건 금지하지 않았다 - **내용이 같으면(analyzed_at 제외) 그대로 통과**
시킨다. "같은 입력으로 재실행해 값이 같은지 확인"이 이 프로젝트의 표준
검증 관행이기 때문이다(v3.19 sensitivity 근본수정, v3.32 33종목 전건 재실행).
내용이 다를 때만 `FileExistsError`를 던지고, 의도한 갱신(정성조사 결과 반영)은
`overwrite=True`로 명시하게 했다.

**③ 보험사 유보율이 정의역(0~1)을 벗어나도 통과**
3년 창에 손실 연도가 섞이면 배당성향이 음수가 되어 유보율 +2.50 →
지속가능성장률이 ROE보다 큰 30%가 나온다. 배당>순이익이면 유보율이 음수가
되어 성장률 부호가 뒤집힌다. 대재해 노출 재보험사(ACGL 유형)에서 충분히
현실적이다. 합산 순이익≤0이면 교차검증을 건너뛰고 사유를 기록한다 -
**분석 전체를 막지는 않는다**(is_insurer는 "병기, 자동판정 안 함" 경로이므로).

**④ 결정성 테스트 부재**
엔진은 실제로 결정적이었다(동일 입력 2회 실행 시 `analyzed_at` 외 전 필드
동일 - 실측 확인). 그러나 이를 고정하는 테스트가 0건이라 향후 비결정적 요소가
들어와도 잡히지 않았다.

### P1 2건 - 계산은 그대로, 사실만 드러냄

- **`data_completeness_pct`가 상수로 굳어 있었다**: ledger **34종목 전부**가
  기본값 0.9다 - 실제 데이터 완전성에서 산출된 적이 한 번도 없다.
  `confidence_score`에서 `round(0.9*15)=14`점이 무조건 부여되므로, Confidence는
  "데이터 완전성을 반영한다"고 표방하면서 **그 축의 판별력이 정확히 0**이다.
  자동 계산하지 않는다(무엇이 "완전한 데이터"인지 근거가 없어 새 공식을 만들면
  계약서 5.2절 위반). 점수도 안 바꾼다. 기본값을 그대로 썼다는 **사실만**
  `data_limitations`에 남긴다.
- **`VALIDATION_STATUS` 상수 신설**: ERP 매핑=`HEURISTIC_MAPPING`(실증근거
  없음), Confidence=`UNCALIBRATED`(**확률이 아님**)를 기계 판독 가능하게 명시.
  기존 docstring은 "고정 규칙, 임의 조정 금지"라는 규범만 말하고 인식론적
  지위는 말하지 않았다.

### ⚠️ 의도적으로 바꾸지 않은 것

ERP 매핑값, Lynch 캡, 판정 경계(±5%p), DRS 가중치, RAR 공식 - **하나도 안
바꿨다.** 근거 없이 유지하던 숫자를 근거 없는 다른 숫자로 바꾸는 것은 개선이
아니고(P/B 임계값·LYNCH_TYPE_CAPS와 동일 판단), 바꾸면 축적된 과거 ledger
전체와 비교 불가능해진다.

### DEFERRED (지금 하지 않는 것과 그 이유)

Point-in-Time / Provenance / Historical Replay / Confidence Calibration은
전부 보류했다. 핵심 이유: **원자료가 분석 스크립트 안 dict 리터럴로만 존재**해
Provenance를 제대로 하려면 데이터 수집 계층 자체를 신설해야 하고(최소 변경이
아니라 아키텍처 재작성), 과거 34종목의 `filing_date`는 지금 알 수 없어
`PIT_UNKNOWN`으로 둘 수밖에 없는데 그 상태에서 replay를 만들면 **"PIT 검증됨"
이라는 허위 표시**를 낳는다. Calibration은 표본이 5건(thesis_monitor 1차
실행)뿐이라 지금 만들면 표본 5건으로 확률을 주장하게 된다. 재개 조건을
`docs/change_plan.md`에 명시했다.

### 검증

저장된 34종목 ledger 입력값으로 **전건 재실행**해 Gap/RAR/판정/DRS/Confidence/
Realistic Growth/Implied Growth/강건성flip **8개 지표가 1e-12 정밀도로 완전
일치**함을 확인했다(v3.19/v3.32와 동일한 검증 방식). **ledger 파일은 하나도
수정하지 않았다.** 테스트 255 → 265개.

## 개별주식 스크리닝 2026-08-13 - 3종목 전수탈락, 원인 3갈래로 분리

(사용자 요청 "개별주식 스크리닝 가지고 있는 모든 것 이용해서 실행해") 원자료
출처를 Alpha Vantage MCP(INCOME_STATEMENT/CASH_FLOW/BALANCE_SHEET)로 바꿔
`scripts/screen_2026_08_13.py`를 실행했다 - 과거 배치는 WebSearch로 SEC
공시를 직접 뒤졌지만 이번엔 구조화된 MCP 도구가 있어 훨씬 빠르게 원자료를
확보했다. FMP MCP도 시도했으나 `quote`/`company`/`search-company-screener`
전부 세션 도중 간헐적으로 플랜 제한에 걸려(AAPL/META/MSFT는 되고 MCO/BSX/PODD는
안 되는 비일관적 패턴 - 재현 안 돼 원인 특정 보류) 시가총액 일부는 WebSearch로
보완했다.

**절차**: Trefis "S&P 500 52-Week Lows" 연재(2026-07-24~08-10)로 52주 저점
후보를 수집한 뒤, screener.py의 4분류 체크리스트로 재무데이터를 긁기 전에
먼저 걸러냈다. 9개 후보 중 3개(META/MCO/BSX)만 정량 스크리닝까지 진행했고
나머지 6개는 서사 확인 단계에서 제외했다(ORCL은 FCF 적자전환/Model N/A,
ROL/POOL은 실적 자체가 진짜 나빠짐, SNPS/NRG는 M&A가 5y CAGR 구간에 걸림
- GEN/BRO/ROP와 동일 패턴, APTV/LII/CHTR은 기존 배치에서 이미 제외된 종목
재등장).

**결과: 3종목 전수 탈락(0/3)** - 07-31/08-06 배치에 이어 세 번째 완전탈락
배치이나, 이번엔 탈락 사유가 종목마다 갈려 유용한 신호가 됐다:
- **META**: 현실적성장률 추정 12.86%로 성장은 넉넉히 통과 - AI capex가
  FY2020~2024 평균 20.63%/매출에서 FY2025 34.68%/매출로 +14.05%p 급증했음에도
  v3.20 capex 재검토 플래그가 **발동하지 않았다**(rg가 이미 8% 위라 재검토
  조건 자체가 안 걸림). 탈락 원인은 순수 밸류에이션(FCF수익률 3.13% vs 필요
  4.97%) - "capex 급증이 저평가를 만든다"는 가설이 실측으로 기각된 사례,
  $1.47조 시총 자체의 프리미엄이 주원인이었다.
- **BSX(Boston Scientific)**: 가장 근접(내재성장률 추정 5.77% vs 임계값
  5.5%, 필요 FCF수익률 5.20%에 실제 4.93%로 근소 미달). 성장(현실적성장률
  추정 13.63%)은 압도적으로 통과, 가이던스 소폭 컷(-2.3%)에 리콜·소송·
  WATCHMAN 성장둔화 우려가 겹쳐 서사가 과장된 공포과잉 패턴으로 판단했으나
  밸류에이션이 아직 그 정도로 싸지지는 않았다. 재확인 가치가 가장 높은 종목.
- **MCO(Moody's)**: 성장(4.26%)과 밸류에이션(내재성장률 추정 7.63%) 둘 다
  미달 - "레이팅업계 매크로 우려"라는 서사가 실제로는 2022년 금리급등발
  채권발행 급감(worst_yoy -12.06%)으로 뒷받침되는 실측 성장둔화였다.
  "공포과잉"처럼 보였지만 재무데이터를 긁어보니 "혼재"였던 사례.

**PODD(Insulet)는 프레임워크 자체가 안 맞아 정량모델에서 제외**: FCF가
2019~2022년 4개년 연속 적자였다가 2023년에야 처음 흑자전환(2023 $70.1M ->
2025 $377.7M) - 5y CAGR 프레임은 시작값이 음수면 성립하지 않는다(v3.19
가드와 동일 원리). cagr_base_year_override가 요구하는 "폭락 후 회복"과도
다르다(적자에서 흑자로 최초 전환하는 사례라 성격이 다름) - 매출 5y CAGR
24.52%·GLP-1 수요파괴 우려가 RMD와 동일하게 실측 반박됨 등 정성적으로는
긍정적이나, 억지로 5y 프레임에 밀어넣지 않고 정직하게 제외했다. FY2026
실적까지 확보되면 3y CAGR로 재시도 가능.

**이번 배치에서 엔진 코드 버그는 발견되지 않았다** - screener.py의 capex
재검토 로직(META)·5y CAGR 음수시작 가드 원리(PODD)·M&A CAGR왜곡 사전배제
원칙(SNPS/NRG) 전부 설계대로 작동했다. 스크립트 초안 작성 중 자체 검토로
잡은 실수 2건은 남겨둔다(이 프로젝트의 반복 원칙 - 결과를 먼저 안 뒤 정정한
사례를 숨기지 않음): META의 capex 재검토 플래그가 실제로는 안 걸렸는데
docstring 초안에 "플래그가 걸렸다"고 잘못 적었다가 screen() 실제 실행 결과로
정정했고, BSX의 현실적성장률을 암산으로 23.79%로 잘못 적었다가 재계산해
13.63%로 정정했다.

테스트 212개 불변(스크리닝 스크립트는 engine/screener.py를 그대로 호출할
뿐 엔진 코드를 건드리지 않음).

**2차 라운드(동일 08-13 세션, 사용자 "계속 스크리닝" 요청)**: 07-31 배치가
"특별처리 필요, 보류"로 남겨뒀던 APP(AppLovin)를 원인까지 구체적으로
재확인했고, 신규로 ALGN(Align Technology)을 조사했다 - 둘 다 정량모델
통과에는 이르지 못했다.

- **APP(AppLovin)**: 2024년 모바일게임 Apps 사업부 매각(Tripledot 매각,
  2024년 중 종결)이 분기 매출에 뚜렷한 구조단절을 남겼다(2024 Q1 $1,058M
  -> Q2 $711M 급락 후 재상승) - PTC Kepware/ThingWorx 매각과 동일 유형.
  Alpha Vantage 연결매출 시계열 자체도 재작성(discontinued operations
  재분류로 추정) 영향으로 보이는 이상치가 있어(2022년 $2.82B -> 2023년
  $1.84B 역성장) 5y CAGR을 그대로 계산하면 왜곡된다(2020->2025 FCF CAGR
  78.18%는 유기적성장이 아니라 상당부분 믹스효과). 대신 매각 이후로만
  완전히 클린한 TTM 비교(2025Q3~2026Q2 합산 vs 2024Q3~2025Q2 합산 - 둘 다
  구조단절 없는 구간)로 매출성장 47.6%를 확인했고, 이는 회사가 발표한 Q2
  실적 "53% YoY"와 근접해 신뢰성을 교차검증했다. 펀더멘털은 매우 강함
  (FY2025 EBITDA마진 79.4%, FCF $3,942.8M) - 하락은 회사 최초 가이던스
  미스(Q2 2026, Wells Fargo/Piper Sandler 하향)에 이전 공매도 의혹
  (CapitalWatch, 자금세탁방지 통제 관련 - 회사는 부인)이 겹친 결과였다.
  **PODD와 마찬가지로 5y CAGR 프레임에 억지로 밀어넣지 않고 이번에도
  정량모델에서 제외했다** - 2차 출처(세그먼트별 매출)와 Alpha Vantage
  연결매출이 서로 정합적이지 않아, SEC 10-K 원문을 직접 대조하지 않고는
  믿을 만한 세그먼트조정 CAGR을 만들 수 없다고 판단했다(정직하게 미해결로
  남김).
- **ALGN(Align Technology)**: 4분류 3번(진짜나빠짐)으로 신규 제외. Alpha
  Vantage 연간매출 실측: 2021년 $3.95B -> 2022년 $3.73B(역성장) -> 2023년
  $3.86B -> 2024년 $4.00B -> 2025년 $4.03B - 4년째 사실상 정체. 회사 자체
  Q1 2026 실적도 YoY ~2% - 경쟁심화(저가 투명교정 대체재)·시장포화 서사가
  최근 4년 추세와 최신 분기 실적 둘 다로 뒷받침됐다. 표면 5y CAGR
  (2020->2025)은 10.29%로 통과권처럼 보이지만 2020년이 코로나로 눌린
  저점이라(BKNG와 동일한 기저효과 함정) 부풀려진 숫자였다 - 재무데이터를
  더 긁지 않고 서사 확인 단계에서 제외했다.

테스트 212개 불변(2차 라운드도 엔진 코드 무변경).

## BSX 정식분석 - screen()이 문서화된 "worst error"(거짓 탈락)를 실제로
낸 첫 사례 (2026-08-13, 사용자 요청 "BSX 정식분석")

스크리닝에서 임계값에 가장 근접했던 BSX(내재성장률 추정 5.77% vs 5.5%)를
`scripts/analyze_bsx_2026_08_13.py`로 정식분석했다. Alpha Vantage 원자료를
2015~2025 11개년으로 확장(10y CAGR 산출 가능), 경쟁구도는 WebSearch로
실측(EP 전체시장 J&J 54% vs BSX 9%, 다만 고성장 PFA 하위세그먼트에서는
Farapulse가 Medtronic 대비 74% 점유 - J&J가 "이 시장 싸움은 매우
개인적"이라 할 만큼 반격 중).

**결과: "저평가 가능성" 통과** (Gap +5.87%p, DRS 43.80, Realistic Growth
13.86%, Implied Growth 8.00%(two_stage 채택, single_stage와 괴리 2.21%p로
경고 임계값 3%p 미만), RAR +0.4926, 강건성점검 flip 없음, Confidence
94/100). 스크리너가 예측한 방향(밸류에이션 문턱 근접)이 맞았고, 실제
경쟁강도를 반영하니 통과권으로 들어왔다.

**Lynch 유형 분류 애매 - 논바인딩이라 영향 없음**: 엔진이 자동으로
"cyclical"을 채택하며 "[분류 애매]" 경고를 냈다(cyclicality_raw_score
16.4가 cyclical 기준을 충족하지만 revenue_cagr_5y 15.16%도 fast_grower
기준을 충족). 원인은 2020년 코로나로 인한 일시적 매출감소(YoY -7.66%)가
cyclicality 점수를 밀어올린 것 - BSX 자체가 경기순환에 취약한 사업이라서가
아니다. 다만 Realistic Growth(13.86%)가 cyclical 상한(20%)·fast_grower
상한(25%) 어느 쪽에도 안 걸려 있어 이 분류 논쟁은 결과에 실질적 영향이
없다 - lynch_type_override는 쓰지 않았다.

**⚠️ 핵심 발견 - screener.py가 스스로 문서화해둔 "가장 나쁜 오류"(거짓
탈락)가 실제로 발생한 첫 사례.** `tests/test_screener.py`의 골든테스트
(`test_screener_reproduces_known_buy_verdicts`)가 이 ledger를 자동으로
집어 재확인하다가 실패했다 - `screen()`이 방금 "저평가 가능성"으로 확정된
BSX를 **탈락시킨다**. 원인을 분해해보니 DRS 5개 구성요소 중 정확히
competition_intensity 하나 때문이었다(screener 추정 DRS 50.6 vs 정식
DRS 43.8, 격차 6.8점이 전부 이 항목에서 발생 - leverage/cyclicality는
거의 일치, revenue_volatility/margin_volatility는 서로 반대방향으로 정확히
상쇄됨). `estimate_drs()`는 competition_intensity를 상수 12.0(ledger
중앙값)으로 가정하는데, BSX의 실제 연구된 값은 5.4(경쟁자 J&J·Medtronic
둘 다 위협도를 상대적으로 낮게 평가 - RMD와 정확히 같은 값). **이건
engine/screener.py가 이미 문서화해둔 "알려진 한계 2건째"(PDD 실사례,
competition_intensity 상수 placeholder)와 같은 결함의 반대방향 사례다** -
PDD는 실제값이 상수보다 높아 Gap이 과대평가됐을 뿐 판정 방향은 안 바뀌었는데,
BSX는 실제값이 상수보다 낮아 **판정 자체가 뒤집혔다**(스크리너 탈락 vs
정식분석 통과).

**상수를 고치지 않았다** - ledger 34종목 전수로 competition_intensity
분포를 재확인한 결과 중앙값은 여전히 정확히 12.0(평균 11.91)이었다.
문제는 상수가 틀려서가 아니라, median 대체 방식 자체가 median에서 크게
벗어난 개별종목(RMD·BSX 5.4대, VRSN·ROP 3.6~4.8대)에서는 구조적으로
오분류한다는 것 - 이건 애초에 screener.py가 "1차 필터일 뿐, 정식분석으로만
확정할 것"이라 못박아둔 이유 그 자체다. 상수를 낮추면 다른 종목들의 판정이
조용히 바뀔 위험이 있어(예: median을 낮추면 실제로 경쟁강도가 높은 종목들이
대거 통과권으로 잘못 이동) 손대지 않고, 대신 `engine/screener.py` docstring에
이 실사례를 기록하고 `tests/test_screener.py`에 `KNOWN_SCREENER_FALSE_
REJECTIONS = {"BSX"}`로 문서화된 예외를 남긴 뒤, 이 예외의 근거(DRS 추정치가
정확히 50.6이고 여전히 탈락하는지)가 유효한 채로 남아있는지 확인하는 회귀
테스트(`test_bsx_false_rejection_is_still_reproducible`)를 추가했다 -
나중에 엔진이 바뀌어 이 discrepancy가 저절로 사라지면 테스트가 실패해
예외를 빼야 한다는 신호를 준다.

`ENGINE_VERSION`은 그대로 v3.41(엔진 로직 무변경 - docstring과 테스트만
수정). 테스트 212 -> 213개.

## v3.42 — Thesis Monitor 신설: 프로젝트 최초의 외부 검증 루프
(2026-08-13, 사용자 요청 "프로젝트를 다른 차원으로 발전시킬 아이디어")

**진단**: 이 프로젝트의 모든 통제가 답하는 질문은 하나였다 - "계산이 자기
자신과 일치하는가". 단위규약·버전스탬프·ledger무결성·골든테스트·판정규칙
단일화가 전부 내부 일관성이다. **"계산이 현실과 맞았는가"에 답하는 장치는
단 하나도 없었다.** 2026-08-01 감사가 High-1로 이미 지적했고, 그때 대응은
*전제조건*(`falsification_conditions`, `price_at_analysis`)을 만든 것이었다.

**12일 뒤 확인해보니 그 전제조건이 이미 데이터로 익어 있었다** - 반증조건에
적어둔 트리거 날짜 5건(DUOL 08-05 / PGR 08-04 / TTD 08-06 / MNDY 08-10 /
SE 08-11 Q2 실적)이 **전부 기한이 지났는데 아무도 열어보지 않았다.**
`engine/thesis_monitor.py`가 그 루프를 닫는다.

### 두 축으로 감시한다

1. **반증조건 기한도래**(`scan_falsification_conditions`) - 반증조건 원문에서
   날짜를 추출해 기한도래분을 골라낸다. 사전등록된 이진 예측이라 **통계적
   검정력이 필요 없다** - 분석 이력이 3주뿐인 지금 유효한 유일한 검증이다.
2. **시가총액 부식**(`recompute_gap_at_market_cap`) - 펀더멘털 입력을 전부
   고정하고 시총만 갱신해 Gap 재계산. 새 방법론 0줄.

### ⚠️ 설계상 못박은 두 가지

**(1) 재계산 결과를 `ledger/`에 절대 저장하지 않는다.** `run_analysis()`
재호출은 오늘 날짜로 스탬프된 완전한 결과를 만드는데, 저장하면 같은 티커
ledger가 2개가 되어 "종목당 1건" 규칙이 즉시 깨진다(v3.32 WM/WCN/IDXX 중복이
CLAUDE.md 통계까지 오염시킨 사고와 동일 유형). 메모리 계산 + `reports/`
출력만 한다 - 재계산값은 공식 판정이 아니라 모니터링 신호다(is_insurer/
sbc_cross_check와 동일한 "병기, 자동판정 안 함").
테스트로 고정: `test_recompute_never_writes_to_ledger_dir`.

**(2) 기한이 지난 날짜를 자동으로 "반증됨"이라 판정하지 않는다.** 정규식은
트리거 날짜와 서술적 날짜를 구분 못 한다 - 실제로 TCOM의
`2024-04-30~2026-01-13`은 증권소송 **집단기간**(과거 서술)이라 확인할 이벤트가
아니었다. 첫 실행에서 정확히 이 오탐이 났고, 자동 필터링 대신 **날짜 주변
문맥을 함께 반환해 분석자가 즉시 분류**하게 했다(false negative가 이 용도에서
가장 나쁜 오류다).

### 1차 실행 결과 - 5건 전건 검증(2026-08-13)

| 종목 | 조건 | 실측 | 판정 |
|---|---|---|---|
| **TTD** | Q2 매출 < 가이던스 $750M(조건4) 등 | **$715M**, Q3 가이던스 -12.1%YoY 역성장, CFO·CMO·커머셜총괄 동시교체 | **4개 중 3개 발동 - 반증** |
| SE | Shopee EBITDA 전년比 감소(조건1) | **+12.2%YoY 증가**, 그룹 EBITDA +10.6% | 미발동 - **우려 정면반전** |
| DUOL | DAU <20%YoY(조건2) | **+23%YoY, 전분기 대비 가속** | 미발동 - **강화** |
| PGR | 컴바인드레이쇼 ≥90(조건3) | 87.3%(전년 86.2%, 6월 단월 90.0) | 미발동, 방향은 악화 |
| MNDY | 상단코호트 NDR <110%(조건1) | 전사 NDR 109%만 공개, **코호트별 미확보** | **확인필요**(추측 안 함) |

**TTD는 이 프로젝트 반증장치가 실제로 발동한 첫 사례다.** 매수리스트에
4.80% 편입돼 있는 S등급 종목이라 판정 재검토가 필요하다 - 다만 **공식 판정은
건드리지 않았다**(ROP 승격 때와 동일하게 사용자 명시적 승인이 필요한 사안).

### ⭐ 가장 중요한 발견 - Gap 단독 사용이 가치함정을 만든다(실측 입증)

시가총액 부식 분석이 TTD에서 결정적 사례를 보여줬다:

```
TTD  주가 -26.3%  →  Gap +17.01%p → +20.96%p  (부식 +3.95%p = 더 싸 보임)
     같은 기간에 반증조건 3개 동시 발동 = 사업 서사는 무너짐
```

**주가가 빠지면 Gap은 반드시 벌어진다**(Implied Growth만 내려가고 Realistic
Growth는 재무제표에서만 나오므로 불변) - 즉 **사업이 나빠져서 주가가 빠진
경우에도 이 엔진의 핵심 지표는 "더 사라"고 말한다.** 이게 가치함정의
수학적 원리이고, Gap을 반증조건과 **반드시 함께** 봐야 하는 이유가 처음으로
실측으로 입증됐다. 테스트로 이 성질 자체를 고정해뒀다
(`test_gap_widens_when_price_falls_and_narrows_when_price_rises`).

반대 방향도 확인됐다 - SE는 반증조건이 미발동하며 서사가 강화됐는데 주가가
+19.3% 올라 Gap이 -2.14%p 부식됐다(좋은 소식이 이미 가격에 반영됨).

### 부수 발견 - `price_at_analysis` 부재 비용이 처음 실측됐다

34종목 중 **24종목은 부식 계산 자체가 불가능**했다. `price_at_analysis`가
v3.24(2026-08-01) 도입 필드라 그 이전 분석에 없기 때문이다. v3.24가 "향후
백테스트의 전제조건"이라며 만든 필드의 가치가 12일 만에 구체적 비용으로
드러난 셈 - **앞으로 모든 분석에서 반드시 채울 것**(TTD만 예외적으로 과거
시세를 별도 조회해 메웠다 - 반증조건이 실제 발동한 종목이라).

`ENGINE_VERSION` v3.41 -> v3.42(신규 모듈 배선, v3.32 규칙). 테스트 213 ->
226개. 리포트: `reports/thesis_monitor_2026-08-13.json`.

## v3.43 — Growth Scorecard 신설: 주가보다 빠른 외부 검증축
(2026-08-13, v3.42에 이어진 같은 세션)

v3.42 thesis_monitor가 **이진** 예측(반증조건)을 감시한다면, 이 모듈은 엔진의
핵심 **연속값** 주장인 Realistic Growth를 회사가 실제로 내놓는 숫자와 대조한다.
**주가 검증은 몇 년이 걸리지만 성장률 검증은 분기마다 가능하다** - 지금 확보
가능한 가장 빠른 외부 검증축이다.

**승격 근거 - 이미 손으로 5번 했다.** ROP(엔진 12% 캡 vs 오가닉 5~6%)·KEYS
(1.47% vs 가이던스 28%)·TCOM(12.39% vs +3~8%)·GEN(14%대 vs 6.5~8.5%)·MNDY
(FCF CAGR 230% 기각을 회사가 자인) 전부 정성조사 중 우연히 나온 부산물이었고,
ROP는 `realistic_growth_override` 신설로, KEYS는 일회성 크로스체크 스크립트로
각각 따로 대응했다. 실증사례 5건이면 `capex_intensity`가 NVO 1건으로 배선된
선례보다 근거가 두텁다.

### ⚠️ 가이던스를 정답지로 쓰지 않는다 - 관측치 종류를 절대 섞지 않음

Realistic Growth는 n≈12년짜리 **장기** 개념이고 가이던스는 **1개년 예측**이다.
직접 비교해 채점하면 CLAUDE.md가 KEYS 크로스체크에서 이미 경고한 실수를 그대로
반복한다. `OBSERVATION_KINDS`로 증거력을 라벨링해 저장하고 섞어 평균내지
않는다: `realized_multiyear`(다년 실현, **유일하게 공식판정 승격 근거**) >
`realized_quarterly`(분석 이후 1개 분기) > `guidance_annual`(예측 - 괴리
플래그로만 사용). `usable_as_override` 필드가 이 자격을 명시한다.

### ⭐ 읽는 법에 비대칭이 있다(오독 방지)

Realistic Growth는 `min(FCF CAGR, 매출가중CAGR) x (1-구조적할인)`이라 **설계상
원시 성장률보다 낮게 나온다.** 따라서 **관측치 > 엔진추정은 정상**(할인이
의도대로 작동)이고, **관측치 < 엔진추정이 경고**다(이미 보수적으로 깎은
추정치조차 회사가 못 내고 있다는 뜻이라 해석 여지가 좁다).

### 신설: 저평가 판정 최소선(breakeven_growth)

Implied Growth는 시총·FCF0·r·n에서만 나와 **성장률 입력과 완전히 독립**이므로,
"저평가로 판정되려면 Realistic Growth가 최소 몇 %여야 하는가" = Implied Growth
+ 판정밴드(5%p)는 **분석자 주관이 개입할 수 없는 객관적 기준선**이다. ETF
엔진의 `required_growth_thresholds()`(v3.34)가 확립한 발상을 회사 엔진에 그대로
옮긴 것 - 두 엔진이 같은 사고방식을 공유하게 됐다. 안내값이 실제로 그 판정을
내는지 테스트로 고정했다(v3.35가 ETF에서 잡은 자기모순의 회사판 예방).

### 1차 실행 결과 - 11개 관측치 / 9종목 (2026-08-13)

| 종목 | 엔진RG | 저평가최소선 | 관측 | 판정변화 |
|---|---|---|---|---|
| **TTD** | 16.70% | 4.69% | Q2 실적 **+3.0%** / Q3 가이던스 **-12.1%** | **저평가→적정가 / 저평가→과대평가** |
| TCOM | 12.39% | 9.84% | 가이던스 +3~8%(중간 5.5%) | **저평가→적정가** |
| KEYS | 1.47% | 20.84% | 가이던스 28% | **과대평가→저평가**(반대방향) |
| DUOL | 25.00% | 10.55% | Q2 +18.3% / FY가이던스 +16.3% | 불변(여유 큼) |
| MNDY | 25.00% | 6.72% | Q2 +22% | 불변 |
| SE | 23.56% | 9.38% | Q2 **+48.1%** | 불변(대폭 상회) |
| PGR | 16.87% | 0.11% | Q2 순보험료 +6% | 불변(IG 음수라 쿠션 큼) |
| GEN | 12.00% | 4.82% | 가이던스 6.5~8.5% | 불변 |
| ROP | 5.50% | 9.26% | 오가닉 5.5% | 불변(**통제사례** - v3.28에서 이미 정정돼 괴리 0.00%p) |

**⭐ TTD가 완전히 독립적인 세 번째 경로로 확인됐다.** v3.42는 반증조건(이진
사건)으로, 이 모듈은 성장률 입력(연속값)으로 같은 결론에 도달한다 - **저평가
판정 최소선이 4.69%인데 회사가 직접 제시한 Q3 가이던스는 -12.1% 역성장**이다.
서로 다른 검증축이 같은 종목을 가리키는 건 우연일 가능성이 낮다.

**ROP가 통제사례로 작동한 것도 중요하다** - 이미 정정된 종목이라 괴리가 정확히
0.00%p로 나온다. 채점표가 "이미 닫힌 루프"를 올바르게 인식한다는 뜻이다.

`ENGINE_VERSION` v3.42 -> v3.43. 테스트 226 -> 237개(손으로 만든 선례 2건을
1e-12/1e-9 수준으로 재현하는 골든테스트 포함 - 재현 못 하면 승격 자체가
틀린 것이므로). 리포트: `reports/growth_scorecard_2026-08-13.json`.

## v3.44 — Gap Distribution 신설: BSX 사건의 일반해, 그리고 가설 기각
(2026-08-13, v3.42/v3.43에 이어진 같은 세션)

BSX screener false-rejection(이 세션 앞부분)의 본질은 "큰 분산을 가진 값
(경쟁강도, 실측범위 3.6~20.0)을 점 하나(중앙값 12.0)로 대체했더니 판정이
뒤집혔다"는 사건이었다. 정식 엔진도 구조가 같다 - Gap 하나를 ±5%p 밴드에
대고 판정하는데, 그 Gap을 만드는 주관적 DRS 입력(demand_sensitivity_pct·
competition_intensity)이 얼마나 흔들리는 값인지는 아무도 몰랐다.
`engine/gap_distribution.py`가 이 질문에 답한다 - 이 프로젝트가 34종목에서
**실제로 쓴 값의 범위**로 삼각분포를 만들어(지어낸 사전분포 아님) Monte
Carlo로 Gap·판정의 흔들림을 측정한다.

**⭐ 처음 제안한 가설("P(저평가)=73% 형태")이 실측으로 기각됐다 - 그대로
기록한다.** 34종목 전부 P=100%/0%, 취약 판정 0건. 원인은 가짜 발견이 아니라
엔진 설계의 실제 성질이다: `erp_from_drs(drs) = 0.05 + (drs/100)*0.03`이
DRS 0~100 전체를 ERP 5~8%로만 사상한다. demand_sensitivity_pct는
cyclicality_score에 최대 +4점만 기여하도록 공식 자체가 상한을 걸어뒀고,
competition_intensity를 관측범위 끝에서 끝까지(3.6→20.0) 흔들어도 DRS는
~16.4점만 움직인다 - 합쳐도 ERP는 최대 ~0.6%p, 경계(±5%p)에 가장 근접했던
PTC(공식 Gap +5.53%p)조차 표준편차 0.17%p로 한 번도 경계를 못 넘었다.

**⭐ 결론 - 취약성은 성장률 축에 있지 DRS 축에는 없다.** 이건 v3.43
growth_scorecard의 발견과 정확히 대비된다 - growth_scorecard는 TTD/TCOM/KEYS
3종목의 판정이 **성장률 가정 하나만으로** 실제로 뒤집힘을 보였는데,
gap_distribution은 같은 종목들이 **경쟁강도·수요민감도 가정**으로는 이
프로젝트가 실제 축적한 범위 안에서 전혀 안 뒤집힘을 보인다. 두 모듈을
나란히 놓으면 "판정을 재검토할 가치가 있는 곳은 성장률 입력이지 DRS
리스크스코어링 입력이 아니다"라는 구체적 결론이 나온다 - 원래 가설(확률
분포로 흔들리는 판정을 찾겠다)은 기각됐지만, 그 기각 자체가 더 유용한
답을 줬다(이 프로젝트가 반복 실증해온 "가설을 실측으로 검증하고 틀리면
정직하게 정정한다" 원칙과 동일선상 - META capex 가설 기각과 같은 계열).

**설계**: 재구성한 DRS/r/Gap이 perturbation 없이(분석자의 실제 판단값
그대로) ledger 원본과 1e-9~1e-12 수준으로 정확히 일치하는지부터 테스트로
고정했다(자기일치 없이는 나머지 전부 무의미) - 5개 티커로 검증 완료.
관측범위는 하드코딩하지 않고 corpus에서 직접 뽑는다(screener.py의
competition_intensity 중앙값 재검증과 동일 원칙 - corpus가 자라면 범위도
갱신됨). 재현성을 위해 seed 고정. 결과는 `reports/`로만, `ledger/`는
불변(병기, 자동판정 안 함).

`ENGINE_VERSION` v3.43 -> v3.44. 테스트 237 -> 250개. 리포트:
`reports/gap_distribution_2026-08-13.json`.

## v3.45 — Market-Relative Gap 신설: 회사·ETF 두 엔진 최초 연결, 그리고
경계를 스스로 좁힘 (2026-08-13, v3.42~v3.44에 이어진 같은 세션)

사용자가 제안한 "다른 차원 아이디어" 4개 중 마지막 - 회사 엔진과 ETF
엔진을 연결해 "이 종목이 싼가, 아니면 시장 전체가 싼가"에 처음 답한다.
ETF 엔진(v3.33~)이 이미 계산해온 VOO의 시장요구성장(breakeven ≈7.07%)이
정확히 그 기준선인데 두 엔진이 만들어진 이후 한 번도 연결된 적이 없었다.

**⭐ 원래 구상(요인분해)을 실제로 짜보고 스스로 기각했다.** 처음엔 Gap을
"시장 재평가분 + 종목 고유분"으로 가르는 팩터모델을 생각했으나, 구현하며
확인한 것은 **이 코드베이스에서 그 분해가 애초에 정당화되지 않는다**는
사실이었다 - 회사 엔진의 r은 DRS(개별위험도)에서, ETF 엔진의 r은 ERS
(지수위험도)에서 나오며 **공유하는 계산 경로가 전혀 없다**(팩터모델이려면
공통 베타/공통 할인율원천이 있어야 "시장이 움직이면 이만큼 같이 움직인다"는
인과적 분해가 성립하는데, 이 둘은 애초에 독립 설계다). gap_distribution
(v3.44)이 가설을 실측으로 기각한 것과 같은 계열 - 이번엔 실측 전에
설계단계에서 스스로 막았다.

**대신 정직하고 검증 가능한 걸 만들었다 - 벤치마크 차감**(`engine/
market_relative.py`):
  - `relative_gap = Gap_company - Gap_VOO` - "VOO를 그냥 사는 것보다 이
    종목이 몇 %p 더/덜 싸 보이는가". 투자자의 실제 선택지가 "이 종목이냐
    시장 평균 공정가치냐"가 아니라 "이 종목이냐 그냥 인덱스냐"이므로 오히려
    실무적으로 더 맞는 질문이기도 하다.
  - `growth_premium = IG_company - IG_VOO` - "시장이 이 종목에 시장평균보다
    몇 %p 더 낙관적인 성장을 이미 가격에 반영했는가". Gap과는 다른 축.

**⚠️ 분자가 다르다는 한계도 명시했다** - 회사는 FCF 기반, VOO는 이익
기반 Gordon 역산이라 레벨을 정밀하게 읽으면 안 되고 방향·순위만 신뢰할 것.

VOO 기준선(가장 보수적 = 가장 비싼 P/E, `etf_pipeline.format_comparison_
table()`과 동일 규칙): Implied Growth 7.07%, Gap +0.90%p.

### 1차 실행 - 34종목 전수, 새로운 취약성 신호 1건 발견

**VRT가 두드러진다** - 절대 Gap은 겨우 +1.72%p(CLAUDE.md에 이미 "사실상
동전던지기"로 기록된 종목)인데, **성장프리미엄은 +9.92%p로 34종목 중
1위**다. 시장이 이 종목에 시장평균보다 훨씬 낙관적인 성장을 이미 가격에
반영해뒀다는 뜻 - 이미 얇은 마진 위에 낙관까지 얹혀 있다는, 기존에 없던
각도의 취약성 신호다. KEYS·PH(둘 다 과대평가 판정)는 예상대로 "시장평균
보다 낙관적으로 가격됐고 실제로도 고평가"로 일관되게 나왔다. PDD·ACGL·
PGR·TTD·GEN 등 다수는 "시장평균보다 비관적으로 가격됐는데 실제로는
저평가"(성장프리미엄 뚜렷한 음수 + Gap 뚜렷한 양수) 조합으로, 시장이 이
종목들을 시장 전체보다 더 비관적으로 보고 있다는 걸 별도 축에서 확인해줬다.

테스트로 고정한 것: 대수 항등식 자기검증(relative_gap이 순수 뺄셈과 정확히
일치), 시장 Gap이 0인 가상 케이스에서 relative_gap이 절대 Gap과 일치하는
회귀 케이스, 공식 판정을 재계산하지 않고 그대로 병기하는지.

`ENGINE_VERSION` v3.44 -> v3.45. 테스트 250 -> 255개. 리포트:
`reports/market_relative_2026-08-13.json`.

이로써 사용자가 제안받은 4개 방향(반증조건 감시견/성장률 채점표/Gap
확률분포화/두 엔진 연결)을 전부 구현했다 - v3.42~v3.45, 신규 모듈 4개,
테스트 213->255개.

## 개별주식 스크리닝 2026-08-14 - 3종목 전수탈락(screen() 미호출), 서사-실측
괴리 1건 (사용자 요청 "스크리닝 실행")

Trefis 52주 저점 연재(2026-08-13/14)로 KR(Kroger)·AGCO(AGCO Corp)·RBA(RB
Global) 3종을 수집해 조사했다 - 원자료는 Alpha Vantage MCP(INCOME_STATEMENT/
CASH_FLOW)로 확보하고 WebSearch로 하락 서사를 확인하는 기존 방식을 그대로
따랐다. **이번 배치는 4분류 체크리스트 단계에서 3종목 전부 걸러져 `screen()`을
한 번도 호출하지 않았다** - `CANDIDATES` 리스트가 비게 된 첫 사례다.

- **KR(Kroger) - 4분류 3번(진짜나빠짐), 서사보다 실제가 더 나빴던 사례.**
  WebSearch로 발견한 "flat sales" 서사를 그대로 믿지 않고 Alpha Vantage
  연간 손익계산서로 직접 대조했다: 매출은 실제로 4년째 $147~150B 박스권
  (FY2023 $150.04B→FY2024 $147.12B→FY2025 $147.64B)으로 서사와 일치했지만,
  **영업이익이 서사에 언급도 안 된 채 훨씬 심각하게 무너지고 있었다**
  (FY2023 $4.13B→FY2024 $3.10B→FY2025 $3.85B→FY2026(FYE 2026-01-31)
  $1.89B, 최근 1년새 -50.9%). 자동화 물류창고 축소·점포 구조조정 비용,
  할인점(Aldi/Lidl) 경쟁강도 심화가 마진 붕괴로 직접 확인됨 - **이번
  프로젝트가 지금까지 봐온 "서사가 실제보다 과장된" 패턴(BSX)과 정반대로,
  서사가 오히려 실제보다 관대했던 사례**라 별도로 기록해둔다.
- **AGCO(AGCO Corp, 농기계) - 4분류 3번(진짜나빠짐), 확정.** 매출이 FY2023
  고점 $14.41B에서 FY2024 $11.66B(-19.1%YoY)→FY2025 $10.08B(-13.5%YoY)로
  2년 연속 두 자릿수 역성장, 영업이익은 같은 기간 $1.70B→$698.7M로 -59%
  급감. 유럽(특히 독일) 수요 예상치 하회·남미 여신경색·가이던스 하향이
  손익계산서에 그대로 반영돼 재무데이터 확보 전부터 이미 명확했다.
- **RBA(RB Global, 산업장비 경매) - 프레임워크 부적합(M&A가 5y CAGR 구간에
  걸침), FRAMEWORK_MISMATCH로 분류. 서사와 실측이 정반대로 갈린 유일한
  사례.** WebSearch 서사(서비스 테이크레이트 -110bp, 사업믹스 저마진화)만
  보면 4분류 3번처럼 보였으나, Alpha Vantage 분기 실측을 대조하니
  **정반대였다** - 가장 최근 분기(2026Q2) 매출 +11.05%YoY, 2026Q1
  +11.37%YoY로 감속 기미가 전혀 없고 영업이익도 분기마다 $2.0~3.2억대로
  견조했다. 문제는 연간 5y CAGR 쪽에서 나왔다 - IAA(자동차 경매) 인수가
  2023년에 종결되며 매출이 2022년 $1.73B에서 2023년 $3.68B로 +112% 급증
  (GEN/BRO/ROP/SNPS/NRG와 동일한 'M&A가 CAGR 구간 중간에 걸리는' 패턴),
  5y CAGR(2020→2025)이 매출·FCF 둘 다 27~28%로 나오지만 유기적성장이
  아니라 대부분 인수 단계상승이다. 인수 이후로만 깨끗한 분기별 YoY
  (+7~11%, 전부 2024Q3 이후 구간)는 오히려 견조한 성장을 보여주지만,
  screener.py의 5y CAGR 프레임(5y 기준으로 보정된 임계값)에 억지로
  밀어넣으면 왜곡된 숫자가 되므로 SNPS/NRG와 동일하게 세그먼트조정 없이는
  정량모델에 넣지 않기로 했다. **서사만 보고 판단했다면 '진짜나빠짐'으로
  잘못 분류했을 후보** - 재무데이터 대조가 실제로 판단을 뒤집은 사례로
  기록해둔다.

`scripts/screen_2026_08_14.py` 신규(CANDIDATES가 빈 채로 실행되는 최초
케이스 - `PREFILTERED_OUT`/`FRAMEWORK_MISMATCH`만으로 결과를 출력하도록
`main()`에 분기 추가). 엔진 코드 변경 없음, 테스트 255개 불변.

**후속 조사(동일 08-14 세션, 사용자 "계속" 요청) - NOW(ServiceNow), RBA와
유형은 같으나 결론이 다른 사례.** Trefis 52주 저점 풀이 소진돼(최신 리스트가
이미 다룬 종목만 반복 등장) Morningstar "3 Stocks to Buy in August" 등으로
출처를 넓혀 ServiceNow(NOW)를 발견했다 - "AI 에이전트가 엔터프라이즈 SW
좌석을 대체한다"는 공포 서사(2026-01 촉발)로 52주 고점($194.73) 대비 -53%
하락. Alpha Vantage 실측: 분기 매출 YoY가 오히려 가속(Q3'25 +21.8% →
Q4'25 +20.7% → Q1'26 +22.1% → **Q2'26 +24.0%**) - 서사와 정반대라 처음엔
RBA와 같은 '서사 vs 실측 반전' 패턴으로 보였다. **그런데 대차대조표에서
goodwill이 Q1'26 $4.54B → Q2'26 $9.84B로 한 분기만에 2배 이상 급증**한 걸
발견해 조사를 확장했고, ServiceNow가 연쇄 M&A를 진행 중임을 확인했다
(Moveworks $2.85B 2025-12 종결, Veza 2026H1 종결, Armis $7.75B - 사상 최대
규모 - 2026H2 종결예정). **RBA와 동일한 'M&A가 성장구간에 걸치는' 패턴**이
단일 인수가 아니라 연쇄 인수라 더 심하게 오염됐을 가능성이 높다 - 방금 확인한
"가속하는" 분기 매출 성장이 순수 유기적성장이 아니라 상당부분 인수효과일 수
있다는 뜻. 게다가 애초 서사 조사에서 확인했던 "organic cRPO growth
decelerating to high teens"(M&A와 무관하게 보고되는 유기적 계약잔액 지표)는
**실제 유기적 성장 둔화 신호**였다 - 즉 이 종목은 공포과잉이 아니라 (a)
연결 실적은 M&A로 부풀려져 5y CAGR 프레임에 넣을 수 없고, (b) 그 왜곡을
걷어낸 유기적 지표조차 이미 감속 중이라는 이중의 문제를 가진다. RBA처럼
FRAMEWORK_MISMATCH로 분류하되, RBA와 달리 "정량모델만 못 쓸 뿐 서사는
과장됐다"고 볼 근거가 약해 재무데이터를 추가로 긁지 않고 여기서 멈췄다 -
**같은 'M&A가 CAGR 구간에 걸침' 표면 패턴이라도 유기적 지표까지 확인해야
결론이 갈릴 수 있다**는 걸 RBA와의 대비로 보여주는 사례.

추가로 CL(Colgate-Palmolive)은 서사·실측이 일치하는 완만한 조정(-14%YTD,
EPS -6%, 마진압박 21.1%→19%)이지만 완숙 소비재 stalwart로 이 스크리너의
MIN_REALISTIC_GROWTH(8%) 임계값과 구조적으로 안 맞아 재무데이터 확보 전
제외. AA(Alcoa, 원자재 경기순환주)·NBIS(Nebius, AI인프라 초고성장주지만
상장이력이 5y CAGR 프레임엔 너무 짧음)도 같은 이유로 확인 전 단계에서
제외했다. 엔진 코드 변경 없음, 테스트 255개 불변.

**후속 조사 2차(동일 08-14 세션, 사용자 "계속 스크리닝" 요청) - 러셀3000
확장, 8종목 전수탈락.** S&P500 52주 저점 풀이 완전히 소진돼 Trefis
"11 Stocks Hit 52-Week Lows On Thursday"(2026-08-14, Russell 3000 전체
대상)로 범위를 넓혔다. 신규 8종목(GPI/UVV/AMSF/LMB/ARDX/WBTN/EMAT/SECZ)
전부 정량 스크리닝 이전 단계에서 제외됐다 - 이번엔 서사-실측 반전 같은
발견은 없었고, 규모·업종구조·상장이력 부적합, 그리고 이미 실적으로 확인된
악화가 반복됐다:
- **GPI(Group 1 Automotive)·UVV(Universal Corp)**: 각각 자동차 딜러(경기
  순환 저마진 소매업)·담배잎 공급업체(저성장 성숙산업) - CL과 동일하게
  MIN_REALISTIC_GROWTH(8%)와 업종 자체가 구조적으로 안 맞고, 시총도
  소형($4.14B/$1.31B).
- **AMSF(AMERISAFE)**: 워커스컴프 특화 소형 보험사, 시총 $556.82M.
  is_insurer 경로(v3.22)가 있지만 관측 사례(ACGL·PGR, 둘 다 대형 종합
  보험사)가 2건뿐이라 이 정도 소형 특화보험사에 적용할 근거가 약함.
- **LMB(Limbach Holdings)**: 52주 고점 대비 -61.1%. Q2 2026 EPS가 컨센서스
  대비 -30.9% 큰 폭 미스, 매출도 소폭 미달, 헬스케어·기관시장 마진압박,
  FY EBITDA 가이던스도 컨센서스 하회 - 서사가 아니라 실적으로 이미 악화가
  확인된 4분류 3번.
- **ARDX(Ardelyx)**: 상업화 초기 제약사, OCF가 여전히 마이너스라
  FCF0<=0일 가능성이 매우 높음 - ORCL(2026-08-13 배치)과 동일하게
  screener.py 가드상 Model N/A로 즉시 탈락 처리될 후보. FY2026 가이던스도
  payer 장벽으로 하향 조정돼 서사도 약화 중.
- **WBTN(WEBTOON)·EMAT(Evolution Metals)**: 각각 2024년 IPO(상장이력
  부족, PODD/APP과 동일 프레임워크 부적합 유형)·신생 투기적 소형주(하루
  만에 +39.38% 급등할 정도의 변동성) - 재무이력 자체가 부재.
- **SECZ**: WebSearch로 실체를 확인하지 못함(존재하지 않거나 상장폐지
  가능성) - 추측하지 않고 확인불가로 남김.

엔진 코드 변경 없음, 테스트 255개 불변.

**후속 조사 3~4차(동일 08-14 세션) - ONON(On Holding), 원자료 신뢰도 문제를
SEC XBRL API로 실제로 해결한 사례.** "quality stock oversold" 계열 검색으로
On Holding(ONON)을 발견 - 52주 고점 대비 큰 폭 하락에도 Q2 2026 순매출
+21.6%(CC), DTC +34%, 총마진 사상최대 65.4%로 서사만 보면 4분류 1번(공포
과잉) 후보였다. 가이던스도 '최소 23%'→'20%대 초반'으로 소폭 하향에 그쳤다.

**3차: FCF 원자료를 확보하는 과정에서 심각한 출처 간 불일치가 드러났다** -
Alpha Vantage INCOME_STATEMENT의 FY2025 매출(CHF 2,878.5M)이 회사 1차
출처(공식 보도자료+20-F 표지, CHF 3,014.0M, +30.0%YoY)와 4.7% 차이났고,
CASH_FLOW 엔드포인트는 ONON 심볼에서만 실패했다(AAPL 등 다른 심볼은 정상).
2차 출처(stockanalysis.com, WebSearch 요약)끼리도 FY2022 OCF 부호가
정반대로 나왔다. SEC 20-F 원문 본문엔 재무제표가 없었고 첨부 PDF는 10MB
파싱 한도 초과, WSJ은 접근 차단됐다.

**4차(사용자 "계속 더 파고들어봐" 요청) - HTML 스크레이핑을 포기하고 SEC
XBRL companyfacts API(`data.sec.gov/api/xbrl/companyfacts/CIK0001858985
.json`, 회사가 직접 제출한 구조화 원자료)를 직접 조회해 실제로 해결했다.**
`RevenueFromContractsWithCustomers`/`CashFlowsFromUsedInOperatingActivities`/
`PurchaseOfPropertyPlantAndEquipmentClassifiedAsInvestingActivities` 태그를
조회한 결과 stockanalysis.com의 2차 조회값과 정확히 일치했다(FY2025 매출
CHF 3,014.0M/OCF 359.5M/capex 72.9M) - Alpha Vantage 매출이 부정확했던
것으로 확정됐다(첫 WebSearch 요약이 잘못됐던 것이지 stockanalysis 자체는
정확했다).

**그런데 정확한 원자료를 확보하고 나니 새로운 문제가 드러났다** - FCF(OCF-
capex)가 2022년 재고 CHF 273M 급증(급성장기 스케일업, WebSearch로 구조적
악화가 아님을 확인)으로 심각한 적자(-287.3M)를 기록해, 5y CAGR의 표준
시작점 후보(2020년 -25.7M/2021년 -7.7M) **둘 다 음수라 CAGR 자체가
정의되지 않는다**(v3.19 가드 해당 - PODD와 같은 유형이나, PODD의 '다년
연속 적자 후 최초 흑자전환' 단조패턴과 달리 ONON은 '흑자(2021)→성장통
적자(2022)→흑자 재개'라는 비단조 패턴이라는 점이 다르다). 유일하게 계산
가능한 건 2023→2025 2년 CAGR(23.04%)뿐인데 screener.py의 5y 기준 임계값과
정합적이지 않아 그대로 못 쓴다. 매출 자체는 견조(5y CAGR 47.9%)해 서사는
여전히 유효해 보이지만, PODD/APP과 동일 원칙으로 `FRAMEWORK_MISMATCH`에
최종 분류했다(1차 조사에서 만들었던 `UNRESOLVED_DATA_QUALITY` 카테고리는
문제가 실제로 해소되며 필요 없어져 제거).

**교훈**: 2차 출처 간 불일치가 발견되면 SEC EDGAR HTML을 직접 스크레이핑
하기보다 XBRL companyfacts API를 먼저 시도할 것 - 문서 전체 파싱(10MB
제한, 리스크팩터만 방대한 본문) 없이 필요한 태그만 정확히 뽑을 수 있다.

**후속 조사 5차(동일 세션, 사용자 "계속 스크리닝" 요청) - AMD, 이번 배치
유일의 실제 screen() 통과 시도.** "analyst buy rating maintained despite
selloff" 계열 검색으로 발견 - 2026-08-04 Q2 실적발표에서 매출 $11.54B
(+50.1%YoY, 컨센서스 상회)·데이터센터 매출 2배 이상·EPS 컨센서스 상회에도
시간외 -8.82% 급락했다. 원인은 실적 미스가 아니라 실적발표 직전 5거래일간
+21% 랠리로 기대치가 '블로아웃'급으로 과열된 상태에서 Q3 매출총이익률
가이던스가 전분기와 동일(약 56%)했던 점 - 공포과잉과는 결이 다른 '기대치
과열' 유형이라 판단해 이번 배치 유일하게 `CANDIDATES`에 넣어 `screen()`을
실제로 실행했다.

**결과: FAIL** - 내재성장률 추정 9.76%가 임계값(5.5%)을 크게 상회, FCF
수익률(0.79%)이 필요치(4.86%)에 크게 못 미쳤다. 매출 5y CAGR 28.82%·FCF
CAGR 53.85%(마진 확장 중, capex/매출 2020-2024 평균 2.32%→2025 2.92%로
소폭 상승에 그쳐 META급 급증과는 성격이 달라 v3.20 재검토 대상 아님)로
성장 자체는 넉넉히 통과했지만, 시가총액이 이미 $847.59B(주가가 52주 범위
상단 $584.73 근처까지 반등한 상태 - 급락 이벤트가 이미 지나갔고 시장이 그
이후 되돌린 것으로 추정)로 워낙 커 밸류에이션 단계에서 탈락했다 - META와
동일한 4분류 2번('실적은 훌륭한데 이미 비쌈') 패턴으로 확인된 사례.

**후속 조사 6차(동일 세션, 사용자 "계속" 요청) - AVGO(Broadcom), M&A-CAGR
왜곡으로 FRAMEWORK_MISMATCH.** AMD와 같은 검색에서 함께 발견된 반도체
대형주 - 2026-08-14 하루 -5.0% 하락했으나 WebSearch가 "회사 고유의 새로운
악재 공시 없음 - 심리·포지셔닝에 가까움"이라 명시해 AMD와 동일한 '기대치
과열' 유형으로 판단했다. Alpha Vantage INCOME_STATEMENT 확인 결과 FY2024
(2023-11~2024-10) 매출이 전년 $35.82B → $51.57B로 +44.0% 급증 - VMware
인수($690억, 2023-11-22 종결)가 5y CAGR 구간 한가운데 걸린 결과였다
(GEN/BRO/ROP/SNPS/NRG/RBA/NOW와 동일한 'M&A가 CAGR 구간에 걸리는' 패턴).
반도체 사업만의 유기적성장을 세그먼트 분리 없이 5y CAGR로 그대로 쓰면
왜곡되므로 SNPS/NRG와 동일하게 재무데이터를 더 긁지 않고
`FRAMEWORK_MISMATCH`로 분류했다 - AMD가 이미 시총 과열로 밸류에이션
탈락했다는 선례상, 설령 세그먼트조정을 하더라도 통과 가능성은 낮다고
판단했다.

**후속 조사 7차(동일 세션, 사용자 "계속" 요청) - ACN(Accenture), 성장
단계에서 FAIL, capex 재검토 로직 실전 작동을 처음 확인한 사례.**
"guidance cut 20% 30% overreaction" 검색으로 발견 - 2026년 -50%(YTD),
특히 6월 단일세션 -18~20% 급락(FY26 Q3 실적발표 후). AI가 IT컨설팅
수요를 구조적으로 대체한다는 서사·신규수주 순차감소·연방정부 지출삭감
우려가 겹쳤으나, 정작 가이던스 하향폭은 작았다(현지통화 매출성장
3~5%→3~4%, 겨우 1%p) - 주가 낙폭(-50%)과 극도로 비대칭적이라 확인해볼
가치가 있었다. 최근 분기 매출은 여전히 +5.6%YoY로 "실적 붕괴"는 아니었다.

**결과: FAIL** - 매출 5y CAGR 9.47%는 임계값을 넘지만 FCF CAGR(7.38%)이
병목이 돼 현실적성장률 추정 6.64% < 8.0%로 탈락. **capex 재검토 플래그가
실제로 발동했으나**(매출 CAGR만 보면 8.51%로 통과) capex/매출이 오히려
감소해(-0.2%p, v3.7 임계값 3%p 미만) `margin_erosion`으로 재분류되며
구제되지 않았다 - v3.20 `capex_intensity_from_series`/`fcf_conservatism_
adjustment` 로직이 growth_investment와 margin_erosion을 정확히 구분해
재분류로 구제하지 않는 설계대로 작동함을 이번에 처음 실전에서 확인한
사례다(NVO 스크리닝 이후 이 로직이 배선됐지만, 지금까지의 분석 대상
중 실제로 이 플래그가 발동해 구제 여부까지 판정된 사례는 처음이다).

**후속 조사 8차(동일 세션, 사용자 "계속" 요청) - MU(Micron Technology),
새로운 유형의 CAGR 계산 함정.** "AI infrastructure sell-off buy the dip"
계열 검색에서 발견 - "장기 펀더멘털은 그대로인데 월간 -15% 하락"이라는
서술이 공포과잉 후보를 시사했으나, 서사 자체는 메모리 다운사이클 우려·
섹터 전반 셀오프(삼성 실적 부진, DeepSeek 반도체 계획)가 겹친 혼재
유형이었다(Citigroup은 목표가는 낮췄지만 Buy 등급 유지).

Alpha Vantage 실측으로 확인해보니 서사보다 더 근본적인 계산 함정이
드러났다 - 메모리 반도체 특유의 극심한 boom/bust 사이클로 FY2023
매출이 **-49.5%YoY 역성장(영업손실 전환)**했고, 5y CAGR 시작점(FY2020)의
FCF가 $83M로 거의 0에 수렴하는 값이었다. 이를 시작점으로 CAGR을 계산하면
**FCF CAGR이 82.23%라는 터무니없는 숫자**가 나온다(매출 5y CAGR 11.76%와
전혀 무관 - 시작값이 우연히 거의 0이라는 순수 계산 아티팩트, PODD/ONON과는
또 다른 새로운 CAGR 함정 유형: PODD는 '음수→흑자전환', ONON은 '흑자→적자
→흑자', MU는 '양수지만 거의 0에 수렴하는 값이 시작점'이라 셋 다 겉으로는
비슷해 보여도 원인이 각각 다르다). v3.19 가드(시작값 음수)에는 안
걸리지만(시작·끝 둘 다 양수) 5y CAGR 하나로 이 정도 변동성을 대표할 수
없다고 판단해 PODD/APP/ONON과 동일 원칙으로 `FRAMEWORK_MISMATCH` 처리했다.
`worst_yoy_revenue`(-49.5%)만으로도 이미 이 업종이 이 스크리너가 가정하는
'완만한 경기민감도'와 성격이 다름을 보여준다.

**후속 조사 9차(동일 세션, 사용자 "하나씩 하지말고 여러 기업 1차
스크리닝을 해" 요청) - 8종목 동시 서사 확인, 재무데이터 확보 없이 4분류
체크리스트만 병렬 적용.** 지금까지 8차례에 걸쳐 후보 하나씩 순차 딥다이브
해온 방식을 이번엔 사용자 지시에 따라 병렬 1차 분류로 전환했다.

- **NKE(Nike)**: FY2026(FYE 2026-05) 매출 $46.4B, 전년 $46.9B 대비
  -1%YoY 역성장 실측 확인 - 4분류 3번 즉시 확정.
- **UAA(Under Armour)**: Barclays가 Underweight로 다운그레이드하며
  스포츠웨어 업종 전체에 회의적 태도 - 4분류 3번 가능성.
- **CRWD(CrowdStrike)**: 6월 가이던스 실망으로 하락했으나 8월 시점엔
  이미 신고가 경신 중 - '현재 급락 중'이라는 스크리닝 전제 자체가
  성립하지 않아 타이밍 부적합으로 제외.
- **SNOW(Snowflake)**: 뚜렷한 급락 신호 확인 못함(직전 분기 제품매출
  +30%YoY로 견조) - 애초에 스크리닝 대상이 아님.
- **FIGMA**: 최근 IPO로 상장이력이 5y CAGR 프레임을 채우기엔 너무 짧음
  (WBTN·NBIS와 동일 유형) - 프레임워크 부적합.

나머지 3종목은 서사가 애매해(공포과잉 가능성은 있으나 확신 부족) 신규
카테고리 `PASSED_INITIAL_SCREEN`(재무데이터 확보 필요)으로 분리했다:
- **SAP**: -29.6%YTD/-40.5%(12개월), 하락원인이 M&A(Dremio·Prior Labs,
  2026-07 종결) 이익 희석이지 실적 악화가 아님 - 클라우드 매출은 오히려
  견조(+22%YoY). **주의**: 독일 발행사(20-F/6-K)라 ONON과 동일한 원자료
  신뢰도 리스크가 있을 수 있고, 동시에 최근 M&A가 5y CAGR 구간에 걸려
  SNPS/NRG/AVGO급 왜곡 위험도 겹친다 - 재무데이터 확보 시 이 두 가지를
  먼저 점검할 것.
- **DDOG(Datadog)**: 매출·이익 모두 컨센서스 상회, AI 고객 750개 이상
  신규 추가했음에도 조정 총마진(80%)이 예상(80.7%)에 근소 미달했다는
  이유만으로 -15% 급락 후 곧바로 +11.48% 반등 - 마진 0.7%p 미스 대비
  하락폭이 비대칭적인 전형적 공포과잉 패턴이나, 이미 상당폭 되돌린
  상태라 '현재 저평가' 여부가 불확실.
- **PLTR(Palantir)**: 매출 +81%YoY로 성장은 압도적이나 -30%YTD, 밸류
  에이션이 매출의 60~70배(트래커 역대 최고 수준) - AMD/AVGO가 이미
  시총 과열로 탈락한 선례상 밸류에이션 통과는 어려울 것으로 예상되나,
  성장률 자체가 Lynch 상한(25%)에 바인딩될 가능성이 있어 확인 가치는
  있다.

다음 단계에서 이 셋의 재무데이터를 확보해 실제 `screen()`을 시도할 것.

**후속 조사 10차(동일 세션, 사용자 "1차 통과 기업 따로 분류해두고 전체
스크리닝 계속 실행해" 요청) - 신규 8종목 병렬 1차 분류.**
- **HON(Honeywell)**: 가이던스 대폭 하향(유기적 매출성장 7~9%→4~5%)에
  이익 미스·재고 진부화까지 겹쳐 -20%. 원인이 매크로가 아니라 '공급망·
  납기 실행력' 재평가라는 분석 확인 - 4분류 3번 확정.
- **UWM(UWM Holdings)**: 분기 순손실 $452M + 상장 이후 최초 배당
  중단으로 -33% - 손익계산서 자체에서 확정된 악화, 4분류 3번 확정.
- **AI(C3.ai)**: Q3 미스와 함께 FY2026 매출 전망을 거의 절반으로
  하향 - 가이던스 삭감 폭이 이번 배치 최대, 4분류 3번 확정.
- **MTZ(MasTec)**: 가이던스 오히려 상향+백로그 성장인데 EPS만 소폭
  미달해 -9.6% - 서사상 공포과잉에 가깝지만 하락폭 자체가 이 배치
  기준(대부분 -20%p 이상)보다 훨씬 작아 스크리닝 대상에서 제외.
- **KPTI(Karyopharm)·GRAL(Grail)**: 단일 임상시험 실패로 인한 이진적
  리스크 실현 - ARDX와 동일한 초기단계 바이오텍 프레임워크 부적합.
- **EOSE(Eos Energy)**: 초기단계 배터리저장 기업, FCF 적자 가능성이
  높아 확인 전 제외.
- **MP(MP Materials)**: 애널리스트 Buy 유지에도 52주 저점이라
  흥미로웠으나 AA(Alcoa)·NRG와 동일한 자본집약 원자재 업종 자본구조
  문제로 `FRAMEWORK_MISMATCH` 처리(다만 미국 국방부 지분투자 등
  정부지원이 얽혀 있어 향후 재검토 여지는 남겨둠).
- **IONS(Ionis Pharmaceuticals)**: 심장약 임상실패로 -30%지만
  KPTI·GRAL과 달리 이미 승인받은 제품(Spinraza 등)의 매출·로열티가
  있는 다각화된 회사라 `PASSED_INITIAL_SCREEN`에 추가.

`PASSED_INITIAL_SCREEN` 누적 4건(SAP·DDOG·PLTR·IONS) - 다음 단계에서
재무데이터를 확보해 실제 `screen()`을 시도할 대상.

엔진 코드 변경 없음, 테스트 255개 불변.

**후속 조사 11차(동일 세션, 사용자 "계속 스크리닝" 요청) - AMAT(Applied
Materials), 반도체 장비섹터 밸류에이션 탈락 세 번째 사례.** "AI데이터
센터 관련 반도체·클라우드·소프트웨어 급락" 계열 검색에서 발견 -
2026-08-14 하루 -5.2%(Broadcom -5.5%와 같은 날, 소비지출 지표 실망으로
인한 섹터 전반 셀오프), **4분기 매출 가이던스를 오히려 컨센서스 상회로
제시했음에도 하락**했다는 점이 흥미로웠다. KEYS/KLAC에서 이미 확인된
'반도체 장비 trailing CAGR이 AI 수요 인플렉션을 과소추정' 패턴과 정확히
같은 하위섹터라 확인 가치가 높았다.

**결과: FAIL** - 매출·FCF 5y CAGR 둘 다 10~11%대로 성장은 확실히
통과했고(capex 재검토 플래그도 미발동) 현실적성장률 추정(9.09%)이
임계값(5.5%)을 넉넉히 넘겼지만, 시가총액이 $437.77B로 매우 커 FCF수익률
(1.30%)이 필요치(4.75%)에 크게 못 미쳐 밸류에이션 단계에서 탈락했다 -
AMD(0.79%)와 정확히 같은 4분류 2번 패턴. **KEYS/KLAC와 달리 AMAT는
성장률 자체는 통과하고도 시총이 커진 것만으로 막혔다** - '반도체
장비섹터가 AI랠리로 이미 밸류에이션 문턱이 높아졌다'는 것이 KEYS/KLAC의
성장과소추정 문제와는 별개의 장벽임을 세 번째로(AMD/AVGO에 이어) 확인한
사례.

추가로 Peloton(PTON)은 다음 회계연도 매출 역성장을 자체 전망한 뒤 -14% -
4분류 3번으로 확인 전 단계에서 제외했다.

엔진 코드 변경 없음, 테스트 255개 불변.

**후속 조사 12차(동일 세션, 사용자 "계속" 요청) - 8종목 병렬 분류, API
한도 도달로 2종목은 재무데이터 확보 보류.**
- **UPS(United Parcel Service)**: Amazon 물량을 2026-06까지 50% 이상
  감축하는 합의로 3분기 미국 일평균 물량이 중한자릿수%대 역성장할
  것으로 회사가 직접 가이던스 제시 - 저마진 아마존 물량을 고마진
  헬스케어·SMB 고객으로 대체하는 전략적 마진개선 서사는 있으나, 회사
  스스로 매출 감소를 확정적으로 예고한 상태라 이 스크리너의 8% 임계값을
  구조적으로 넘기 어렵다고 판단 - 확인 전 제외.
- **CVS(CVS Health)·UNH(UnitedHealth)·CNC(Centene)**: 셋 다 헬스케어
  보험업의 구조적 압박(CVS는 Aetna 의료급여율 급등, UNH는 상환율
  정체+의료비 급증 격차 확대로 수십년 만에 첫 매출감소 경고, CNC는
  Medicare Part D 보조금 폐지 우려) - 서사가 아니라 정책·손해율이라는
  실측 가능한 요인에서 나온 4분류 3번에 가깝고, 업종 전체가 동시에
  눌리는 구조적 문제라 개별종목 미스프라이싱보다 섹터 리스크에 가깝다고
  판단, 확인 전 제외.
- **AFRM(Affirm)·SOFI(SoFi Technologies)**: BNPL·디지털뱅크 특유의
  신용손실충당금 변동성+상업화 초기 FCF 불안정 가능성으로 ARDX·EOSE와
  유사한 프레임워크 부적합 소지 - 확인 전 제외.
- **주택건설업(Lennar·D.R. Horton 등)**: 10년물 국채금리 고착으로
  모기지 금리 상승, 신용평가사가 단독주택 착공 전망을 '증가'에서
  '-4.5% 역성장'으로 하향 - 개별종목이 오분류된 게 아니라 업종 전체가
  금리환경에 동시에 눌리는 매크로 현상이라 이 스크리닝(개별 미스프라이싱
  탐지)의 목적과 안 맞다고 판단, 확인 전 제외.

**PYPL(PayPal, 5년 고점 대비 -83%·AI 결제모델 붕괴 공포)과 BABA(Alibaba,
중국 소비둔화+경쟁+규제, PDD 스크리닝에서 이미 확인된 competition_
intensity 상수 한계가 적용될 사례)는 확인 가치가 높다고 판단했으나,
Alpha Vantage 일일 API 한도(25회) 도달 + FMP도 플랜 제한으로 이번
세션에서 재무데이터 확보를 시도하지 못했다** - `PASSED_INITIAL_SCREEN`에
서사만 기록해두고 다음 세션(API 한도 리셋 후)으로 넘겼다. 누적 6건
(SAP·DDOG·PLTR·IONS·PYPL·BABA)이 재무데이터 확보 대상으로 쌓여 있다.

엔진 코드 변경 없음, 테스트 255개 불변.

## TTD 재검토 완료 - Confidence 하향 + 매수리스트 비중 축소
(2026-08-13, v3.42~v3.45가 만든 3개 독립 검증축이 전부 지목한 종목, 사용자
명시적 승인 후 실행)

thesis_monitor(반증조건 3/4 발동)·growth_scorecard(저평가 최소선 4.69% vs
회사 Q3 가이던스 -12.1% 역성장, 판정 자체가 뒤집힘)·gap_decay(주가 -26.3%에
Gap이 오히려 +17.01%p→+20.96%p로 벌어짐 - 가치함정 실측)까지 **서로 독립인
세 경로가 전부 TTD를 가리켰다.** 2026-08-03 정성조사가 이미 "S등급 7종목
중 최심각"(연방 증권사기소송+CEO 내부자거래 혐의)이라며 Confidence를
72까지 낮춰뒀는데, 이번 재검토는 그 우려가 **리스크 단계에서 확인된
악화 단계로 실제로 넘어갔음**을 보여준다.

**⚠️ 공식 ledger(Gap +17.01%p/RAR/판정 "저평가 가능성")는 건드리지 않았다.**
growth_scorecard가 코드로 못박은 원칙 그대로다 - TTD의 새 증거는
`realized_quarterly`(Q2 실적 1개 분기)와 `guidance_annual`(회사의 1개년
예측)뿐이라 `usable_as_override`가 아니다(ROP가 확립한 기준 - 다년 실현
실적만 공식판정을 바꿀 자격이 있다). 검증 안 된 1~2개 데이터포인트로
Realistic Growth를 손대는 것은 이 프로젝트가 KEYS 크로스체크에서 이미
경계한 실수를 반복하는 것이다.

**대신 매수리스트에서 즉시 반영 가능한 조치를 했다**
(`scripts/build_buylist_2026_08_03.py`): `CONFIDENCE_ADJ["TTD"]`를 72->45로
하향(근거 전문을 새 증거 3축과 함께 기록), 신설한 `THESIS_BROKEN_FLAG`로
추가 0.85x 패널티를 걸었다 - 기존 `SEVERE_FLAG`(2026-08-03 시점 거버넌스
"리스크")와는 축이 달라(이번엔 확인된 "결과") 별도로 곱한다. 결과: **TTD
비중 4.80% -> 2.70%**로 축소, 차액은 growth_platform 버킷 내 나머지 6종목
(PDD/SE/DUOL/MNDY/UBER/WDAY)에 quality_score 비례로 자동 재분배됐다(버킷
목표비중 41.00%는 불변 - 코드 수정 없이 기존 규칙이 그대로 작동함).

**완전히 제외하지 않고 소액이나마 남긴 이유**: 증거가 아직 "확정"이 아니라
"1개 분기+가이던스 1건"이라는 얇은 축적 수준이고, CEO의 2026-03 $148M
개인자금 직접매수(2026-08-03 정성조사가 확인한 유일한 강세신호)가 아직
반증되지 않았기 때문이다 - 완전 제외는 남은 근거를 0으로 취급하는 것이라
과도하다고 판단했다.

`reports/thesis_monitor_2026-08-13.json`의 TTD 항목에도 이 재검토 완료
사실을 기록했다. 다음 체크포인트는 FY2026 Q3 실적(2026-11월경 예상) -
그때 두 번째 실현 분기가 쌓이면 `realized_multiyear`에 준하는 증거력을
갖출 수 있어 공식판정 재검토(override) 여부를 다시 논의할 것.

## v3.47 — Phase 3: PIT 토대 + 과거기록 자동 대조 (2026-08-15, 계약서 STEP
1-16 이어서, 사용자 "모두 진행")

v3.46 Phase 0/1/2에 이어, 감사에서 **DEFERRED로 분류했던 근거가 실제로 바뀐
것 하나**와 **미사용 경로로 확인된 것 하나**를 처리했다.

### ① Point-in-Time — "검증"이 아니라 "모른다고 말할 수단"을 먼저 만든 것

Phase 0에서 PIT를 보류한 사유는 "과거 34종목의 `filing_date`를 알 수 없으므로
지금 만들면 허위 표시를 낳는다"였다. **그 판단은 지금도 유효하다.** 바뀐 건
하나뿐이다 - *필드가 아예 없으면 앞으로도 영원히 채울 수 없고*, `PIT_UNKNOWN`
이라는 상태 어휘가 없으면 **"모른다"조차 기록할 방법이 없다.**

- `AnalysisInputs.analysis_as_of` / `filing_dates_by_year` (둘 다 opt-in -
  기존 34종목 영향 0)
- `evaluate_point_in_time()`: `filing_date <= analysis_as_of` 규칙
- **`PIT_INVALID`면 실행을 거부한다.** 이 프로젝트의 다른 경고는 전부 "병기,
  자동판정 안 함"인데 여기만 예외인 이유 - 미래정보로 계산한 결과는 해석의
  여지가 있는 게 아니라 **계산 전제 자체가 무너진 것**이다.
- `PIT_UNKNOWN`이면 `data_limitations`에 기록. `meta["point_in_time"]`에 병기.

**⚠️ 기존 34종목은 전부 `PIT_UNKNOWN`이고 소급해서 채우지 않는다** -
filing_date를 추정해 넣는 순간 "빈칸 채우기"(추측을 기록으로 위장하는 것)가
된다. 테스트가 **PIT_VALID로 위장되지 않는지**를 고정한다. 즉 현재 상태는
정직하게 말해 **수단은 생겼고 데이터는 0건**이다.

**앞으로 새 분석에서는 `analysis_as_of`와 `filing_dates_by_year`를 채울 것** -
`price_at_analysis`가 v3.24에 만들어지고 12일 뒤 thesis_monitor에서 "24종목은
부식 계산 자체가 불가능"이라는 구체적 비용으로 드러난 것과 같은 일이 여기서도
반복된다. 채우지 않으면 Historical Replay(C-11)는 영원히 시작할 수 없다.

### ② 과거기록 자동 대조 - 55개 스크립트 중 1개만 부르던 경로

감사 실측: `cross_check_prior_record()`는 분석 스크립트 55개 중 **1개(BKNG)**
에서만 호출된다. 그런데 이 프로젝트가 잡아낸 사고 대부분(PH 모델선택, RAR
100배)이 과거 기록과 대조하다 발견된 것이고, **그 대조는 사람이 우연히 한
것**이었다. 문서로만 둔 규칙이 무력화된 걸 이미 세 번 겪었다(run_self_check ·
confidence_score · claim/lock 프로토콜). 네 번째를 만들지 않는다.

`save_ledger()`가 같은 티커의 직전 ledger를 찾아 대조하고 결과를
`prior_cross_check`에 병기한다 - **모든 분석 스크립트가 이미 `save_ledger()`를
부르므로 배선 누락이 구조적으로 불가능하다.** CLAUDE.md v3.32가 이미 "부작용이
가장 작은 방향"이라고 지목해둔 설계를 그대로 구현한 것이다.

부작용 억제 3가지: **예외를 절대 던지지 않고**(대조는 조언이지 차단이 아니다 -
깨진 JSON이 있어도 저장은 성공해야 한다. 대조 실패로 새 분석이 유실되는 게 더
나쁘다), **판정을 건드리지 않으며**(병기 원칙), `cross_check=False`로 끌 수
있다(골든테스트가 저장소 상태에 의존하면 안 되므로 - v3.32가 배선을 미룬 이유가
정확히 이 부작용이었다).

**구현 중 실제로 잡힌 회귀**: `prior_cross_check`는 저장본에만 있는 파생
필드인데, v3.46 덮어쓰기 가드의 동일성 비교가 이를 몰라 **같은 입력 재실행을
"내용 다름"으로 오판**했다(이 프로젝트의 표준 검증 관행인 전건 재실행이 막힐
뻔했다). `test_save_ledger_allows_identical_rerun`이 잡아냈고
`_ledger_payload_without_timestamp()`에서 제외하도록 고쳤다.

### 검증

**34종목 전건 재실행 → Gap/RAR/DRS/Realistic Growth/Implied Growth/판정/
Confidence/강건성flip 8개 지표 완전 동일, 실패 0건, ledger 파일 무수정.**
테스트 270 → 282개. `ENGINE_VERSION` v3.46 → v3.47.

## v3.48 — 신호에서 결정으로: Thesis / Prediction / Experiment 배선
(2026-08-15, 사용자 "IRS 핵심 투자기능 고도화" 실행 프롬프트)

이 프로젝트는 **"이 종목이 싼가"까지만 답하고 있었다.** `judgment`(저평가/
적정가/과대평가)가 사실상 매수리스트였고(`build_buylist`의 `grade in ("S","A")`),
싸다는 것과 사야 한다는 것 사이에 있어야 할 관문이 코드 어디에도 없었다.
이번 작업은 새 밸류에이션 방법론을 만들지 않고, **기존 계산을 실제 투자판단과
사후검증에 연결**한다.

### 신규 모듈 4개 (전부 기존 엔진 재사용, 새 valuation 로직 0줄)

**① `engine/gap_analysis.py` — Gap을 숫자 하나로 끝내지 않는다**
다섯 질문(gap_level / gap_change / gap_drivers / evidence_strength /
model_uncertainty)에 답한다. 2·4·5는 이미 만들어 둔 thesis_monitor(v3.42)·
growth_scorecard(v3.43)·gap_distribution(v3.44)을 **호출만** 하고, 3만 새로
만들었다.

⚠️ **gap_drivers에서 근거 없는 attribution을 만들지 않았다.** 최상위는 정확한
대수 항등식(ΔGap = ΔRealisticGrowth − ΔImpliedGrowth)이지만, ΔImplied Growth를
다시 쪼개는 것은 비선형이라 유일하지 않다. one-at-a-time + **잔차 명시** 방식만
쓰고 가중치를 지어내 100%로 맞추지 않는다.

**실측으로 확인한 것 — 이 잔차는 장식이 아니다.** CDNS로 시총 ×0.6 · FCF0 ×1.4 ·
r +2%p를 동시에 바꿔보니 개별 기여도의 합은 **+0.00038**인데 실제 변화는
**−0.00456**으로 **부호까지 반대**였고, 잔차가 전체 변화의 **108%**였다. 즉
개별 기여도만 인용하면 "Implied Growth가 올랐다"고 정반대로 읽게 된다.
테스트가 이 성질 자체를 고정한다
(`test_multi_factor_change_reports_interaction_residual_openly`).

**② `engine/thesis.py` — Signal과 Decision의 분리를 구조로 강제**
`decide()` 같은 함수를 **의도적으로 만들지 않았다.** Gap을 넣으면 BUY가 나오는
함수가 있으면 미검증 신호(RESEARCH_HYPOTHESIS)가 곧바로 자본배분이 된다.
대신 `record_decision()`이 6개 관문(신호요약·사업품질·재무품질·리스크·
밸류에이션·포트폴리오맥락) 근거가 하나라도 비면 **실행을 거부**한다 —
`model_choice_reason`·`subjective_input_basis`에서 이미 쓴 "근거 없으면 거부"
패턴 그대로다. 액션(BUY/ADD/HOLD/WATCH/REDUCE/SELL)은 **분석자가 고르고**
코드는 고르지 않는다.

테스트가 이 경계를 지킨다 — `test_no_function_maps_gap_to_action`이 공개 함수
시그니처를 훑어 `gap`/`grade`를 인자로 받는 함수가 생기면 실패한다.

기록 구조: thesis 코어는 **변경 불가**(생각이 바뀌면 새 날짜로 새 thesis),
decisions/evidence는 **append-only**. 반증조건 발동은 분석자가 명시적으로
표시해야만 되며 코드가 텍스트를 읽고 자동 판정하지 않는다(v3.42가 확립한
원칙 — 정규식은 트리거 날짜와 서술적 날짜를 구분하지 못한다).

§7 상태(STRENGTHENING/STABLE/WEAKENING/INVALIDATED)는 **투명한 집계 규칙**이며
`STATUS_RULE_VALIDATION = IMPLEMENTED_NOT_VALIDATED`로 지위를 명시했다.
사전등록 반증조건이 발동하면 지지 증거가 아무리 많아도 INVALIDATED가 이긴다 —
"그래도 좋아 보인다"고 넘어가는 것이 정확히 사후합리화다.

**③ `engine/prediction_ledger.py` — 결과를 알고 난 뒤 예측을 고칠 수 없다**
이 모듈의 유일한 핵심 불변조건이다. 없으면 예측 기록이 무의미하다(결과를 보고
범위를 슬쩍 넓히면 적중률이 100%가 된다). 강제 방식은 **코어 SHA-256 해시** —
기록 시 코어(지표·기한·범위·가정)를 해싱해 저장하고, 결과를 붙일 때 다시
해싱해 대조한다. 한 글자라도 바뀌었으면 거부한다. 이미 해소된 예측의 재해소도
거부한다.

`falsification_conditions`(v3.24)가 자유 텍스트라 **얼마나** 틀렸는지 잴 수
없었던 한계를 메운다. `forecast_error`는 **부호를 유지**한다 — "나는 체계적으로
낙관적인가"에 답하려면 절댓값만으로는 알 수 없다. 데이터를 못 구하면
UNRESOLVABLE로 정직하게 닫고 추측으로 채우지 않는다.

**④ `engine/experiment_registry.py` — 실패한 실험을 지울 수 없다**
`results`는 append-only, 실험 코어는 등록 후 변경 불가(해시 대조), **삭제 함수
자체를 제공하지 않는다**(코드에 경로가 있으면 언젠가 쓰인다). 실패를 지우면
남는 것은 '성공한 실험만 모아둔 목록'이고 그건 연구 기록이 아니라 광고다.

### ⚠️ 구현 중 스스로 잡은 결함 — 죽은 무결성 검사

`record_result()`의 코어 변경 검사가 **절대 발동할 수 없는 코드**였다.
append 직전/직후의 코어를 비교했는데 append는 코어를 건드리지 않으므로 항상
같았다. 테스트를 쓰다 발견했고(초판 테스트도 같은 이유로 무의미했다),
prediction_ledger의 `core_hash()`를 **재사용**해 등록 시점 지문과 대조하도록
고쳤다(해시 로직을 복제하면 두 구현이 어긋난다 — Simplicity First).

### EXP-001 등록 — 미검증 가설이 이미 자본배분을 움직이고 있다는 사실을 드러냄

"Valuation-Implied Requirement와 Evidence-Supported Forward Expectation의
괴리가 미래 위험조정수익률과 관계가 있는가?" — `experiments/EXP-001.json`.
**결과를 미리 가정하지 않는다**(가설 문구가 방향을 단정하지 않는지 테스트가
확인한다).

**status=BLOCKED로 등록했고 사유는 실측이다**: 분석일이 2026-07-25~08-13에
몰려 12개월 보유수익률 구간이 아예 없고, `price_at_analysis`가 34종목 중
**9건**(ACGL·BSX·DUOL·MNDY·ROP·SE·TCOM·UBER·WDAY)뿐이라 나머지 25종목은
진입가를 모른다. 9건을 5분위로 나누면 분위당 최소표본을 못 채운다.
(작성 중 "10건"으로 적었다가 실측해 9건으로 정정했다.)

이 등록의 의미: **매수리스트는 이미 Gap 기반 등급으로 만들어지고 있다.**
즉 미검증 가설이 이미 자본을 배분하는 중이며, 등록부는 그 상태를 숨기지 않는다.
그래서 `gap_analysis.GAP_SIGNAL_STATUS = "RESEARCH_HYPOTHESIS"`다.

### 검증

전체 워크플로를 BSX 실데이터로 끝까지 실행해 §12 완료 기준을 확인했다
(`scripts/workflow_demo_2026_08_15.py`): 경제적 현실 → 가격이 요구하는 조건
→ Gap → 왜 다른가 → Thesis → 결정(WATCH) → 반증조건 → 예측 봉인 → 실제
비교(MISS, 오차 −0.0200) → thesis 상태 WEAKENING.

⚠️ **이 데모는 기본적으로 임시 디렉터리에 쓴다.** 사용자가 검토하지 않은
투자논거를 저장소에 남기면 그 자체가 이 프로젝트가 금지해온 "빈칸 채우기"다.
실제로 쓰려면 내용을 검토·수정한 뒤 `--thesis-dir thesis`로 실행할 것.

테스트 282 → 355개. **34종목 전건 재실행 8개 지표 완전 동일, ledger 파일
무수정** — 이번 작업은 기존 계산 경로를 한 줄도 건드리지 않았다.
`ENGINE_VERSION` v3.47 → v3.48.

### 아직 하지 않은 것 (DEFERRED)

- **`build_buylist`를 thesis 경로로 옮기지 않았다.** 지금도 `grade in ("S","A")`로
  유니버스를 만든다 — §5가 금지한 "Gap = BUY"에 가장 가까운 코드지만, 기존
  사이징 규칙을 이번에 재작성하면 검증된 산출물이 흔들린다. 새 구조는 병렬로
  두고 실제 사용 경험이 쌓인 뒤 이관 여부를 판단한다.
- **Thesis 자동 모니터링 없음.** §7이 "완전 자동화보다 구조와 데이터 연결을
  먼저"라고 명시한 대로 증거는 분석자가 기록한다.
- **EXP-001 실행 불가** — 위 차단 사유 3건이 해소돼야 한다.

## v3.49 — PIT에 실제 데이터 공급: SEC 제출일 조회 + 34종목 미래정보 감사
(2026-08-15, v3.48 보고의 "다음 단계 제안" 1번 실행)

v3.48 보고에서 "가장 값이 큰 것은 새 분석부터 PIT 필드를 채우는 것"이라 적었다.
그런데 **채울 수단이 없었다** - v3.47이 필드와 검증 규칙을 만들었지만 실제로
채운 종목은 34건 중 **0건**이었다.

### ⚠️ v3.47이 금지한 것은 '추정'이지 '조회'가 아니다

v3.47은 "과거 filing_date를 추정해 넣으면 빈칸 채우기가 된다"고 못박았고 그
판단은 지금도 옳다. 그런데 **SEC XBRL companyfacts의 각 항목에는 `filed`
필드로 최초 공시일이 그대로 들어 있다** - 이건 1차 자료의 사실이지 추측이
아니다. 이 구분이 이번 작업 전체를 가능하게 했다.

이 프로젝트는 ONON에서 이미 이 경로를 확립했다(2차 출처끼리 어긋났을 때
companyfacts로 확정). 같은 API에서 재무수치와 제출일을 함께 가져오므로 출처가
갈릴 여지도 없다.

### `engine/filing_dates.py` - 설계상 조심한 두 지점

- **최초 제출일(min)을 택한다.** 같은 회계연도 수치는 여러 연차보고서에
  비교표로 반복 등장하는데, PIT에서 의미 있는 건 **처음 공개된 날**이다.
- **태그를 하나로 고정하지 않고 전부 훑는다.** 제출일은 태그가 아니라 그
  공시의 속성이고, 회사마다 쓰는 매출 태그가 달라(us-gaap vs ifrs-full,
  Revenues vs RevenueFromContract…) 하나를 고르면 **조용히 빈 결과**가 나온다.
  외국 발행사(20-F + ifrs-full)가 정확히 그 함정이라 테스트로 고정했다.

`pit_inputs_for()`는 새 분석에서 한 줄로 쓰도록 만들었다 - 문서로만 둔 규칙이
무력화된 사례를 이미 네 번 겪었으므로(run_self_check·confidence_score·
claim/lock·cross_check_prior_record) 다섯 번째를 만들지 않으려면 한 줄이어야
한다. **최근 회계연도 제출일을 못 찾으면 필드 자체를 빼서 `PIT_UNKNOWN`으로
정직하게 떨어뜨린다** - 억지로 채우면 "검증한 척"이 된다.

BSX 실데이터로 확인: PIT 미기입 시 `PIT_UNKNOWN` → 헬퍼로 11개년 실제 제출일
주입 시 `PIT_VALID`, Gap/RAR/DRS/판정 전부 불변(순수 기록 경로), 분석일을
2026-01-01로 낮추면 미래정보로 실행 거부.

### 34종목 미래정보 감사 - 위반 0건

`scripts/pit_audit_2026_08_15.py`가 34종목 전건에 대해 SEC 최초 제출일과
`analyzed_at`을 대조했다. **미래정보 사용 흔적 0종목, 조회 실패 0종목**
(`reports/pit_audit_2026-08-15.json`). 최근 회계연도 제출일이 분석일보다
평균 5개월 이상 앞서 있어 여유가 컸다.

### ⚠️ 그런데도 34종목을 PIT_VALID로 바꾸지 않았다

이 감사가 답하는 것은 **"그 시점에 공시돼 있었는가"**뿐이다. **"ledger의
숫자가 그 시점 값이었는가"**는 답하지 못한다 - 재무데이터는 분석 시점에 Alpha
Vantage에서 가져왔고, 이후 재작성(restatement)됐다면 지금 조회한 값과 다를 수
있다. 그 검증은 원자료 스냅샷이 있어야 가능하다(change_plan C-09 Provenance,
여전히 DEFERRED).

검증하지 못한 축이 남아 있는데 VALID로 표시하면 **검증하지 않은 것을 검증했다고
주장**하는 게 된다(계약서 5.1절). 그래서 감사 결과는 `reports/`에만 남기고
ledger는 한 파일도 건드리지 않았다.

**부수 관측**: CDNS 2개년·DSGX 1개년은 제출일을 못 찾았다(SEC XBRL 태깅이
시작되기 전의 오래된 비교연도로 보인다). `unknown_years`로 드러내되 위반으로
단정하지 않는다 - 조용히 통과시키면 검증이 실제보다 관대해진다.

테스트 355 → 369개. 34종목 전건 재실행 8개 지표 완전 동일, ledger 무수정.
`ENGINE_VERSION` v3.48 → v3.49.

**다음 분석부터 반드시**: `AnalysisInputs(..., **pit_inputs_for(ticker,
analysis_as_of, revenue_by_year), price_at_analysis=...)`. 이 둘이 채워져야
EXP-001(현재 BLOCKED)과 Historical Replay가 언젠가 시작될 수 있다.

## v3.50 — 최종 명세 이행: Provenance / Investment Case / Drift 3분할 /
연구 순서 고정 (2026-08-15, 사용자 "IRS 최종 방향성 및 실행 명세")

§19가 정한 구현 우선순위를 따랐다. 우선순위 7·8(Historical Replay, 첫 OOS
연구)은 구조적으로 막혀 있어 DEFERRED로 남기고 2·3·5·6만 실행했다.

### ① §6 Provenance — `data_sources` 자유문자열로는 답할 수 없던 것

기존 출처 기록은 `["Alpha Vantage (2026-07-25)"]` 같은 문자열 리스트뿐이라
"FY2023 매출이 **어느 공시**에서 왔고 **언제 공개**됐으며 **통화**가
무엇인가"에 답할 수 없었다. `engine/provenance.py`가 값 하나에 §6이 요구한
7개 축을 붙인다(source/publication_date/period/value/unit/currency/
retrieval_date).

**`unit`과 `currency`를 분리했다** — 비율에는 통화 개념이 없고, 이 프로젝트는
단위 사고를 두 번 겪었다(RAR 100배, 실질/명목 EPS). PDD가 CNY인데 ledger에
표시가 없던 M-6 문제도 구조적으로 막힌다.

**자동 생성이 핵심이다** — `provenance_from_sec_facts()`가 v3.49의 SEC
companyfacts에서 만들어낸다. 손으로 적게 만들면 결국 아무도 안 적는다.
BSX 실측: 11개년 중 10개년 생성(FY2015는 XBRL 태깅 이전이라 `missing_fields`로
드러남), **SEC 값이 ledger 값과 정확히 일치**(20,074,000,000) — 출처 생성이
데이터 교차검증까지 겸한다.

⚠️ **기존 34종목에 소급 생성하지 않는다.** 지금 조회한 값을 그때 값인 양
붙이면 허위 출처가 된다(§6). 전부 `PROVENANCE_UNKNOWN`이며, 테스트가 그
상태를 사실 그대로 고정한다.

### ② §3 Investment Case — 새 계산 0줄의 얇은 계층

§3이 열거한 14개 필드는 **전부 이미 계산되고 있었다**. 문제는 흩어져 있어
한 종목의 판단 전체를 한눈에 볼 수 없다는 것이었다. `engine/investment_case.py`
가 조각을 묶기만 한다 — 새 valuation도 새 score도 없다(§18).

**`PASS`를 어휘에 추가**했다(§3). WATCH와 다르다 — PASS는 "검토했고 투자
대상 아님으로 결론", WATCH는 "아직 아니지만 계속 봄". 구분이 없으면 "안 산다"가
전부 WATCH로 뭉뚱그려져 감시 목록이 무한히 늘어난다. **기존 6개 어휘는 하나도
바꾸지 않았다**(§3 compatibility).

⚠️ **구현 중 잡은 설계 오류**: 초판이 `gap_change`/`gap_drivers`를 '누락'으로
세서 **갓 분석한 종목이 전부 INCOMPLETE**로 찍혔다. 이 둘은 비교 대상이
생겨야 정의되는 값이라(분석 직후엔 변한 게 없으니 변화도 없다) 시간 의존
필드로 분리했다. 그러지 않으면 플래그 자체가 무의미해진다 — 34종목 전부
`SIGNAL_ONLY`가 정확한 상태다.

### ③ §9 Drift 3분할 — 가치함정을 구분하는 유일한 방법

기존 `thesis_monitor`는 가격 변화만 봤다. `decompose_drift()`가 셋으로 나눈다:

| drift | 무엇이 변했나 | 출처 |
|---|---|---|
| Price | 시가총액만 | OAT 분해(잔차 동반) |
| Fundamental | ΔRealisticGrowth | 재무제표에서만 나옴 |
| Expectation | ΔImpliedGrowth | 시장 요구치 |

정확한 항등식 `ΔGap = ΔRG − ΔIG`로 자기검증하며 잔차가 0임을 테스트로 고정했다.
⚠️ **Expectation Drift는 Price Drift를 포함한다** — 둘을 더하면 이중계상이라
합계를 아예 만들지 않는다.

**가장 위험한 조합에 경고를 붙였다** — 펀더멘털이 낮아졌는데 Gap은 벌어진
경우(가치함정). v3.42 TTD가 정확히 이 패턴이었다. BSX로 재현한 테스트
시나리오가 Gap **+3.95%p**를 만드는데, TTD 실측 확대폭과 같은 크기다.

⚠️ **잠복 버그를 하나 잡았다**: `gap_drivers`가 `implied_growth_two_stage`의
튜플 반환을 스칼라로 다뤄 TypeError를 냈다. 골든케이스(CDNS)가 single_stage라
v3.48 테스트를 통과했고, two_stage 종목(BSX)에서만 터졌다. 회귀 테스트 추가.

### ④ §10/§12/§14 실험 등록부 — 순서를 결과보다 먼저 못박는다

- **§10 재현 좌표 4개 필수화**: `analysis_as_of`/`data_version`/
  `methodology_version`/`transaction_cost_assumption`. 비용 가정이 없으면
  총수익이 체계적으로 과대평가된다(§13).
- **§14 결과 판정 신설**: REJECTED/INCONCLUSIVE/PROMISING/VALIDATED.
  실행 상태(status)와 **다른 축**이다 — COMPLETED인 실험도 REJECTED일 수 있다.
  판정이 바뀌면 `finding_history`에 쌓아 덮어쓰지 않는다.
- **§12 연구 순서를 코어에 고정**: `depends_on`으로 H-001 → H-002/H-003,
  H-004는 H-001+H-002 이후. 결과를 본 뒤 순서를 바꿀 수 있으면 검증이 아니라
  튜닝이다(§13). 선행이 COMPLETED이고 **finding까지 정해져야** 충족으로 본다.
  ⚠️ 실행을 막지는 않는다 — 어기고 있다는 사실만 드러낸다(병기 원칙).

**H-001~H-004를 데이터가 하나도 없는 지금 등록했다** — 순서를 미리 박아두는
것이 목적이다. 넷 다 의문형이고 방향을 단정하지 않으며(테스트로 고정), 생존
편향 통제와 왕복 20bp 비용 가정이 규칙에 명시돼 있다.

**EXP-001은 삭제하지 않고 `SUPERSEDED`로 남겼다** — H-001과 가설은 같지만
§10 스키마를 갖추지 못했다. "실패한 실험도 지우지 마라"(§10)는 구버전에도
똑같이 적용된다.

### 검증

테스트 369 → 424개. **34종목 전건 재실행 8개 지표 완전 동일, ledger 파일
무수정.** `ENGINE_VERSION` v3.49 → v3.50.

### DEFERRED (§19의 7·8) — 구조적으로 막힘

- **Historical Replay(§7)**: PIT가 확보된 과거 시점 분석이 **0건**이다.
  2026년 데이터로 2023년 결과를 재구성하는 것은 §7이 명시적으로 금지한다.
- **첫 OOS 연구(§8)**: 분석일이 3주에 몰려 12개월 구간이 없고, 진입가를 아는
  종목이 34건 중 9건뿐이다. 9건을 5분위로 나누면 분위당 최소표본을 못 채운다.
- **`build_buylist` 이관**: 지금도 `grade in ("S","A")`로 유니버스를 만든다 —
  §3이 금지한 "Gap = BUY"에 가장 가까운 코드다. 다만 검증된 사이징 산출물을
  재작성하면 흔들리므로 새 구조를 병렬로 두고 실사용 후 판단한다.

## v3.51 — 가정집합 Gap 범위: 판정을 점에서 범위로 (2026-08-15,
투자가치 감사의 SINGLE NEXT ACTION 실행)

`docs/AUDIT_2026-08-15_investment_value.md`가 지목한 단 하나의 작업을 실행했다.

### 왜 — 강건성 도구가 잘못된 축을 보고 있었다

감사 실측(34종목 재계산):

| 요인 | Gap 변화 중앙값 | 판정 뒤집힘 |
|---|---|---|
| Realistic Growth 관측대조 괴리 | **8.70%p** | 4/11 |
| 모델 선택 교체(single↔two) | 2.04%p | **11/34 (32%)** |
| DRS **전체 제거** | 0.23%p | 1/34 (3%) |

판정 밴드는 ±5%p다. **가장 큰 오차원이 밴드보다 크고**, 기존 강건성 도구 두 개
(`sensitivity_check` v3.19, `gap_distribution` v3.44)는 **둘 다 판정을 3%만
바꾸는 축(DRS)만** 검사하고 있었다. v3.44는 "취약성은 성장률 축에 있다"고
정확히 결론냈으나 그 결론에 따른 도구는 만들어지지 않았다.

### 무엇을 — `gap_range_over_assumptions()`

정당화 가능한 가정집합(모델 2종 × r±1%p × g_terminal±1%p × n±2년, 중복 제거
30조합)에서 Gap 범위를 계산하고 `judgment_set`·`robust`·`flip_drivers`를 낸다.
**새 방법론 0줄** — 기존 `implied_growth_*`를 격자 위에서 다시 부를 뿐이다.

### 실측 결과 — 21/34가 robust=False

| | |
|---|---|
| robust=False | **21/34** |
| 기존 `sensitivity_check`가 잡던 것 | 2종목(COR·DSGX) |
| 새로 잡은 것 | **19종목** |
| 기존만 잡고 놓친 것 | **0** |
| 뒤집는 축 | model_choice 11 · discount_rate 6 · terminal_growth 2 · 조합만 6 |

**가장 심한 9종목은 모델 선택만으로 Gap이 12~20%p 움직인다** — 판정 밴드의
2.5~4배다. KLAC(20.49%p)·CDNS(18.23%p)·GWRE·MNST·VRT·IDXX·PH·WCN·WM.
이 종목들의 판정은 사실상 분석자의 모델 선택이 결정하고 있었다.

### ⚠️ 내가 세운 성공기준이 빗나갔다 — 격자를 조정하지 않았다

감사에서 성공기준을 "robust=False 8~15종목"으로 잡았는데 실제는 21이다.
원인을 규명한 결과 **격자 설계 오류가 아니라 내 추정 오류**였다 — 감사에서
model_choice 11 / discount_rate 6을 **각각** 측정해놓고 성공기준은 앞의 하나만
근거로 잡았다. 둘을 합치면 이미 15를 넘고, 축 조합으로만 뒤집히는 6종목이
더해진다.

**격자 폭을 결과에 맞춰 조정하지 않았다**(§21 금지). 감사가 실패기준으로 정한
"0종목 또는 30종목 이상"에는 해당하지 않으므로 격자는 그대로 둔다. 독립
검증으로 model_choice 11·discount_rate 6이 감사 실측과 **정확히 일치**했다.

### ⚠️ 이 범위는 실제 불확실성의 하한이다

Realistic Growth를 **고정**한 채 계산한다. 그런데 감사가 측정한 최대 오차원이
바로 그 축이다(관측 대조 괴리 중앙값 8.70%p). 고정한 이유는 성장률을 흔들
정당한 폭이 이 저장소에 없기 때문이다(LYNCH_TYPE_CAPS도 근거 없고 관측치는
11건뿐) — 근거 없는 격자를 지어내지 않는다.

따라서 **`robust=True`는 "확실하다"가 아니라 "이 격자 안에서는 안 뒤집힌다"**는
뜻이다. 테스트로 이 경고를 고정했다.

### 사전등록 — H-005

**결과가 존재하기 전에** 등록했다(12개월 보유수익률 관측 0건). `robust` 정의와
격자를 코드 상수로 고정해뒀으므로 나중에 결과를 보고 정의를 조정할 여지가
원천적으로 없다. 결과 방향은 가정하지 않는다 — 강건한 종목이 이미 시장에
알려져 초과수익이 오히려 낮을 가능성도 열어뒀다.

⚠️ **감사가 확인한 것은 "판정이 가정 하나로 뒤집힌다"는 내부 사실이고, 그것이
투자 성과와 관계가 있는지는 전혀 검증되지 않았다.** `robust` 라벨을 성과
예측으로 읽지 말 것.

### 검증

테스트 425 → 435개. 34종목 8개 지표 완전 동일, ledger 무수정(병기 경로).
`ENGINE_VERSION` v3.50 → v3.51. 리포트: `reports/gap_range_2026-08-16.json`.

## Historical Replay 감사 — §66 STOP CONDITION 공식 발동 + 예측봉인 개시
(2026-08-16, 사용자 "POINT-IN-TIME HISTORICAL REPLAY" 실행 프롬프트)

**핵심 발견부터**: 진짜 Historical Replay(T0 스냅샷 동결 → 미래 아웃컴 공개
→ 비교)는 **지금 수행할 수 없다.** 원인은 데이터 품질이 아니라 시간이다 -
전체 프로젝트 이력(137개 커밋)이 **2026-07-25~08-16, 22일**뿐이고 ledger
34종목의 분석일도 그 안에 전부 몰려 있다. §33이 요구하는 1M조차 채우는
종목이 없다. **§66 STOP CONDITION을 공식 발동**하고
(`reports/historical_validation/limitations.md`), 진짜 미래를 기다리는
대신 "T0를 3주 전으로 재정의"하는 유혹도 거부했다 - 그건 결과가 나왔다는
사실 자체를 성과로 포장하는 것과 같은 함정이다(§52가 경고한 유형).

### 문서 서술과 코드 사실의 괴리 2건 (§0 원칙 적용 결과)

이번 감사가 "DOCUMENTED CLAIM vs CODE-VERIFIED FACT"를 엄격히 구분하라고
요구해 실제로 코드를 파싱했더니, 이전 서술이 부정확했던 지점이 2건 나왔다:

1. **v3.47/v3.49가 "기존 34종목은 PIT_UNKNOWN"이라 적었으나**, 실제로는
   `meta.point_in_time` **키 자체가 없다**(상태값이 아니라 필드 부재 -
   그 ledger들이 v3.19~v3.41 엔진으로 생성돼 v3.47 이후 재실행된 적이 없다).
   결과적 의미(PIT 검증 안 됨)는 같지만 메커니즘 서술이 틀렸다.
2. **v3.48이 "인프라 완성"이라 기록했으나**, `predictions/`·`thesis/`
   디렉터리가 **파일시스템에 존재하지도 않았다**(실사용 0건). 435개 테스트가
   무결성을 검증한 것과 실제로 쓰인 것은 다른 질문이었다.

이건 v3.32(버전스탬프 거짓말)·v3.47(PIT 서술)에 이어 **세 번째로 반복된
"문서가 코드보다 낙관적으로 서술되는" 패턴**이다.

### 실제로 존재하는 것 - 17건의 초단기(9~11일) T0→결과 관측

새로 만들지 않고 이미 저장소에 있던 것을 재발견·엄격 재분석했다:
`growth_scorecard` 11건(그중 1건 ROP는 오버라이드 통제사례라 독립표본에서
제외, 실질 n=10) + `thesis_monitor.falsification_scan` 6건(진짜 반증조건
사전등록→발동확인, 9~11일 후).

**TTD가 유일한 INCORRECT 사례**라 5-why로 전개했다: 4개 반증조건 중 3개
동시발동(Q2 매출 가이던스 하회, Q3 역성장, 경영진 5번째 교체) →
근본원인은 "Realistic Growth가 구조적으로 과거 재무제표 요약이라 거버넌스
리스크를 담을 그릇이 아님". **경고 자체는 있었다**(2026-08-03 정성조사가
이미 Confidence 94→72로 낮춤) - "병기, 자동판정 안 함" 원칙이 정성적 경고를
공식 Gap에 자동반영하지 않도록 막고 있어서, 반증조건이 실제 발동하기
전까지는 라벨이 그대로 유지된 것. 이건 설계된 트레이드오프이지 버그가
아니나, 그 비용(라벨 갱신 지연)이 실측으로 확인됐다.
**Decision Impact는 HIGH** - 공식 판정은 안 바뀌었지만 매수리스트 비중은
실제로 4.80%→2.70%로 축소됐다(2026-08-13, 이미 실행됨).

TCOM 케이스는 v3.42가 스스로 경고해둔 "날짜추출이 서술적 날짜(소송 집단기간)를
오탐할 수 있다"는 함정을 **실제로 재현**했다 - 검증 자체가 불가능(verification=None)한
채로 falsification_scan에 잡혔다.

### 기존 감사 5개 핵심주장 독립 재현 - 전부 일치

`docs/AUDIT_2026-08-15_investment_value.md`의 주장(corr(Gap,FCF수익률)=+0.801,
DRS제거시 판정변경 1/34, 모델선택 단독 판정flip 11/34)과 v3.51의 주장
(gap_range robust=False 21/34)을 **원 스크립트를 재실행한 게 아니라 raw
데이터에서 새로 계산**해 재현했다 - 4건 전부 정확히 일치. [DOCUMENTED
CLAIM] → [EMPIRICALLY VALIDATED at reproduction level]로 승격.

### 신규 절제실험 - 구조적할인/Lynch캡 제거

기존엔 DRS(3%)와 모델선택(32%)만 절제했는데, 이번에 **구조적할인율+Lynch캡을
제거**(원시 CAGR 가중평균만 사용)하는 절제실험을 추가했다: **판정변경
4/34(12%)**. 새 밸류에이션 로직 없이 ledger에 이미 저장된
`growth.breakdown.base_growth_before_fcf_check`를 그대로 재사용했다.

MCK가 가장 순수한 신호다(오버라이드도 캡도 없이 **구조적할인율 단독**으로
Gap이 −0.02%p→+6.36%p, 판정 뒤집힘) - `structural_discount_rate()`가
`VALIDATION_STATUS`에 이미 HEURISTIC으로 표기돼 있었는데, 그 표기가 실제
판정에 미치는 영향력(최대 12% 종목)을 이번에 처음 정량화했다.
ROP는 반대로 **이 절제실험의 함정**을 보여준다 - 원시CAGR(14.01%)로
되돌리면 뒤집히는데, 이건 v3.28이 오버라이드를 도입한 이유(M&A롤업 성장을
캡이 못 거름)가 재확인된 것이지 새 결함이 아니다.

**세 절제실험 비교**: DRS(3%) ≪ 구조적할인/캡(12%) ≪ 모델선택(32%).

### §1 우선순위 조치 - 34종목 예측을 오늘 봉인

근본원인: 예측/논거 인프라(v3.48)가 완성됐으나 실사용 0건이라, 3개월 뒤에도
1년 뒤에도 검증이 시작되지 않는 상태였다. `scripts/freeze_predictions_
2026_08_16.py`가 34종목 전부에 대해 "다음 회계연도 매출 YoY 성장률" 예측을
동결했다.

**새 판단을 발명하지 않았다** - `expected_low/high`는 `growth.breakdown.
revenue_cagr_inputs`(3y/5y/10y, 엔진이 이미 계산해둔 값)의 최소~최대를
그대로 썼다. `thesis_id="NO_THESIS_SIGNAL_ONLY"`로 실제 Investment Thesis가
없다는 사실을 정직하게 남겼다. `prediction_date`는 원분석일이 아니라
**실제 동결일(2026-08-16)** - backdate하면 거짓 인상을 준다.

`engine/`는 한 줄도 안 건드렸다(순수 데이터 동결) - `ENGINE_VERSION` 변경
없음. 신규 테스트 6건(범위가 ledger 원본과 정확히 일치하는지 등)이 이
불변조건을 고정한다.

### 작성 중 자체적으로 잡은 오류 1건

case_results.md 초안에 "DUOL·SE가 모델선택에 취약하다"고 검증 없이 적었다가,
`reports/gap_range_2026-08-16.json`을 직접 대조하니 **둘 다 robust=True,
flip_drivers 전부 빈 값**이었다 - 실제로 취약한 건 6종목 중 TCOM 하나뿐
이었다. 즉시 정정했다(이 프로젝트의 반복 원칙 - 발견 즉시 정정하고 숨기지
않는다).

### 산출물

`reports/historical_validation/`(12개 문서: executive_summary·methodology·
limitations·model_evolution_ledger·forecast_validation·decision_attribution·
error_analysis·baseline_comparison·ablation_analysis·model_improvements·
holdout_validation·case_results), `data/historical_validation/`(5개 CSV),
`predictions/`(신규, 34건).

**단일 IRS 점수를 만들지 않았다**(§61 명시적 금지 - 축별로만 보고).
**§59 마스터 케이스 테이블을 34행 전부 채우지 않았다** - 대부분의 필드가
[NOT AVAILABLE]인데 그걸 34번 반복하는 건 정보가 아니라 표만 길어지는
것이라 판단, 실제 값 있는 6종목만 상세 기록하고 나머지는 사유를 한 번만
설명했다.

테스트 435 → 441개. 34종목 8개 지표 완전 동일, ledger·engine 무수정.

## v3.52 — structural_discount_rate() 외부 경제적 근거 조사 (2026-08-16,
Historical Replay 감사 "다음 감사가 반드시 할 일" 2번 실행)

### PROBLEM

`structural_discount_rate()`는 34종목 판정의 **12%를 좌우**하는데(ablation
실측, `ablation_analysis.md`) 코드 주석 한 줄 말고는 근거가 없었고, `v3.46`이
만든 `VALIDATION_STATUS`(계약서 40절 5단계 인식론적 사다리) 딕셔너리에
**항목 자체가 없었다** - `erp_from_drs`·`lynch_type_caps`·`rar` 등은 라벨이
있는데 이 함수만 라벨조차 안 붙어 있었다. 함수는 사실 서로 다른 두
메커니즘을 하나로 합친 것: (1) 추세둔화 조정(`trend_delta` = 10y CAGR -
3y CAGR, 둔화면 할인 확대), (2) 초대형주 가산(시총≥1000이면 +3%p, ≥200이면
+1%p 고정 가산, 10억 단위).

### EXTERNAL EVIDENCE (§49 - 1차 학술문헌 우선)

**Chan, Karceski, Lakonishok, "The Level and Persistence of Growth Rates"**
(Journal of Finance Vol 58 No 2, 2003, pp.643-684 / NBER Working Paper
w8282) - 3개 독립 출처(SSRN 등재정보·NBER 논문페이지·Illinois Experts
저자소속 초록페이지)로 실재를 확인. **원문 PDF는 2회(NBER·저자 소속사 LSV
Asset Management) 모두 텍스트 추출 실패**(FlateDecode 압축 바이너리만
반환) - 확보한 근거는 초록 수준(Illinois Experts HTML)이라는 한계를
명시한다.

확인된 헤드라인: **"장기 이익성장에 우연 이상의 지속성이 없다"** - 최근
고성장을 그대로 미래로 연장하면 안 된다는 `trend_delta` 메커니즘의 핵심
전제와 방향이 정확히 일치한다. "다양한 예측변수를 써도 예측력이 낮다",
IBES 애널리스트 장기성장 전망이 "과도하게 낙관적"이라는 발견도 함께
확인됐다. **초록에는 기업규모(firm size) 관련 언급이 없다** - 초대형주
가산 메커니즘을 지지하는 근거는 확보하지 못했다(지지도 반박도 아닌
[원문 미확인]). 별도로 "규모×성장평균회귀" 관계 자체를 검색한 결과도
혼재됐다(단조적으로 "클수록 할인 확대"를 지지하는 문헌을 찾지 못함).

### 판정

두 구성요소를 **분리해서** 라벨링했다(하나로 뭉뚱그리면 근거 있는 절반이
근거 없는 절반을 가려버린다):
- **trend_delta 메커니즘**: `ECONOMICALLY_SUPPORTED`로 승격(방향은 문헌
  일치, 단 `deceleration_sensitivity=0.5` 반응계수 자체는 여전히 임의값이라
  `EMPIRICALLY_SUPPORTED`나 `CALIBRATED`는 아님).
- **초대형주 가산(+3%p/+1%p)**: `IMPLEMENTED_NOT_VALIDATED` 그대로 유지,
  근거 없음을 명시.

**코드 로직은 바꾸지 않았다** - 혼재된 외부검색 결과만으로 가산항을
지금 수정하면 이 프로젝트가 반복 확립한 원칙("근거 없이 유지하던 걸
근거 없는 다른 숫자로 바꾸는 것은 개선이 아니다" - LYNCH_TYPE_CAPS·P/B
임계값·ERP매핑과 동일 판단)을 정확히 위반하는 것이 된다. `VALIDATION_
STATUS` 딕셔너리에 두 구성요소의 근거 수준을 라벨로만 추가했다 - 로직
변경 0줄. 전문은 `reports/historical_validation/structural_discount_
research.md`.

### 검증

테스트 441개 전부 통과, 34종목 골든재현 8개 지표 완전 동일. `ENGINE_
VERSION` v3.51 → v3.52(VALIDATION_STATUS 라벨 신설 - v3.32 규칙에 따라
`engine/` 변경 시 상수 갱신).

### 남은 한계

논문 원문 본문(회귀계수·규모효과 세부 분석)에 끝내 접근하지 못해 확보한
근거는 초록 수준이다 - 초록에 규모 관련 언급이 없다고 해서 원문 본문에도
없다는 뜻은 아니므로 [원문 미확인]으로 정직하게 남겨둔다. 다음에 이 함수를
다시 볼 여력이 생기면 초대형주 가산 하위요소부터 검토할 것(구조적할인
12% 영향력 중 상대적으로 더 취약한 부분이라는 사실을 이번에 확정했다).

## 조합축에서만 취약한 6종목 개별 축 분해 (2026-08-16, Historical Replay
감사 "다음 감사가 반드시 할 일" 3번 실행)

### PROBLEM

v3.51 `gap_range_over_assumptions()`가 34종목 중 21종목을 `robust=False`로
판정했는데, 그중 `flip_drivers`(축을 하나씩만 흔드는 OAT 점검)가 **4개 축
전부 False**인데도 `robust=False`인 종목이 6개(BRO·COR·TCOM·TYL·VRSN·ZTS)
있었다 - 단일 축만으로는 절대 안 뒤집히는데 전체 30격자 어딘가에서는
뒤집힌다는 뜻이다. v3.51은 이 존재만 확인하고 원인은 미분해로 남겼다.

### 방법

`scripts/decompose_combination_flips_2026_08_16.py` - 새 밸류에이션 로직
0줄, `engine/gap_analysis.py`의 `ASSUMPTION_GRID`/`_implied_growth_at`/
`judgment_from_gap`을 그대로 재사용해 6종목 각각의 30격자 전체를 순회하며
"판정을 뒤집는 데 필요한 최소 동시변경 축 개수"를 특정했다.

### 결과 - 6종목 전부 같은 메커니즘, 필요 축 개수만 다르다

| 종목 | 최소조합축수 | 관여 축 | 30격자 중 flip 지점 |
|---|---|---|---|
| BRO·COR·TYL·VRSN | 2 | model_choice + discount_rate | 4~7 |
| **TCOM** | **3** | discount_rate + terminal_growth + growth_duration_n(**model_choice 불참**) | 1 |
| **ZTS** | **4**(전축) | 전부 동시 | 1(가장 강건) |

**메커니즘**(BRO 예시): 단독 model_choice는 Gap 7.43%p→6.44%p(경계 안 넘음),
단독 discount_rate(r+1%p)는 7.43%p→6.48%p(경계 안 넘음), **둘을 동시에**
움직이면 7.43%p→**4.78%p**(±5%p 경계 통과). 각 축은 혼자서는 부족하지만
같은 방향으로 겹치면 넘는다 - OAT가 "안전하다"고 답한 게 거짓이 아니라
"상관된 가정 오차"까지는 원래 못 본다는 뜻이었을 뿐이다.

⚠️ **주의**: 여기의 `discount_rate` 축(Implied Growth 역산에 쓰이는 DCF
할인율 `r`, ±1%p)은 같은 날 조사한 `structural_discount_rate()`(Realistic
Growth 계산에 쓰이는 별개 함수)와 **이름만 같고 다른 것**이다 - 혼동 주의.

**TCOM은 유일하게 model_choice가 관여하지 않는 예외**다(다른 5종목과
구조적으로 다름 - two_stage가 안정적으로 채택돼 모델 교체 자체의 영향이
작은 것으로 추정되나 직접 검증 안 됨, [추정, 미검증]으로 남김). **ZTS는
가장 강건**하다(30격자 중 정확히 1개, 전축이 동시에 극단으로 움직이는
코너에서만 뒤집힘).

### 판정

이 6종목은 v3.51이 이미 지목한 9종목(KLAC·CDNS 등, 모델선택 단독으로
12~20%p 움직이는 "단일 가정 하나가 이미 위험"군)과는 다른 위험군이다 -
"개별로는 안전하지만 가정들이 서로 독립이 아닐 때(예: 시장충격 한 번이
할인율과 모델선호를 동시에 바꾸는 경우) 취약"한 집단이다. **코드는 바꾸지
않았다** - "축 간 상관 시나리오" 격자를 추가하는 것은 검토할 수 있으나
관측 6건만으로 상관구조를 코드화하는 건 근거 부족(P/B 임계값·앵커표와
동일 원칙 - DEFER).

### 검증

새 `engine/` 코드 없음(순수 분석 스크립트) - 테스트 441개·34종목 골든재현
그대로 유효(재확인 완료, 변동 0건). 상세는 `reports/historical_validation/
combination_flip_decomposition.md`.

## 모델선택 D3 연구 — 규칙화 REJECT, 등급·유니버스 병기 ADOPT (2026-08-16,
연구 모듈 §28 D3 적용)

시스템 최대 취약축(판정 flip 32%, DRS 3%·구조적할인 12%를 압도)인 모델선택
(single_stage vs two_stage)을 조사했다. `model_used`는 분석자가 직접 지정하고
통제는 `model_choice_reason` 자유서술 필수화(v3.19)뿐이라 **규칙이 아예 없다.**

### 이론 기준이 실제 선택을 전혀 설명하지 못한다 (핵심 실험)

교과서 기준(성장이 g_terminal에 근접→Gordon, 크게 상회→다단계)을 34종목에 적용:

| 선택 | RG − g_terminal 범위 | n |
|---|---|---|
| single_stage | +1.18%p ~ +8.53%p | 11 |
| two_stage | −2.00%p ~ +21.53%p | 23 |

**두 구간이 거의 완전히 겹친다**(single 구간 안에 two_stage 9종목). 결정적 반례:
GWRE/BKNG 둘 다 +8.04%p인데 반대 선택, BRO·CDNS/GEN 둘 다 +8.53%p인데 반대 선택,
**KEYS는 성장이 터미널보다 낮은데(−2.00%p) two_stage** — 이론상 가장 명백히 틀린
조합이다. 즉 관측 가능한 성장프로파일로는 실제 선택을 예측할 수 없다.

사유 34건은 **세 논리가 섞여 있다**: 과거기록 답습 7(BRO·IDXX·MNST·PH·VRSN·
WCN·WM — 경제논리 아님, 원 근거는 v3.13~v3.15대라 복원 불가) / 성장단계 이론 다수 /
괴리 3%p 초과 시 재검토 4(GWRE·KLAC·VRT·KEYS — 셋 중 **유일한 실질 경제검정**,
내재성장률이 회사 가이던스 대비 달성가능한지 확인).

### H5 선택편향은 기각됐다

"유리한 답이 나오는 모델을 골랐는가": 선택모델 Gap이 더 높은 경우 18/34, 낮은
경우 16/34, 중앙값 +0.15%p. 결정적으로 **VRT(−8.68%p)·KEYS(−7.67%p)·PH(−7.11%p)·
DSGX(−3.17%p)는 불리한 쪽을 일부러 택했다**(VRT는 GWRE/KLAC와 같은 절차를 거쳐
"이번엔 결론이 반대"라고 명시). 문제는 편향이 아니라 기준 부재다.

### ⚠️ §22 검증에서 자본이 걸린 사례 — 판정 수준만 보면 놓친다

**조사 중 내가 실제로 밟은 오류를 기록해둔다**: 처음에 판정(3단계)만 보고 "현재
매수리스트 중 모델취약 0건"이라 결론냈으나 **틀렸다.** 자본을 배분하는 경로는
판정이 아니라 등급(S~F) → 매수 유니버스(`grade in S/A`)다:

| 의존 수준 | 종목수 | 종목 |
|---|---|---|
| 판정(3단계) | 11 | BSY·CDNS·DSGX·GWRE·IDXX·KLAC·MNST·PH·VRT·WCN·WM |
| **등급(S~F)** | **13** | 위 11 + **BRO·BSX** |
| **매수 유니버스** | **4** | **BRO·BSX·DSGX·VRT** |

- **BRO는 현재 포트폴리오 6.75% 보유 중**이고 A등급(+7.43%p, A/B경계 +7%p 바로
  위)이 single_stage 선택에 달려 있다(대안모델이면 +6.44%p → B → 유니버스 이탈).
  그 선택 사유 전문은 *"과거 큐28 기록(v3.15)이 single_stage 4.25%를 사용했음이
  트래커에 명시되어 동일 모델 채택"* — **경제논리가 아니고 원 근거는 복원 불가.**
- BSX(B→A)·DSGX(C→A)·VRT(C→A)는 반대로 대안모델이면 **유니버스에 진입**한다 —
  BSX 스크리너 사건이 확립한 최악의 오류유형(거짓 탈락, 배제된 종목은 아무도
  다시 안 본다)과 같은 구조.
- **`build_buylist_2026_08_03.py`는 이 취약성을 전혀 소비하지 않는다**(`robust`/
  `gap_range`/`model_used`/`flip` 미참조). v3.51이 계산해둔 취약성이 자본배분
  계층에 전달되지 않는다.
- 과거기록 답습 ∩ 판정의존 = 5종목(IDXX·MNST·PH·WCN·WM), 넷은 대안모델이면
  '적정가'→'과대평가'로 바뀐다.

### 결정 (§29)

| 대상 | 결정 | 근거 |
|---|---|---|
| 결정론적 모델선택 규칙 신설 | **REJECT** | 이론 기준이 데이터를 분리 못함이 실증됨. 임계값을 지금 만들면 근거 없는 휴리스틱을 근거 없는 다른 것으로 대체하는 것(LYNCH_TYPE_CAPS·P/B임계값·ERP매핑과 동일 판단) |
| 등급·유니버스 수준 모델의존성 병기 | **ADOPT** | 새 파라미터 0개, 판정 무변경, 순수 투명성 |
| 과거기록 답습 7종목 재검토(BRO 우선) | **EXPERIMENT** | 자본이 걸려 있으나 지금 모델을 바꾸면 그 자체가 근거 없는 변경 — 경제적 사유 확보 후 |
| 공식 판정·Gap·등급 변경 | **하지 않음** | 어느 모델이 옳은지 판정할 근거가 이 저장소에 없다 |

### ⚠️ 수익률 개선은 일절 주장하지 않는다

Rasmussen et al.(2019, SSRN, S&P500 727종목 2003~2018)은 Gordon 방식과 value-driver
방식이 **완전히 다른 특성의 종목을 고르면서도 수익률·위험은 유사**했다고 보고한다.
전이된다면 모델선택은 개별 라벨만 바꿀 뿐 포트폴리오 성과에는 중립일 수 있다
(§17 H6). Eriksson(2011)은 "보편적 모델은 없다"며 Gordon·DCF 부진의 원인으로
**과거 데이터만으로 성장률을 추정한 것**을 지목하는데, IRS의 Realistic Growth가
정확히 그 구조라 성장률이 진짜 병목이면 모델선택 개선의 한계효용은 낮다.

전문: `reports/research/model_choice_2026-08-16.md`(§28 D3 15개 절), 재현:
`scripts/model_choice_sensitivity_2026_08_16.py`. 테스트 441 → 447개,
`engine/` 무변경이라 `ENGINE_VERSION` v3.52 유지.

## Realistic Growth D3 연구 — 축소기제 재해석 + H-006 사전등록 (2026-08-16)

모델선택 연구가 남긴 문장("성장률이 진짜 병목이면 모델선택 개선의 한계효용은
낮다")과 v3.52에서 확보한 Chan/Karceski/Lakonishok(2003)의 **"장기 이익성장에
우연 이상의 지속성이 없다"**를 결합하면 IRS 핵심 전제가 직접 위협받는다.

### Gap 순위를 지배하는 것은 성장기계다

34종목 Spearman 순위상관: **Gap↔Realistic Growth +0.8704** > Gap↔FCF수익률
+0.7846 (FCF수익률↔RG는 +0.4845). 즉 성장기계는 장식이 아니라 **순위의 주된
결정자**이므로, 이 입력에 예측력이 없다면 결함이 시스템 전체로 전파된다.

### 순진한 베이스라인과의 정면 경쟁 — 두 갈래로 갈린다

`growth_scorecard` 11건 중 순환참조인 ROP(override로 관측치와 일치시킨 값)를
제외한 **독립 10건**, §18대로 증거등급 분리 유지. 절대오차 중앙값/평균(%p):

| 추정량 | realized_quarterly(n=5) | guidance_annual(n=5) |
|---|---|---|
| **IRS Realistic Growth** | **10.87 / 11.76** | **8.70 / 15.08** |
| 원시 5y 매출CAGR | 19.40 / 17.93 | 23.05 / 20.65 |
| 원시 3y 매출CAGR | 19.44 / 18.81 | 28.28 / 27.01 |
| 터미널 고정(3.47%, 무정보 상수) | 14.83 / 16.20 | 12.83 / **11.80** |

**(a) IRS는 순진한 CAGR 연장을 명확히 이긴다** — 오차가 대략 절반, 두 등급 모두
일관. 구조적할인율+Lynch상한 조합이 실제로 작동하고 있다.
**(b) 무정보 상수 대비 우위는 취약하다** — 중앙값은 이기나 guidance_annual
평균에서 상수에 진다(꼬리가 두꺼움: KEYS 26.5%p, TTD 28.8%p).

### ⭐ 재해석 — IRS가 이기는 기제는 '예측'이 아니라 '축소(shrinkage)'다

함의된 축소계수 `w=(RG−g_term)/(raw−g_term)` 34종목 전수: **중앙값 0.807**,
평균 0.741, 범위 0.19(ROP)~1.12(KEYS). 즉 원시 성장률을 **19%만** 터미널 쪽으로
당긴다. 세 갈래가 독립적으로 **더 공격적인 축소**를 가리킨다:
1. 표본내 최적 단순축소가 두 증거등급에서 각각 **w=0.45 / w=0.25**
2. 부호검정 **10건 중 8건이 IRS 과대추정**(단측 p=0.0547, **양측 0.1094 —
   n=10에서 유의하지 않다**)
3. 외부문헌의 성장 지속성 부재

### ⚠️ 왜 엔진을 바꾸지 않았는가

표본내 최적 w는 **5개 점에 1-파라미터를 적합한 과적합값**이다. 승격하면 §43
위반이자 이 프로젝트가 LYNCH_TYPE_CAPS·P/B임계값·ERP매핑·초대형주가산에서 반복
확립한 금지사항의 재발이다. 게다가 그 10건은 **가설을 만든 데이터**라 같은
데이터로 검정하면 순환이다.

### 결정 (§29)

| 대상 | 결정 | 근거 |
|---|---|---|
| 성장기계를 FCF수익률 스크린으로 대체 | **REJECT** | Gap↔RG 상관이 더 높고 순진한 베이스라인을 명확히 이긴다 — 제거하면 정보를 잃는다 |
| 축소계수를 표본내 최적값으로 조정 | **REJECT** | n=5 적합 = 과적합 |
| "RG가 체계적으로 과대추정한다" 가설 | **EXPERIMENT** | `experiments/H-006.json` 사전등록 완료 |
| 공식 Gap·판정·RG 수치 변경 | **하지 않음** | 위와 동일 |

**H-006은 H-001~H-005와 달리 `status=REGISTERED`(BLOCKED 아님)다** — 주가·12개월
수익률이 필요 없고 **실현 매출성장률만** 있으면 되므로, 2026-08-16 동결한 34건
예측이 해소되는 대로 검정 가능한 **이 저장소 최초의 실행가능 실험**이다. 동기가
된 10건은 순환 방지를 위해 검정 표본에서 명시적으로 제외했다.

⚠️ **수익률 개선은 일절 주장하지 않는다** — 예측정확도와 투자성과는 별개 질문이고
후자는 H-001 계열(전부 BLOCKED)이 다룬다. n=10·개발데이터·9~11일 초단기 창이라
어떤 결론도 통계적으로 확립되지 않았으며, 관측 대상(1개 분기/1개년)과 추정 개념
(n≈12년)의 기간 불일치도 미해소다.

전문: `reports/research/realistic_growth_2026-08-16.md`. `engine/` 무변경
(v3.52 유지), 테스트 447개, 34종목 골든재현 완전 동일.

## 모델선택 ADOPT를 실제 결정경로에 배선 + 순위파일 노후화 발견 (2026-08-16)

모델선택 D3 연구가 "등급·유니버스 수준 모델의존성 병기"를 **ADOPT**로 결정했는데,
실제로는 오프라인 리포트만 만들어졌고 **자본배분 경로에는 전달되지 않고 있었다.**
이 프로젝트는 "문서로만 둔 규칙은 지켜지지 않는다"를 이미 네 번 겪었다
(run_self_check·confidence_score·claim/lock·cross_check_prior_record) - 다섯
번째를 만들지 않으려고 `scripts/build_buylist_2026_08_03.py`에 경계검토를
배선했다.

### 안전성 - 비중은 하나도 안 바뀐다

`weight_final`이 전부 확정된 **뒤에** 키만 덧붙이는 구조라 구조적으로 불변이며,
실측으로 기존 산출물과 전 필드 완전 동일함을 확인했다(신규 키
`model_dependent_universe` 1개만 추가, 합계 정확히 1.0). 이전 감사가
`build_buylist` 재작성을 DEFER한 이유(검증된 사이징 산출물이 흔들림)를 그대로
지킨다. 회귀 테스트 6건(`tests/test_buylist_boundary_review.py`)으로 고정.

### 경계검토가 드러내는 3가지

1. **보유 중인데 편입근거가 모델선택에 달린 종목** — BRO(6.75% 보유),
   A(+7.43%p) → 대안모델이면 B(+6.44%p)로 **유니버스 이탈**, 사유는 과거기록 답습.
2. **거짓탈락 후보** — DSGX(C→A), VRT(C→A). BSX 스크리너 사건이 확립한
   "배제된 종목은 아무도 다시 안 본다" 유형.
3. **⭐ 순위파일 노후화로 아예 고려조차 안 된 종목(신규 발견)** — 유니버스는
   `reports/portfolio_ranking_2026-08-02.json`에서 만드는데 **BSX는 2026-08-13
   분석이라 그 파일에 아예 없다**(Gap +5.87%p '저평가 가능성', 34종목 중 유일).
   즉 BSX는 어떤 판정도 받지 않은 채 **파일이 자기보다 먼저 만들어졌다는 이유만으로**
   매수 유니버스에서 빠져 있었다. **판정에 의한 탈락과 노후화에 의한 탈락은 전혀
   다른 것**이라 구분해 표시한다(순위 파일을 이번에 재생성하지는 않았다 - BSX는
   B등급이라 S/A 유니버스에 어차피 안 들어가므로 비중에 영향이 없고, 재생성 자체는
   이번 작업 범위를 넘는다).

`reports/buylist_boundary_review_2026-08-16.json`에 저장. 파일이 없으면 조용히
"의존 없음"으로 처리하지 않고 **"미확인"으로 명시**한다(is_insurer·
sbc_cross_check·holdings_overlap의 "측정 불가 ≠ 없음"과 동일 원칙).

테스트 447 → 453개, `engine/` 무변경(`ENGINE_VERSION` v3.52 유지), 34종목
골든재현 완전 동일.

## 외부 벤치마크 기반 IRS 진화 보고 — n 민감도 진단 구현 (2026-08-16)

세계 최상위 투자·리서치·데이터 조직의 관행을 IRS와 대조했다. §17 "빠진 기능이
아니라 빠진 차원"을 실제 코드에 대고 확인한 결과:

**W-1 (구조적 공백)**: `engine/` 전체에 ROIC·incremental return·moat·reinvestment
히트 **0**(ROE는 is_insurer 분기 2종목 opt-in 안에만 존재). 품질투자 진영이 성장
지속가능성 판단에 쓰는 표준축(ROIIC — 신규 자본의 한계 생산성)이 IRS에 없고,
**바로 그 때문에 성장지속기간 n을 종목별로 차등화할 근거도 없다**(34종목 전부 12).
두 공백은 같은 뿌리다. 단 FCF=OCF−capex와 min() 경로가 자본집약도를 **암묵적으로
부분 반영**하므로 "자본효율을 무시한다"로 읽으면 안 된다 → **DEFER**(NOPAT·
투하자본 신규 입력 필요).

**W-2 (실측 확정, 구현 완료)**: `ASSUMPTION_GRID`의 `n_delta=(-2,0,2)`는 n∈{10,12,14}만
보는데 엔진은 `capped_n(8~15)`를 허용 — **격자가 엔진 허용범위의 부분집합**이었다.
전 범위로 보면 BSX·DSGX·PTC의 `flip_drivers.growth_duration_n`이 False→True로
바뀐다(이미 취약했으나 **n축이 안전하다고 잘못 귀속**돼 있었다 — flip_drivers는
분석자에게 무엇을 재검토할지 알려주는 필드라 단순 카운트 오류가 아니다). ROP는
robust True→False로 **신규 취약**(n 단독 아닌 조합 경로).

⚠️ **`ASSUMPTION_GRID`는 수정하지 않았다** — H-005가 그 상수로 `robust`를 사전등록
했으므로 결과를 본 뒤 정의를 바꾸면 사후합리화와 구분되지 않는다. 대신
`gap_range_over_assumptions(ledger, grid=...)`의 **이미 존재하는 오버라이드 인자**만
사용해 진단했다 — 엔진 코드 0줄 변경, 사전등록 보존(테스트로 고정).

**W-3 (자체 정정 포함)**: MNDY·UBER의 `fcf_by_year` 최초 연도가 음수인 걸 보고
"v3.19 가드 미작동"으로 잠정 판단했으나 실제 `cagr_5y_base_year`를 확인하니
**가드는 정상 작동**(MNDY 2020→2021, UBER 2021→2022로 기준을 앞당김)이었다.
버그가 아니다. 실제 문제는 가드가 음수 기준을 **근사-0 기준으로 바꿔놓고 알리지
않는다**는 것 — MNDY 기준 FCF는 최종값의 0.8%이고 필드명은 `fcf_cagr_5y`인데
실제 span은 4년이다. 34종목 중 **11종목**이 이 패턴. 현재는 `min()`이 이 값들을
자동으로 버려 결과가 보호되지만 **그 보호는 우연적**이라, 그 값을 다른 용도로
쓰는 순간 깨진다(매출−FCF CAGR 격차는 Gap과 상관 −0.04로 독립 차원임이 확인됐으나
바로 이 아티팩트 때문에 현 형태로는 **REJECT**).

**§31 안티기능 등록부 신설** — 멀티에이전트/벡터DB/자동매매/단일합성점수/LLM
직접계산/상관행렬최적화를 **의도적으로 만들지 않는 것**으로 등록했다(현재 병목은
조율·검색이 아니라 입력의 근거 부재다).

산출물: `docs/irs_evolution_report_2026-08-16.md`(§40 13개 절 + §33 갭 매트릭스),
`docs/research_decision_record.md`(결정 18건 + 개정이력 R-1),
`scripts/n_sensitivity_diagnostic_2026_08_16.py`, `reports/n_sensitivity_2026-08-16.json`.
테스트 453 → **459개**, `engine/`·ledger·공식판정 전부 무변경, 34종목 골든재현
8개 지표 완전 동일, `ENGINE_VERSION` v3.52 유지.

## v3.53 STAGE 1 — Growth Quality: 성장의 양과 경제성 분리 (2026-08-16)

IRS 고도화 5단계(Growth Quality → Duration → Robustness → PIT Replay → Attribution)
중 STAGE 1. **새 코드를 먼저 쓰지 않고 RQ-001 증분정보 연구부터 수행했다.**

**코드 확인으로 드러난 공백**: `operating_income_by_year`는 **필수 입력**인데 엔진은
이걸 `margin_volatility_score`(표준편차) **하나로만** 쓴다 — 마진 **수준**은 어디에도
쓰이지 않는다. 즉 영업이익률 32.8%(BKNG)와 3.4%(GWRE)가 변동성 말고는 동일 취급된다.
`capex_by_year`도 필수 입력이나 `capex_intensity_from_series`는 주관적
`capex_classification`이 있을 때만 작동하며 **34종목 중 사용 0건**이다.

**⭐ 초기 가설이 실측으로 기각됐다.** "추세(trend)가 새 정보"라 예상했으나 반대였다 —
마진추세 slope는 마진변동성과 **0.675**, RealisticGrowth와 0.594, Gap과 0.512로 크게
중복된다(적자→흑자 전환 기업이 큰 slope와 큰 변동성을 동시에 갖기 때문). 반면
**수준(level)이 독립적**이었다: 마진수준 vs 마진변동성 **−0.069**, vs DRS **−0.043**.

**FCF/영업이익도 기각했다** — SBC/FCF와 순위상관 **+0.571**(n=8), 상위 3종목
(GWRE 6.83·WDAY 3.85·DUOL 2.73)이 전부 고SBC로 "현금전환 우수"가 실은 SBC 강도를
재는 아티팩트다. v3.23이 드러내려던 편향을 새 지표로 재도입할 뻔했다.

**10개 후보 중 2개만 채택**: 영업이익률 수준(ADOPT) · capex/매출 수준(ADOPT,
PROXY_ONLY). ROIC/ROIIC는 **BLOCKED**(투하자본 시계열이 입력 스키마에 없음) —
`engine/growth_quality.py`는 **proxy를 ROIIC라고 부르지 않으며** 테스트가 이를 강제한다.

**구조는 D(독립 보관)를 택했다.** 미리 C(둘 다 반영)를 정답으로 가정하지 말라는
지시대로, 두 축이 성과와 관계있다는 증거가 **0건**이라는 사실이 D를 강제한다 —
반영하면 미검증 변수가 34종목 판정을 즉시 바꾼다. `experiments/H-007.json`으로
사전등록 완료(H-006 의존, 방향 미지정).

**§12 STAGE 1 완료 조건 충족**: 관측 중앙값 기준 고성장·고마진 8종목(중앙 마진
27.4%)과 고성장·저마진 9종목(13.1%)이 분리되는데 **RealisticGrowth는 오히려 저마진
집단이 높다**(12.19% vs 16.87%) — 기존 IRS가 표현하지 못하던 차이다.

**실제 포트폴리오 함의(병기, 자동판정 안 함)**: 매수리스트 12종목 중
고성장·고마진 56.07% / **고성장·저마진 43.93%**이고, **MNDY(6.27% 보유)는 최근
영업이익률이 −0.14%로 적자**인데 Lynch 상한 바인딩으로 RealisticGrowth 25.00%,
Gap +23.28%p의 S등급을 받고 있다. **비중은 조정하지 않았다** — 두 축의 예측력이
미검증이므로 조정하면 근거 없는 변경이 된다.

전문 `reports/research/RQ-001_growth_quality_2026-08-16.md`. 테스트 459 → **472개**,
34종목 골든재현 8개 지표 완전 동일, ledger 무수정, `growth_quality`는 `pipeline`에
**미배선**(테스트로 고정). `ENGINE_VERSION` v3.52 → v3.53.

## STAGE 3 — 투자판정 강건성 감사(R-001 사전등록, 2026-08-16)

지금까지의 강건성 도구 두 개(v3.19 `sensitivity_check`, v3.44 `gap_distribution`)는
DRS 축만 봤고, v3.51 `gap_range_over_assumptions`는 `ASSUMPTION_GRID`가 n∈{10,12,14}만
써서 **엔진 허용범위(`capped_n` 8~15)의 부분집합**이었다 — §24가 말하는 False
Robustness("격자가 실제 허용범위보다 좁음")에 정확히 해당한다. 그런데 그 상수는
H-005가 `robust` 정의로 사전등록한 것이라 고치면 사후조정이다(결정 #16 REJECTED).

**결정 #16이 남긴 재개조건("새 실험 ID로 재등록하는 경우에 한해")을 따라 H-005 격자는
그대로 두고 `experiments/R-001.json`을 독립 가정공간으로 새로 사전등록했다**(6축,
근거수준 LEVEL 1~5 명시, LEVEL 6 미사용). 유효 시나리오 9,675개(종목당 평균 285).

⚠️ **Judgment Stability는 확률이 아니다.** "유효 가정공간 중 같은 판정이 나온 비율"일
뿐이고 보정된 적이 없다. "82%"는 "82% 확률로 맞다"가 아니라 "285개 중 234개에서 같은
라벨"이라는 뜻이다.

### ⚠️ 감사 중 자체 발견 — 사전등록 6축 중 1축이 조용히 죽어 있었다

초판 실행에서 "SBC 하방 미검증 34/34"가 나온 걸 이상하게 여겨 확인하니, ledger에
없는 키(`fcf0_after_sbc`)를 읽고 있었다(실제 스키마는 `fcf0_sbc_adjusted`). **사전등록
공간보다 좁게 감사한 셈**이라 키를 고쳐 재실행했다 — 범위 변경(§34 금지)이 아니라
구현을 등록에 맞추는 수정이다. 차이가 작지 않았다:

| 종목 | 판정안정 | 유니버스안정 | 주요 취약축 |
|---|---|---|---|
| WDAY | 100% → **58%** | 100% → **53%** | — → **fcf0** |
| TTD | 100% → 86% | 100% → 76% | — → fcf0 |
| 전 가정공간 판정유지 | 10/34 → **7/34** | | |

WDAY가 `fcf0` 단독으로 뒤집히는 것은 v3.23이 이미 문서화한 사실(트래커에서 SBC 차감
시 판정이 뒤집히는 유일한 종목)의 **독립 재현**이라, 감사 구현 신뢰도의 부수 증거가 된다.

### ⭐ 이번 감사의 핵심 구분 — "판정 취약" ≠ "자본 취약"

매수 유니버스는 `grade in (S, A)`로 결정되므로 **등급이 S/A 경계를 넘느냐**만 자본에
닿는다. 이 구분을 하니 v3.51의 `robust=False` 21종목이 두 갈래로 갈렸다:

- **거짓 취약 8종목**(CDNS·GWRE·IDXX·KEYS·MNST·PH·VRSN·WM): 판정안정 21~99%인데
  **유니버스안정 100%**. 전부 C·D등급이라 어떤 시나리오에서도 S/A로 안 올라간다.
  극단은 KLAC(판정안정 **12%** / 유니버스 99.6%). → `robust=False`를 자본 리스크로
  곧바로 읽으면 과잉반응이다. 단 이건 "안전"이 아니라 "**현재 유니버스 규칙 아래서만**
  무해"이며, B등급을 편입하는 순간 즉시 유효해진다.
- **진짜 자본 취약 4종목, 비중 합계 24.28%**: TCOM 10.25%(유니버스안정 57%, RG 1축) ·
  BRO 6.75%(62%, model_choice 1축) · WDAY 4.51%(**53%**, fcf0 1축) · TTD 2.70%(76%,
  fcf0). 나머지 8종목(75.79%)은 96~100%.

**BRO는 판정은 2축이 필요한데 유니버스는 1축이면 바뀐다** — 등급 경계(A/B, +7%p)가
판정 경계(±5%p)보다 base에 가깝기 때문이다. 판정 수준만 보면 놓친다(2026-08-16
모델선택 연구에서 실제로 밟았던 오류의 재확인). 게다가 BRO의 `model_choice_reason`은
*"과거 큐28 기록(v3.15) 답습"*이라 **원 근거가 복원 불가능하다** — 6.75%의 자본이
복원할 수 없는 근거 위에 있다는 뜻이라 최우선 추가조사 대상으로 남긴다.

### ⚠️ 거짓 강건성 — 캡 바인딩이 RG 가정공간을 상방으로만 열어놓는다

`realistic_growth` 축은 기업 자신의 3y/5y/10y 매출 CAGR 범위(LEVEL 2)를 쓰는데,
Lynch 상한이 바인딩된 종목은 base(캡)가 그 범위보다 **아래**에 있다:

| 종목 | RG(=캡) | 자체 CAGR 범위 | 하방 시나리오 |
|---|---|---|---|
| DUOL·MNDY·PDD | 25.00% | 33~49% | **없음** |
| GEN·BRO·CDNS | 12.00% | 12~17% | **없음** |
| ROP | 5.50%(override) | 13.7~14.5% | **없음** |

**DUOL·MNDY·PDD·GEN(보유 합계 39.63%)의 "판정안정 100%"는 하방 RG 시나리오가 공간에
존재하지 않기 때문일 수 있다.** 가정공간을 지금 바꾸지 않는다 — 캡보다 낮은 성장률의
근거가 이 저장소에 없고, 없는 범위를 지어내는 건 §8 위반이다(R-002 사안이며 전제는
Lynch 캡 자체의 근거 확보, v3.24 M-1이 이미 "근거 없음"으로 기록해둔 항목).

같은 이유로 **SBC 하방을 검증하지 못한 25/34 종목은 "미검증일 뿐 안전한 것이 아니다"** —
확보된 9종목 중 1건이 실제로 뒤집혔다.

### 부수 확인

- **DRS를 정의역 전체(0↔100)로 흔들어도 판정이 바뀌는 종목은 7/34**(Gap 변동 중앙
  4.90%p, 최대 6.04%p VRT). v3.44가 관측범위 안에서 낸 결론("취약성은 성장률 축이지
  DRS 축이 아니다")을 더 강한 조건에서 재확인했다.
- **`r ≈ g_terminal` 발산 위험은 현재 실재하지 않는다**(two_stage 23종목 최소 여유
  6.72%p, R-001 최악조합 2%p 잠식 후에도 4.72%p 남음). 다만 그 이유는
  `default_terminal_growth`(2.0~4.5% 클리핑)와 `erp_from_drs`(하한 rf+5%)이며 둘 중
  하나만 완화돼도 즉시 유효해진다.
- **`sbc_to_fcf_pct`는 이름과 달리 비율(0.6=60%)을 담는다.** 엔진 내부는 일관되므로
  계산 오류는 없으나 단위 사고 이력을 감안해 기록해둔다.
- **§2 검증 결과 Stage 2(Growth Duration)는 실행된 적이 없다** — `n`이 34종목 전부
  12로 균일하며 경제적 차등화 근거가 0건이다. R-001은 그 사실을 **전제로** n의
  판정영향만 측정했고 n을 차등화하지 않았다(Stage 2의 몫).

**공식 Gap·판정·등급·Confidence는 하나도 바꾸지 않았다** — 어느 가정이 옳은지 판정할
근거가 없기 때문이다. 취약성은 "틀렸다"가 아니라 "가정에 달렸다"는 뜻이다.
`engine/`·`ledger/` 무수정(`ENGINE_VERSION` v3.53 유지), 34종목 골든재현 8개 지표
완전 동일, baseline fingerprint `fbd34322…` 불변. 테스트 472 → 482개.

## 오픈소스 통합 P0-01~P0-10 — Data Core 계층 신설(2026-08-19, v3.54~v3.56)

사용자가 제시한 "FINAL OPEN-SOURCE INTEGRATION PROMPT"에 따라 이미 조사된
GitHub 오픈소스의 강점을 IRS-native로 흡수했다. 추적표는
`docs/opensource_integration.md`(§18 attribution / §21 완료보고).

**의존성은 여전히 0개다.** 통합 후에도 `engine/` 전체가 stdlib만 쓴다 — 이게
계획서의 ADAPT 판정 두 건을 뒤집은 이유다.

| 단계 | 판정 | 산출물 |
|---|---|---|
| P0-01 Source Registry | REIMPLEMENT | `engine/data/governance/source_registry.py` |
| P0-02 Provider Interface | REIMPLEMENT | `engine/data/providers/base.py` |
| P0-03 SEC Adapter | **ADAPT→REIMPLEMENT** | `engine/data/providers/sec.py` |
| P0-04 DART Adapter | **DEFER** | 근거·재개조건만 기록 |
| P0-05/06/07 Canonical·Normalize·Reconcile | REIMPLEMENT | `engine/data/canonical.py`·`reconcile.py` |
| P0-08 Provenance | **DUPLICATE→연결** | `FinancialFact.to_provenance()` |
| P0-09 Snapshot | REIMPLEMENT | `engine/data/snapshot.py` |
| P0-10 PIT | **DUPLICATE→연결** | `ProviderResult.to_pit_inputs()` |

### 계획서에서 벗어난 판정 3건 — 전부 실측 근거

- **P0-03 ADAPT→REIMPLEMENT**: edgartools `pyproject.toml`을 직접 열어보니
  런타임 의존성 **21개**(pandas·pyarrow·lxml·pydantic 등)인데 IRS는 **0개**이고
  CI는 `pip install pytest` 한 줄이다. ADAPT는 의존성 관리 파일 신설 + CI 변경을
  요구해 §1.14·§1.15에 걸린다. REJECT가 아니라 REIMPLEMENT — XBRL 태그 표준화와
  어댑터 경계 두 설계는 실제로 가져왔다.
- **P0-04 DEFER**: `ledger_krx/` 31건이 **전부 ETF 래퍼**(`pe_by_source`만 있고
  재무제표 없음)라 IRS가 분석한 한국 **기업**은 0건이다. DartLab의 핵심(시장
  중립 인터페이스)은 P0-02에서 이미 흡수했으므로, 한국 기업을 실제로 분석할 때
  `DartProvider`를 추가하면 도메인 코드는 안 바뀐다.
- **P0-08/P0-10 DUPLICATE→연결**: `provenance.py`·`filing_dates.py`가 이미
  있어 새로 만들지 않고 변환·연결만 했다(§1.11).

### ⭐ 가장 중요한 발견 — ledger 손입력값과 SEC 1차자료가 광범위하게 어긋난다

8종목 336개 값 전수 대조(`scripts/reconcile_ledgers_vs_sec_2026_08_19.py`):

| 지표 | 값수 | EXACT | **MATERIAL** | 중앙편차 | 최대편차 |
|---|---|---|---|---|---|
| revenue | 84 | 75 | **6** | 7.33% | 27.2% |
| operating_income | 84 | 63 | **21** | 16.15% | **141.4%** |
| operating_cashflow | 84 | 71 | **12** | 13.58% | 26.4% |
| capex | 84 | 52 | **28** | 14.17% | 71.9% |

건수로는 **capex**, 크기로는 **operating_income**이 최악이다. 둘 다 FCF·DRS를
통해 판정에 직결된다. **분포가 이봉(bimodal)** — 중간 등급이 8건뿐이라 차이가
"측정 잡음"이 아니라 **"정의가 다르다"** 쪽임을 시사한다(임계값은 실행 전에
고정했고 결과에 맞춰 조정하지 않았다).

**판정 영향: SEC 전면대체 시나리오에서 8/8 판정 불변**(Gap 최대 −0.83%p, ROP).
다만 **ROP의 DRS가 출처 선택만으로 31.5→47.5(16점)** 움직인다 — R-001 감사가
DRS 전 정의역 섭동으로 Gap 중앙 4.90%p를 얻었던 것과 대비하면, **데이터 출처
선택이 그 축의 상당 부분을 차지할 수 있다**는 뜻이다. CDNS는 SEC 시계열이
불완전해 측정 불가(미해결로 남김).

⚠️ **어느 값이 옳은지는 확정하지 않았다.** 벤더 영업이익이 틀린 게 아니라
정규화값(일회성 항목 제외)일 수 있다. `reconcile.py`는 물질적 불일치를 **자동
해결하지 않고** `requires_review=True`로 남기며 권위 서열은 제안으로만 준다 —
자동으로 SEC를 택하면 34종목 입력이 조용히 바뀌고 추적이 불가능해진다.
**ledger는 한 파일도 수정하지 않았다.**

### ⭐ 이 저장소 최초의 `PIT_VALID` 분석

v3.47이 PIT 필드를 만들고 v3.49가 조회 수단을 만들었지만 **34종목 중 채운 것이
0건**이었다. BSX를 provider → 스냅샷 → provenance(44건 커버) → PIT(11개년) →
`run_analysis()`로 관통시켜 `PIT_UNKNOWN` → **`PIT_VALID`**(위반 0건)를 처음
확인했다. Gap/판정/DRS/Confidence는 완전 동일(순수 기록 경로).

발견한 결함: `pit_inputs_for()`는 제출일을 **별도로 다시 조회**하므로, 값이
벤더에서 오고 날짜가 SEC에서 오면 **PIT가 쓰지도 않은 값의 날짜를 검사**한다.
`ProviderResult.to_pit_inputs()`는 같은 사실에서 만들어 그 괴리를 원천 차단한다.

⚠️ **`available_at`의 정확한 의미**: BSX FY2016~2018 제출일이 전부 2019-02-19로
나온다 — 세 해 모두 `Revenues` 태그로는 FY2018 10-K에서 처음 등장하기 때문
(ASC 606 전환). 즉 "회계연도가 처음 공시된 날"이 아니라 **"그 수치가 이 태그로
처음 등장한 날"**이다. PIT상 보수적이지만 거짓 양성 가능성이 있다.

### 통합 과정에서 테스트가 잡은 실질 결함 5건

1. `RateLimiter._last=0.0` 초기화가 **첫 요청도 대기**시킴(monotonic에선 우연히 안 걸림)
2. `check_use()`가 **불확실성으로 "아니오"를 "아마도"로 완화** — 판정 순서 역전
3. SEC 태그 "첫 태그에서 멈춤"이 **BSX FY2015 매출을 통째로 버림**(ASC 606 전후)
4. `strict=False`에서 **`None`이 시계열에 섞여** `AnalysisInputs`로 흘러감
5. `fcf0` 축 키 오타로 R-001 감사에서 **사전등록 6축 중 1축이 조용히 비활성**
   (STAGE 3에서 발견, 같은 유형)

### 등록부가 드러낸 것 — 6건 중 4건이 미확인

Alpha Vantage 약관은 실제로 **확인에 실패**했다(공개 약관이 PDF인데 본문 판독
불가). 추측해 채우지 않았다. 그 결과 IRS의 현재 관행(벤더 원자료를 `ledger/*.json`에
담아 공개 저장소에 올리고 그로부터 나온 판정을 공개)이 전부 `PROHIBITED`
(=승인한 적 없음)로 나온다 — **위법이라는 뜻이 아니라 확인한 적이 없다는 뜻이다.**

테스트 482 → **561개**. 34종목 골든재현 8개 지표 완전 동일, baseline fingerprint
`fbd34322…` 불변, **ledger·공식 판정 무수정**.

## 오픈소스 통합 P0-11~P0-18 완료 — 정성 경로에 계약을 세움(2026-08-19, v3.57~v3.58)

P0-01~10이 **데이터 계층**을 세웠다면, P0-11~18은 이 저장소의 **정성 경로**에
처음으로 계약을 붙였다. 상세는 `docs/opensource_integration.md`.

| 단계 | 판정 | 산출물 |
|---|---|---|
| P0-11 Document Index | REIMPLEMENT(범위 축소) | `data/providers/sec_documents.py` |
| P0-12 Evidence Engine | REIMPLEMENT | `engine/evidence.py` |
| P0-13 Hard Gates | REIMPLEMENT | `engine/evaluation/gates.py` |
| P0-14 Research Lenses | **ADAPT(축은 IRS 것)** | `engine/research_lenses.py` |
| P0-15 Valuation | **DUPLICATE** | `expectation_gap_engine.py`가 이미 담당 |
| P0-16 Refresh Boundary | ADAPT | `thesis.py`에 opt-in 추가 |
| P0-17 Historical Replay | **DEFER** | STOP CONDITION 기발동(2026-08-16) |
| P0-18 Evaluation Lab | 대부분 DUPLICATE | 마지막 조각을 P0-12/13이 채움 |

### 이 저장소 정성 경로에는 계약이 **아예 없었다**

`thesis.build_evidence()`의 `source`가 **자유 문자열**이라 §15가 요구하는
Document·Location·Verification·Confidence가 전혀 없었다. 그 결과:
- **TYL SBC 3배 오류**(62% vs 실제 24.4%): 인용에 문서·위치가 없어 **어디서
  왔는지 되짚을 수 없었다**
- S/A등급 13종목 + B/C/D등급 20종목 정성조사: 전부 **채팅 요약으로만 존재**

`Citation`(location **필수**) → `Evidence` → `Claim` → `EvidenceMatrix`로 계약을
세웠고, 강제 3가지를 걸었다: **2차 출처를 `VERIFIED_PRIMARY`로 표시 불가**(TYL
사고의 형태) / **반대 증거가 찬성 증거 수와 무관하게 우선**(사후합리화 방지) /
**삼각검증은 서로 다른 출처 2개 이상**(같은 출처 반복은 IWM P/E 사건이 보여준
대로 편향이 안 드러난다).

### ⭐ 실제 BSX 분석에 하드 게이트를 걸었더니 — `GATE.RECON` 실패

| 게이트 | 결과 |
|---|---|
| FABRICATION / SCALE / DIRECTION / LOOKAHEAD / MATCH | 통과 |
| **RECON** | **실패 — 미해결 출처 충돌 14건** |

calibrated uncertainty도 `acknowledged=False`(시스템이 아는 미확인 3건을 메모가
언급 안 함). **BSX 판정을 무효화한다는 뜻이 아니라, 출처를 대조하지 않은 채
발행됐다는 사실을 이제야 탐지할 수 있게 됐다는 뜻이다.**

게이트는 **실증 사고가 있는 것만** 골랐다(FABRICATION=TYL / SCALE=RAR 100배 /
DIRECTION=capex 부호 / RECON=P0-07의 67건 / LOOKAHEAD=PIT / MATCH=self_check_v2
위임). 원본의 BRIDGE·FREELUNCH·BASIS는 IRS에 해당 사고가 없어 **넣지 않았고 부재를
테스트로 고정했다** — 실증 없이 게이트를 늘리면 통과 의례만 늘어난다.

### P0-14에서 축을 바꾸지 않은 이유

ai-berkshire의 4대 관점(段永平·버핏·멍거·리루) 렌즈를 **가져오지 않았다.** IRS는
33종목에 실제로 적용해 축적한 자체 5축(자본배분·회계품질·거버넌스·희석·경쟁환경)이
있고, 남의 분류로 갈아타면 **그 33종목 관측이 새 축에 매핑되지 않아 축적이
끊긴다.** 가져온 것은 Disqualifier와 Mandatory Inversion 둘뿐이다.

**6차원 별점(★★★★★)은 REJECT** — 정확히 "단일 합성점수"이고 §31 안티기능
등록부 항목이다. 중요도가 다른 축이 같은 무게가 되고 공백이 점수 뒤에 숨는다.

### P0-16 — Mira의 5개 개념 중 4개는 이미 있었다

빠진 것은 **Refresh Boundary 하나뿐**이라 그것만 `thesis.py`에 opt-in으로 얹었다.
근거는 실증이다 — **반증조건 트리거 날짜 5건이 전부 지났는데 12일간 아무도
열어보지 않은** 사건(v3.42가 뒤늦게 발견). 논거가 언제 낡는지 스스로 말하지
않으면 확인이 기억에 의존한다.

⚠️ **`UNBOUNDED`는 `FRESH`가 아니다**(경계 미설정 ≠ 신선함), **조건 발동이
기한보다 우선한다**("아직 기한 전이니 괜찮다"가 그 실패 방식이었다).

### 남은 리스크

1. **`evidence`·`research_lenses`·`thesis/`에 실제 데이터가 0건이다.** 구조만
   만들었고 과거 정성조사 33종목을 **소급 입력하지 않았다** — 소급 작성은
   사후합리화다(`falsification_conditions` 원칙과 동일).
2. **게이트가 어떤 분석 경로에도 자동 배선돼 있지 않다.** 자동 배선하면 34종목
   기존 분석이 전부 막힌다(대부분 GATE.RECON 실패할 것).
3. 5축이 옳다는 근거는 "33종목에 써봤다"뿐이고 **투자 성과와의 관계는 증거 0건**
   이다(`VALIDATION_STATUS`에 명시).

테스트 561 → **641개**. 34종목 골든재현 8개 지표 완전 동일, baseline fingerprint
`fbd34322…` 불변, **ledger·공식 판정 무수정**.

## Repository 01~30 전수 판정 + 연구검증 계층 신설(2026-08-19, v3.59)

Master Execution Prompt에 따라 30개 저장소를 **기능 단위로**(§1) 판정했다.
전문: `docs/repo_integration_ledger_2026-08-19.md`(§23 형식 12개 절),
요약: `docs/opensource_integration.md`.

**신규 구현 1건**(Repo 04 mlfortrading + Repo 17 cpz-quant의 일부 기능) —
`engine/quant/validation.py`. 나머지 29건은 이전 P0 통합에서 완료됐거나
REJECT/DEFER.

### §18 경고대로 기능 존재를 확인했다

cpz-quant(Apache-2.0)를 직접 열어 `CombinatorialPurgedCV`·
`probability_of_backtest_overfitting`(CSCV)·Deflated/Probabilistic Sharpe 게이트·
`WalkForward`가 **실제로 존재함**을 확인했다. 그런데도 의존성으로 채택하지
않았다 — **입력(수익률 시계열)이 IRS에 없다.** 넣으면 쓰이지 않는 의존성이 되고,
억지로 돌리면 "정밀해 보이는 허구"가 된다.

| 기능 | 판정 | 근거 |
|---|---|---|
| look-ahead / PIT | DUPLICATE | `check_lookahead`·`GATE.LOOKAHEAD`·`available_at`이 담당 |
| **multiple testing** | **REIMPLEMENT** | 지금 계산 가능하고 실제 필요 |
| **survivorship bias** | **REIMPLEMENT** | 탈락 기록이 남아 있어 계산 가능 |
| Purged CV·Walk-Forward·PBO·DSR | DEFER | 수익률 시계열 부재 |

### ⭐ 이 저장소가 같은 표본을 몇 번 봤는지 처음으로 셌다

| 항목 | 실측 |
|---|---|
| 같은 34종목 검정 횟수 | **9,702회**(리포트 28 + 실험 9, R-001이 9,675 시나리오) |
| 기대 위양성(α=0.05, 독립가정 상한) | **485건** |
| FWER | 1.0으로 **포화** — 계산 오류가 아니라 지표가 정보를 잃는 지점 |
| 스크리닝 고려 → 탈락 | **83종목 → 75종목** |
| ledger 34 중 스크리닝 경로 | **8종목**(나머지 26은 스크리닝 이전 큐 경로) |
| 생존율 하한(스크리닝 부분집합) | **9.6%** |
| PBO/DSR | `available=False` — 계산 시도조차 안 함 |

⚠️ **"IRS가 틀렸다"는 뜻이 아니다.** IRS의 분석 대부분은 가설검정이 아니라
감사·서술이라 nominal p값이 없고, **보정을 적용할 대상 자체가 없다**
(`correction_applied=False`). 이 리포트의 목적은 그 사실을 드러내는 것이다.

### ⚠️ 테스트가 잡은 결함 2건 — 둘 다 탈락을 과소집계하는 방향

1. **`CANDIDATES`가 항상 0건**이었다. 스크리닝 스크립트는 `CANDIDATES = []`로
   시작해 `.append(Candidate(ticker="META", ...))`로 채우는데, 리터럴 블록만
   파싱한 초판이 후보를 통째로 놓쳤다(83 → 20종목 과소집계).
2. **딕셔너리 키가 `"KR(Kroger)"` 형태**라 괄호를 안 벗기면 매칭이 안 됐다.

고치지 않았으면 생존편향이 실제보다 작아 보였을 것이다.

### 나머지 29건

| 그룹 | 판정 | 사유 |
|---|---|---|
| 01·02·05·06·07·08·09 | 이미 완료 | P0-02~16 (05=TieOutBench 리다이렉트로 확인) |
| 03 qlib | PIT=DUPLICATE, 나머지 DEFER | PIT는 P0-02/10이 담당, 최초 `PIT_VALID` 달성 |
| 11·12·13·14·29 Agent | REJECT/DEFER/DUPLICATE | §31 등록("병목은 조율이 아니라 입력 근거 부재")을 R-001이 재확인 |
| 15·16·23·24 Portfolio | **전부 REJECT** | §31 등록("수익률 시계열 없음") — `price_at_analysis` 9/34 재확인 |
| 18·25·26·27 | DEFER/DUPLICATE | 27은 `thesis.py`+P0-16이 담당 |
| 19 | 부분 DUPLICATE | `Claim.is_triangulated()`가 이미 강제 |
| 20·21·22 Screening | DUPLICATE/DEFER | `screener.py` 380줄이 전체 파이프라인 담당 |
| 10·28·30 | DEFER/DUPLICATE | 10은 STOP CONDITION 기발동 |

⚠️ **REJECT/DEFER가 "그 저장소가 나쁘다"는 뜻이 아니다.** 이유는 전부 IRS 쪽
입력 부재이거나 이미 담당하는 모듈 존재이며, 재개 조건을 각 항목에 명시했다.

**의존성 추가 0개**(stdlib만 — 테스트가 numpy·pandas·scipy 부재를 고정).
테스트 641 → **666개**, 34종목 골든재현 8개 지표 완전 동일, baseline fingerprint
`fbd34322…` 불변, ledger·공식 판정 무수정.

## RQ-002 — SBC 하방 미검증: 매수 포트폴리오 63.46%의 회계 가정을 처음 확인
(2026-08-21, 투자판단 개선 루프 1사이클)

**이번 사이클은 새 기능을 만들지 않았다.** 병목을 찾아 원인을 규명했더니
막고 있던 것이 데이터 하나였고, 그 데이터를 확보하는 수단은 이틀 전
(2026-08-19 P0-03)에 이미 생겨 있었다.

### 병목 — 실제 매수 포트폴리오의 63.46%가 한 번도 확인되지 않았다

`FCF = OCF − capex` 정의는 SBC를 주주 귀속 현금처럼 취급한다. v3.23이
`sbc_by_year` opt-in 필드로 대안 시나리오 병기 수단을 만들었으나 **34종목 중
25종목이 미확보**였고, 그 25종목이 매수리스트 비중의 **63.46%**였다
(GEN 18.00 · PGR 12.00 · TCOM 10.25 · PDD 9.07 · SE 7.39 · BRO 6.75).

증거 3갈래: ①WDAY 선례(SBC 차감 시 판정 실제 flip, 기저율 1/9) ②TYL 사건
(경량검증 62% vs SEC 실제 24.4% — **추정치로 대신할 수 없음이 증명됨**)
③R-001이 이미 25/34를 `FCF_DOWNSIDE_NOT_TESTED`로 등록(높은 안정도가
"안전해서"가 아니라 **그 축이 가정공간에 없어서**일 수 있음).

### 외부 조사를 하지 않았다 — 내부만으로 해결 가능했으므로

원인은 방법론이 아니라 **데이터 가용성 하나**였고, 결정 #40의 재개조건
("SEC 원자료 확보")이 P0-03으로 충족돼 있었다. **새 밸류에이션 로직 0줄 ·
새 엔진 함수 0개 · 새 의존성 0개 · `engine/` 무변경**(`ENGINE_VERSION`
v3.59 유지).

### 결과 — 커버리지 34/34

`scripts/sbc_harvest_2026_08_21.py`가 ledger 저장값(market_cap·r·n·
g_terminal·realistic_growth·fcf0·model_used)을 그대로 읽고 SEC에서 **SBC만**
새로 가져와 `run_analysis()`의 `sbc_cross_check` 블록과 동일 계산을 재현한다.
**자기일치 검증: 기존 9종목 × 6필드, 1e-12 정밀도 불일치 0건**(이것 없이는
신규 25건이 전부 무의미하다).

**판정 flip 3건(신규)**: BSX(+5.87→+4.86%p) · BSY(+5.70→+4.89) ·
PTC(+5.53→+2.08) — 전부 저평가→적정가. **BSX는 스크리너 거짓탈락 사건의
주인공이자 가장 최근 정식분석 종목**(2026-08-13)인데 그 판정이 SBC 가정
하나에 달려 있다.
**등급 변경 5건**: 위 3건(B→C) + KEYS(D→**F**, 트래커 최초) + **TCOM(A→B)**.

**⭐ 자본이 실제로 걸린 결과 — 합계 17.46%**:

| 종목 | 비중 | 등급 → SBC 차감 시 | 결과 |
|---|---|---|---|
| **TCOM** | **10.25%** | A(+7.55%p) → **B(+5.28%p)** | **유니버스 이탈** |
| WDAY | 4.51% | A(+11.42) → C(+1.11) | 유니버스 이탈 |
| TTD | 2.70% | S(+17.01) → B(+5.51) | 유니버스 이탈 |
| BRO | 6.75% | A(+7.43) → A(**+7.03**) | 경계까지 **+0.03%p** |

**WDAY·TTD 데이터는 2026-08-01부터 ledger에 있었다.** 그런데
`build_buylist`가 `sbc_cross_check`를 **한 번도 읽지 않아** 자본배분 경로에
드러난 적이 없었다 — 데이터가 있어도 결정경로에 배선되지 않으면 없는 것과
같다는, 이 프로젝트가 네 번 겪은 패턴의 다섯 번째 사례다. 이번에 매수리스트
경계검토에 배선했다(기존 `model_dependent_universe`와 동일 패턴, **비중 불변**).

### ⭐ TCOM은 서로 독립인 두 축에서 동시에 이탈한다

`realistic_growth`(R-001 유니버스안정 **57%**) + SBC 차감(A→B). TTD가 세
독립 경로에서 지목됐을 때 실제로 서사가 무너져 있었다 — **TCOM(10.25%)이
S/A 유니버스 최우선 재검토 대상**이다. BRO는 이미 결정 #41(모델선택 사유
복원 불가)로도 플래그돼 있고 SBC 여유가 0.03%p다.

### 구조적 성질 — 이 검증은 거짓편입 필터다

SBC>0이면 fcf0가 줄어 Implied Growth가 오르므로 **Gap은 반드시 감소한다**
(34종목 실측 위반 0건, 테스트로 고정). 유니버스 **이탈만** 만들 수 있고
진입은 만들 수 없다 — 최우선 판단기준의 *"나쁜 기업을 좋은 기업으로 판단하는
오류 감소"*에 정확히 대응한다.

### ⚠️ 자체 정정 2건 (숨기지 않는다)

1. **실행 중 정렬 키를 바꿨다.** 초판은 `OCF−capex`로 회계연도를 맞췄고
   3종목(WCN·MCK·PDD)이 실패했는데, 파보니 **셋 다 OCF는 상대오차 0.0으로
   정확히 일치**하고 capex만 어긋났다(엔진 자신이 WCN capex에 `[태그 혼재]`
   경고를 낸다). 연도가 틀린 게 아니라 capex **정의**가 달랐다. 허용오차
   0.5%는 그대로 두고 키만 모호성 없는 항목으로 바꿨으므로 결과를 본 뒤
   임계값을 완화한 것이 아니며, 이 수정으로 풀린 두 종목(MCK 4.57%·WCN
   6.51%)은 **판정을 하나도 바꾸지 않는다**.
2. **내 아티팩트 가설이 부분 기각됐다.** PTC의 FCF CAGR +13.41%p 급등을
   "순수 근사-0 아티팩트"로 잠정 판단했으나 엄격기준(기준연도 FCF < 최종연도
   10%)으로 재보니 **11.5%로 기준 위**였다. PTC 기준연도 FCF의 **46%가 SBC**
   라는 건 아티팩트가 아니라 실제 경제적 사실이다.

### 알려진 한계 — 성장경로 비대칭(DEFER)

현행 교차검증은 SBC를 **수준(fcf0)에만** 적용하고 **성장경로(FCF CAGR)에는
적용하지 않는다.** 이번에 크기를 처음 쟀다: FCF CAGR이 GEN −1.07%p ~
**PTC +13.41%p**로 움직이며 **대부분 상방**(= 현행이 SBC 순영향을 **과대**
평가할 수 있음). 따라서 SBC 차감 Gap은 정밀한 값이 아니라 **방향 신호**로
읽을 것. 그렇다고 확장이 정답은 아니다 — 기준연도 FCF에서 SBC를 빼면
근사-0 기준연도 문제(결정 #15)가 재발한다. **DEFER, 크기만 기록.**

### 부수 소견 — MCK capex가 SEC보다 71% 높다 (P0-07 연장)

ledger 745M vs SEC 436M. OCF는 정확히 일치하므로 연도가 아니라 정의 차이다
(원인 **미확인**). fcf0를 309M(5.7%) 낮춰 Gap에 직접 흘러드는데 **MCK는
Gap −0.02%p로 판정 경계 한가운데**라 이 5.7%가 판정을 바꿀 수 있는 위치다.
어느 값이 옳은지는 확정하지 않는다(P0-07 `requires_review=True` 원칙).

### 부수 수정 — 기준선 검증이 스스로를 오염시키고 있었다

`scripts/freeze_baseline_2026_08_16.py`에 **검증 경로가 CLI로 없어서**,
fingerprint를 확인하려면 재동결 함수를 부를 수밖에 없었고 그때마다
`engine_version_at_freeze`가 오늘 버전으로 조용히 재스탬프됐다 — 이번에 실제로
**v3.58 → v3.59로 바뀌는 것을 확인**했다. 이름이 '동결 시점'인 필드가 거짓이
되는, v3.32가 잡은 버전 스탬프 문제와 같은 유형이다. 기본 동작을 **검증(비파괴)**
으로 바꾸고 재동결은 `--freeze`를 명시하게 했으며, 재동결해도 동결 시점 버전은
보존한다(테스트 2건으로 고정).

### 회귀검증

테스트 666 → **677 통과**(신규 11) · **34종목 골든 재현 8지표 불일치 0건** ·
baseline fingerprint `fbd34322…` **불변** · **ledger 0건 수정** ·
매수리스트 기존 필드 **0건 변경**(비중 합계 1.0000000000 유지) ·
`engine/` 무변경.

**다음 병목**: TCOM(10.25%) 재검토 — 두 독립 축에서 동시에 이탈하는 유일한
종목이며, 정성조사(2026-08-04)의 "회사 자체 Q2 가이던스 +3~8%가 Realistic
Growth 12.39%를 크게 하회"와 성장률채점표(v3.43)의 "저평가→적정가"에 이어
**세 번째 축**이 붙었다.

## PHASE 1 — 신호 독립성 적대적 검증: 어제 결론을 하루 만에 반증
(2026-08-21, §6 Evidence Dependency Rule을 내 자신의 결론에 적용)

**RQ-002가 바로 전에 쓴 "TCOM은 서로 독립인 두 축에서 동시에 이탈한다"는
결론이 틀렸다.** SBC 축은 독립 증거가 아니라 **적용 비대칭의 산물**이었다.

| TCOM 시나리오 | RG | Gap | 등급 |
|---|---|---|---|
| base (공식) | 12.39% | +7.55%p | A |
| SBC를 **fcf0에만** (= RQ-002가 보고한 것) | 12.39% | **+5.28%p** | **B — 이탈** |
| SBC를 **fcf0+성장경로 둘 다** (일관 적용) | 14.60% | **+7.50%p** | **A — 이탈 안 함** |
| R-001 RG축 low (자체 CAGR 최소) | 9.29% | +4.45%p | C — 이탈 |
| growth_scorecard 가이던스 5.5% | 5.50% | +0.66%p | C — 이탈 |

일관 적용 시 base 대비 **−0.06%p**만 움직인다. SBC의 경제적 순효과는 사실상
0이었다.

### Mechanism — 두 효과가 반대 방향이다

TCOM의 RG는 **FCF CAGR 경로**로 결정된다(매출 가중평균 29.76%보다 낮은
13.04% 채택). SEC 원자료로 기준연도(FY2019 SBC 1,714M)·종료연도(FY2025 SBC
2,270M) 양쪽에서 빼면 FCF CAGR 13.04%→**15.37%**, RG 12.39%→**14.60%**.

- fcf0 ↓ → Implied Growth ↑ → Gap ↓
- FCF CAGR ↑ → RG ↑ → Gap ↑

### ⭐ 구조적 규칙 — 왜곡 가능 구간이 정확히 특정된다 (§21 LEVEL 3)

| 구간 | 종목수 | 부분 vs 일관 | 매수 유니버스 |
|---|---|---|---|
| **A. `fcf_cagr` driver + 캡 미바인딩** | **7** | **다를 수 있음** | ACGL 12.00% · TCOM 10.25% · TTD 2.70% |
| B. `revenue_weighted` driver | 20 | 항상 동일(RG 변화 0) | PGR·SE·UBER·WDAY |
| C. Lynch 캡 바인딩 | 7 | 항상 동일(RG 고정) | GEN·PDD·BRO·DUOL·MNDY |

B·C 27종목은 `min()`/캡이 RG를 고정해 두 Gap이 **1e-12 정밀도로 정확히 같다**
(테스트 고정). A구간에서 판정을 흔든 것은 2종목뿐이고 **방향이 반대**다:

- **TCOM** ΔRG **+2.22%p** → +5.28(B)→+7.50(A) — **CANCELLED**
- **TTD** ΔRG **−10.75%p** → +5.51(B)→**−5.24(D)** — **SURVIVES, 오히려 증폭**

TTD는 SBC/FCF 61.7%(트래커 최고권)라 종료연도가 크게 깎여 CAGR이 무너진다.

### TCOM의 독립 증거는 1개다 (§6)

R-001 RG축과 growth_scorecard 가이던스축은 **같은 파라미터·같은 mechanism,
다른 데이터** → §6 분류 **B(동일 underlying driver)**. 서로를 강화하는 확증이지
독립 증거 2개가 아니다. SBC축은 같은 원자료가 RG에도 흘러들어 일관 적용 시 소멸.

**→ TCOM 재검토는 여전히 필요하나 근거는 성장률 하나다. SBC를 근거로 들면
안 된다** — 잘못된 근거로 10.25% 자본을 움직일 뻔했다. 그 1개 축은 서로 다른
두 데이터(자체 CAGR 하한 9.29%, 회사 가이던스 5.5%)가 뒷받침해 여전히 강하다.

### 배선 — CANCELLED와 SURVIVES를 자본배분 경로에서 구분한다

`build_buylist`의 SBC 경계검토가 `sbc_signal_verdict`를 함께 출력한다
(비중 불변, 기존 필드 0건 변경). CANCELLED면 "SBC를 이 종목의 재검토 근거로
삼지 말 것"을 명시하고, UNKNOWN이면 "'무해'가 아니라 '미확인'"으로 남긴다.

### ⚠️ 일관 적용을 권하지 않는 새 실증 근거 (결정 #45 DEFER 유지·보강)

SBC 차감 시 **기준연도 FCF가 음수가 되는 종목 5건**(DUOL −2.7M · GWRE −14.4M ·
MNDY −70.9M · SE −70.7M · UBER −1,403M)을 확인했다. WDAY는 계산은 되지만
SBC 차감 FCF CAGR이 **208.92%** — 결정 #15가 등록한 근사-0 기준연도 아티팩트의
교과서적 실례다(다행히 `revenue_weighted` driver라 결과 무영향).
**34종목 중 최소 6건에서 정의 자체가 무너진다.** 현행 부분 적용이 "옳아서"가
아니라 대안이 더 나쁘다는 증거가 늘었다.

### 왜 어제는 못 봤나

RQ-002는 성장경로 비대칭의 **크기를 측정하고 DEFER까지 했으면서**, 그 비대칭이
**자기가 방금 보고한 이탈 신호 자체를 무효화할 수 있다**는 점을 연결하지 못했다.
측정과 함의를 따로 다룬 것이 원인이다. RQ-002 문서는 덮어쓰지 않고 그대로 둔다
(§23) — PHASE 1 리포트가 그 개정본이다.

### 회귀검증

테스트 677 → **686 통과**(신규 9) · 34종목 골든재현 8지표 **불일치 0건** ·
fingerprint `fbd34322…` **불변** · ledger **0건 수정** · 매수리스트 기존 필드
**0건 변경**(비중 합계 1.0000000000) · `engine/` 무변경(v3.59 유지).

## PHASE 2 — 미검증 가정 자본 노출 감사: 노력과 자본 영향이 역비례한다
(2026-08-21)

자본 배분 경로 4구간 중 **마지막(유니버스→비중)이 한 번도 감사된 적이 없었다.**
`build_buylist`의 상수 8개가 비중 100%를 결정하는데 전부 미검증이다.

절제 실측(turnover = Σ|Δw|/2 — 그 가정 하나를 바꿨을 때 옮겨야 하는 자본):

| 미검증 가정 | turnover | 최대 이동 | 등급 |
|---|---|---|---|
| **버킷 분산 강제**(매핑+목표) | **18.82%** | GEN −12.86%p | **CRITICAL** |
| **버킷 목표 40/30/20/10** | **16.75%** | TCOM +12.25%p | **CRITICAL** |
| 버킷 달성률 바닥 90% | 6.00% | GEN −6.00%p | HIGH |
| 종목당 상한 12% | 4.96% | BRO −3.71%p | HIGH |
| `quality_score`의 Confidence 축 | 2.39% | TTD +1.65%p | MEDIUM |
| **정성 심층조사 반영**(`CONFIDENCE_ADJ`) | **2.33%** | TTD +1.78%p | MEDIUM |
| SEVERE/THESIS_BROKEN ×0.85 | 0.94% | TTD +0.94%p | LOW |

### ⭐ 노력과 자본 영향이 완전히 역비례한다

이 프로젝트가 **가장 많은 노력을 들인 축**(정성 심층조사 S/A 13종목 + B/C/D
20종목, 5축)의 결과물 `CONFIDENCE_ADJ`가 자본에 미치는 영향은 **2.33%**다.
**아무 근거 없이 정한 버킷 목표 40/30/20/10은 16.75%** — **8배 차이**.

개별 종목은 근거 없는 상수 하나로 **4배 이상** 변한다:

| 종목 | 최소 | 공식 | 최대 |
|---|---|---|---|
| GEN | **5.14%** | **18.00%** | 22.50% |
| TCOM | **4.28%** | **10.25%** | 22.50% |

**GEN 18.00% 중 12.86%p가 quality 기여가 아니라 버킷 규칙의 산물이다**
(최대 보유 · Confidence 70 최하위권 · 캡바인딩). v3.30이 "이 집중이 의도한
것인지 매번 확인해야 한다"고 경고한 것의 크기가 처음 정량화됐다.
**TCOM은 `travel` 버킷 유일 종목**이라 명목목표 10%가 곧 비중이다 — 10.25%는
밸류에이션 판단이 아니라 "여행 섹터에 10% 배분"이라는 근거 없는 결정에 가깝다.
PHASE 1이 TCOM 편입 여부를 파고들었지만 같은 자본이 이 숫자에 더 크게 좌우된다.

### ⚠️ 감사 도구가 조용히 무력화된 **세 번째** 사례

초판에서 `bucket_floor_removed` turnover가 **0.00%**로 나왔는데 v3.30 기록
("GEN 12%→18%")과 정면 모순이었다. 원인은
`effective_bucket_targets(min_achievement=MIN_BUCKET_TARGET_ACHIEVEMENT)`의
기본 인자가 **함수 정의 시점**에 0.90으로 바인딩돼 모듈 상수를 바꿔도 반영되지
않은 것 — **절제 미적용을 "이 가정은 무해하다"로 읽을 뻔했다.** 함수를 래핑해
고치니 turnover 6.00% · GEN 정확히 −6.00%p로 기록과 일치했다.

R-001 `fcf0` 축 키 오타 → PHASE 1(데이터 없어 못 흔든 축을 "안 뒤집힘"으로 오독)
→ PHASE 2(기본 인자 바인딩). **감사 결과가 "영향 없음"이면 먼저 도구를 의심할 것.**
회귀 테스트로 고정했다.

### 두 축은 서로 다른 질문에 답한다

앞 구간(Gap·등급·유니버스)은 **"이 종목이 편입되는가"**, 뒤 구간(사이징)은
**"얼마나 담는가"**. ⚠️ 두 노출은 **단순 합산 불가**(단위가 다르고 사이징 절제는
유니버스를 바꾸지 않는다 — 테스트 고정).

### 결정

버킷 목표비중·매핑은 **바꾸지 않는다** — 근거 없는 숫자를 근거 없는 다른 숫자로
바꾸는 건 개선이 아니다(LYNCH_TYPE_CAPS·P/B 임계값과 동일 원칙). 상관행렬
최적화는 §31 안티기능 등록(수익률 시계열 부재). **CRITICAL 노출은 미해소로
유지되며 그 사실이 이제 기록·측정된다.**

### 회귀검증

테스트 686 → **693 통과**(신규 7) · 34종목 골든재현 8지표 **불일치 0건** ·
fingerprint `fbd34322…` **불변** · ledger·공식 매수리스트 **0건 수정**(감사
실행 후 원복, 테스트 고정) · `engine/` 무변경(v3.59).

## PHASE 3 — MCK capex 불일치: ledger가 옳고 provider가 좁았다 (2026-08-21, v3.60)

RQ-002가 **원인 미확인**으로 남긴 "MCK ledger capex 745M vs SEC 436M(+71%)"을
규명했다. **ledger가 옳다.**

MCK 8개년 전수: `ledger == PaymentsToAcquirePropertyPlantAndEquipment +
PaymentsToAcquireSoftware` **8/8 완전 일치**. MCK는 그 합을
`PaymentsToAcquireProductiveAssets`(넓은 정의)로 직접 태깅하기도 한다. 반면
`METRIC_TAGS["capex"]`는 좁은 정의를 1순위로 둬서, 두 태그를 **모두** 보고하는
회사에서 소프트웨어 자본화가 통째로 빠졌다.

**넓은 태그 값이 있는 연도는 ledger와 39/39 일치**(ACGL 11·MCK 7·WCN 9·WM 12).

### 코드 변경 3건 (§17 게이트 통과)

1. **우선순위 역전** — `PaymentsToAcquireProductiveAssets`를 1순위로.
   실측: **34종목 중 3종목만 변경, 전부 ledger 일치 개선**
   (MCK 0→**7/8** · WCN 7→**11/12** · WM 5→**12/12**). 넓은 태그가 없는 31종목은
   폴백으로 **동작 불변**.
2. **`[capex 정의 공존]` 경고** — 넓은 쪽을 채택하되 조용히 해결하지 않는다.
3. **소프트웨어 자동 합산은 거부** — MCK FY2026은 넓은 태그가 아직 없어 436M이
   채택되지만 회사 보고 총액은 745M이다. **자동으로 더하지 않는다**(회사에 따라
   유형자산 취득에 이미 포함됐을 수 있어 이중계상 위험 + 관측 1종목뿐, §21 LEVEL 1).
   `[capex 소프트웨어 별도 보고]` 경고로만 드러낸다.

### ⚠️ P0-07 reconcile 해석이 뒤집힌다

P0-07이 "capex 28/84 mismatches, 최대 71.9%"를 material divergence로 보고했는데
**그 상당수가 provider 결함이지 ledger 오류가 아니었다.** "SEC가 1차 자료이니
ledger를 고치자"고 판단했다면 정반대로 갔을 것이다.

### ⚠️ 자체 정정 — 초판 가설을 실측으로 기각

"MCK ledger 시계열 내부에서 capex 정의가 바뀐다"고 잠정 판단했다가 기각했다.
실제 ledger는 8/8 일관됐고, 내가 비교표에 옮겨 적은 값(390/431/537)이 실은 PPE
단독 값이었다(실제 558/687/859). 가설이 맞았다면 훨씬 심각한 결함이었으므로
검증 없이 보고했다면 **존재하지 않는 문제를 만들 뻔했다.**

### 미해결

- **WCN FY2025 1.28% 차이** — 원인 미확인, C등급이라 자본 영향 없음.
- **나머지 10종목 capex 불일치**(GWRE 71.9% 등) — 넓은/소프트웨어 태그가 둘 다 없고
  ledger가 SEC 어느 태그와도 안 맞는다. **원인이 다르며 범위 밖.**

### 회귀검증

테스트 693 → **697 통과**(신규 4) · 34종목 골든재현 8지표 **불일치 0건** ·
fingerprint `fbd34322…` **불변** · ledger·매수리스트 **0건 수정** ·
`ENGINE_VERSION` v3.59 → **v3.60**.

## PHASE 4 — RG 정보 공백: 두 blocker가 풀렸지만 둘 다 채택 못 했다 (2026-08-21)

§12 후보 9개 중 8개는 RQ-001(결정 #19~#34)이 이미 판정했다. 새로 물은 것은
**BLOCKED 사유가 아직 유효한가** 하나다.

### ROIC/ROIIC — 데이터는 풀렸는데 blocker가 **정의**로 이동했다

SEC 실측: equity 34/34 · 유효세율 34/34 · ROIC 22~33/34(#31의 **2/34**에서 해소).
그런데 **투하자본 정의 두 가지의 ROIC 비율이 중앙값 1.50 · 범위 0.60~206.72**다.
자기자본이 근사-0/음수인 유통업에서 분모가 폭발한다 — **MCK 1347.77%**(자기자본+
부채−현금) vs 6.52%(총자산−현금), COR 38.87% vs 2.53%. ROIIC는 실질 **9/34**.

§13 조건 3·7·8 정면 위반 → **core도 진단축도 REJECT.** 결정 #32가 proxy ROIIC를
"업종간 비교 불가"로 REJECT한 이유가 **exact 정의에서도 재현**됐다.
단 ROIC는 기존 지표와 독립적이다(RG −0.371·Gap −0.361·DRS +0.009) — **정보량은
있으나 측정 품질이 막는다.** 재개조건을 "데이터 확보"에서 **"자기자본 근사-0/음수
기업을 다루는 정의 확립"**으로 정정했다.

### ⚠️ 희석 드래그 — 틀린 결과를 배선했다가 철회했다 (이번 사이클 최대 실수)

`WeightedAverageNumberOfDilutedSharesOutstanding` **34/34** 확보로 결정 #26의
공백을 잴 수 있게 됐다. 초판 결과: **TTD 총 +19.17% → 주당 −24.92%**,
**TCOM 총 +13.04% → 주당 −21.18%**, 매수 유니버스 **46.74%**가 −5%p 초과.
"총 FCF는 성장했는데 주당은 감소"라는 극적인 결론을 얻고 **매수리스트 진단축에
배선까지 했다.**

**검증에서 전부 무너졌다** — 주식수 시계열을 연도별로 보니:
TTD FY2021 **×10.2**(10:1 주식분할) · TCOM **×8.4**(ADS 비율변경) ·
DUOL/MNDY/UBER(IPO 직후) · PDD/BRO(ADS·보통주 단위 혼재).
**주식분할·IPO는 희석이 아니다.** 가장 극적이던 4종목이 전부 오염이었다.

즉시 배선을 철회하고(원복 동일성 확인), 오염 검출을 **사전 정의된 도메인 제약**
으로 넣었다(인접 연도 1.5배 — 정상적인 연간 희석이 50%를 넘는 건 사실상 불가능,
결과를 보고 고른 값이 아니다). 정정 후 신호는 46.74% → **12.16%**, 매수 유니버스
커버리지 **6/12**(측정 불가 비중 41.33%)로 **최종 배선하지 않았다.**

**왜 놓쳤나**: 커버리지와 상관만 확인하고 **원자료 시계열을 눈으로 보지 않았다.**
PHASE 3에서 "검증 없이 보고했다면 존재하지 않는 문제를 만들 뻔했다"고 적고도
같은 실수를 했다 — 그때는 보고 전에 잡았고 **이번엔 배선 후에** 잡았다.
테스트로 고정했다.

### 희석 드래그는 SBC와 중복이 아니다

순위상관 −0.597. `sbc_to_fcf_pct`는 보상비용의 **크기**, 희석 드래그는 주주 지분의
**순변화**(발행−자사주매입). 반례: VRT SBC 2.4%인데 −7.66%p / WDAY SBC 58.6%인데
−2.98%p. BRO 정성조사의 "M&A 주식대가 다일루션(SBC와 무관)"이 이 채널이다.

### 부수 확인

`cagr_5y_base_year`가 없는 **v3.19판 ledger 9종목**이 두 조사 모두에서 계산
불가였다 — v3.32가 기록한 스키마 비균일이 횡단면 연구를 세 번째로 막았다.

### 회귀검증

테스트 697 → **705 통과**(신규 8) · 34종목 골든재현 8지표 **불일치 0건** ·
fingerprint `fbd34322…` **불변** · ledger·매수리스트·boundary review **0건 수정**
(배선 철회 후 원복 확인) · `engine/` 무변경(v3.60).

## PHASE 5 — Historical Replay readiness: NOT READY, 그리고 시간이 해결 못 하는
축이 하나 있다 (2026-08-21, v3.61)

§14가 **"Historical Replay를 구현하지 마라, readiness만 판단하라"**고 명시해
그대로 따랐다. 2026-08-16에 이미 §66 STOP CONDITION이 발동돼 있었고 그때 사유는
**시간** 하나(전체 이력 22일)였다. 이번엔 그 판단이 시간이 지나면 자동으로
해소되는지를 물었다 — **아니다.**

**10축 판정(전체 NOT READY, 가장 약한 축이 결정 — 평균내지 않는다)**:

| 축 | 판정 | 측정 | 시간이 해결? |
|---|---|---|---|
| A1 과거 공시 확보 | READY | 32/34 · 없음 0 | — |
| A2 분석 시점 입력값 | **NOT_READY** | `point_in_time` **0/34** | 아니오 |
| A3 원본 입력 보존 | PARTIAL | 필수 8종 34/34 · `cagr_5y_base_year` 부재 9 | 아니오 |
| A4 시장 데이터 | **NOT_READY** | `price_at_analysis` **9/34** · 가격 시계열 계층 없음 | 아니오 |
| A5 논거 시점 | **NOT_READY** | `thesis/` **0건** | 아니오 |
| A6 예측 상태 | PARTIAL | 봉인 34 · 해소 **0** | **예** |
| A7 밸류에이션 입력 | READY | 34/34 | — |
| A8 출처 가용성 | **NOT_READY** | 구조화 provenance **0/34** | 아니오 |
| A9 재작성 정보 | **NOT_READY** | 아래 | **아니오, 영구** |
| A10 정보집합 재현성 | **NOT_READY** | 분석일 범위 **19일**(07-25~08-13) | **예** |

### ⭐ 핵심 발견 — ledger는 최신 판본이다. 원자료 스냅샷 없이는 영구히 안 풀린다.

재작성 자체는 흔하지 않다 — 복수 공시된 기간 1,566건 중 **133건(8.5%)**
(1% 초과 101 / 5% 초과 55 / 20% 초과 23). 그런데 **ledger 입력값이 어느 판본인지**를
재보니 결론이 뒤집혔다:

| 재작성 기간에 걸친 ledger 입력값 | **148건** |
|---|---|
| **최신 판본**과만 일치 | **101** |
| 최초 판본과만 일치 | **5** |
| 둘 다 일치(변화 미미) / 어느 쪽도 아님 | 38 / 4 |

**101 vs 5.** ⚠️ **이것이 "기존 34종목 분석이 틀렸다"는 뜻은 아니다** — 전부
2026년 분석이고 2026년 시점 최신 판본을 쓰는 것은 옳다. 문제는 **오직 하나의
용도**에서 발생한다: 이 ledger를 더 이른 T0의 스냅샷으로 재사용하는 순간
값 자체가 미래 정보가 되며, `analysis_as_of`/`filing_dates_by_year`를 붙여
`PIT_VALID`를 받아도 **look-ahead는 해소되지 않는다.**

**함의**: A10(19일 이력)·A6은 기다리면 풀리고 A2·A4·A5·A8은 앞으로 채우면
풀리지만, **A9는 기존 34종목에 대해 소급 불가다.** 따라서 §7 Historical Replay는
**기존 34종목이 아니라 앞으로의 분석에서만** 시작될 수 있다.

### 부수 발견 — 회계연도 라벨 충돌이 provider 출력을 한 해씩 밀고 있었다

A9를 재느라 기간을 `(start, end)` 쌍으로 식별하다 발견했다. `int(end[:4])` 규칙은
52/53주 회계연도 회사에서 무너진다 — 회계연도가 1월 초에 끝나면 다음 해 라벨을 받고,
두 기간이 같은 라벨로 충돌하면 `min(filed)` 규칙상 **이른 회계연도가 이기고 늦은
쪽이 조용히 사라진다.** CDNS 실측(provider vs ledger): FY2015 7.1% · FY2021 10.2% ·
FY2022 **16.1%** 불일치, FY2014·FY2020은 통째로 빔. 34종목 중 CDNS·GEN 2종목.

**자동 재라벨링은 하지 않았다** — 규약이 회사마다 반대다(CDNS는 1월 초 종료를
**전년**으로, GEN은 3월 말 종료를 **당해**로 센다). 관측 2종목뿐이라 §21 LEVEL 1이며,
여기서 규칙을 만들면 근거 없는 휴리스틱을 근거 없는 다른 것으로 바꾸는 것이다.
`[회계연도 라벨 충돌]` 경고만 배선했다(실측: CDNS 1건 발동 · GEN 0 · BSX 0).
**이 결함이 지금까지 무해했던 것은 우연이다** — ledger 34종목은 전부 손입력이고
provider는 교차검증(P0-07)에만 쓰였다. provider를 입력 경로로 승격하는 순간
CDNS·GEN은 조용히 한 해 밀린 값을 받는다.

### ⚠️ 자체 정정 2건

**① 재작성 빈도 11.8% → 8.5%**: 같은 세션 ad-hoc 측정이 "1,144건 중 135건
(11.8%)"이었는데, 스크립트로 재현하니 **어떤 태그·form 조합으로도 분모 1,144가
나오지 않는다**(10-K만 1,459 / 10-K+10-K/A+20-F 1,566). ad-hoc 값은 재현 불가라
폐기하고 **8.5%(133/1,566)**를 채택한다. 교훈: 스크립트로 고정하지 않은 수치는
보고하지 말 것(v3.32 중복 ledger 집계 오염과 같은 계열).

**② 감사 도구가 조용히 잘못 작동한 네 번째 사례**: 초판 스크립트가
`cagr_5y_base_year`를 `growth.breakdown`에서 찾았으나 실제 위치는 `derived`다.
그대로였다면 **"34종목 전부 성장경로 재구성 불가"라는 실재하지 않는 차단조건**을
보고할 뻔했다(실제는 v3.19판 9종목). R-001 `fcf0` 키 오타 → PHASE 1 → PHASE 2
기본 인자 바인딩 → PHASE 5 필드 경로. **측정 결과가 극단적(0건 또는 전건)이면
결론을 내기 전에 도구를 먼저 의심할 것.**

### 다음 사이클에 가장 값이 큰 단일 행동

**새 분석에서 `pit_inputs_for()` + `provenance_from_sec_facts()` + `price_at_analysis`를
채우는 것.** `price_at_analysis`가 v3.24에 만들어지고 12일 뒤 thesis_monitor에서
"24종목은 부식 계산 불가"라는 비용으로 드러난 일이 A2·A8에서 그대로 반복되고 있으며,
이번엔 그 비용이 **"Historical Replay를 영영 시작할 수 없다"**로 나타난다.

**검증**: 테스트 708 → **717 통과**(신규 9 + provider 3) · 34종목 골든재현 8지표
**불일치 0건** · fingerprint `fbd34322…` **불변** · ledger·매수리스트·공식 판정
**0건 수정** · `ENGINE_VERSION` v3.60 → **v3.61**(sec.py 경고 추가).

## v3.62 — 자동 아침 스크리닝 인프라: GitHub Actions 기반 무인 1차 스크리닝
(2026-08-22/23, 사용자 제약 - 평일 오전 폰 접근 불가, Claude 세션 예약실행 버그로
차단됨)

Claude 세션 내 예약실행(Routine) 기능이 이 환경에서 승인 UI 자체가 뜨지 않는
버그로 막혀(재시도해도 동일 실패) GitHub Actions로 전면 대체했다 - 클로드 세션
상태와 완전히 무관하게 GitHub 서버에서 평일 09:00 KST에 자동 실행된다.

**파이프라인**: Finviz(robots.txt 화이트리스트 프리셋만, 커스텀 필터 `f=` 금지) →
정크 필터 → SEC XBRL(`engine/data/providers/sec.py` 재사용, 신규 계산 로직 0줄) →
Alpha Vantage 시가총액(하루 20회 예산, 무료한도 25회 중 5회는 저녁 대화용으로
남김) → `engine/screener.py::screen_all()` 그대로 호출 → GitHub Issue에 결과
게시. Notion을 시도했으나 토큰 401이 반복돼(발급 절차 자체가 병목) 추가 시크릿이
전혀 필요 없는 GitHub Issue로 교체했다 - `secrets.GITHUB_TOKEN`은 워크플로가
자동으로 받는다.

**실전에서 잡은 버그**: Finviz가 `quote.ashx?t=`에서 `stock?t=`로 링크 구조를
바꿔 1차 시도가 후보 0건을 냈다 - `workflow_dispatch`로 실제 실행한 뒤
`download_workflow_run_artifact`로 받은 HTML을 직접 대조해 앵커 텍스트==href
티커 조건으로 재작성, 10/10 재현 확인 후 반영했다.

`engine/data/governance/source_registry.py`에 `"finviz"` 항목 신설
(`tier=UNVERIFIED`, `allowed_use=("internal_research",)`, robots.txt 화이트/
블랙리스트 조사 결과를 `reliability`에 명시). 서사확인(2단계) 자동화는 검토 후
불필요하다고 판단해 만들지 않았다 - 깔때기가 이미 하루 0~2종목으로 좁혀져
수동 확인으로 충분하다.

`ENGINE_VERSION` v3.61 → v3.62. 회사 엔진 계산 로직 무변경(신규 데이터 출처
등록만) - 34종목 골든재현 불변.

## v3.63 — 스크리닝·밸류에이션 기준 외부검증: 저명 학술문헌·오픈소스 대조
(2026-08-23, 사용자 요청 "저명한 학술지와 논문, 기업분석 스크리닝 오픈소스
등에서 기준을 자료조사하고 분석 후 다시 반영")

`engine/screener.py`(MIN_REALISTIC_GROWTH=8%/MAX_IMPLIED_GROWTH=5.5%/tier 경계)와
`engine/expectation_gap_engine.py`(`default_terminal_growth` ceiling=4.5%)가
도입된 이래 외부 학술문헌·오픈소스 스크리닝 프로젝트와 대조된 적이 한 번도
없었다. 전문은 `reports/research/screening_criteria_external_2026-08-23.md`.

**Finding 1 (ADOPT, 라벨만 승격)** - `implied_growth`가 쓰는 reverse-DCF 비교
아키텍처(시장가에서 성장률을 역산해 독립 추정치와 대조) 자체는 **Gebhardt/
Lee/Swaminathan(2001, Journal of Accounting Research, 피어리뷰 2000+인용)**의
"내재자본비용" 역산 방법론과 수학적으로 동형이고, Rappaport&Mauboussin의
"Expectations Investing"·Damodaran(NYU Stern)의 강의체계와 독립적으로
수렴한다. `VALIDATION_STATUS["implied_growth"]`를 `ECONOMICALLY_SUPPORTED`로
승격했다 - **단, 이 승격은 아키텍처에 대한 것이지 IRS가 고른 특정 숫자(5.5%
등)를 정당화하지 않는다**(v3.52가 structural_discount_rate에서 지킨 경계와
동일, 테스트로 고정).

**Finding 2 (REJECT, 무변경)** - `MIN_REALISTIC_GROWTH`(8%)의 절대 하한 설계
자체에 학계 반례가 있다: Greenblatt Magic Formula는 성장 입력이 아예 없고,
Chan/Karceski/Lakonishok(2003 JF, structural_discount_rate 근거로 이미 인용된
문헌)은 "장기 이익성장에 우연 이상의 지속성이 없다"고 본다. 8%를 없앨 근거는
아니다(이 스크리너는 예측력이 아니라 34~74종목 corpus 상관을 근거로 쓴다) -
다만 이 긴장관계를 처음으로 정직하게 기록했다. `MIN_REALISTIC_GROWTH`/
`MAX_IMPLIED_GROWTH`/tier 경계 어느 수치도 바꾸지 않았다 - 조사한 오픈소스
스크리닝 프로젝트(hjones20/fundamental-analysis, FinanceToolkit 등) 전부
절대 컷오프를 하드코딩하지 않고 사용자 파라미터로만 둔다는 사실도 이 결정을
뒷받침한다(외부에 대체할 만한 "표준값" 자체가 없음).

**Finding 3 (REJECT 지금은, 실측 영향 0건 확인 후 라벨만 기록)** -
`default_terminal_growth`의 ceiling(4.5%)이 Damodaran이 통상 인용하는 성숙기업
영구성장률 범위(명목GDP성장률 앵커, 2.0~3.5%)보다 100bp 높다. **34/34 ledger
전수 실측 결과 g_terminal은 3.47~3.69%로 ceiling에 한 번도 도달한 적이 없다**
(`discount_rate.g_terminal` 직접 대조) - 코드는 v3.52의 초대형주가산과 동일
원칙으로 바꾸지 않고, VALIDATION_STATUS와 함수 docstring에 발견을 기록했다.
재개조건(향후 실제 g_terminal이 4.0%를 넘는 사례 발생)을 테스트
(`test_default_terminal_growth_ceiling_never_binds_on_existing_ledgers`)로
직접 감시하게 배선했다 - 이 프로젝트가 반복 지적해온 "재개조건을 문서에만
적어두면 아무도 안 지킨다"는 패턴(run_self_check·confidence_score·claim/lock·
cross_check_prior_record)의 다섯 번째 재발을 막기 위함이다.

**Finding 4** - Damodaran/CFA 커리큘럼 인용은 이번 조사에서 1차 원문 직접
대조에 실패해(PDF 텍스트 추출 불가) 2차 출처 요약 수준으로만 확보했다 -
TYL SBC 3배 오류(2차 출처 인용을 검증 없이 신뢰)와 같은 위험 계열이라
`reports/research/`에 이 한계를 명시했다. PEG 경계값(GARP 실무관행 <1.0)과
IRS 통과경계(IG/RG=0.6875)의 느슨한 수치 일치는 지표 정의 자체가 달라
(PEG는 P/E·EPS성장, IRS는 FCF수익률·DCF역산성장) 검증으로 쓰지 않고
기록만 했다 - Fama-French 38% 평균회귀율을 성장축소계수 근거로 오인했다가
REJECTED된 결정#4와 같은 "표면 유사성≠구조적 유사성" 함정을 피하기 위함.

`docs/research_decision_record.md`에 결정 #14(ceiling REJECT)·#15(implied_growth
ADOPT)·#16(screener 임계값 REJECT) 3건을 추가했다.

**검증**: 테스트 748개 전부 통과(신규 8) · 34종목 골든재현 8지표 완전 동일 ·
fingerprint `fbd34322…` 불변 · ledger·매수리스트·공식 판정 0건 수정 ·
`ENGINE_VERSION` v3.62 → **v3.63**(VALIDATION_STATUS 라벨 갱신 - v3.32 규칙에
따라 `engine/` 변경 시 상수 갱신).

## v3.64 — 일일 보유종목 감시 자동화 + 확인상태 추적 (2026-08-23, 사용자
"GitHub Actions를 결정론적 투자연구 파이프라인으로" 아이디어의 1단계)

사용자가 자동화를 "매일 09시 스크리너"에서 "시장관찰→자동분석→기대괴리
탐지→연구우선순위→Thesis 모니터링→결과축적→자체검증"의 일일 파이프라인으로
확장하자고 제안했다. **한 번에 다 만들지 말고 구조를 분석한 뒤 가장 가치
높은 단계부터** 하라는 지시에 따라 구조 분석 → 1단계만 구현했다.

### 구조 분석에서 드러난 것 — 문제는 기능 부족이 아니라 배선 부재였다

| | |
|---|---|
| 자동 실행되는 것 | `daily_screen_ci.py`(screener만) + pytest. **그게 전부** |
| 수동 1회성으로만 돈 것 | thesis_monitor(08-13) · growth_scorecard(08-13) · gap_range(08-16) · market_relative(08-13) |
| 감시받는 종목 | **0종목** — 매일 신규 후보만 찾고 이미 분석한 34종목·매수리스트 12종목은 아무도 안 본다 |

사용자가 요구한 6개 출력 중 **1·2·3·4·6번은 이미 engine/에 구현돼 있다**
(`decompose_drift`·`implied_growth`·`realistic_growth_estimate`·`gap_analysis`·
`scan_falsification_conditions`). 자동으로 안 돌 뿐이었다. 그래서 1단계는
새 기능이 아니라 **이미 만든 것을 매일 돌리는 배선**으로 잡았다.

### ⭐ 자동화 직전에 발견한 구조적 결함 — 그대로 돌렸으면 악화됐다

`scan_falsification_conditions()`를 2026-08-23 기준으로 돌리면 past_due 6건
(DUOL·MNDY·PGR·SE·TCOM·TTD)이 나온다. 그런데 **이 6건은 08-13에 이미 전부
실제로 검증됐다**(TTD 반증 확정 → 매수비중 4.80%→2.70% 축소까지 완료).
검증했다는 사실이 어디에도 기록되지 않아 매일 같은 6건이 다시 뜨는 것이다.

**이 상태로 매일 자동 실행하면 결과는 개선이 아니라 악화다** — 며칠 만에
사람이 알림 전체를 무시하게 되고, 그러면 진짜 신규 항목이 묻힌다. v3.42가
발견한 실패("반증조건 기한 5건이 지났는데 12일간 아무도 안 봄")가 알림
피로라는 다른 경로로 재발한다. 실제로 thesis_monitor 자체가 08-13 이후
**10일간 한 번도 안 돌았다** — 감시도구가 감시받지 않은 것이다.

따라서 자동화의 전제조건은 "매일 돌리기"가 아니라 **"확인된 것을 확인됐다고
기록할 곳"**이었다.

### 구현 — `engine/monitor_state.py` (신규) + `scripts/daily_monitor_ci.py`

- **자동 판정 안 함**: 이 모듈은 반증조건 발동 여부를 **판정하지 않는다**.
  v3.42 원칙 그대로 — 정규식은 트리거 날짜와 서술적 날짜를 구분 못 하고
  (TCOM의 소송 집단기간 2024-04-30이 실제 오탐), 발동 여부는 실적을 읽어야
  안다. 추적하는 건 오직 **"사람이 확인했는가"**뿐이다. 테스트가 이 경계를
  강제한다(`decide/judge/auto_resolve` 류 함수가 생기면 실패).
- **확인은 사람의 행위**: CI는 `monitor/acknowledgements.json`을 **읽기만**
  한다. 쓰기 권한이 없으므로 자동 커밋 사고가 원천 불가능하다. AST 기반
  테스트로 쓰기/삭제 경로 부재를 고정했다(주입 테스트로 가드 작동 역검증).
- **"확인 못함"은 닫힌 상태가 아니다**: `INCONCLUSIVE`는 재부상한다. 08-13
  MNDY가 정확히 이 경우였다(조건이 지정한 코호트별 NDR을 회사가 미공개).
  이걸 '확인 완료'로 닫으면 영영 안 돌아온다 — "데이터 없음 ≠ 안전" 원칙
  (is_insurer·sbc_cross_check·holdings_overlap)의 시간축 버전이다.
- **초기 상태는 손으로 안 썼다**: `scripts/seed_monitor_acknowledgements_
  2026_08_23.py`가 `reports/thesis_monitor_2026-08-13.json`의 실제
  `verification.verdict`를 그대로 전사한다(7건). 기록된 판정을 나중에 고쳐
  쓰는 것은 사후합리화이므로 테스트가 이를 고정한다.

**실측 동작**: 2026-08-23 실행 → 알림 **0건**(6건 다 확인 완료), TTD만
발동상태로 계속 노출. 2026-08-28 시뮬레이션 → **PDD 1건 발화**(EU DSA
컴플라이언스 시한). 즉 조용해야 할 때 조용하고 떠야 할 때 뜬다.

### 취약점 하나를 같이 고쳤다

감시는 네트워크 의존이 **전혀 없는데**(ledger + 날짜만 본다) 스크리닝
하류에 있어 Finviz/Playwright 장애 시 같이 멈춘다 — 이 감시가 막으려는
실패 그 자체다. 워크플로에 `if: failure()` 대체 단계를 추가해 스크리닝이
죽어도 감시는 단독으로 보고하게 했다. 평소에는 **한 코멘트로 합쳐** 나가
알림은 하루 1건을 유지한다.

### 남은 단계 (의도적으로 안 만든 것)

사용자 제안의 나머지(Gap 부식 추적·시장기대 역산 일일 갱신·연구 우선순위화)는
**전부 일일 시세가 필요한데 그 출처가 아직 없다** — Alpha Vantage 무료
한도 25회/일 중 20회를 스크리닝이 이미 쓰고, 34종목 시세에는 34회가 필요하다.
Stooq는 이 샌드박스 프록시에서 차단돼 도달성을 검증하지 못했다(GitHub
Actions에서는 다를 수 있으나 **확인 전까지 가정하지 않는다**). 시세 출처가
확보되면 `recompute_gap_at_market_cap`·`decompose_drift`가 이미 있으므로
배선만 하면 된다.

**검증**: 테스트 748 → **765개 전부 통과**(신규 17) · 34종목 골든재현 8지표
**불일치 0건** · fingerprint `fbd34322…` **불변** · **ledger·매수리스트·공식
판정 0건 수정** · `ENGINE_VERSION` v3.63 → **v3.64**.

## v3.65 — 통과 후보 자동 심층분석(Deep Screen) (2026-08-23, 사용자가 v3.64
아이디어의 다음 조각으로 지정: "통과 종목 자동 Fundamental Analysis")

### 설계 전제 — 공식 정성판단은 자동화하지 않는다

`run_analysis()`는 `model_choice_reason`·`subjective_input_basis`(경쟁강도·
수요민감도) 없이는 **실행을 거부하도록 설계돼 있다**(v3.19). 이걸 자동
파이프라인이 대신 채우면 "빈칸 채우기"(추측을 근거로 위장) 그 자체이고,
2026-08-16 모델선택 연구가 이미 "이론 기준이 실제 선택을 설명하지 못한다"
(구간 완전중첩)며 규칙화를 REJECT해뒀다. 그래서 이 단계는 **공식 ledger를
만들지 않는다** - 대신 재무제표만으로 객관적으로 계산 가능한 부분만 공식
엔진 함수 그대로 재계산하고, 나머지(경쟁강도·수요민감도·순부채·모델선택)는
`screener.py`가 이미 쓰는 corpus 중앙값으로 명시적으로 고정한다.

### `engine/deep_screen.py` 신규 - 새 계산 로직 0줄

CAGR 윈도우 산출과 결과 조립만 새 코드고, 안에서 부르는 건 전부 기존
검증된 함수다: `revenue_volatility_score`·`margin_volatility_score`·
`leverage_score`·`cyclicality_score`·`DRSInputs`·`erp_from_drs`·
`classify_lynch_type`·`structural_discount_rate`·`realistic_growth_estimate`·
`judgment_from_gap`(engine/expectation_gap_engine.py) +
`implied_growth_from_fcf_yield`(engine/screener.py). `screener.py`가 5개
관측값·6개년 창만 쓰던 것을, SEC에서 **최대 11개년**을 다시 받아 실제
3y/5y/10y CAGR로 정밀화한다 - 하루 통과 후보가 보통 0~2종목뿐이라 그 소수만
심층분석하면 비용이 작다.

### ⭐ BSX 실제 ledger로 골든 교차검증 - 설계 의도가 실측으로 확인됨

BSX(2026-08-13 정식분석)의 원시 다년 데이터를 그대로 넣어 대조한 결과:

| 항목 | 공식 ledger | deep_screen | 일치 |
|---|---|---|---|
| revenue_cagr_3y/5y/10y, fcf_cagr_5y, worst_yoy | 실측값 | 동일 | **1e-12 정밀도 완전일치** |
| structural_discount_pct, lynch_type, realistic_growth | 실측값 | 동일 | **완전일치** |
| drs.revenue_volatility, margin_volatility | 8.0 / 4.0 | 8.0 / 4.0 | **완전일치** |
| drs.leverage (순부채 미실측) | 10.0(실측) | 6.0(가정) | 예상된 차이, 라벨됨 |
| drs.competition_intensity (정성조사) | 5.4(실측) | 12.0(가정) | 예상된 차이, 라벨됨 |

즉 **재무제표로 계산 가능한 모든 항목이 정확히 일치**하고, 재무제표 밖
판단이 필요한 항목만 차이가 나며 그 차이는 `assumed_inputs`에 전부 명시된다
- 설계 의도 그대로다.

### ⚠️ 검증 중 실제로 잡은 회귀 1건

초판은 margin_volatility를 확보 가능한 전 구간(최대 11개년)으로 계산해
BSX에서 8.0이 나왔는데 공식값은 4.0이었다. 원인: `pipeline.py`는
`margin_years = sorted(revenue_by_year)[-5:]`(최근 5개년만)로 계산하는데,
전구간을 쓰면 **2020년 코로나 저마진**(영업이익률 6.1% vs 정상 15~20%)이
표준편차를 부풀린다(전구간 stdev 3.5%p vs 최근5년 stdev 1.8%p - 등급 자체가
갈림). BKNG 조회창 문제(2026-07-28)와 같은 계열의 함정을 여기서도 만날
뻔했다 - 최근 5개년으로 고쳐 정확히 일치시켰다.

### 통합 - 스크리닝 코멘트에 합쳐 나간다, 실패는 종목 단위로만 격리

`daily_screen_ci.py`의 `run_deep_dive()`가 통과 후보마다 심층분석을 돌려
`reports/deep_screen/<TICKER>_<날짜>.json`에 저장하고, **전일 스냅샷과
대조해 Gap·판정 변화를 함께 보고**한다(사용자가 요청한 "전일/과거 대비
의미 있는 변화 탐지"). 한 종목의 SEC 데이터 이상(Model N/A 등)이 나머지
종목이나 기본 스크리닝 보고를 막지 않도록 종목 단위로 예외를 격리했다.

**워크플로에 `reports/deep_screen/` 전용 자동 커밋 단계를 추가했다**(`contents:
write` 권한 신규) - 러너가 매번 새로 뜨므로 커밋하지 않으면 "전일 대비"
비교가 영구히 `prior=None`이 된다. **오직 이 디렉터리만** 건드리며(엔진
코드·ledger·CLAUDE.md는 이 경로로 절대 안 바뀐다), 판정이 아니라 SEC 원자료
재계산 스냅샷이라 사람 검토 없이 쌓아도 안전하다는 판단이다 - 반대로 주관적
판단이 필요한 `monitor/acknowledgements.json`(v3.64)은 여전히 사람이
커밋한다. 이 구분(객관적 재계산 스냅샷은 자동 커밋 / 주관적 판단은 사람
커밋)을 명확히 유지할 것.

### 부수 정리

`DEFAULT_NDTE`(0.406, ledger 34종목 실측 중앙값)를 `scripts/daily_screen.py`
에서 `engine/screener.py`로 옮겼다 - `engine/deep_screen.py`도 같은 상수가
필요한데, `engine/`이 `scripts/`를 참조하는 역방향 의존을 만들지 않기
위함이다(`daily_screen.py`는 재export만 해 하위호환 유지).

**검증**: 테스트 765 → **786개 전부 통과**(신규 21) · 34종목 골든재현 8지표
**불일치 0건** · fingerprint `fbd34322…` **불변** · **ledger·매수리스트·공식
판정 0건 수정** · `ENGINE_VERSION` v3.64 → **v3.65**.

## v3.66 — 성장률 상한 외부검증: 캡의 출처를 특정하고 규모 무시 결함을 찾다
(2026-08-23, 사용자 요청 "투자 대가·투자기업·검증된 오픈소스·학술지 기준을
면밀히 조사·분석 후 새로 설정")

**같은 날 오전의 v3.63과 표적이 다르다** - v3.63은 `screener.py`의 1차 필터
임계값(8%/5.5%)을 보고 REJECT로 끝났고, 이번은 **Realistic Growth 자체의
상한**(`LYNCH_TYPE_CAPS`)이다. 34종목 중 **7종목에서 실제로 바인딩**된다
(BRO·CDNS·DUOL·GEN·MNDY·PDD·ROP) - 바인딩되면 그 종목의 Gap은 성장분석 전체를
무시하고 `캡 − Implied Growth`로만 결정된다(v3.24 M-1).

### ⭐ Finding 1 — 캡 수치의 출처가 특정됐다 (v3.24의 "추정"이 사실로 확인)

린치 원저서(*One Up on Wall Street*)의 분류 정의: **stalwart "10-12%"**,
**fast grower "small, aggressive companies that grow around 20-25%"**.
IRS의 상한 12%/25%와 정확히 일치한다.

### ⭐ Finding 2 — 그런데 용도가 다르다 (핵심 결함)

린치의 범위는 **"지금 어느 유형인가"를 가르는 분류 기술어**인데 IRS는
**"앞으로 n=12년간 얼마나 성장할 것인가"의 예측 상한**으로 쓴다. 결정적으로
린치는 fast grower를 **"small, aggressive companies"**로 한정했는데 **IRS는
같은 25%를 매출 $1.04B인 DUOL과 $60.0B인 PDD에 똑같이 적용한다**(58배 차이).

### 외부 실증 — Credit Suisse HOLT base rate (`engine/base_rates.py` 신설)

Mauboussin & Callahan, *The Base Rate Book*(2016-09-26) **원문 PDF를 직접
파싱**했다(2차 출처 요약 아님 - TYL SBC 3배 오류가 2차 출처를 검증 없이
인용해 생긴 사고였다). 전사 정확성은 **12개 표 × 4개 열의 합계가 100%인지**로
검증했다 - 44/48개 열이 99.5~100.5%. 나머지 4개는 `325-700` 구간 하나인데
**원문 자체가** 89~93%라 파싱 오류가 아니며 그대로 기록했다.

모집단이 IRS 자체 표본보다 낫다: 상위 1,000개 글로벌 기업, 1950-2015년(65년),
전 세계 시총의 60%, 그리고 ⭐**소멸기업 포함 = 생존편향 없음**("The population
includes companies that are now dead") - `engine/quant/validation.py`가 IRS
자체 표본의 생존편향(생존율 하한 9.6%)을 경고하고 있는 것과 대비된다.

**10년 CAGR base rate(실질)**: 매출 >$25B 구간에서 ≥20%는 **0.2%**, ≥25%는
**0.0%**. >$50B에서는 ≥20%가 **0.0%**. 반면 $700M-1.25B 구간은 ≥20%가 3.5%.
Mauboussin: *"as firm size increases the mean and median growth rates
decline... temper expectations as companies get larger."*

⚠️ **단위 함정 두 개를 코드로 막았다** - (1) 표는 **실질**(2015년 달러)이고
IRS RG는 **명목**이라 `nominal_to_real()`을 강제한다(v3.35 실질/명목 EPS
함정과 동일 계열). (2) 매출 규모 구간도 2015년 달러라 `deflate_to_2015()`로
환산하지 않으면 더 큰 decile로 잘못 배정된다.

### 34종목 전수 대조 결과 — 같은 캡의 base rate가 17.5배 갈린다

| 종목 | 규모 | RG(명목) | 10년 base rate | 등급 |
|---|---|---|---|---|
| **PDD** | >$25B | 25.00% | **0.2%** | EXTREMELY_RARE |
| **SE** | $12-25B | 23.56% | **0.9%** | EXTREMELY_RARE |
| DUOL·MNDY | $700M-1.25B | 25.00% | 3.5% | RARE |
| BRO·CDNS·GEN | $3-4.5B | 12.00% | 41.2% | COMMON |

**매수 포트폴리오의 41.02%가 "역사적으로 5% 미만의 동종 규모 기업만 달성한"
성장률 위에 서 있고, 16.46%(PDD 9.07 + SE 7.39)는 1% 미만이다.**

### ⭐ 정확한 진단 — 문제는 캡 값이 아니라 규모 무시다

- **stalwart 12%**: 바인딩 3종목 전부 base rate 41.2% = COMMON. **실증상 문제없다.**
- **fast_grower 25%**: 소형(DUOL/MNDY) 3.5%는 희귀하나 가능, **대형(PDD) 0.2%는
  사실상 선례 없음**.

즉 *"25%가 너무 높다"*가 아니라 ***"25%를 $60B 기업에도 똑같이 적용하는 것이
틀렸다"***가 정확하다 - 린치 본인의 원 서술이 이미 "small"이라는 조건을 달고
있었는데 IRS가 그 부분만 떨어뜨린 셈이다.

### 결정 (결정기록 #17~#20)

| 대상 | 결정 |
|---|---|
| `engine/base_rates.py` 신설(외부 실증 병기) | **ADOPT** — 구현 완료 |
| `deep_screen`에 base rate 병기 + 경고 | **ADOPT** — 스크리닝 경로에만 배선, 기존 판정·자본 무변경 |
| base rate를 **하드 탈락 기준**으로 사용 | **REJECT** — Mauboussin 자신이 "reality check"로 처방했고 하드컷을 주지 않았다. v3.19에서 하드 필터의 이중 반영을 이미 실증(BRO·BSY 오탈락) |
| **`LYNCH_TYPE_CAPS` 규모 조건부 변경** | **PROPOSED — 사용자 승인 대기.** 7종목 RG→Gap→판정→매수비중까지 연쇄하는 **자본 재배분**이라 v3.28(ROP override 승격) 선례대로 명시적 승인 필요. 실측 영향은 **PDD 1종목**(매수 9.07%, Gap +29.16%p→약 +19%p) |
| 캡 값 자체를 다른 숫자로 교체 | **REJECT** — stalwart 12%는 실증상 문제없음이 확인됐다. 값이 아니라 규모 무시가 문제이므로 값만 바꾸는 건 오진에 대한 처방 |

**검증**: 테스트 786 → **854개 전부 통과**(신규 68) · 34종목 골든재현 8지표
**불일치 0건** · fingerprint `fbd34322…` **불변** · **ledger·매수리스트·공식
판정 0건 수정** · `ENGINE_VERSION` v3.65 → **v3.66**.

## v3.67 — 규모 조건부 성장상한 배선: 승인, 그리고 내 제안서가 3배 틀렸던 일
(2026-08-23, 사용자 "승인." → 정정 후 "그대로 진행 (thr=1.0%)")

v3.66이 **PROPOSED — 사용자 승인 대기**로 남긴 결정 #19를 실행했다. 이 프로젝트가
지금까지 지켜온 "병기, 자동판정 안 함" 원칙의 두 번째 예외다(첫 번째는 v3.28 ROP
`realistic_growth_override` 승격) — 예외로 둔 이유는 v3.28과 동일하다. 실측 영향이
사전에 명확히 제시됐고 사용자가 그 사실을 알고 명시적으로 지시했다.

### 무엇을 바꿨나 — 캡 값이 아니라 캡의 규모 무시

`LYNCH_TYPE_CAPS`(fast_grower 25% 등)는 **하나도 안 바꿨다**(결정 #20 REJECT 유지).
대신 그 위에 `engine/base_rates.py::size_conditioned_growth_cap()`이 만드는 **규모
조건부 상한**을 얹어 둘 중 낮은 쪽을 채택한다. 근거는 v3.66이 확정한 두 사실이다:
(1) 캡 수치의 출처가 린치 원저서로 특정됐고, (2) 린치는 fast grower를 **"small,
aggressive companies"**로 한정했는데 IRS는 그 조건만 떨어뜨린 채 $1.0B(DUOL)와
$60B(PDD)에 똑같은 25%를 적용하고 있었다(10년 base rate 3.5% vs **0.2%**, 17.5배).

`AnalysisInputs.usd_fx_rate`를 신설했다 — base rate 표가 USD 표시라 비USD 종목은
환산이 필요하다. **환율을 추측하지 않는다** — `currency != "USD"`인데 `usd_fx_rate`가
없으면 규모 상한을 **적용하지 않고** `data_limitations`에 미적용 사실을 남긴다
(추측으로 채우면 그게 "빈칸 채우기"다). PDD만 `usd_fx_rate=7.2`를 명시했다.

### ⚠️ 승인 요청 시 내가 보고한 영향 범위가 틀렸다 — 1종목이 아니라 3종목이었다

제안서에 **"PDD 단 1종목·매수 비중 9.07%"**라고 적었다. 실제는 **PDD·PGR·SE
3종목·28.46%**이고 SE는 등급까지 S→A로 바뀐다.

**원인**: Lynch 캡이 **이미 바인딩된** 7종목만 조사하고 그 집합을 "영향 대상"으로
등치했다. 규모 상한은 **Lynch 캡에 안 걸려 있던 종목도 새로 잡는다** — PGR(16.87%,
>$50B)·SE(23.56%, $12-25B)가 정확히 그 경우다. 사용자에게 "승인하신 근거가 사실과
달랐다"고 공개하고 정정된 범위로 재확인받은 뒤 진행했다.

| 종목 | 규모 구간 | RG | Gap | 등급 |
|---|---|---|---|---|
| **PDD** | >$25B | 25.00% → **17.87%** | +29.16 → **+22.04%p** | S 유지 |
| **PGR** | >$50B | 16.87% → **12.75%** | +21.76 → **+17.64%p** | S 유지 |
| **SE** | $12-25B | 23.56% → **17.87%** | +19.18 → **+13.50%p** | **S → A** |

**매수 유니버스 진입·이탈 0건**(SE는 등급만 이동, 둘 다 S/A 안). 포트폴리오에서
base rate 1% 미만 위에 서 있던 자본 **16.46% → 0.00%**, 5% 미만 **41.02% → 26.23%**.
최종 비중: GEN 18.00 · ACGL 12.00 · PGR 12.00 · TCOM 10.36 · PDD 7.76 · DUOL 7.13 ·
MNDY 7.10 · BRO 6.19 · SE 5.89 · UBER 5.40 · WDAY 5.11 · TTD 3.06.

⚠️ `thr=1.0%`는 **검증된 컷이 아니다.** Mauboussin은 분포를 보라고 했지 컷을 주지
않았다 — 승인 시 이 사실을 명시했고 사용자가 그 값을 선택했다.

### ⚠️ 버전 스탬프가 또 거짓말을 할 뻔했다 (v3.32 규칙의 실전 발동)

`ENGINE_VERSION`이 아직 `v3.66`인 상태로 PDD/PGR/SE ledger를 재생성했다 — 규모
조건부 상한은 **계산 결과를 실제로 바꾸는 신규 기능**이므로 v3.32가 잡은 바로 그
문제다(v3.27로 스탬프된 ledger가 v3.27에 없는 필드를 담고 있던 사건). 상수를
v3.67로 올리고 잘못 스탬프된 파일을 지운 뒤 재생성했다. **이 과정에서 v3.46
덮어쓰기 가드가 정확히 작동했다** — 같은 날짜 파일의 내용이 다르다며 `FileExistsError`로
조용한 교체를 거부했다. 구 파일(`PDD_2026-08-02`·`PGR_2026-08-03`·`SE_2026-08-03`)은
`git rm`으로 폐기(종목당 ledger 1건 규칙 유지).

### 부수 발견 — 매수리스트 캡 강제가 수렴하지 않고 있었다

비중이 바뀌면서 드러났다: GEN이 `0.18000442747320397`로 상한(0.18)을 미세하게
넘긴다. 원인은 **상한에 정확히 걸린 종목이 `else` 분기로 빠져 "미포화 종목"으로
재분류**되는 것이었다 — 재분배를 또 받아 진동하고, 5회 루프가 소진으로 종료되며
위반이 남는다. 흡수 조건을 `weight_final < cap - _EPS`로 좁히고 반복을 50회로
늘렸으며, **수렴 실패 시 조용히 통과하지 않고 `RuntimeError`를 던지도록** `for...else`
가드를 걸었다(문서로 둔 규칙이 무력화된 사례를 이미 다섯 번 겪었다). 검증: 합계
정확히 1.0, GEN 정확히 18.000000%, 위반 0건.

### PHASE 2 사이징 감사 리포트는 자동으로 따라 움직인다 (기록 성격 주의)

비중이 바뀌면서 `reports/sizing_assumption_exposure_2026-08-21.json`의 turnover도
미세하게 이동했다(버킷분산 18.82→17.92% 등). 처음엔 "날짜가 박힌 기록에 다른
날짜의 숫자를 넣으면 허위 기록"이라고 판단해 되돌렸는데, 확인해보니
`tests/test_sizing_assumption_exposure.py::test_audit_restores_official_outputs`가
공식 매수리스트 오염을 검사하려고 `main()`을 실제로 호출한다 — 즉 **테스트를 돌릴
때마다 이 파일이 현재 비중으로 재생성된다.** 되돌려봐야 다음 테스트 실행이 즉시
원복하므로, 되돌린 상태를 커밋하는 것이 오히려 실제와 다른 기록이 된다.

따라서 **재생성된 판본을 그대로 커밋한다.** 파일명의 `2026-08-21`은 데이터 시점이
아니라 **감사 자체의 식별자**로 읽어야 한다(다른 리포트들과 규약이 다르므로 여기
명시해둔다). 값이 바뀌어도 **7개 가정의 순위·CRITICAL 판정은 하나도 바뀌지
않았다** — 버킷분산 > 버킷목표 ≫ 달성률바닥 > 종목당상한 > Confidence축 >
`CONFIDENCE_ADJ` > SEVERE 플래그, GEN 이동 정확히 −6.00%p. 그 감사의 결론(가장
많은 노력을 들인 축이 자본에는 가장 적게 영향을 준다)은 이번 변경 후에도 유효하다.

### 검증

테스트 **864개 전부 통과**(2회 연속) · 34종목 골든재현 8지표 **불일치 0건** ·
baseline fingerprint `fbd34322…` → **`2419df22…`**(3종목 재계산에 따른 의도된 갱신,
스탬프 분포 `v3.67: 3` 추가) · ledger 34종목 중복 0건 · `ENGINE_VERSION` v3.66 →
**v3.67**.

## v3.68 — 일일 파이프라인 실패 사유 보존, 그리고 내 "인프라 장애" 진단이
실측으로 기각된 일 (2026-08-24)

### 발단 — 리포트가 고장과 정상을 구분하지 못했다

2026-08-23/24 정규 실행 실측: Finviz 60 → 정크필터 25 → **SEC 성공 1/25** →
시가총액 0 → **채점 0종목**. 그런데 GitHub Issue에는 "오늘은 통과 후보 없음"
으로만 기록됐다. 정상적인 빈 결과와 파이프라인 고장이 리포트상 **완전히 똑같이
보였다** — 이 프로젝트가 코드에서 반복해 막아온 "데이터 없음을 안전으로 오독"
(is_insurer·sbc_cross_check·holdings_overlap)의 운영판이다.

원인은 `_cached_facts`가 실패 시 bare `None`을 돌려줘 **사유가 통째로
사라진다**는 것이었다. 사유를 잃어버리면 고칠 수 없다.

### 고친 것

- `_cached_facts` → `(facts, reason)` 튜플. HTTP 상태코드까지 보존한다.
- User-Agent가 placeholder(`demo@example.com`)였다 → `filing_dates`의 실제
  연락처 기본값으로 교체(SEC 공정접근 정책은 식별 가능한 연락처를 요구).
- **티커→CIK 매핑표(약 1MB 단일 파일)를 티커마다 새로 받고 있었다** — 25종목
  이면 같은 파일을 25번. 프로세스 내 1회 캐시로 고정(테스트로 강제).
- 리포트가 제외 사유를 분류해 **인프라 장애와 정상 제외를 구분**하고, 깔때기
  각 단계를 그대로 노출한다. 채점 0건이면 "통과 후보 없음"이 아니라 실행
  실패로 명시한다.
- ⚠️ **HTTP 404는 장애가 아니다** — ETF·펀드는 CIK가 있어도 XBRL이 없어 404를
  준다(SPY 실측). 404를 장애로 세면 목록에 ETF가 섞일 때마다 가짜 경보가 떠
  진짜 장애가 묻힌다. 이 수정이 막으려는 알림 피로가 다른 경로로 재발하는
  것이라 별도 분류했다.

### ⚠️ 그리고 내 진단이 틀렸다 — 인프라 장애는 0건이었다

수정 전 나는 "24/25 실패 + 25종목 10초 = fast-fail + 로컬은 3/4 성공"을 근거로
**SEC가 GitHub Actions를 차단하거나 placeholder UA가 거부당하는 것으로 의심**
했다(원인 미확정이라고 적기는 했다). 수정 후 실제로 돌려보니:

```
Finviz 39 → 정크필터 25 → SEC 성공 3 (제외 22) → 시총 3 → 채점 3 → 통과 0
제외 사유:  FCF 기준·최종연도 0 이하 13종목  /  연도 데이터 부족 9종목
            인프라 장애 0종목
```

**HTTP 오류도 네트워크 오류도 한 건도 없었다.** SEC 조회는 처음부터 정상
작동하고 있었다.

### 진짜 원인 — 후보 공급원과 모델의 적용범위가 어긋나 있다

`ta_newlow`(52주 신저가)·`it_latestbuys`(내부자 매수) 프리셋이 내놓는 종목은
마이크로캡·최근 상장·적자기업·은행에 크게 치우친다(실측 제외 목록: ALK·GO·
ZWS·CCAP·NBBK·CWBC 등). 이들은 **FCF 기준연도가 음수이거나 연도 데이터가
부족해 FCF-DCF가 원리적으로 적용되지 않는다**(v3.19 CAGR 가드가 정확히 설계대로
막는 경우다 — PODD·ONON·MU에서 이미 확립된 프레임워크 부적합 유형).

즉 **버그가 아니라 설계상의 불일치**이고, 후보의 약 88%가 이 모델이 답할 수
있는 질문의 범위 밖이다. 파이프라인은 "고장난 채로 조용히 돌고 있던" 것이
아니라 "적용 불가능한 모집단을 계속 떠먹고 있던" 것이다.

**이 문제는 이번에 고치지 않았다** — 프리셋을 바꾸는 것은 이 시스템이 무엇을
쳐다볼지를 바꾸는 것이라 사용자 판단 사항이다. 다만 이제 매일 리포트에 그
비율이 그대로 드러난다.

**교훈**: 타이밍(10초)과 국소 재현(로컬 3/4)만으로 원인을 추정했는데 둘 다
맞는 관측이었지만 결론은 틀렸다. 계측을 먼저 넣고 나서야 답이 나왔다 —
이 프로젝트가 WM/WCN FCF CAGR에서 이미 배운 "그럴듯한 원인을 먼저 떠올리고
검증 없이 기록하지 말 것"과 같은 실수다.

**검증**: 테스트 864 → **871** · 34종목 골든재현 8지표 불일치 0건 ·
fingerprint `2419df22…` 불변 · ledger·매수리스트·공식 판정 0건 수정 ·
`ENGINE_VERSION` v3.67 → **v3.68**.

## v3.69 — 관심종목 자동추적: 발굴과 감시를 분리하다 (2026-08-24, 사용자 요청
"깃헙이 정해놓은 기업리스트 자동으로 IRS엔진으로 분석하게 해줄 수 있어?")

### 스크리너는 발굴이고 이건 감시다 — 섞지 않는다

`daily_screen_ci.py`는 Finviz가 매일 새로 뱉는 후보를 1차 필터링한다(발굴).
`watchlist_ci.py`는 `watchlist.json`에 사람이 정해둔 목록만 본다(감시). 목적도
모집단도 다르므로 별도 스크립트로 뒀다. 이로써 2026-08-24 감사가 지적한
**"34종목을 감시하는 사람이 0명"**이 구조적으로 해소된다.

### 세 갈래 중 하나는 의도적으로 자동화하지 않았다

| 대상 | 함수 | 자동화 |
|---|---|---|
| ledger 있는 종목 → 시총만 갱신, Gap 부식 추적 | `recompute_gap_at_market_cap`(v3.42) | 한다 |
| ledger 없는 종목 → SEC 전면 재계산 | `deep_screen`(v3.65) | 한다 |
| **공식 판정(ledger 생성)** | `run_analysis` | **하지 않는다** |

세 번째를 뺀 이유는 성능이 아니라 설계다. `run_analysis()`는 `model_choice_reason`·
`subjective_input_basis` 없이는 **실행을 거부**하고(v3.19), 자동 파이프라인이 그
자리를 채우면 그게 정확히 "빈칸 채우기"다. 2026-08-16 모델선택 연구가 규칙화를
REJECT한 근거(교과서 기준이 실제 선택을 전혀 분리하지 못함)가 그대로 적용된다.

**새 밸류에이션 로직 0줄** — 기존 검증된 두 함수를 부르기만 한다(테스트로 고정:
`test_watchlist_does_not_reimplement_engine_functions`).

### ⚠️ 최우선 불변조건 — `ledger/`에 쓰지 않는다

`recompute_gap_at_market_cap`은 오늘 날짜로 스탬프된 **완전한** 결과를 만든다.
저장하면 같은 티커 ledger가 2개가 되어 "종목당 1건" 규칙이 즉시 깨진다(v3.32
WM/WCN/IDXX 중복이 CLAUDE.md 통계까지 오염시킨 사고와 같은 유형). 결과는
`reports/watchlist/`에만 남는다 — 공식 판정이 아니라 모니터링 신호다.
**두 겹으로 고정했다**: AST로 `save_ledger` 호출·`LEDGER_DIR` 쓰기 경로 부재를
검사하고, 실제 실행 후 ledger 파일 목록과 mtime이 그대로인지도 확인한다.

### 시가총액 예산 — 이 파이프라인의 유일한 실제 제약

AV 무료 25회/일인데 스크리너가 20회를 잡아둔다. 그런데 **실측상 스크리너는 하루
1~3회만 쓴다**(SEC가 후보 88%를 FCF-DCF 적용불가로 걸러냄 — v3.68 참고). 남는 몫을
관심종목이 쓰되, 34종목을 하루에 다 볼 수는 없어 **날짜 기반 결정적 회전**으로
나눈다(기본 18종목/일 → 2일이면 한 바퀴).

상태 파일을 쓰지 않은 이유: 커밋 타이밍이나 실행 실패로 상태가 어긋나면 **어떤
종목이 조용히 영영 안 뽑힐 수 있다**. day-of-year만 쓰면 그 표류가 구조적으로
불가능하고 같은 날 재실행 시 같은 결과가 나온다(테스트: 14일 안에 34종목 전수
커버 확인).

### 자기일치 검증을 코드에 심었다

시가총액은 성장추정에 영향을 주면 안 된다(Realistic Growth는 재무 시계열에서만
나온다). 어긋나면 계산 경로 버그이므로 조용히 넘기지 않고 `integrity_error`로
리포트 최상단에 띄운다. 실데이터(ACGL) 검증에서 1e-12 정밀도로 일치.

### 실측 — 가치함정 패턴이 또 재현됐다

전 종목 시총 −25% 시나리오로 드라이런: 6종목 전부 Gap이 +3.4~3.7%p 벌어졌고
**TYL은 판정이 '적정가/경계선'→'저평가 가능성'으로 뒤집혔다.** TTD도 +17.01%p →
+20.73%p로 "더 싸 보인다". 이게 v3.42가 TTD 실측으로 확인한 가치함정의 수학적
원리 그대로다 — 주가가 빠지면 Gap은 **반드시** 벌어지므로, 사업이 나빠져서 빠진
경우에도 이 지표는 "더 사라"고 말한다. 그래서 리포트 하단에 반증조건 감시
(v3.64)와 **함께 보라**는 경고를 고정 문구로 넣었고, 이 성질 자체도 테스트로
박아뒀다(`test_price_drop_widens_gap_the_value_trap_property`).

**검증**: 테스트 871 → **884** · 34종목 골든재현 8지표 불일치 0건 ·
fingerprint `2419df22…` 불변 · **ledger·매수리스트·공식 판정 0건 수정** ·
`ENGINE_VERSION` v3.68 → **v3.69**.

## v3.70 — 4축 개선 워크플로 신설 + 축0(회계품질) 1차 실행 (2026-08-24, 사용자
요청 "재무회계·해자·산업·경영진 4축을 수정·보완·개선·발전할 워크플로를 세우고
실행해")

### 먼저 확인한 것 — 축마다 병목의 **성격**이 다르다

코드 실측(문서·기억 아님):

| 축 | 현재 | 병목 성격 |
|---|---|---|
| 0 재무·회계 | 계산 정확(1e-12), **회계품질 판별 0** | 데이터는 있고 **지표가 없었다** |
| 1 해자·경쟁자 | `moat`/`ROIC`/`invested_capital` 히트 **0**, ROIC `BLOCKED` | **정의 문제**(데이터 아님) |
| 2 산업 | `demand_sensitivity_pct` 숫자 하나 | **정량화 대상이 모호** |
| 3 경영진 | 엔진 히트 0, `research_lenses` 실사용 **0건** | **수집 계층 부재** |

**넷이 전부 다르므로 외부 프레임워크 하나를 골라 일괄 적용하는 접근은 틀린다.**
축0은 지표만 있으면 되고, 축1은 지표를 만들어도 정의가 없으면 못 쓰고, 축3은
지표보다 파이프라인이 먼저다. `docs/axis_improvement_workflow.md`에 8단계 관문
(G1 공백확정 → G2 외부근거 → G3 안티기능대조 → G4 도메인확인 → G5 코퍼스측정
→ G6 결정 → G7 배선 → G8 회귀검증)과 축별 전략을 명문화했다.

### ⭐ G5 컷을 실행 전에 고정했고, 그게 실제로 작동했다

순위상관 |rho|≥0.7 중복 / 0.4~0.7 부분중복 / <0.4 독립 — **결과를 보고 옮기지
않았다.**

**1차: 순진한 발생액 `(NI−OCF)/평균총자산` → SBC/FCF와 −0.717 → REJECT.**
발생액 최하위 4종목이 정확히 고SBC SaaS(MNDY·DUOL·WDAY·SE)였다 — SBC가 OCF에
가산되는 비현금비용이라 **이 지표가 사실상 SBC 강도를 재고 있었다.** RQ-001이
`FCF/영업이익`을 같은 이유로 기각한 **함정의 재발**이고, 측정이 잡아냈다.

**2차: SBC를 되돌린 형태 → 전부 독립 → ADOPT.**
SBC/FCF −0.717→**+0.200** · DRS −0.394 · RG −0.314 · Gap −0.216 ·
capex집약도 **+0.020**. 커버리지 **34/34**.

하위권이 WM·WCN(자본집약)이라 D&A 혼동을 의심했으나 capex 집약도 상관 +0.020으로
코퍼스 전체에선 관계 없음을 확인했다(개별 종목 수준의 D&A 원인 가능성은 한계로 남김).

### F-Score를 쓰지 않은 이유 두 가지

①**§31 안티기능 정면 위배** — 9개 신호를 0~9로 합산하는 합성점수다. `research_lenses`의
6차원 별점을 같은 이유로 REJECT한 선례가 있고 사유("중요도가 다른 축이 같은 무게가
되고 공백이 점수 뒤에 숨는다")가 그대로 적용된다. ②**도메인 불일치** — 검증 표본이
고 book-to-market 가치주 1976-1996인데 IRS는 FCF-DCF 성장주다.

### ⚠️ 1차 출처 검증 실패를 그대로 기록한다

Sloan(1996) TAR 71(3) 289-315와 Piotroski(2000) **둘 다 원문 PDF가 스캔본**
(JBIG2/DCTDecode)이라 직접 파싱에 실패했다. **기억으로 계산식·9개 신호를 인용하지
않았다**(TYL 2차출처 3배 오류 교훈). 서지정보는 4개 독립 학술출처로 삼각검증
**VERIFIED**, 계산식 본문은 **UNVERIFIED** — 이 구분을 `VALIDATION_STATUS`와
리포트에 분리 기록했고, 구현은 `PROXY_ONLY`로 라벨했다.

### 배선하지 않았다 — 의도된 것

`engine/accounting_quality.py`는 **`run_analysis()`에 미배선**이다(테스트로 강제).
growth_quality.py와 동일한 판단 — 이 지표가 투자 성과와 관계있다는 증거가 IRS
표본에 **0건**이다(수익률 관측 자체가 없다). 배선하면 미검증 변수가 34종목 판정을
즉시 바꾼다. 반환 dict에 `score`/`judgment`류 키가 없는 것도 의도된 것이며 AST
테스트가 점수 함수 신설을 막는다.

SBC 없이 호출하면 조용히 통과시키지 않고 `[SBC 미반영]` 경고를 남긴다 — 1차 측정의
함정을 호출부가 모르고 재발시키는 것을 구조적으로 막는다.

### 부수 관측 — TTD가 네 번째 축에서도 상위

발생액 상위: VRSN +0.0169 · IDXX +0.0139 · **TTD +0.0129** · ROP +0.0113 ·
KLAC +0.0110. TTD는 이미 세 독립 경로(반증조건·성장률채점표·Gap부식)가 지목한
종목인데 회계 축에서도 상위권이다. ⚠️ **이걸 근거로 판정을 바꾸지 않는다** —
성과 검증 0건이고 절대값도 크지 않다.

### 나머지 3축의 다음 단계 (결정기록 #25·#26)

- **축1 DEFER(정의 선행)** — PHASE 4 실측상 데이터는 34/34인데 투하자본 정의
  두 가지의 ROIC 비율이 중앙 1.50배·최대 206배(MCK 1347% vs 6.5%). Mauboussin
  *Measuring the Moat* 등 1차 출처에서 정의와 음수자본 처리규칙을 확보한 뒤 진행.
  **자체 발명 금지** — 못 구하면 DEFER 유지.
- **축2 DEFER(관측 선행)** — Porter류는 서술형이라 수치 환산 경로가 없고 억지
  점수화는 §31에 걸린다. 실증된 실패 2건(KEYS·KLAC, BKNG·PDD)이 **둘 다 회사
  가이던스로는 잡혔으므로**, 프레임워크가 아니라 `growth_scorecard`의 가이던스-엔진
  괴리 관측(현재 11건) 축적이 먼저다.
- **축3 2차 실행 예정** — 희석 추이는 데이터 확보됨(주식수 34/34). ⚠️ **PHASE 4
  경고 승계 필수**: 주식분할(TTD ×10.2)·ADS 비율변경(TCOM ×8.4)·IPO 직후가
  "희석"으로 잡혀 매수 유니버스 46.74%가 오염됐다가 정정 후 12.16%로 줄어든
  이력이 있다. **원자료 시계열을 눈으로 보기 전에 배선하지 않는다.**

**검증**: 테스트 884 → **896** · 34종목 골든재현 8지표 불일치 0건 ·
fingerprint `2419df22…` 불변 · **ledger·매수리스트·공식 판정 0건 수정** ·
`ENGINE_VERSION` v3.69 → **v3.70**.

## RQ-004 — 축0 Layer1: ledger vs SEC 불일치 원인 전수 규명 (2026-08-25, 사용자
요청 "0번 재무회계 더 깊이 전문적으로 파고들어서 완벽하게 해결")

### 축0을 3개 층으로 나눴다 — 지표 추가만 보면 토대를 놓친다

| 층 | 질문 | 상태 |
|---|---|---|
| **L1 데이터 정확성** | 숫자가 **맞는가** | ← **여기가 미해결이었다** |
| L2 회계 품질 | 숫자를 **믿어도 되는가** | v3.70 발생액 1개 |
| L3 표현 충실성 | 숫자가 **경제를 반영하는가** | v3.23 SBC 1개 |

L1이 깨져 있으면 L2는 모래 위에 짓는 것이다 — `capex`는 FCF→`fcf0`→Implied
Growth→Gap→판정으로, `operating_income`은 `margin_volatility`→DRS로 직결된다.

### 기존 감사가 남긴 것과 이번에 한 것

P0-07(2026-08-19)은 **8종목만** 대조하고 336개 값 중 **67개를 미해결**로 남겼다
(*"원인이 다르며 범위 밖"*). `requires_review=True`는 "어느 쪽이 맞는지 아직 안
정했다"는 뜻인데, **정하지 않은 채 그 값들이 계속 판정을 만들고 있었다.**

이번엔 **34종목 전수 1,256개 값**을 대조하고, 세는 대신 **원인 가설을 기계적으로
검정**했다:

| 원인 | 건수 | 비율 |
|---|---|---|
| 일치(≤5%) | 1,055 | 84.0% |
| SEC_결측 | 101 | 8.0% |
| **B 태그정의** | 32 | 2.5% |
| **F 업종구조** | 22 | 1.8% |
| **D 원인미상** | **22** | **1.8%** |
| **E 정규화의심** | 12 | 1.0% |
| **A 라벨시프트** | 8 | 0.6% |
| **C 재작성** | 4 | 0.3% |

**미해결 67건(8종목) → 22건(34종목 전수, 1.8%).**

### ⭐ 발견 1 — 라벨 시프트가 실재한다(불일치가 아니었다)

CDNS capex: ledger FY2015 **44,808,000** == SEC FY2016 **44,808,000**, 정확히
일치. 불일치가 아니라 **라벨이 한 해 밀린 것**이다(v3.61이 CDNS·GEN에서 발견한
52/53주 회계연도 `int(end[:4])` 오라벨의 구체적 실례 8건).

**자동 재라벨링은 하지 않았다**(결정 #28) — 규약이 회사마다 반대다(CDNS는 1월 초
종료를 전년으로, GEN은 3월 말 종료를 당해로). 관측 2종목으로 규칙을 만드는 건
LYNCH_TYPE_CAPS·P/B 임계값에서 반복 거부한 형태다.

### ⭐ 발견 2 — 벤더가 정규화 영업이익을 쓰고 있다

BSX: FY2015 ledger **+790M** vs SEC **−283M**, FY2020 **+601M** vs **−80M**.
**부호가 반대다** — 대규모 소송충당금·손상차손이 SEC(GAAP)에만 반영됐다는 뜻이고,
ledger(벤더)는 일회성을 제외한 정규화값을 쓴다는 신호다. P0-07이 남긴 가설
(*"벤더 영업이익이 틀린 게 아니라 정규화값일 수 있다"*)이 실측으로 뒷받침됐다.

**왜 중요한가**: `operating_income`→`margin_volatility`→DRS 경로다. 정규화값은
마진 변동성을 **평활화해 위험을 낮게** 만든다 — 체계적 낙관 편향의 후보다.

### ⭐ 발견 3 — 그런데 판정은 안 바뀐다 (측정으로 확인)

영업이익을 SEC GAAP로 전면 교체해 재계산:

| 종목 | margin_vol | DRS | Gap | 판정 |
|---|---|---|---|---|
| BSX(11개년) | 4.0→**8.0** | 43.80→**47.80** | +5.87→**+5.65%p** | 유지 |
| CDNS(3개년) | 4.0→**8.0** | 36.60→**40.60** | +3.29→**+3.17%p** | 유지 |

**뒤집힘 0건.** DRS가 +4.0 올라도 Gap은 −0.21%p만 움직인다 — R-001이 이미 확인한
**DRS→ERP 압축**(`erp_from_drs`가 DRS 0~100 전체를 ERP 5~8%로만 사상) 때문이다.

⚠️ **"데이터 정확성은 중요하지 않다"로 읽으면 안 된다** — 이번 표본에서 안
뒤집혔을 뿐이고 BSX는 판정 경계에서 0.65%p 거리다(결정 #29).

### 발견 4 — 보험업 결측은 결함이 아니다

`operating_income` 결측 56건 중 **34건이 보험·보험중개 3사**(BRO 12·ACGL 11·
PGR 11). 보험사는 `OperatingIncomeLoss`를 아예 보고하지 않는다(손익계산서 구조가
다름 — 수입보험료·발생손해·언더라이팅손익). **구조적이지 결함이 아니라** 별도
분류(F)했다.

### ⚠️ 실행 중 자체 발견한 분류기 결함

초판은 `sec_val is None`이면 **즉시 `SEC_결측`을 반환**해 라벨 시프트 가설을
검정하지 않았다. 그런데 라벨 충돌은 두 기간이 같은 라벨을 다투다 한쪽이 **조용히
사라지는** 형태라 — **결측 자체가 시프트의 증상**이다. 순서를 고치자 A가 1건 →
**8건**이 됐다. 안 고쳤으면 provider 결함을 "SEC에 데이터가 없다"로 오분류할
뻔했다.

### 남은 것 — 정직하게

D_원인미상 **22건**(operating_income 8·operating_cashflow 7·capex 5·revenue 2)은
건별로 10-K 원문 대조가 필요하다. E 12건은 **확정이 아니라 의심**이며 벤더 산출
방식 직접 확인이 남았다. SEC_결측 101건 중 2017년 이후 77건의 성격도 개별 확인
안 했다.

**ledger를 한 건도 수정하지 않았다** — 원인 분류와 영향 측정만 했다(P0-07 원칙).
`engine/` 무변경이라 `ENGINE_VERSION`도 v3.70 그대로다(v3.32 규칙: 코드가 안
바뀌면 버전도 안 바뀐다). 테스트 896개·34종목 골든재현·fingerprint 전부 불변.

## v3.71 — 축0 L2 확장(AR 추세·재작성 이력) + TCOM 통화 라벨 사고 발견
(2026-08-26, RQ-005)

축0(재무·회계)은 RQ-004에서 **L1(데이터 정확성)** 원인 전수 규명을 끝냈지만
**L2(회계 품질)** 에는 v3.70의 발생액 하나뿐이었다. 후보 둘을 G1~G8에 태웠고,
**G5 컷은 실행 전에 고정**했다(|rho| ≥ 0.7 REJECT / 0.4~0.7 재설계 / < 0.4 독립).
새 지표는 **방금 채택한 발생액과도** 독립이어야 한다 — 외상매출 증가는 발생액의
구성요소라 중복 가능성이 실재했다.

**결과: 둘 다 독립(최대 |rho| 0.383), 둘 다 ADOPT.**

| 지표 | 커버리지 | 최대 |rho| | 분포 |
|---|---|---|---|
| AR/매출 **상대**추세 | 30/34 | 0.240 | 중앙 −0.49%/yr, −18.12~+10.27 |
| 재작성 이력 | 34/34 | 0.383 | **34종목 중 24종목이 정확히 0** |

재작성은 그래서 **연속 순위지표가 아니라 이진 사실**("이력이 있는가")로 읽어야
한다 — `VALIDATION_STATUS`에 명시했다. 이력이 있는 10종목: GWRE 0.111 · VRT
0.105 · BSX·SE 0.080 · ROP 0.077 · WCN·WDAY·VRSN·GEN·BRO.

### ⭐ 이번 사이클의 실질 산출물은 지표가 아니라 측정이 잡아낸 결함 세 건

**① 통화를 재작성으로 오탐 — 단위 함정의 재발**
초판에서 PDD·TCOM 재작성률이 **0.829·0.868**로 나왔다. 편차가 **정확히 629.9%**로
반복돼 확인하니 재작성이 아니라 **같은 태그를 CNY와 USD로 동시 보고**하는 것을
합쳐 비교한 것이었다(7.299 = 위안/달러). RAR 100배·실질/명목 EPS와 같은 계열.
단위를 분리하자 둘 다 **0.000**.

**② 절대 기울기는 기업 간 비교가 불가능하다**
AR 추세를 절대 %p/yr로 재면 **TTD가 −8.998%p/yr로 코퍼스 최대 이상치**가 된다.
그런데 TTD의 AR/매출 **수준**이 1.30~1.95로 타 종목(0.004~0.39)의 수 배~수십
배인데, 이는 광고중개업이 채권을 총액(광고주 청구)으로·매출을 순액으로 잡기
때문이지 매출인식이 공격적이어서가 아니다. 기울기는 수준에 비례하므로 같은 6%
상대변화가 TTD에서 9%p, 일반 종목에서 1.8%p로 찍힌다. **상대추세**로 바꾸니
TTD는 −6.15%/yr로 평범해졌고 후보끼리 상관도 −0.405 → −0.345로 내려갔다.

**③ 리포트가 엔진을 재현하지 못했다**
채택 후 엔진에 구현하니 스크립트 사본과 숫자가 갈렸다(VRT 재작성률 **0.217 vs
0.105** — 대상 지표·태그 우선순위 차이). 어느 쪽이 사실인지 알 수 없는 상태라
스크립트 사본을 지우고 **엔진 함수를 호출**하게 고쳤다. Simplicity First가 반복
경고한 "중복 구현이 두 계산을 미묘하게 어긋나게 만든다"가 그대로 재현됐다.

### ⚠️ 부수 발견 — TCOM ledger의 통화 라벨이 실제와 다르다

측정 중 발견해 **34종목 전수 감사**로 확대했다(`scripts/audit_ledger_currency_
2026_08_26.py`). SEC 단위별 시계열과 직접 대조한 결과 **34/34 판별 성공, 이탈
1건**: **TCOM은 선언 USD / 실제 CNY**(9/10개년 CNY와 정확히 일치, USD와는 한 해도
안 맞음). PDD는 CNY로 올바르게 라벨돼 있다.

**원인은 오타가 아니라 필드 의미 중복이다.** v3.24는 `currency`를 **재무제표
통화**로 도입했고(M-6: "PDD가 CNY 기준인데 ledger에 표시가 없던 문제") PDD가 그
관행을 따른다. TCOM 작성자는 이를 **`price_at_analysis`의 거래통화**로 썼다
(스크립트 주석에 "ADS 실제 거래통화는 USD - 재무제표 통화(RMB)와 별개로"라고
명시돼 있고, 44.77은 진짜 USD다). 그런데 **v3.67 규모 조건부 성장상한은 이를
재무제표 통화로 읽는다.**

**현재 피해는 0이다.** Gap은 시총/FCF 비율이라 통화가 약분되고(+7.55%p는 옳다),
TCOM ledger는 2026-08-04판이라 아직 v3.67을 통과한 적이 없다. 다만 규모구간이
갈린다:

| | 2015달러 매출 | 규모구간 | 상한(명목) | RG 12.39% 바인딩? |
|---|---|---|---|---|
| 잘못된 라벨(CNY를 USD로) | 46,228.9M | 25000+ | 17.87% | 아니오 |
| 올바른 라벨(CNY→USD @7.2) | 6,420.7M | 4500-7000 | **23.00%** | 아니오 |

즉 **헤드룸 5.5%p가 조용히 사라진다.** TCOM은 매수리스트 **10.36%**이고, RG가
나중에 17.87%를 넘는 방향으로 재추정되면 **단위 버그가 정당한 발견처럼 보이게
된다.**

**ledger를 수정하지 않았다** — 고치려면 `currency`의 의미를 둘 중 하나로
확정해야 하는데 그건 설계 결정이지 오타 수정이 아니다(**결정 #78 DEFER, 사용자
판단 사안**). 대신 둘을 했다:
1. **provider에 2단계 경고 배선** — (a) 복수 단위 동시보고는 정보성, (b) 채택된
   시계열이 **여러 단위에 걸치면 심각**(한 CAGR 안에서 통화가 갈리면 7배 단위로
   망가진다). 지금은 미발생이나 구조적으로 가능하다.
2. **알려진 이탈로 등록 + 회귀 테스트** — BSX 거짓탈락
   (`KNOWN_SCREENER_FALSE_REJECTIONS`) 선례와 동일하게
   `KNOWN_CURRENCY_LABEL_DIVERGENCE = {"TCOM"}`. 새 이탈이 생기거나 TCOM이
   고쳐지면 테스트가 실패해 알려준다.

### 배선하지 않은 것과 그 이유

세 L2 지표(발생액·AR추세·재작성) 전부 **`run_analysis()`에 미배선**이다(테스트로
고정). **투자 성과와의 관계가 IRS 표본에서 검증 0건**이라 배선하면 미검증 변수가
34종목 판정을 즉시 바꾼다 — growth_quality·accounting_quality와 동일 판단.
**독립성은 채택의 필요조건이지 배선의 충분조건이 아니다.**

합성 M-score/F-Score도 만들지 않았다(§31 안티기능 — 단일 합성점수). F-Score는
검증 도메인도 다르다(고 book-to-market 가치주 1976-1996 vs IRS 성장주).
Beneish DSRI는 **원문 계산식을 검증하지 못해 특정 공식을 구현하지 않고** 원자료
비율과 추세만 낸다(Sloan 원문이 스캔본이라 못 읽은 것과 같은 처리).

### 남은 한계

1. **VRT 재작성률 0.105는 SPAC 잔존 오염 가능성이 높다** — 남은 최대편차가
   491.4%인데 감사된 연간 수치가 4.9배 고쳐지는 일은 없다. 2020년 SPAC 합병 전
   껍데기 법인 재무제표가 10배 컷 아래로 살아남은 것으로 보인다. **컷을 결과에
   맞춰 조정하지 않았다**(결정 #77).
2. **AR 미확보 4종목**(ACGL·BRO·PGR·PDD) — 보험 3사는 손익·대차 구조가 달라 AR
   태그 자체가 없다(RQ-004의 `F_업종구조`와 같은 원인).
3. **재작성률 0은 '깨끗'이 아니다** — 공시가 한 번뿐인 기간은 분모에 없다.
   상장이 짧거나 XBRL 이력이 얕으면 잴 기회 자체가 없다(`multi_filed_periods`를
   함께 낸다).
4. **얇은 채권 3종목**(VRSN 0.0042 · SE 0.0236 · MNDY 0.0258) — 분모가 작아
   상대추세가 증폭된다. SE의 −18.12%/yr가 그 실례로 보인다.

### 검증

테스트 896 → **917 통과**(신규 21: L2 진단 18 + provider 3) · **34종목 전건
재실행 8개 지표 불일치 0건** · baseline fingerprint `2419df22…` **불변** ·
**ledger·매수리스트·공식 판정 0건 수정** · `ENGINE_VERSION` v3.70 → **v3.71**.

## 일일 실행 브리핑 신설 — "그래서 오늘 뭘 하나"에 답하는 단 하나의 산출물
(2026-08-28, 사용자 요청 "제발 이제는 현실에서 사용할 수 있게 좀 해줘")

### 진단 — 조각은 다 있는데 실행 가능한 형태로 합쳐진 적이 없었다

매일 자동 실행되던 것은 두 가지뿐이었고, 둘 다 **의사결정에 바로 쓸 수 없는
형태**였다:

| | 상태 |
|---|---|
| `daily_screen_ci.py` | 신규 후보 탐색 — 실측상 후보의 약 88%가 FCF-DCF 적용 불가라 대부분의 날에 "후보 없음"만 출력 |
| `daily_monitor_ci.py` | 반증조건 기한 감시 — **실제로 작동한다**(2026-08-28 실행에서 PDD 기한 도래를 실제로 잡음) |

구체적으로 세 군데가 끊겨 있었다:
1. **매수리스트에 금액이 없다.** `reports/buylist_*.json`은 `weight_final`만
   갖고 있어 그대로는 주문을 낼 수 없었다.
2. **계좌 구분이 없다.** 미국 개별주 12종목과 KRX ETF 31종목은 **서로 다른
   계좌에서만 매수 가능**한데(ISA는 국내 상장분만) 한 번도 같이 제시된 적이
   없다. KRX ETF 엔진(v3.38~)을 만든 이유가 애초에 ISA 계좌였는데도 그렇다.
3. **감시 알림이 매수 결정과 분리돼 있었다.**

`reports/deep_screen/`·`reports/watchlist/`가 **둘 다 0건**인 것도 함께
확인했다 — 자동화가 산출물을 남기지 못하고 있었다.

### `scripts/daily_brief.py` — 새 분석도 새 밸류에이션도 하지 않는다

이미 저장된 결과를 사람이 실행할 수 있는 형태로 합치기만 한다. 설계상 못박은 것:

- **네트워크 의존 0.** 저장된 파일만 읽는다. Finviz·SEC·시세 API가 전부 죽어도
  브리핑은 나온다(v3.68에서 파이프라인이 조용히 실패하던 경로를 브리핑까지
  전파시키지 않는다). 테스트가 소켓 연결을 막고 생성이 성공하는지 확인한다.
- **새 배분 규칙을 만들지 않는다.** 미국 개별주는 이미 근거가 기록된
  `weight_final`을 금액으로 환산할 뿐이고, **KRX ETF는 비중 배분 규칙이
  존재하지 않으므로 후보 순위만** 낸다. ISA 표에 '금액'·'비중' 열이 생기면
  테스트가 실패한다 — 없는 규칙을 지어내는 것을 구조적으로 막는다.
- **주수를 계산하지 않는다.** 실시간 시세를 보지 않으므로 금액까지만 낸다.
- **성과 미검증 사실을 매수표와 같은 화면에 남긴다.** "실현 수익률을 한 번도
  관측한 적이 없다(예측 34건 동결 · 해소 0건)"가 매번 하단에 붙는다.
- **감시 로직을 다시 구현하지 않는다.** `daily_monitor_ci.run_monitor()`를
  그대로 호출한다(중복 구현이 두 계산을 어긋나게 만든다는 반복 교훈).

워크플로에 `if: always()`로 배선했고, 산출물은 `reports/brief/<날짜>.md`로
커밋된다. 로컬에서는 `python -m scripts.daily_brief --capital 10000000`.

### ⚠️ 작성 중 잡은 결함 2건

1. **파일 선택 오류** — `_latest("buylist_")`가 접두어 매칭이라
   `buylist_boundary_review_2026-08-16.json`(경계검토 리포트)을 공식 매수리스트
   대신 골랐다. 그 결과 매수표가 **전 종목 0%**로 나왔다. 날짜로 끝나는 파일만
   매칭하도록 정규식으로 좁혔고 회귀 테스트로 고정했다.
2. **기본 인자 바인딩** — `_latest(prefix, folder=REPORTS)`의 기본값이 정의
   시점에 바인딩돼 모듈 상수를 바꿔도 반영되지 않았다. **PHASE 2에서 절제실험을
   조용히 무력화시켰던 것과 정확히 같은 함정**이고, 이번에도 테스트가 잡았다
   (`test_missing_inputs_degrade_gracefully`). 호출 시점 해석으로 고쳤다.

### 검증

테스트 917 → **930 통과**(신규 13) · 34종목 골든재현 8지표 불일치 0건 ·
fingerprint `2419df22…` 불변 · ledger·매수리스트·공식 판정 0건 수정 ·
`engine/` 무변경이라 `ENGINE_VERSION`은 **v3.71 그대로**(v3.32 규칙: 코드가
안 바뀌면 버전도 안 바뀐다).

### 남은 한계

- 매수리스트 기준 파일은 `buylist_2026-08-03.json`이다 — TTD 비중 축소는
  반영돼 있으나 그 이후 분석(BSX 등)은 반영돼 있지 않다.
- ISA 계좌용 KRX ETF는 **순위만** 제공한다. 얼마씩 살지는 사람이 정한다.
- 브리핑은 **실시간 시세를 보지 않는다.** Gap 부식(주가 하락에 따른 Gap 확대)은
  `watchlist_ci.py`가 시세를 확보한 날에만 별도로 보고된다.

## 신뢰도 취약점 개선 로드맵 P0-1/P0-2 실행 (2026-08-29)

기업분석 엔진 신뢰도를 객관적으로 재검토한 감사(2026-08-29)가 5개 취약점을
식별했고, 그중 자본 영향 대비 비용이 가장 싼 두 항목을 즉시 실행했다. 나머지
(P1: Evidence/GATE.RECON을 신규 분석부터 배선 / P2: Confidence 보정, 예측 해소
≥10~15건까지 보류 / P3: 52주 저점 표본편향, 구조적 한계로 인정만 함)는 로드맵
문서화만 하고 착수하지 않았다.

**P0-1 — `daily_brief.py` Confidence 무경고 노출 수정**: `confidence_score()`는
스스로 "확률이 아니다·실현결과로 보정된 적이 없다"고 명시하는데, 브리핑 매수표
열 이름이 그냥 "신뢰도"였다 — 시스템이 코드에서 반복 경계해온 패턴을 사용자
대면 화면에서 위반하고 있었다. 열 이름을 "모델점수*"로 바꾸고 표 하단에 "확률이
아니다·검증(calibration)된 적이 없다"는 각주를 추가했다. 회귀 테스트 1건.

**P0-2 — BRO `model_choice_reason` 순환참조 해소(결정 #41 실행)**: BRO의
모델선택 사유가 *"과거 큐28 기록(v3.15) 답습"*이었던 것을 2026-08-16 모델선택
연구가 이미 "원 근거 복원 불가"로 최우선 미해결 항목으로 등록해뒀는데, 실행되지
않은 채 6.75% 자본이 그 위에 서 있었다. BRO는 등급 경계(A/B, +7%p) 바로 위
(+7.43%p)에 있어 모델을 바꾸면 실제로 매수 유니버스에서 이탈하는(1축 취약,
유니버스안정 62%) 유일한 보유종목이라 이 항목이 P0-2였다.

조사 결과: 공식 Realistic Growth(12.00%, stalwart 캡값) 기준 이론기준(RG−
g_terminal=+8.53%p)만으로는 CDNS·GEN과 구분이 안 된다(2026-08-16 연구가 이미
확인한 정확한 반례 - 동일 갭에서 반대 모델 선택). 그러나 12.00%는 Lynch
캡값이지 유기적 성장 신호가 아니다(ROP·GEN 선례와 동일 원칙). 대신 회사가 직접
공시한 오가닉 성장률(2026-08-04 정성조사가 이미 확보: 7분기 연속 감속, 최근
2개 분기 마이너스 -2.8%/-0.7%)은 g_terminal(3.47%)보다 **이미 낮다** — '고성장
→terminal 수렴' 경로를 가정하는 two_stage가 아니라 '이미 정상상태'를 가정하는
single_stage(Gordon)가 BRO의 현재 실제 성장궤적에 부합한다.

`model_used=single_stage`는 **그대로 유지**했다(순환참조가 아니라 경제논리로
재확인된 것) — `model_choice_reason` 텍스트만 교체했고, Gap(+7.43%p)/RAR
(+0.3492)/DRS/판정(저평가 가능성)/등급(A)/Confidence(89)/매수비중 전부 재실행
후 diff로 불변 확인. `ledger/BRO_2026-08-04.json`→`BRO_2026-08-29.json`(git
rename, v3.32 관행), baseline 재동결(fingerprint `2419df22…`→`43f90b61…`,
BRO 1종목만 변경분으로 확인, `engine_version_at_freeze` v3.58 보존).
`tests/test_model_choice_sensitivity.py`의 `reason_is_prior_record` 단언을
True→False로 뒤집었다(회귀가 아니라 이 테스트가 원래 지목한 문제의 해소 —
BRO의 모델선택 취약성 자체는 여전히 사실이라 `buy_universe_depends_on_model`
단언은 그대로 유지).

**해소하지 않고 남긴 것**: 2026-08-16 연구가 지목한 유니버스 취약 나머지
3종목(BSX·DSGX·VRT)은 이번에 다루지 않았다 — 셋 다 아직 매수 유니버스 밖이라
순환참조 근거가 실제 자본을 쥐고 있는 상태가 아니었기 때문이다(BRO만 유일한
"보유 중 + 순환참조 근거" 조합이었음).

상세 근거는 `docs/research_decision_record.md` R-2 항목. `ENGINE_VERSION`은
그대로 v3.71(엔진 코드 무변경 - 이번 작업은 ledger 입력 텍스트 필드 하나와
문서/브리핑 표시만 수정). 테스트 930 → **931 통과**.

## v3.72 — 대규모 스크리닝(Stage 1): 34종목 손입력 큐를 SEC 등록 전체
상장기업(약 1만개)으로 확장 (2026-08-29, 사용자 요청 "34종목만 검사하잖아 -
스크리닝과 기업분석 엔진이 최상급 수준이 되게")

### 왜 - 사용자가 지목한 구조적 결함

이 프로젝트가 지금까지 다룬 종목은 전부 Trefis 52주 신저가 연재 등에서 사람이
손으로 골라온 34개뿐이었다(생존편향 있는 좁은 표본, PHASE 5 등에서 이미
문서화). 미국 SEC 등록 티커는 약 1만개 - "스크리닝"이라 부르기엔 34개는
통계적으로 무의미하다는 지적이 정확했다.

### 시가총액 데이터 접근성 실측 - 이 작업의 핵심 설계 결정

Alpha Vantage(무료 25회/일)로는 1만종목을 도저히 못 채운다. 대안 두 곳을
robots.txt 원문으로 직접 확인했다(Finviz 때 세운 원칙 그대로):
- **Yahoo Finance**(`query1.finance.yahoo.com`): 가격 API는 인증 없이 작동하나
  robots.txt가 `User-agent: *` / `Disallow: /`로 **전체 봇 차단**.
- **Stooq**: 마찬가지로 `Disallow: /`(Bing/Google만 예외) + SHA-256
  proof-of-work JS 챌린지까지 걸려 있음 - 자동화 대상 아님.

**SEC `EntityPublicFloat`**(10-K 표지, 비계열주주 보유 시총)을 근사치로 채택 -
1차자료·완전준수·무료·무제한(SEC 8req/s 안에서). 한계 세 가지를 명시
(계열주주분 제외·최대 낡을 수 있음·20-F 외국발행사 대개 미보고)하고 코드
docstring에 그대로 박아뒀다.

**2단계 설계**: Stage 1(신설, 전체 유니버스, SEC 전용) - `screen()`을 그대로
돌려 수십~수백종목으로 좁힌다. Stage 2(기존 daily_screen_ci.py 경로) - Stage 1
통과자만 정밀 실시간 시총으로 재확인한다. 이번 작업은 Stage 1까지만 배선했다.

### 신규 코드 - 새 밸류에이션 로직 0줄

- `engine/filing_dates.py::full_ticker_universe()` - SEC 전체 티커 목록(약
  1만개, ticker/CIK/title). 기존 `_ticker_map()`과 원본 JSON 캐시를 공유하도록
  리팩터링(캐시 변수명 `_TICKER_MAP_CACHE`→`_TICKER_RAW_CACHE`, 참조하던 테스트
  갱신).
- `engine/data/providers/sec.py::public_float_by_year()` /
  `public_float_from_facts()` - `EntityPublicFloat` 파싱. **`run_analysis()`가
  쓰는 `METRICS`/`FinancialFact` 경로(base.py, "AnalysisInputs 1:1" 불변조건이
  테스트로 고정돼 있음)에는 넣지 않았다** - 스크리닝 전용 근사치를 그 경로에
  섞으면 도메인 연결 테스트가 깨진다. 별도 얇은 함수로 분리.
- `engine/data/providers/sec.py`의 `METRIC_TAGS["capex"]`에
  `PaymentsToAcquireOtherPropertyPlantAndEquipment`를 **최하위(4번째)
  우선순위로 추가**(LLY 실측 - 위 세 태그 어디에도 10-K 연간치가 없었다).
  우선순위 메커니즘상(연도별로 안 채워진 경우에만 다음 태그) 순수 additive라
  34종목 골든재현 불일치 0건으로 확인.
- `scripts/broad_screen.py` - Stage 1 파이프라인. `engine.screener.screen()`/
  `engine.deep_screen._window_cagr`을 그대로 재사용.
- `scripts/broad_screen_post.py` - 결과를 별도 GitHub Issue
  스레드("📈 IRS 대규모 스크리닝 결과(주간)")에 게시(일일 스크리닝 이슈와
  분리 - 성격이 다른 정보를 섞으면 알림 피로).
- `.github/workflows/broad_screen.yml` - 매주 토요일 자동 실행(원자료가
  분기/연 단위로만 바뀌므로 매일 돌릴 이유가 없다).

### 실측 중 잡은 결함 2건 - 둘 다 첫 파일럿에서 발견

**① ASML: 시총 근사치가 17년 낡은 값을 조용히 채택.** `EntityPublicFloat`이
FY2009 값 단 하나뿐인 종목이 있었다(외국발행사가 SEC 보고의무를 축소·중단하면
XBRL 태깅이 그 시점에서 멈춘다) - `max(pf)`가 이를 "최신"으로 골라 시총을
$10.4B로 잘못 계산했고, 그 오류로 ASML이 S등급 통과로 잘못 판정됐다(15종목
파일럿에서 실제 발생). 연 1회 공시 주기의 2배(`PUBLIC_FLOAT_STALE_YEARS=2`)를
컷오프로 걸어 해소 - RESTATEMENT_MATERIAL·ENTITY_CHANGE_THRESHOLD와 동일하게
도메인 근거 기반 임계값.

**② companyfacts 중복 요청 - 300종목 파일럿이 3분 타임아웃을 초과.** 재무지표용
(`SecCompanyFactsProvider`)과 public_float용이 각자 별도로 SEC를 호출해 티커당
요청이 2배였다. `public_float_by_year`를 순수 파싱(`public_float_from_facts`)과
네트워크 조회로 분리해 한 번 받은 JSON을 재사용하도록 고쳤다 - 실측 처리속도가
0.47초/종목으로 개선(300종목 140초).

### 파일럿 실측(300종목, 유니버스 앞부분) - 신호가 실재한다는 근거

15/161 통과(9.3%) - 0%도 100%도 아닌 실제 분별력. **통과 종목 중 PGR·COR는
이 프로젝트가 이미 완전히 독립된 정밀 경로(정식 ledger 분석, 정성조사 포함)로
저평가 판정을 내린 실제 종목**이다 - PGR은 공식 ledger에서 S등급 Gap
+17.64%p(v3.67 이후), 이 Stage 1 근사 경로에서도 S등급 +14.08%p로 같은
방향·비슷한 크기가 독립적으로 재현됐다. 완전히 다른 데이터 경로(공식은
Alpha Vantage 실시간 시총 + 정성 competition_intensity, 이쪽은 public_float
근사 + corpus 중앙값)에서 같은 결론이 나온 것은 이 스크리너가 노이즈가 아니라
실제 신호를 잡고 있다는 근거다.

### 검증

테스트 931 → **953 통과**(신규 22: broad_screen 9 + broad_screen_post 5 +
filing_dates 2 + sec_provider 6). 34종목 골든재현 8지표 완전 동일, fingerprint
`43f90b61…` 불변, ledger·매수리스트·공식 판정 0건 수정. `ENGINE_VERSION`
v3.71 → **v3.72**(sec.py에 신규 함수·태그 추가 - v3.32 규칙).

### 의도적으로 하지 않은 것

- **Stage 2(정밀 재확인) 자동 연쇄 미배선** - Stage 1 통과자를 daily_screen_ci.py의
  Alpha Vantage 경로로 자동 넘기지 않는다. Stage 1 자체가 아직 1회 파일럿만
  거쳤고, 전체 유니버스 실제 실행 결과를 사람이 한 번 검토한 뒤 연결하는 게
  순서라고 판단했다(측정 먼저, 배선은 그다음).
- **이름 사전필터 정확도 개선** - 정규식 기반이라 오탐이 있다(테스트로 기록만
  해뒀다). 정확도는 무관하다 - 다운스트림(SEC 재무제표 유무)이 실제 거름망이고,
  사전필터는 순수 시간 절약용이라 이 이상 정교화할 근거가 아직 없다.
- **전체 유니버스(약 8,900종목) 실제 완주** - 파일럿은 300종목까지만
  실행했다(약 70분 예상, 이번 세션에서 실시간 확인은 안 함). 워크플로는
  배선해뒀고 다음 토요일 자동 실행이 첫 전체 실행이 된다.

## v3.73 — PIT 백테스트: 34종목 넘어 "실제로 맞았는가"를 처음 검증
(2026-08-29, 사용자 요청 "스크리닝과 기업분석 엔진이 최상급 수준이 되어서
사람의 분석보다 조금 더 나은 판단을 해줄 수 있는걸 원해")

### 왜 - v3.72(커버리지)와 나란히 요청받은 두 번째 축

사용자가 지목한 두 번째 결함: 예측 34건 동결·해소 0건, 이 엔진이 "맞았다"는
증거가 이 저장소 어디에도 없었다. 2026-08-16 §66 STOP CONDITION은 **기존
ledger를 과거 시점으로 재라벨링하는 것**을 금지했다(재작성 반영된 최신값을
"그때 알았던 값"으로 위장하는 것 - PHASE 5가 재작성 148건 중 101건이 최신판과만
일치함을 실측). 이번엔 그 경로를 쓰지 않는다 - **SEC에서 `filed<=T0`인 원자료만
새로 받아 그 시점 판정을 처음부터 재구성**한다. ledger 재사용이 아니라 진짜
과거 시점 재현이라 STOP CONDITION에 걸리지 않는다.

### 신규 코드

- `engine/data/providers/sec.py::fetch_annual_financials()`에 `as_of` 파라미터
  추가 - `filed<=as_of`인 항목만 채택. `retrieved_at`(코드를 실행한 오늘)과
  `as_of`(재현할 과거 시점 T0)를 명확히 분리했다(docstring에 명시) - **초판
  구현에서 실제로 `retrieved_at=as_of`로 잘못 넘길 뻔했고, 실행 전에 자체
  발견해 고쳤다**(테스트 `test_fetch_pit_series_uses_real_retrieved_at_not_as_of`
  로 회귀 고정). `public_float_from_facts()`에도 동일하게 `as_of` 추가.
  None(기본값)이면 기존 모든 호출부(`run_analysis`/`deep_screen`/
  `broad_screen`) 완전히 무영향.
- `scripts/pit_backtest.py` - `broad_screen.py`와 같은 300종목 유니버스
  (오늘 통과 여부와 무관하게 고정된 순서 - 사후선택 편향 차단)를 T0로
  truncate해 재채점한다. 새 밸류에이션 로직 0줄(`build_candidate`/
  `screen_all` 그대로 재사용).

### 실측 - T0=2021-06-30, 300종목 유니버스

21종목이 T0 시점 데이터만으로 "저평가 가능성"으로 플래그됐다(DELL +43.24%p·
WBD +19.93%p·BMY +17.81%p·FIX +17.52%p·**PGR +16.92%p**·... NOC +3.36%p).
**PGR이 여기서도 다시 나온다** - v3.72의 "오늘" 파일럿(300종목, Gap
+16.92%p 근방)과 이 프로젝트의 완전히 독립된 공식 ledger 분석(PGR S등급)
셋이 서로 다른 시점·다른 데이터 경로에서 같은 종목을 가리켰다.

### 실제 수익률 대조 - n=4로 중단(Alpha Vantage MCP 장애)

T0 플래그 21종목 + 비플래그 22종목(체계적 표본, 사후선택 없음) 총 43종목의
2021-06-30→2026-08-28 실현수익률을 대조하려 했으나, Alpha Vantage MCP가
세션 중 지속적으로 502(Cloudflare `origin_bad_gateway`)를 반환해(AAPL처럼
가장 기본적인 티커도 동일하게 실패 - 티커별 문제가 아니라 커넥터 자체
장애로 확인) **flagged 4종목만 확보한 채 중단됐다**:

| 종목 | T0(2021-06-30) 가격 | 최신(2026-08-28) 가격 | 실현수익률 |
|---|---:|---:|---:|
| DELL | $45.91 | $456.24 | **+893.7%** |
| FIX | $77.07 | $1,518.73 | **+1870.6%** |
| PGR | $87.71 | $218.64 | **+149.3%** |
| SCHW | $67.94 | $110.16 | **+62.1%** |

**4건 전부 실측이고 전부 강한 양(+)의 수익률이다** - 방향성은 고무적이나
**control(비플래그) 표본이 0건**이라 "T0 시점에 다른 대안보다 나았는가"는
아직 답할 수 없다(같은 기간 시장 전체가 올랐다면 flagged 아닌 종목도 올랐을
수 있다 - 비교군 없이는 신호인지 시장베타인지 구분 불가). n=4는 통계적
결론의 근거가 아니다 - "완전히 틀리지는 않았다"는 정도로만 읽을 것.

`reports/pit_backtest/pit_price_validation_2026-08-29.json`에 진행상태·
확보값·다음 단계를 정직하게 기록해뒀다(중단된 채로, 추측으로 채우지 않음).
**다음 세션에서 MCP 복구 확인 후 나머지 39종목(flagged 17 + control 22)을
`outputsize=compact`로 순차 조회해 이어서 완성할 것** - 코드·표본·T0는
전부 고정돼 있어 새로 판단할 것 없이 그대로 이어가면 된다.

### 검증

테스트 961개 전부 통과(신규 7: sec_provider `as_of` 4건 + pit_backtest 3건).
34종목 골든재현 8지표 완전 동일, fingerprint `43f90b61…` 불변, ledger·
매수리스트·공식 판정 0건 수정. `ENGINE_VERSION` v3.72 → **v3.73**.

## v3.74 — PIT 성적표 완성: 3개 T0 재현 테스트, 그리고 헤드라인이 뒤집힌 일
(2026-08-29, v3.73에 이어 같은 세션)

v3.73이 "control 표본 0건이라 아직 성적표가 아니다"로 남긴 것을 완성했다.
**표본을 고르지 않는다** - T0에 채점된 128종목 **전부**(flagged 21 +
not_flagged 107)를 쓴다. 부분표본을 뽑으면 "어느 종목을 골랐나"가 결과를
좌우하므로 고르는 행위 자체를 없앴다.

### 가격 출처 - 세 경로가 막힌 뒤 찾은 것

Alpha Vantage MCP는 502(Cloudflare origin_bad_gateway)로 세션 내내 불통이었고,
FMP `chart`는 날짜 파라미터가 유료 전용이었으며(승인 프롬프트 자체도 오류),
Yahoo·Stooq는 robots.txt가 `User-agent: *` / `Disallow: /`로 전체 봇 차단이다.
**stockanalysis.com**은 robots.txt가 `/e/`·`/p/`만 금지(2026-08-29 원문 확인)
하고, 이미 이 저장소 `source_registry`에 등록된 출처다.

⚠️ **교차확인 후 채택**: AAPL 2021-06 배당조정 종가가 stockanalysis 133.387,
FMP `historical-price-eod-dividend-adjusted` 133.39로 일치. TYL SBC 3배 오류
(2차 출처 무검증 인용)의 재발 방지 절차다. 자발적 0.7초 간격 + 디스크 캐시.

### ⭐ 단일 T0만 봤다면 정반대로 틀렸다

T0=2021-06-30 하나만 보면 압도적이다 - flagged 동일가중 **+283.1%** vs
not_flagged +104.5% vs SPY +92.3%, 중앙값 +149.1% vs +74.1%. 아웃라이어에도
강건해 보였다(절사평균 +187% vs +86%).

**그런데 섹터를 절제하니 뒤집혔다.** flagged 21종목 중 반도체/하드웨어가
6종목(29%)인데 not_flagged는 7/107(7%) - 4배 집중이다. 그 6종목을 빼면
flagged 중앙값 **+62.1%**로, not_flagged(+75.1%)와 SPY(+92.3%) **양쪽에
모두 뒤진다.** 2021~2026 AI capex 슈퍼사이클에 올라탄 섹터 베팅이었다.
(단, 같은 섹터 안에서도 flagged 반도체 중앙값 +429.5% vs not_flagged
반도체 +60.5%로 크게 앞서 - 섹터만으로는 설명되지 않는 부분도 있다. n=6이라
운일 수 있다.)

### 재현 테스트 - 3개 T0

그래서 T0를 2개 더 돌렸다(2018-06-30, 2023-06-30). 보유기간이 달라(8년/5년/3년)
**T0끼리 절대 수익률은 비교하지 않고**, 각 T0 안의 상대비교만 본다.

| 지표 | 2018(f17/n109) | 2021(f21/n107) | 2023(f27/n123) | 재현 |
|---|---|---|---|---|
| 동일가중 | ✅ +533 vs +292 | ✅ +283 vs +105 | ✅ +115 vs +112 | 3/3 |
| **중앙값** | ✅ +291 vs +173 | ✅ +149 vs +74 | ❌ **+54 vs +65** | **2/3** |
| **하위25%** | ✅ +197 vs +113 | ✅ +33 vs +28 | ✅ +30 vs +27 | **3/3** |
| **최저 종목** | ✅ +20 vs −44 | ✅ −6 vs −72 | ✅ −13 vs −62 | **3/3** |
| 벤치마크 초과비율 | ✅ 53% vs 36% | ✅ 57% vs 45% | ❌ 41% vs 42% | 2/3 |
| 상위5 제외 평균 | ✅ +242 vs +216 | ✅ +98 vs +83 | ❌ **+54 vs +79** | 2/3 |

### 결론 - 이 엔진이 실제로 하는 일

**3/3 재현되는 것은 하방 방어다.** 최저 종목과 하위25% 구간이 세 T0 전부에서
flagged가 낫다(최저: +20 vs −44, −6 vs −72, −13 vs −62). FCF수익률 기반
밸류에이션 스크린이 원리적으로 해야 하는 일 - **비싼/악화되는 기업을
피하는 것** - 과 정확히 일치한다.

**상방 선택력은 재현되지 않는다.** 중앙값·강건평균·벤치마크 초과비율은
2/3이고, 실패한 2023 코호트에서는 상위5 제외 평균이 +54% vs +79%로 **뚜렷이
뒤진다.** 2018·2021의 우위도 상위 몇 종목에 몰려 있다(상위5 제외 시 격차가
2.7배 → 1.18배로 축소).

⚠️ **"엔진이 시장을 이긴다"는 주장의 근거가 아니다.** T0당 flagged가 17~27종목,
단일 시장국면, 유니버스 앞부분 300종목(대형주 편중), 거래비용·세금 미반영이다.
p-value를 내지 않은 이유는 `engine/quant/validation.py`가 이미 측정해둔
다중검정 문제(같은 표본 9,702회 검정, 기대 위양성 485건, FWER 포화) 때문이며,
단일 T0·단일 기간이라 관측이 독립도 아니다.

**확보 실패 0종목** - 세 T0 전부에서 상장폐지·피인수로 인한 생존편향은
발생하지 않았다(유니버스 자체의 대형주 편중은 별개 한계로 남는다).

### 신규 코드

- `scripts/pit_price_validation.py` - 실현수익률 수집(배당조정 월봉, 디스크
  캐시로 다중 T0 검증을 싸게 만든다 - 캐시가 없으면 "T0 하나만 보고 결론내는"
  함정에 구조적으로 빠진다).
- `scripts/pit_scorecard.py` - 성적표. **집중도 지표(상위 k종목 제외 평균)를
  상설로 넣었다** - 평균만 보면 반드시 오독한다는 걸 이번에 실측했기 때문이다.
  섹터 분류는 코드에 하드코딩하지 않았다(근거 없는 분류체계를 상수로 박지
  않는다는 이 프로젝트의 원칙 - P/B 임계값·Lynch 캡과 동일 판단).
- `scripts/pit_multi_t0_summary.py` - 지표별 재현 횟수. 단일 T0 오독 방지가
  존재 이유다.

테스트 961 → **972 통과**. 34종목 골든재현 8지표 완전 동일, fingerprint
`43f90b61…` 불변, ledger·매수리스트·공식 판정 0건 수정.
`ENGINE_VERSION` v3.73 → **v3.74**.

### 다음에 할 것

1. **T0를 더 늘린다**(2019·2020·2022) - 지금 캐시가 있어 T0당 ~3분이면 된다.
   하방 방어 3/3이 6/6까지 가는지가 핵심.
2. **유니버스를 앞부분 300종목에서 전체로** - 대형주 편중이 지금 가장 큰
   구조적 한계다. 주간 워크플로가 전체를 돌면 그 결과로 재검증할 것.
3. 하방 방어가 계속 재현되면, 그건 매수리스트가 아니라 **제외 리스트**로
   쓰는 게 근거에 맞는 사용법일 수 있다.

## v3.75 — 6-T0 재검증 결과 정정(가설 뒤집힘) + 1차 스크리닝 "B" 게이트 신설
(2026-08-30, v3.74가 남긴 다음 단계 1·2번을 그대로 실행)

**① 6-T0(2018~2023) × 1200종목 유니버스 재검증 — v3.74 헤드라인이 뒤집혔다.**
v3.74는 300종목·3-T0로 "하방 방어(최저종목·하위25%)는 3/3 재현, 상방
선택력(중앙값·벤치마크초과율)은 2/3만 재현"이라 결론냈다. 유니버스를
1200종목·6-T0로 넓히자 **정반대**가 나왔다:

| 지표 | v3.74(300종목,3-T0) | v3.75(1200종목,6-T0) |
|---|---|---|
| 동일가중 | 3/3 | 6/6 |
| **중앙값** | 2/3 | **6/6** |
| **벤치마크 초과비율** | 2/3 | **6/6** |
| **하위25%(최악구간)** | 3/3 | **4/6** |
| **최저 종목** | 3/3 | **4/6** |
| 상위5 제외 평균 | 2/3 | 4/6 |

**중앙값·벤치마크초과율이 6/6으로 완전 재현되고, 오히려 하방 지표(하위25%·
최저종목)의 재현률이 내려갔다.** v3.74가 "다음에 할 것 1번"으로 직접 지목한
검증("하방방어 3/3이 6/6까지 가는지")을 실행한 결과가 가설과 반대로
나온 것 - 표본이 작을 때(n=17~27/T0, 3-T0) 우연히 관찰된 패턴이었을
가능성이 높다. **"제외 리스트로 쓰는 게 낫다"(v3.74 다음 단계 3번)는
제안은 이 재검증으로 근거를 잃었다** - 지금 시점에서는 상방 선택력이
오히려 더 일관되게 재현되므로 원래 프레이밍(저평가 판정 = 매수 후보)이
더 정당화된다. `reports/pit_backtest/pit_multi_t0_summary.json`이
6-T0 전체로 갱신됐다(구 3-T0 요약은 덮어씀 - 원자료 `pit_backtest_*.json`/
`pit_returns_*.json` 6개 세트는 그대로 남아있어 재현 가능).

⚠️ 여전히 미해결: 대형주 편중(SEC 티커목록 앞부분 1200종목), 단일
방법론(같은 `screen()` 판정), 거래비용은 세후계산에 반영되나 세율 등
가정 일부는 미검증(engine/after_cost.py 참고).

**⚠️⚠️ 2026-08-30 정정 — 위 6/6 재현 수치는 생존편향 위에 서 있다.**
다음 날 스크리너 적용범위를 조사하다 이 백테스트의 **유니버스 구성 자체**에
결함이 있음을 실측했다. `full_ticker_universe()`는 SEC의 **오늘자**
`company_tickers.json`을 읽는데, Alpha Vantage `LISTING_STATUS`로 대조하니:

| | |
|---|---|
| 2018-06-30 시점 상장 주식 | 5,773 |
| 그중 현재까지 상장폐지 | **2,321 (40.2%)** |
| 그중 오늘자 SEC 목록에 남은 것 | 173 (7.5%) |
| **백테스트에서 구조적으로 누락** | **2,148건** |

즉 T0에 존재했던 회사의 **40%가 생존 여부로 걸러진 채 표본에서 빠졌다.**
`pit_price_validation.py`가 세던 "가격 확보 실패"(flagged 0건)와는 **다른
층위의 문제**라 리포트상 전혀 보이지 않았다 — 그쪽은 "유니버스에 들어온 뒤
가격을 못 구한 종목"이고 이쪽은 "유니버스에 애초에 못 들어온 종목"이다.

**⚠️ 편향의 방향은 단정하지 않는다(같은 날 자체 정정).** 초판에 "죽은 회사만
빠졌으니 수익률이 위로 편향"이라 적었다가, 사라진 목록을 실제로 열어보고
**부정확함을 확인해 고쳤다** — 셋이 섞여 있다:

  - **AKORN**(2020 파산)·**ACETO**(2019 파산) → 빠지면 수익률 **위로** 편향
  - **HESS**(2024 셰브론 피인수) → 프리미엄 인수라 빠지면 **아래로** 편향
  - **ALABAMA POWER**·**SPIRE ALABAMA** → 자회사 채권 필자, 애초에 티커 없음

폐지 사유(파산 vs 피인수)를 구분할 데이터가 없다(`LISTING_STATUS`가 사유를
주지 않는다). 그러므로 확실한 것은 **"표본이 생존으로 선택됐다"**는 사실뿐이고,
그 결과 수치가 어느 쪽으로 얼마나 틀어졌는지는 **미해결**이다. 어느 방향이든
6/6 재현을 성과 근거로 인용하지 말 것.

제대로 답하려면 T0 시점 유니버스를 폐지 종목까지 포함해 재구성해야 한다 —
**병목이던 ticker→CIK 매핑은 SEC full-index로 해소했다**(v3.76 참고,
`engine/data/providers/sec_full_index.py`). 남은 벽은 폐지 종목의 **주가**로,
이건 유료 데이터라 확보 경로가 없다.
실측 재현: `scripts/diagnose_screen_scope_2026_08_30.py`.

**② 1차 스크리닝 "B" 게이트 신설 — `engine/survival_gate.py`.**
사용자와 협의해 1차 재무건전성 필터를 "A(계산가능성, 기존 유지) + B(극단적
존속위험만, 신규)"로 확정했다. **B를 설계할 때 v3.19가 이미 겪은 실패를
정면으로 피했다** - 당시 `net_debt/EBITDA`·`worst_yoy`를 하드필터로
걸었다가 BRO(레버리지 3.50배)·BSY(2.63배)를 오탈락시켰는데 둘 다 나중에
저평가로 확인된 사건이다. 원인은 이중반영 - 레버리지·매출변동성은 이미
DRS→ERP→r→내재성장률 경로로 연속반영되는데 그 위에 하드컷을 또 걸면 같은
증거로 두 번 벌점을 준다.

그래서 이번 B게이트는 **DRS가 이미 다루는 축(레버리지 비율·매출변동성·
경기민감도·마진변동성·경쟁강도)을 절대 다시 컷하지 않는다.** 대신 DRS가
원리적으로 볼 수 없는 축만 본다: **자기자본과 영업현금흐름이 동시에
마이너스**인 상태(장부와 현금 양쪽이 동시에 파산 방향을 가리키는 신호).

**왜 "자기자본 마이너스" 단독이 아닌가**: AutoZone·Domino's·Colgate·MCD류
우량기업은 공격적 자사주매입으로 장부상 자기자본이 흔히 마이너스이지만
영업현금흐름은 강하게 플러스라 재무위험과 무관하다. 단독 컷은 정확히 이런
우량주를 오탈락시켜 BRO/BSY 사고를 다른 지표로 재발시킨다. **자기자본<0
AND 영업현금흐름<0**(둘 다 최신연도 기준, 각 시계열 최신연도를 독립적으로
사용)일 때만 거른다.

`SecCompanyFactsProvider`가 이미 `shareholders_equity` 태그
(`StockholdersEquity` 등)를 지원해 새 SEC 태그 추가는 불필요했다.
`scripts/broad_screen.py`(대규모 스크리닝)와 `scripts/pit_backtest.py`(PIT
백테스트) 양쪽에 동일하게 배선했다 - 두 파이프라인이 어긋나면 PIT 검증이
실제 운영 스크리닝과 다른 걸 검증하는 셈이 된다. 데이터 미확보 시 거르지
않는다(`is_insurer`/`sbc_cross_check`와 동일한 "판단 불가 ≠ 안전" 원칙).

⚠️ **이 게이트는 아직 PIT로 재검증되지 않았다** - 방금 완료한 6-T0
백테스트는 B게이트 도입 **이전** 로직으로 돌았다. 다음 주간 스크리닝부터
자동 반영되며, 향후 PIT 재실행 시 B게이트가 실제로 존속위험 낮은 종목을
잘 거르는지(위양성 없이) 별도로 확인할 것.

테스트 1000 → **1007개 전부 통과**. 34종목 골든재현 8지표 완전 동일,
fingerprint `43f90b61…` 불변, ledger·매수리스트·공식 판정 0건 수정.
`ENGINE_VERSION` v3.74 → **v3.75**.

## v3.76 — 스크리너 적용범위 표시 + 생존편향 발견 (2026-08-30, "259개는
매수후보로 못 쓴다"에서 출발)

첫 전체 유니버스 실행이 259개 법인을 통과시켰는데 Gap 상위가 VATE($30M,
+93%p)·JANL($10M)·HTZ 같은 초소형·부실회복주였다. **임계값을 결과 보고
조정하는 것은 하지 않았다** — 이 프로젝트가 LYNCH_TYPE_CAPS·P/B 임계값·
ERP 매핑·screener competition_intensity에서 반복 거부해온 바로 그 수법이다.

### 문제의 정확한 이름은 "임계값"이 아니라 "적용범위"다

`screen()`의 임계값과 `estimate_drs()`의 상수 대체값은 **전부 34종목 ledger
코퍼스**에서 나왔다. 그 코퍼스의 실측 관측범위는 좁다:

    Gap      : -14.36%p(KEYS) ~ **+24.38%p**(ACGL)
    시가총액 : **$3.74B**(MNDY) ~ $817.9B(PDD), 중앙값 $42.6B

통과 목록에는 Gap **+93%p**(코퍼스 최대의 3.8배)와 시총 **$10M**(최소의
1/374)이 있다. BSX 거짓탈락과 같은 구조 — 그때는 중앙값에서 벗어난 **한
종목**이었고 지금은 코퍼스 **범위 자체를 벗어난 집단**이다.

### ⭐ 실측으로 답하려다 도구가 편향돼 있음을 발견했다

"범위 밖에서도 스크린이 작동했는가"를 PIT 6개 T0로 재봤다. 구간 경계는
**수익률을 보기 전에** 코퍼스 관측범위로 고정했다(사후조정 방지):

| 구간 | n | 중앙 수익률 | 플러스비율 |
|---|---:|---:|---:|
| Gap ≤ +24.38%p | 342 | +97.7% | 88% |
| Gap > +24.38%p (외삽) | 73 | +92.2% | 79% |
| 시총 ≥ $3.74B | 334 | +76.0% | 85% |
| 시총 < $3.74B (외삽) | 81 | **+237.7%** | 93% |

**가설이 기각됐다** — 소형주가 오히려 훨씬 나았다. 그런데 이 수치를 그대로
믿기 전에 생존편향을 확인하니 **결과 자체가 무효였다**: 백테스트 유니버스가
오늘자 SEC 티커목록 기반이라 **T0 시점 상장주식의 40.2%(2,321건)가 이후
폐지됐고 그중 2,148건은 오늘 목록에 없어 애초에 채점되지 못했다.** 표본이
생존 여부로 선택된 것이라 **범위 밖 구간의 유효성은 현재 실측 불가 상태다**
(v3.75 항목에 정정 기록 추가).

⚠️ **편향의 방향은 단정하지 않는다** — 사라진 목록에 파산(AKORN·ACETO,
위로 편향)과 피인수(HESS, 아래로 편향)가 섞여 있고 폐지 사유를 구분할
데이터가 없다. 초판에 "죽은 회사만 빠져 위로 편향"이라 적었다가 실제
목록을 열어보고 정정했다.

⚠️ 이건 `pit_price_validation.py`가 세던 "가격 확보 실패"(flagged 0건)와
**다른 층위**라 리포트상 전혀 보이지 않았다 — "유니버스에 들어온 뒤 가격을
못 구함" vs "유니버스에 애초에 못 들어옴".

### 그래서 무엇을 했나 — 거르지 않고 표시만 한다

`engine/validated_scope.py` 신설. `out_of_scope_reasons(gap, market_cap)`이
코퍼스 관측범위를 벗어난 축을 **사유 문자열**로 돌려준다. `is_insurer`·
`sbc_cross_check`·`holdings_overlap`과 동일한 "병기, 자동판정 안 함" 원칙 —
범위 밖은 **"틀렸다"가 아니라 "확인된 적이 없다"**는 뜻이다.

**컷오프를 새로 만들지 않았는데도 실효가 크다**: 259개 → **범위 안 86개 /
범위 밖 173개**(Gap 축 67건, 시총 축 162건, 중복 포함). 범위 안 상위가
완전히 달라진다 — CROX·LNTH·EQT·**ACGL**·SIGI·OKTA·CDE·MEDP·COP·RYAN·
FIX·CHDN. **ACGL은 정식 ledger에서 이미 S등급인 종목**이라, 완전히 독립된
경로(public_float 근사 + corpus 중앙값)에서 같은 종목을 재발견한 교차검증
사례다.

거르는 함수를 두지 않는 것 자체를 테스트로 고정했다
(`test_module_exposes_no_filter_or_verdict_function` — 이름에 filter/reject/
verdict 등이 들어간 공개 함수가 생기면 실패). bool이 아니라 사유 리스트를
반환하는 것도 고정했다(bool이면 호출부가 곧바로 필터로 쓰게 된다).

### 남은 것

범위 밖 173개의 유효성은 **T0 유니버스를 상장폐지 종목까지 포함해 재구성
해야** 답할 수 있다. 병목은 delisted 종목의 ticker→CIK 매핑(SEC 현재 파일에
없음). 원자료는 `data/listing_status/`에 보관했다.

테스트 1,012 → **1,024개 통과**. 34종목 골든재현 8지표 완전 동일,
fingerprint `43f90b61…` 불변, ledger·매수리스트·공식 판정 0건 수정.
`ENGINE_VERSION` v3.75 → **v3.76**.

## v3.77 — 생존편향 실측: 백테스트가 flagged의 1/3만 보고 있었다 (2026-08-30)

v3.76이 "생존편향이 있다"까지 밝히고 **크기는 미해결**로 남긴 것을 실제로 쟀다.

### 병목이던 ticker→CIK 매핑을 SEC full-index로 뚫었다

폐지 종목의 CIK를 얻는 두 경로를 비교했다:

| 경로 | 결과 |
|---|---|
| SEC `cik-lookup-data.txt` 이름 매칭 | 단일 CIK 확정 **65.9%**뿐(모호 18.2%·미매칭 15.9%) — 불충분 |
| **SEC full-index (`company.idx`)** | **CIK 직접 제공, 매칭 불필요** |

`engine/data/providers/sec_full_index.py` 신설. 분기별 색인에서 연차보고서
제출 CIK를 뽑는다 — 서식 집합은 `ANNUAL_FORMS`를 **그대로 재사용**해(복제
금지) 유니버스 정의와 스크리너가 읽는 데이터가 어긋나지 않게 했고,
`filed <= as_of`로 미래 제출분을 배제한다.

### ⭐ 실측 — 사라진 집단도 같은 비율로 저평가 판정을 받는다

`scripts/measure_survivorship_bias_2026_08_30.py`가 **백테스트가 못 본 집단을
직접 채점**했다(표본은 CIK 오름차순 앞 600개 — CIK는 등록 시점 부여라 생존
여부와 무관해 사후선택이 원천 불가능):

| | 보이지 않는 집단 | 현재 백테스트 |
|---|---|---|
| 채점 | 114 | 375 |
| 저평가 판정 | **18** | 56 |
| **통과율** | **15.8%** | **14.9%** |

**거의 동일하다.** 즉 사라진 4,118개도 같은 비율로 판정을 받았을 것이고,
단순 비례로 확대하면 놓친 판정이 **약 110건 — 보이는 56건의 2배**다.
백테스트는 flagged 모집단의 **약 1/3만** 보고 있었다.

### ⭐ 편향 방향은 여전히 단정할 수 없다 — 명단이 그 이유다

| 놓친 판정 | Gap | 시총 | 이후 |
|---|---:|---:|---|
| WESTMORELAND COAL | +56.33%p | $0.09B | 파산 |
| LANNETT | +41.72%p | $0.61B | 파산 |
| AKORN | +20.08%p | $2.32B | 파산 |
| **ALLEGHANY** | +37.71%p | $8.82B | **피인수(2022-10-28)** |
| **CELGENE** | +11.50%p | **$101.58B** | **피인수(2019-11-22)** |
| COHERENT·IDT·CAMBREX·ELECTRO SCIENTIFIC | +7~14%p | $0.4~3.9B | 피인수 |

**"소형주가 주로 빠졌다"는 내 가정도 틀렸다** — CELGENE $101.58B가 반증이다.
사라진 집단은 규모 전 구간에 걸쳐 있고, 파산과 프리미엄 피인수가 섞여 있어
방향을 정할 수 없다(`LISTING_STATUS`가 폐지 사유를 주지 않는다).

### 측정 불가를 **구간**으로 바꿨다

`scripts/bound_backtest_performance_2026_08_30.py`. 폐지 종목 주가는 유료
데이터라 못 구하지만, **놓친 판정의 개수를 알므로** 극단 가정으로 보고값이
얼마나 움직이는지 묶을 수 있다(T0=2018-06-30, 벤치마크 SPY +220.5%):

| 시나리오 | 중앙값 | 평균 |
|---|---:|---:|
| 현재 보고값(보이는 것만) | +163.7% | +251.8% |
| 최악: 놓친 것 전부 −100% | **+95.3%** | +173.6% |
| 중립: 놓친 것 = 벤치마크 | **+206.1%** | +244.8% |
| 최선: 놓친 것 = 보이는 중앙값 | +163.7% | +232.2% |

**중앙값 구간 +95.3% ~ +206.1%.** 어느 시나리오에서도 벤치마크 초과비율은
32% → 25%로 내려간다. ⚠️ 이건 **하한**이다 — 4,118개 중 600개만 표본조사했다.

### ⚠️ 작업 중 자체 발견한 결함 — T0 이전 폐지 기업이 유니버스에 들어왔다

WESTMORELAND COAL(폐지 2018-04-24)·MICROSEMI(2018-05-29)가 T0=2018-06-30
유니버스에 있었다. 직전 12개월에 10-K를 냈지만 **T0 시점엔 이미 살 수 없는
종목**이다 — "그 시점에 재무제표가 있었는가"와 "그 시점에 투자 가능했는가"는
다른 질문이고 유니버스는 후자여야 한다. `universe_at(delisted_before=...)`로
고쳤고 회귀 테스트로 고정했다. 폐지 정보 출처가 CIK를 주지 않으므로 매핑
책임은 호출부에 뒀다(이 모듈이 특정 출처에 의존하지 않게).

### 결론 — 무엇이 확정되고 무엇이 남았나

**확정**: PIT 백테스트의 flagged 표본은 약 1/3만 담고 있다. v3.75의 "6/6
재현"과 v3.76의 소형주 +237.7%는 **성과 근거로 인용할 수 없다.**

**미해결**: 폐지 종목 주가(유료), 폐지 사유 구분, T0=2018 외 5개 T0.
근본 해결은 CRSP류 유료 데이터 없이는 불가능하다 — 그때까지는 이 구간
추정이 도달 가능한 한계다.

테스트 1,034 → **1,037개 통과**. 34종목 골든재현 8지표 완전 동일,
fingerprint `43f90b61…` 불변, ledger·매수리스트·공식 판정 0건 수정.
`ENGINE_VERSION` v3.76 → **v3.77**.

## v3.78 — 생존편향 6개 T0 전수 측정: 우위는 살아남지만 절반으로 줄고 2/6에서
뒤집힌다 (2026-08-30)

v3.77이 T0=2018 하나로 낸 결론을 6개 T0 전부로 확장했다. 그 과정에서 **내
계산의 비대칭과 도구 결함을 각각 하나씩 잡았다.**

### ⚠️ ① 도구 결함 — full-index 부분 실패를 조용히 삼키고 있었다

T0=2022 유니버스가 **2,867개**로 나왔다(다른 T0는 7,149~8,730). 1년 만에 62%
줄어들 수 없다. 원인은 `universe_at()`이 분기 색인 조회 실패를 `except`로
삼킨 것 — 재조회하니 네 분기 전부 HTTP 200이라 **일시적 실패**였고 출력에는
아무 표시가 없었다.

**편향을 측정하는 도구가 편향을 축소 보고하고 있었다** — 유니버스가 작아지면
"사라진 기업"이 적게 잡히기 때문이다. 실측 차이:

| T0=2022 | 결함 상태 | 수정 후 |
|---|---:|---:|
| 유니버스 | 2,867 | **8,730** |
| 안보임 | 1,171 | **3,722** |
| 전수추정 놓친 판정 | 18 | **105** |

**5.8배 과소평가.** 재시도 3회 후에도 실패하면 `RuntimeError`를 던지고,
계속하려면 `allow_partial=True`를 명시하게 고쳤다.

⚠️ 이건 같은 패턴의 **세 번째 재현**이다: v3.68(daily_screen이 SEC 실패를
"후보 없음"으로 보고) → 같은 날 오전(감시 스크립트가 프록시 403을 "실행
안 됨"으로 오독) → 이번. 전부 **"장애가 정상 결과와 구별되지 않는다"**가
뿌리다. 기존 테스트 `test_one_failing_quarter_does_not_abort_the_rest`가
바로 그 결함 동작을 고정하고 있어 의도에 맞게 갱신했다(회귀가 아니라
테스트가 지목하던 문제의 해소 — BRO `model_choice_reason` 때와 같은 상황).

### ⚠️ ② 내 계산의 비대칭 — 놓친 flagged만 더하고 있었다

초판 시나리오는 놓친 **flagged**만 더했다. 그런데 사라진 집단에는
not_flagged도 같이 있었다(T0=2018 기준 채점 114 중 flagged 18, **not_flagged
96**). 한쪽만 더하면 flagged에 일방적으로 불리해져 "flagged가 더 나았는가"라는
원래 질문이 무너진다. 두 그룹에 같은 가정을 넣는 `symmetric` 시나리오를 추가했다.

### ⭐ 최종 결과 — flagged 중앙값 우위(%p), 두 그룹 대칭 보정

| T0 | 보고값 | 최악(양쪽 −100%) | 중립(양쪽 벤치마크) |
|---|---:|---:|---:|
| 2018-06-30 | +14.2%p | **−18.1%p** | +5.4%p |
| 2019-06-30 | +40.4%p | +27.9%p | +12.5%p |
| 2020-06-30 | +55.4%p | +39.4%p | +0.0%p |
| 2021-06-30 | +14.2%p | +17.5%p | +3.9%p |
| 2022-06-30 | +22.1%p | +2.0%p | +16.7%p |
| 2023-06-30 | +4.4%p | **−1.8%p** | +7.7%p |

**결론 세 줄**:
1. **우위는 생존편향의 산물이 아니다** — 최악의 대칭 가정에서도 **4/6**에서
   유지된다(보고값 기준으로는 6/6).
2. **그러나 크기는 절반 이하로 줄고**, 2018·2023 두 T0에서는 **부호가
   뒤집힌다.** v3.75의 "6/6 재현"을 그대로 인용하면 안 되는 이유다.
3. 놓친/보이는 flagged 비율은 **0.9~2.0배(중앙값 1.2배)** — 백테스트는
   flagged 모집단의 절반 남짓만 보고 있었다.

⚠️ 2020 중립 시나리오의 `+0.0%p`는 **퇴화 사례**다 — 채워 넣는 값(벤치마크)이
양쪽 중앙값을 동시에 잠식해 차이가 정확히 0이 된 것이지, 우위가 사라졌다는
관측이 아니다.

### 남은 한계

- 구간은 전부 **하한**이다(사라진 집단 중 600개만 표본조사).
- 폐지 사유(파산 vs 프리미엄 피인수)를 구분할 데이터가 없다 — 명단에
  AKORN·LANNETT(파산)과 ALLEGHANY·CELGENE(피인수)이 함께 있다.
- 근본 해결은 폐지 종목 주가(CRSP류 유료 데이터)가 있어야 가능하다.

테스트 1,037 → **1,040개 통과**. 34종목 골든재현 8지표 완전 동일,
fingerprint `43f90b61…` 불변, ledger·매수리스트·공식 판정 0건 수정.
`ENGINE_VERSION` v3.77 → **v3.78**.

## v3.79 — 스크리닝↔매수리스트 연결: 연구 우선순위 큐 (2026-08-30, 사용자 요청
"창의적이지만 완성도높은 아이디어 여러 개 짜고 가장 괜찮은 것 추진")

### 먼저 실측 — 연결 지점의 실제 크기

| | |
|---|---|
| 주간 스크린 통과(중복제거) | 259개 법인 |
| 그중 검증 코퍼스 관측범위 안 | 86 |
| 이미 ledger 정식분석 완료 | 5 (ACGL·BSY·COR·GEN·PGR) |
| 이미 매수리스트 보유 | 3 (ACGL·GEN·PGR) |
| **한 번도 분석 안 된 신규** | **81** |
| ledger 34종목 중 이번 스크린 통과 | **5** (29종목 미통과) |

마지막 줄이 중요하다 — 스크린과 정식분석은 **상당히 다른 답**을 낸다(추정 DRS
vs 정성조사 DRS, public_float 근사 vs 실측 시총). 두 Gap을 직접 비교하는 설계는
전부 사과-오렌지가 된다.

### 검토한 5개 안과 기각 사유

| 안 | 판정 |
|---|---|
| A. 자동 승격(스크린 통과자를 매수리스트 유니버스에 편입) | **기각** — `run_analysis()`는 `model_choice_reason`·`subjective_input_basis` 없이 실행을 거부하고, `build_buylist`는 종목별 `BUCKET`·`CONFIDENCE_ADJ`(정성조사 결과)를 요구한다. 자동 편입은 그 빈칸을 지어내는 것 |
| B. 섀도 포트폴리오(스크린 전용 병행 추적) | 기각 — 위험 0이지만 몇 년 뒤에나 값이 나온다. 지금 병목을 못 푼다 |
| C. 두 트랙 병기(정식/후보 나란히) | 기각 — 연결이 아니라 나열. 81개를 그냥 보여주면 여전히 못 쓴다 |
| D. 교체 후보(보유 종목별 더 나은 후보 매칭) | 기각 — 스크린 Gap과 ledger Gap이 다른 계산 경로다(위 5/34가 증거) |
| **E. 지속성 기반 연구 우선순위 큐** | **채택** |

### 왜 E인가 — 진짜 병목은 "다음에 무엇을 분석할 것인가"다

정식 분석 한 건에 사람의 주관적 입력과 정성조사가 들어가므로 81개를 다 할 수
없다. **연결의 정체는 "자동 승격"이 아니라 "분석 우선순위"다.**

그리고 E에는 **지금 버려지던 신호**가 있다 — 주간 스크린은 매번 결과를 새로
쓰고 이전 주와 대조하지 않는데, **4주 연속 통과와 한 주 반짝 통과는 전혀
다르다**(후자는 시총 근사 오차나 일시적 재무 왜곡일 수 있다). 큐가 실행 이력을
누적하면 그 구분이 공짜로 생긴다.

### ⚠️ 합성 점수를 만들지 않았다

우선순위를 숫자 하나로 합치지 않는다(§31 안티기능 등록부). **객관적 사실의
사전식 정렬**을 쓴다: ①검증범위 안 → ②미분석 → ③연속통과 횟수 → ④Gap.
각 단계가 확인 가능한 사실이고 가중치가 없다. Gap을 맨 뒤에 둔 이유는 그
값이 이 파이프라인에서 **가장 거친 추정치**이기 때문이다.

상태(IN_BUYLIST/ANALYZED/QUEUED)도 **ledger·매수리스트에서 파생**한다 —
`monitor/acknowledgements.json` 같은 사람이 유지하는 상태 파일을 하나 더
만들면 낡는다.

### ⭐ 첫 실행에서 즉시 잡힌 결함 — "필드 없음"을 "범위 안"으로 읽었다

첫 실행의 최우선 분석 후보가 **VATE(Gap +93%p, 시총 $0.03B)**로 나왔다. 명백히
범위 밖인데 "검증범위 안"으로 찍혔다. 원인: 이 스크린 결과 파일은
`out_of_validated_scope` 필드 추가(v3.76) **이전**에 생성돼 키가 없는데,
`row.get(...) or []`가 그것을 빈 리스트(=범위 안)로 읽었다.

**데이터 없음을 안전으로 오독하는 이 프로젝트의 반복 패턴**이라(is_insurer·
sbc_cross_check·holdings_overlap이 매번 경계해온 것), 키가 **없을 때만**
gap·시총으로 재계산하도록 고쳤다(둘 다 이미 갖고 있다). 이미 계산된 빈
리스트는 그대로 믿는다 — 회귀 테스트로 양쪽을 고정했다.

수정 후 상위 목록이 완전히 달라진다: CROX +23.97%p($4.8B) · LNTH · EQT ·
SIGI · OKTA · CDE · MEDP · COP · RYAN · FIX — **259개가 실제로 쓸 수 있는
15개 연구 목록**이 됐다.

### 배선

- `engine/research_queue.py`(순수 함수) + `scripts/update_research_queue.py`
- `broad_screen.yml`이 스크리닝 직후 큐를 갱신하고 `reports/research_queue.json`을 커밋
- `daily_brief.py`에 "다음에 분석할 종목" 섹션 — **큐가 결정 경로에 도달하는
  유일한 지점**이다(리포트에만 있으면 아무도 안 본다)

⚠️ **매수리스트는 건드리지 않는다.** 큐는 사이징도 판정도 내놓지 않으며, 그
경계를 테스트로 고정했다(`weight`/`size`/`buy`/`verdict` 류 공개 함수가
생기면 실패). 브리핑 표에 '비중'·'금액' 열이 생겨도 실패한다.

⚠️ **지속성 축은 오늘 아무것도 구분하지 못한다** — 스크린 실행이 1회뿐이라
전 종목이 `times_seen=1`이다. `persistence.discriminating=False`로 그 사실을
표시하고 브리핑에도 경고를 띄운다(없는 신호를 있는 척하지 않는다).

테스트 1,040 → **1,058개 통과**. 34종목 골든재현 8지표 완전 동일,
fingerprint `43f90b61…` 불변, ledger·매수리스트 0건 수정.
`ENGINE_VERSION` v3.78 → **v3.79**.

## 날짜별 이슈 + 제목 긴급도 — 알림을 열어볼지 제목만 보고 정할 수 있게
(2026-09-01, 사용자 요청 "일일 스크리닝 결과를 매일 다른 이슈로 알려줘.
제목에는 날짜와 긴급도를 포함해 / 주 스크리닝결과는 다른 제목으로")

### 무엇이 문제였나

자동 실행 4종(일일 스크리닝·감시 대체경로·관심종목·브리핑)이 전부 **제목이
고정된 이슈 하나**(`📊 IRS 일일 스크리닝 결과`)에 댓글을 쌓고 있었다. 그래서:

  1. **폰 알림 제목이 매일 똑같아** 열어보기 전에는 오늘 볼 게 있는지 없는지
     알 수 없다. 며칠 뒤부터는 안 열게 되고, 그게 v3.64가 막으려던 알림 피로다.
  2. 하루치 기록을 찾으려면 수백 개 댓글을 스크롤해야 한다.

이제 **하루에 이슈 하나**이고 제목이 날짜·종류·긴급도를 담는다:

    2026-09-01 · 📊 일일 스크리닝 · 🛑 긴급 (감시 3건)
    2026-08-30 · 📈 주간 대규모 스크리닝 · 🔵 후보 (범위 안 12종목)

### 하루 알림 1건은 그대로 유지한다

이슈를 4개 만들지 않는다 — 그날 **먼저 도는 쪽이 만들고** 나머지는 제목
앞부분(`날짜 · 마커`)으로 찾아 댓글을 단다. 각자 만들면 하루 알림이 4건이
되어 1번 문제가 형태만 바꿔 재발한다.

### ⚠️ 긴급도는 올라가기만 한다

감시가 `🛑 긴급`으로 만들어둔 제목을 뒤이어 도는 브리핑이 `⚪ 정상`으로 덮으면
**이미 발견한 조치사항이 제목에서 사라진다** — 이 프로젝트가 반복 경계해온
"미확인·데이터 없음을 안전으로 표시하는" 실패의 제목판이다. 그래서 브리핑·
관심종목은 아예 `routine`으로만 올리고(새 사실을 발견하지 않는 단계다),
`escalates()`가 하강 갱신을 구조적으로 막는다.

### 긴급도 서열의 근거

    action     🛑 긴급  — 반증조건 기한도래·예측 해소기한(사람이 볼 것)
    broken     🔧 장애  — 파이프라인이 실행되지 못함(정상적인 빈 결과와 다름)
    candidates 🔵 후보  — 통과 후보가 나옴
    routine    ⚪ 정상

**감시(action)가 장애(broken)보다 위**인 이유: 감시는 네트워크 의존이 전혀
없어(ledger·날짜만 본다) 스크리닝이 통째로 죽어도 정상 작동한다(v3.64). 즉
감시가 찾은 조치사항은 파이프라인 상태와 무관하게 확실한 사실이고, 장애는
"오늘 결과를 못 얻었다"는 뜻이라 조치 우선순위가 낮다. `scored == 0`도 장애로
센다 — v3.68이 확립한 대로 "통과 후보 없음"과 구분돼야 한다(`scored`가 None인
경우, 즉 스크리닝이 아예 안 돈 경우는 장애로 세지 않는다).

**주간 스크리닝 제목의 후보 수는 검증범위 안만 센다** — 2026-08-30 첫 전체
실행에서 통과 259종목 중 173종목이 코퍼스 관측범위 밖이었다. 259를 제목에
적으면 실제로 볼 게 얼마인지 알림에서 알 수 없다. 본문 표도 같은 이유로
범위 안/밖을 분리했다(섞어 정렬하면 Gap이 큰 초소형주가 늘 맨 위에 온다 —
첫 실행에서 실제로 VATE +93%p/$0.03B가 1위였다).

### ⚠️ 작성 중 잡은 결함 — 키 이름이 어긋나 건수가 조용히 0이 될 뻔했다

`daily_urgency()`가 감시 결과에서 조치 건수를 읽는데, 초판이 키를 `thesis`로
적었다. 실제 `run_monitor()` 반환 키는 `falsification`이다 — 예외가 나지
않고 **조용히 0건**이 되어 제목이 `(감시 3건)` 대신 밋밋한 `감시 조치필요`로
나온다. R-001 감사에서 `fcf0` 키 오타 하나로 사전등록 6축 중 1축이 통째로
죽어 있던 것과 **정확히 같은 유형**이다. 가짜 dict로 테스트했으면 못 잡았을
것이라, 실제 `run_monitor()`를 돌려 키 존재와 건수 집계를 확인하는 테스트로
고정했다(`test_daily_urgency_reads_real_monitor_keys`).

### 라벨은 붙이지 않았다

제목이 이미 날짜·종류·긴급도를 전부 담아 라벨이 추가로 거르는 게 없고, 라벨
생성 실패라는 실패 지점만 늘어난다(Simplicity First — 실증된 필요만큼만).

### 그 밖의 설계 결정

- **닫힌 이슈도 재사용한다**(`state=all`로 조회) — 사람이 오늘 이슈를 일부러
  닫았는데 자동화가 새로 만들면 되살리는 셈이 된다(닫힌 이슈에도 댓글은 달린다).
- 게시 실패는 **예외를 던지지 않는다** — 게시는 부가기능이고, 게시가 죽었다고
  스크리닝·감시 본체가 멈추면 부가기능 하나가 본체를 멈추는 것이다.

`engine/` 무변경이라 `ENGINE_VERSION`은 **v3.79 그대로**(v3.32 규칙: 코드가
안 바뀌면 버전도 안 바뀐다). 테스트 1,058 → **1,080개**(신규 24, 이관·정리 2),
34종목 골든재현 8지표 완전 동일, fingerprint `43f90b61…` 불변, ledger·
매수리스트·공식 판정 0건 수정.

## CROX 정식 분석 — 연구 우선순위 큐 1호 실행, 그리고 잠복 버그 3건
(2026-09-01, 사용자 요청 "최대한효율적으로 분석실행")

### 결과 — 스크리너 추정치가 크게 틀렸다, 원인이 정확히 예측한 대로였다

`reports/research_queue.json` 1순위(스크린 Gap 추정 +23.97%p, S등급)를
SEC XBRL 1차자료 + 실제 시가총액 + 실측 경쟁구도로 정식 분석했다.

| | 스크리너 추정 | 정식분석 |
|---|---|---|
| Gap | +23.97%p | **+9.89%p** |
| 등급 | S | **A** |
| Realistic Growth | ~24%(5y CAGR 그대로) | **10.34%** |

**원인은 M&A 단계상승이었다**(GEN/BRO/ROP/SNPS/NRG/RBA/NOW/AVGO와 동일
패턴) — HEYDUDE 인수(2022-02 종결)가 5y CAGR 구간(2020->2025) 한가운데
걸려 매출 5y CAGR이 **23.86%**로 나오는데(스크리너가 그대로 인용한 값과
정확히 일치), 인수 종결 이후로만 깨끗한 3y CAGR(2022->2025)은 **4.36%**
뿐이다. `realistic_growth_estimate()`의 3y/5y/10y 가중평균(0.5/0.3/0.2)이
3y에 과반 가중치를 줘 왜곡을 상당 부분 자동 흡수했다(10.34%는 4.36%와
23.86% 사이) - `cagr_base_year_override`는 쓰지 않았다(세그먼트 완전분리
데이터 없이 3y 단독 강제는 근거 부족).

부수 발견: FY2025 영업이익 붕괴($1,021.9M->$149.5M, -85.4%)는 HEYDUDE
상표권·영업권 손상차손 $738.1M(전부 비현금, 회사 공시로 확인) - 영업현금
흐름은 견조(FY2025 $710.4M)했다. 손상차손 조정 없이 GAAP 그대로 썼다
(margin_volatility·leverage 양쪽 다 - is_insurer/sbc_cross_check와 동일
"임의 정규화 안 함" 원칙). PIT_VALID(위반 0건), SBC 차감 시에도 판정
불변(+9.31%p), 강건성점검 flip 없음, Confidence 94.

### ⭐ 실데이터가 처음 들어오면서 드러난 잠복 버그 3건 (R-001/PHASE5와 같은 계열)

**① `inputs_from_ledger()`가 `filing_dates_by_year`를 정수로 안 돌려주고
있었다.** v3.47(PIT)이 이 필드를 만든 뒤 실제로 채운 ledger가 CROX가
처음이라(v3.49~v3.72가 반복 기록한 "PIT 필드는 있는데 34종목 전부
미기입") 이 경로가 한 번도 실행되지 않았다 - `_YEAR_KEYED_FIELDS`
(engine/thesis_monitor.py)에 목록을 추가해 해소했다.

**② `sbc_harvest_2026-08-21.json`은 "SBC 미확보 25종목"이 아니라 그 시점
ledger 전수(34종목) 스냅샷이었다.** 새 ledger가 추가되면 문자 그대로
`==` 비교가 깨진다 - BSX 거짓탈락·TCOM 통화라벨과 동일한 "알려진 예외"
패턴(`KNOWN_POST_SNAPSHOT_LEDGERS`)으로 등록. 겸사겸사 `main()`이 인라인
으로 쓰던 "sbc_cross_check 없는 것만" 필터를 `_missing_sbc_ledgers()`로
뽑아 재사용 가능하게 했다.

**③ `test_all_existing_ledgers_are_provenance_unknown`이 "저장소의 모든
ledger는 UNKNOWN"을 문자 그대로 강제하고 있었다.** v3.59가 처음부터
provenance를 채운 정당한 신규 분석까지 막을 근거는 없다(§6이 금지하는
건 **소급** 생성이지 신규 분석의 provenance가 아니다) - 동일한 "알려진
예외" 패턴(`KNOWN_PROVENANCE_RECORDED_LEDGERS`)으로 CROX를 등록했다.

셋 다 "34종목 고정"을 문자 그대로 코드에 박아둔 채 아무도 35번째를
넣어본 적이 없어서 드러나지 않았던 결함이다 - 이 프로젝트가 반복 겪은
"실데이터가 들어와야만 드러나는 잠복 결함"의 다섯 번째(R-001 fcf0 오타·
PHASE 1 fcf0 오타 재발견·PHASE 2 기본인자 바인딩·PHASE 5 필드경로 오류에
이어)이자 규모로는 최대(3건 동시 발견)다.

### 배선

- `watchlist.json`에 CROX 추가(34->35, ledger 전수 자동추적 원칙 유지).
- `reports/research_queue.json` 재생성 - CROX가 `QUEUED`->`ANALYZED`로
  전환되며 큐 1순위에서 자동으로 빠졌다(LNTH가 새 1순위).
- **매수리스트는 건드리지 않았다** - `run_analysis()`는 여기까지만
  하고, 편입에는 `BUCKET`·`CONFIDENCE_ADJ`(정성 심층조사)가 별도로
  필요하다(v3.79가 이미 확립한 경계, research_queue.py의 "자동 승격
  경로는 원리적으로 없다" 원칙 그대로).

### 검증

baseline을 35종목으로 재동결(`--freeze`, `engine_version_at_freeze`는
v3.58 보존 - v3.68이 고친 대로). fingerprint `43f90b61…` ->
**`81a8a0f5…`**(34종목 기존 값 전부 불변 확인 후 CROX 추가분만 반영).
테스트 1,080개 전부 통과(신규 실패 3건을 위 버그 수정으로 해소, 신규
테스트 추가 없이 기존 불변조건을 35종목 현실에 맞게 정정). `ledger/`에
CROX 1건 신규 추가(그 외 34종목 0건 수정), `engine/` 변경(thesis_monitor.py
버그수정)이라 `ENGINE_VERSION` v3.79 -> **v3.80**.

## LNTH 제외 — 정식분석 대상 아님(피인수 확정, 진행중 인수합병) (2026-09-01)

연구 우선순위 큐 2순위(스크린 Gap 추정 +22.96%p, B등급)를 조사하던 중,
2026-08-03에 **Curium이 Lantheus를 인수하는 확정 계약**이 체결됐음을 확인했다
(WebSearch: CNBC/Curium 공식발표/GlobeNewswire, $102.50/주 현금 + 최대 $12.00/주
CVR(총 최대 $114.50/주, 거래총액 최대 ~$80억), 2027년 상반기 종결 예정, 주주·
규제 승인 대기 중).

**FRAMEWORK_MISMATCH로 분류하고 ledger를 만들지 않았다** - 이 프로젝트가 이미
확립한 원칙(PODD/ONON/MU/RBA/NOW/AVGO 등)과 같은 계열이나 사유는 새롭다: 확정된
피인수 대상의 주가는 회사의 독립적 성장전망이 아니라 **딜 조건(현금+CVR)과
거래 성사 확률·시점**을 반영한다. Implied Growth(시가총액 역산)가 "시장이 이
회사의 성장을 얼마로 보는가"가 아니라 "시장이 딜이 예정대로 끝날 확률을 얼마로
보는가"를 담게 되므로, Gap을 계산해도 저평가/과대평가 판정이 성립하지 않는다 -
정확히는 "이 인수가격이 공정한가"라는 전혀 다른 질문이 되어버린다.

**부수 발견 - 데이터 함정 하나 (엔진 코드는 안 고침, n=1)**: 조사 과정에서
SEC XBRL `PaymentsToAcquireProductiveAssets`(METRIC_TAGS 1순위 capex 태그,
v3.60에서 MCK 사례로 승격)가 LNTH FY2022에 **$260.0M**을 담고 있었는데, 이는
실제 PP&E capex가 아니라 **POINT Biopharma 라이선스 계약의 IPR&D 일시상각
비용**(R&D비용으로 손익계산서에 계상, 대체 용도 없는 취득 IPR&D는 GAAP상 즉시
비용화)이었다(WebSearch로 10-K 본문 확인). 실제 그 해 routine capex는
`PaymentsForCapitalImprovements` 태그의 $18.347M이 맞다 - 이 태그로 2020~2025년
전체가 일관되게 커버된다. **MCK 사례(넓은 정의 채택이 옳았음)와 정반대로, 이번엔
"넓은" 태그가 오분류된 사례**라 METRIC_TAGS 우선순위는 바꾸지 않는다(단일
관측치로 근거 없는 재조정을 하지 않는다는 이 프로젝트의 반복 원칙) - 다음에
비슷한 사례가 또 나오면(n=2) 태그 우선순위 재검토를 고려할 것.

`watchlist.json`·`reports/research_queue.json` 어느 쪽도 건드리지 않았다 -
LNTH는 QUEUED 상태로 남고, 다음 주 스크리닝에서도 다시 상위권에 뜰 수 있다
(이 프로젝트에 "조사했지만 제외" 상태를 기록하는 필드가 아직 없다 - 필요성이
더 쌓이면 추가를 검토할 것, 지금은 이 CLAUDE.md 기록이 유일한 근거다).

## EQT 제외 — FRAMEWORK_MISMATCH(M&A 단계상승 + 원자재 가격 사이클 이중고)
(2026-09-01)

큐 3순위(스크린 Gap 추정 +21.40%p, A등급). SEC XBRL 실측: 매출이
2018년 $4.71B -> 2020년 $2.65B -> 2022년 $12.11B -> 2023년 $5.04B로
극심하게 요동친다(천연가스 E&P, 원자재 가격 사이클 - 2018~2021년 영업손실
4개년 연속, 2022년 유럽 에너지위기로 영업이익 +$2.72B 급등). AA/NRG와
동일한 "자본집약 원자재 업종" 유형.

**여기에 M&A 단계상승까지 겹친다** - **2024-07-22 Equitrans Midstream
인수 완료**(올스톡 $5.5B, 회사 스스로 "변혁적 인수"라 명명, 파이프라인·
가스처리 자산을 E&P에 수직통합). GEN/BRO/ROP/SNPS/NRG/RBA/NOW/AVGO/CROX와
동일한 'M&A가 CAGR 구간에 걸림' 패턴이 원자재 사이클과 동시에 발생 - 5y
구간(2020->2025)은 가격저점(2020)에서 시작해 인수 이후(2025)로 끝나고,
3y 구간(2022->2025)은 가격고점(2022)에서 시작해 역시 인수 이후로 끝나
어느 창으로도 깨끗한 CAGR을 얻을 수 없다. 세그먼트 분리(E&P vs 미드스트림)
데이터 없이는 재무데이터를 더 긁지 않고 FRAMEWORK_MISMATCH로 분류했다.

## SIGI 정식 분석 — 세 번째 예외 세트 확장, 보험업 FCF-DCF 왜곡의 극단 사례
(2026-09-01/02)

큐 3순위(LNTH·EQT는 각각 진행중 인수합병·M&A+원자재 사이클로
FRAMEWORK_MISMATCH). Selective Insurance Group(SIGI, 손해보험) 정식분석
결과: **Gap +23.67%p, S등급**(Realistic Growth 12.00%[stalwart 캡 바인딩],
Implied Growth **-11.67%**, RAR +1.8245, DRS 43.32, Confidence 89).

### ⚠️ 데이터 함정 - "회계연도 라벨 충돌"의 새 변종(Q4단독 vs 연차)

SEC 자동 fetch가 `operating_income`(세전이익, PGR/ACGL 선례대로 영업이익
대용)을 통째로 `[미확보]` 처리했다 - **안전한 실패**였다. 원인: 2016~2020년
`IncomeLossFromContinuingOperationsBeforeIncomeTaxes...` 태그에 같은
회계연도로 두 값이 동시 존재(예: 2016년 219,955,000 vs 50,326,000) - 큰
쪽은 `start=1월1일`(정상 12개월), 작은 쪽은 `start=10월1일`(4분기 단독
3개월)이었다. CDNS/GEN의 "회계연도 라벨이 한 해 밀림"과는 다른 함정
(52/53주 문제가 아니라 분기-연차 혼재)이라 별도 기록해둔다. 자동 피커가
조용히 틀린 값을 고르지 않고 정직하게 실패를 반환한 것도 확인 - 설계대로
작동함.

### ⭐ 핵심 발견 - 보험업 FCF-DCF 왜곡이 이번엔 부호까지 뒤집었다

v3.13(ACGL) 노트가 "Gap+31.44%p는 FCF-DCF를 보험업에 적용한 데서 오는
과장일 가능성"이라 추정했던 것을, SIGI가 **더 극단적으로** 보여준다 - FCF
수익률(fcf0 $1.19B / 시총 $5.43B ≈ 22%)이 할인율(r=11.05%)의 **2배**에
달해, 역DCF가 이를 정당화하려면 Implied Growth가 **음수(-11.67%)**여야
했다. 성장중인 기업의 내재성장률이 음수로 나오는 것은 경제적으로
비정상적인 신호이고, 원인은 원자료 오류가 아니라 **보험업 OCF가 플로트
(보험료 선수취) 성장으로 구조적으로 부풀려진다**는 이미 알려진 문제
(v3.22)가 이번 사례에서 특히 두드러진 것으로 판단된다.

`insurer_cross_check`(is_insurer=True)가 그 완충 역할을 확인해준다 -
지속가능성장률(ROE 10.87% × 유보율 75.8% = **8.24%**)이 Realistic
Growth(12.00%, stalwart 캡)와 3.76%p 이내로 근접해 **성장 추정 자체는
합리적**임을 보여준다. 즉 이번 사례의 극단적인 Gap은 성장 과대추정이
아니라 **밸류에이션 쪽(FCF-DCF의 분모)의 구조적 왜곡**에서 온다는 것이
분리 확인됐다 - PGR·ACGL과 동일한 진단 패턴이 세 번째로 재현됨.

**공식 판정은 그대로 두었다**(is_insurer/sbc_cross_check와 동일 "병기,
자동판정 안 함" 원칙) - 다만 이 종목은 셋 중 가장 왜곡이 커 Implied
Growth 부호 자체가 뒤집혔다는 점에서, 향후 매수리스트 편입을 검토할
경우 이 캐비어트를 반드시 최우선으로 확인할 것.

### 경쟁구도(2026-09 WebSearch)

Travelers(합산비율 84%대, 업계 최고 수준 규율) > Cincinnati Financial(SIGI와
유사하게 98%대로 악화 중) > SIGI 자신(Q2 2026 합산비율 98%, 순보험료
전년比 -5% 역성장 - 경쟁압력+의도적 언더라이팅 조정이 실측으로 확인됨).

### 배선

`watchlist.json`에 SIGI 추가(35->36). 세 번째 "알려진 예외" 세트 확장
(BSX 거짓탈락 → TCOM 통화라벨 → CROX → **SIGI**로 이어지는 패턴):
`test_monitor_state.py`(n_ledgers 35→36), `test_provenance.py`
(`KNOWN_PROVENANCE_RECORDED_LEDGERS`에 SIGI 추가), `test_sbc_harvest.py`
(`KNOWN_POST_SNAPSHOT_LEDGERS`에 SIGI 추가) - CROX 때 이미 만든 예외 등록
패턴이 두 번째 신규 ledger에도 그대로 재사용 가능함을 확인했다(1회성
땜질이 아니라 실제로 재사용되는 설계였음).

baseline 36종목으로 재동결(fingerprint `81a8a0f5…`→`af6fda7d…`). 테스트
1,080개 전부 통과. `ENGINE_VERSION` 무변경(v3.80 유지 - engine/ 코드
변경 없음, 데이터 배선만).

## OKTA 정식 분석 — 스크리너 추정치가 실적발표 급등을 놓친 사례
(2026-09-02)

연구 우선순위 큐 다음 순위(LNTH/EQT는 이미 FRAMEWORK_MISMATCH로 제외 -
큐 자체에는 "제외" 상태 필드가 아직 없어 계속 상위에 남아있으므로 건너뜀)
인 OKTA를 정식분석했다(스크리너 추정 Gap +20.65%p, tier B, 시총 근사
~$16.4B).

### ⭐ 핵심 발견 — 스크리너 시총 근사치가 8개월 전 스냅샷이라 최근 +29%
실적 랠리를 완전히 놓쳤다

스크리너가 쓰는 `EntityPublicFloat`(2025-07-31 시점 $16.4B)는 이번에도
v3.72가 이미 명시한 한계(최대 낡을 수 있음)를 그대로 보여준 사례다.
2026-08-27 Okta가 FY2027 Q2 실적을 발표(매출 $805M 컨센서스 상회, 신제품이
전체 예약의 약 30%·ACV +40%, FY2027 가이던스 $3.216B~$3.226B로 상향) -
주가가 2026-08-26 $134.42 → 2026-08-27 $172.91로 **하루 만에 +28.6%**
급등했다(8개 대형 증권사가 목표가를 최대 $180까지 일제 상향). 분석 시점
(2026-09-02) 주가 $166.43 기준 실시간 시총(~$29.76B, 희석가중주식수
178.808M 적용)은 스크리너 추정치의 **1.8배**다.

### 결과 — "적정가/경계선"(C등급), Gap +2.80%p, 방향은 유지되나 크기는
스크리너의 1/7

| | 스크리너 추정 | 정식분석 |
|---|---|---|
| Gap | +20.65%p | **+2.80%p** |
| 등급 | B | **C** |
| 시총 | ~$16.4B(낡음) | $29.76B(실시간) |

**PTC 사례("분석시점 주가가 실적발표 후 +21% 랠리구간에 걸려있어 Gap이
stale일 가능성")와 같은 함정이지만, 이번엔 랠리가 정식분석 대상 시점
자체에 이미 반영돼 있어 그 함정을 자동으로 피했다** - 실시간 시총을 그대로
쓴 덕분에 판정이 급등 이후 수준으로 정확히 재조정됐다.

### 부수 발견 — 가이던스가 trailing CAGR보다 낮다(KEYS/KLAC와 반대 방향)

FY2026→FY2027 가이던스 중간값 성장률 +10.35%YoY인데 trailing 매출 CAGR은
3y 16.26%/5y 28.43%로 훨씬 높다 - trailing CAGR이 최근 감속을 과소반영하는
KEYS/KLAC 계열과 정확히 반대(여기서는 trailing이 가이던스보다 낙관적).
Realistic Growth(17.47%)가 가이던스보다 여전히 높게 나와 model_choice_reason·
falsification_conditions에 이 괴리를 명시적으로 기록했다(1개년 가이던스는
override 자격 없음 - v3.43 원칙).

### SBC 교차검증 — 판정이 뒤집힌다(WDAY와 같은 유형)

SBC/FCF **62.2%**(TTD·WDAY급) - SBC를 실제 비용으로 차감하면 Gap
+2.80%p→**-8.59%p**, 판정이 "적정가/경계선"→**"과대평가 가능성"**으로
뒤집힌다(WDAY가 이 경로로 실제 뒤집힌 유일한 선례였는데, OKTA가 두 번째
사례가 됐다). 공식 판정은 SBC 미차감 기준으로 유지(병기 원칙), 강건성점검도
모델선택(single_stage 8.17% vs two_stage 14.67%, 괴리 6.50%p) 축에서 flip
발생 - Confidence 79로 두 경고가 함께 반영됐다.

### 배선

`watchlist.json`에 OKTA 추가(36→37, MNST-PDD 사이). 네 번째 "알려진 예외"
세트 확장(BSX 거짓탈락 → TCOM 통화라벨 → CROX → SIGI → **OKTA**):
`test_monitor_state.py`(n_ledgers 36→37), `test_provenance.py`
(`KNOWN_PROVENANCE_RECORDED_LEDGERS`에 OKTA 추가), `test_sbc_harvest.py`
(`KNOWN_POST_SNAPSHOT_LEDGERS`에 OKTA 추가).

baseline 37종목으로 재동결(fingerprint `af6fda7d…`→`da16197b…`). 테스트
1,080개 전부 통과. `ENGINE_VERSION` 무변경(v3.80 유지 - engine/ 코드
변경 없음, 데이터 배선만).

## CDE 제외 — FRAMEWORK_MISMATCH(M&A 단계상승 + 귀금속 가격 사이클
이중고, EQT와 동일 유형) (2026-09-02)

연구 우선순위 큐 다음 순위 CDE(Coeur Mining, 은/금 광산업체, tier S, 스크리너
Gap 추정 +20.13%p)를 조사했다. SEC XBRL 매출 시계열에서 **2024→2025
+96.4%YoY**($1,054M→$2,070M)라는 극단적 단계상승을 확인 - WebSearch로
확인한 결과 2025-02-14 종결된 SilverCrest Metals 인수($1.58B, 전량주식
대가, 2억3,933만주 신주발행, 고품위 저비용 Las Chispas 은·금 광산 편입)가
원인이었다. 회사 자체 가이던스도 2025년 생산량이 금 +20%YoY·은 +62%YoY
증가할 것으로 명시 - 인수 효과가 그대로 매출 성장으로 잡힌다.

EQT와 동일한 이중고 구조다 - (1) M&A 단계상승이 5y CAGR 구간
(2020→2025) 종료연도에 걸려 있고, (2) 귀금속(은/금) 채굴업 자체가 상품
가격 사이클에 극심하게 노출돼(CLAUDE.md에 이미 기록된 AA(Alcoa)·NRG·
MP Materials급 "자본집약 원자재" 업종 문제) `demand_sensitivity_pct`·
`competitor_threat_weights` 같은 경쟁구도 기반 주관 입력 자체가 이
업종에는 부적합하다. 정량모델(FCF-DCF)로는 다룰 수 없다고 판단해
FRAMEWORK_MISMATCH로 분류했다 - ledger를 만들지 않았고 watchlist·
테스트·baseline 어느 것도 건드리지 않았다(LNTH/EQT와 동일한 축약
경로).

## MEDP 정식 분석 — BSX와 동일한 스크리너 거짓탈락 메커니즘의 두 번째
실사례 (2026-09-02)

큐 다음 순위 MEDP(Medpace Holdings, CRO/임상시험수탁기관, tier A, 스크리너
Gap 추정 +19.21%p, 시총 근사 ~$7.00B)를 정식분석했다.

### 발견 1 — 2018년 매출 태그 정의 전환(ASC 606) - M&A 아님

2017→2018 매출 +61.5% 급증을 provider가 `[태그 혼재]` 경고로 자동 표시.
WebSearch로 확인: Medpace가 2018-01-01자로 ASC 606을 전면 도입하며 총액
표시(gross) 방식으로 전환됐다(회사 자체 2018 가이던스는 "2017 순용역매출
$386.5M 대비 19.3~22.4% 성장"). CROX/BSX가 겪은 것과 같은 계열의 정의불연속
이지만, **3y(2022→2025)/5y(2020→2025) CAGR 창이 이 경계를 건드리지 않아
실제 채택되는 성장률은 오염되지 않는다** - 10y 창(가중치 0.2)만 영향을
받아 `cagr_base_year_override`는 쓰지 않았다.

### 결과 — "저평가 가능성"(A등급), Gap +9.80%p, Confidence 94, 강건성점검
flip 없음, SBC 교차검증도 flip 없음(SBC/FCF 5.1%로 매우 낮음)

부채 전액 상환(FY2019 이후 $0), 순현금 -$497M, 공격적 자사주매입(발행주식
2024→2026 30.76M→28.38M). PIT_VALID. 실시간 시총($16.55B)이 스크리너의
`EntityPublicFloat` 스냅샷(2025-06-30, $7.00B)보다 14개월 이상 최신이라
크게 다르다 - OKTA와 같은 계열의 "스크리너 근사치 시차" 사례이지만 원인은
실적급등이 아니라 순수 float 스냅샷 노후화.

### ⭐ 발견 2 — BSX와 완전히 동일한 스크리너 거짓탈락 메커니즘, 두 번째
실사례

정식분석(DRS 23.0, competition_intensity=4.2)과 달리 `screen()`은 MEDP를
탈락시킨다(`estimate_drs()`의 상수 competition_intensity=12.0 가정 때문에
DRS가 34.6까지 과대평가). BSX(RMD와 동일하게 5.4)에 이은 두 번째로,
"median 대체 방식이 median에서 크게 벗어난 개별종목에서는 구조적으로
오분류한다"(screener.py 문서화된 한계)는 진단이 재확인됐다. 상수 자체는
조정하지 않는다(median 12.0 여전히 정확) - `tests/test_screener.py`의
`KNOWN_SCREENER_FALSE_REJECTIONS`에 MEDP 추가 + BSX와 동일한 회귀 테스트
(`test_medp_false_rejection_is_still_reproducible`) 신설.

### 배선

`watchlist.json`에 MEDP 추가(37→38, MCK-MNDY 사이). 다섯 번째 "알려진
예외" 세트 확장(BSX 거짓탈락 → TCOM 통화라벨 → CROX → SIGI → OKTA →
**MEDP**, 이번엔 처음으로 **screener 거짓탈락 계열**과 **provenance/
sbc_harvest/monitor_state 신규 ledger 계열** 두 갈래가 동시에 갱신됨):
`test_monitor_state.py`(n_ledgers 37→38), `test_provenance.py`
(`KNOWN_PROVENANCE_RECORDED_LEDGERS`에 MEDP 추가), `test_sbc_harvest.py`
(`KNOWN_POST_SNAPSHOT_LEDGERS`에 MEDP 추가), `test_screener.py`
(`KNOWN_SCREENER_FALSE_REJECTIONS`에 MEDP 추가 + 회귀 테스트 신설).

baseline 38종목으로 재동결(fingerprint `da16197b…`→`52bf1b90…`). 테스트
1,081개 전부 통과. `ENGINE_VERSION` 무변경(v3.80 유지 - engine/ 코드
변경 없음, 데이터 배선만).

## RYAN 정식 분석 — Up-C 이중클래스 구조의 시가총액 함정 (2026-09-02)

큐 다음 순위 RYAN(Ryan Specialty Holdings, 특수보험 도매유통, tier A,
스크리너 Gap 추정 +18.83%p)을 정식분석했다.

### ⭐ 핵심 발견 — Class A 주식만으로 시총을 잡으면 FCF수익률이 2배로
왜곡된다

RYAN은 2021-07 IPO 시 전형적인 Up-C 구조로 상장했다 - 재무제표는 운영LLC
전체(100% 연결)를 담는데, 상장주식(Class A)은 지분의 일부일 뿐이고 나머지는
사전IPO 소유주가 LLC Unit(Class A와 1:1 교환가능, 경제적 지분 동일)+
Class B 무경제권 의결주 형태로 보유한다. **Class A만으로 시총을 잡으면
분자(전체 FCF)와 분모(부분 지분가치)가 어긋나 FCF수익률이 실제의 약
2배로 부풀고 내재성장률이 허위로 낮게 나온다.** 2026-07-27 10-Q 표지
(WebSearch 재인용): Class A 122,077,702주 + Class B 133,737,083주 = 총
경제적 지분 255,814,785주 - **총주식수 기준 시총(~$10.83B)**을 채택했다
(Class A만 쓰면 ~$5.43B로 실제로 일부 데이터소스가 이렇게 잘못 보고하고
있음을 확인).

### 결과 — "저평가 가능성"(A등급), Gap +13.77%p, Confidence 94, 강건성점검·
SBC 교차검증 모두 flip 없음

DRS 36.56(leverage 20.0 - 최고위험, net_debt/EBITDA ≈4.08x로 실제로 높은
레버리지가 정확히 반영됨). PIT_VALID. 모델괴리 0.84%p(경고 임계값 3%p 미만,
양호한 일치).

### 부수 발견 1 — 2021년 capex $343.2M은 M&A 관련 일회성 항목(계산에는
영향 없음)

All Risk 인수(2021, IPO와 동시 진행) 관련 지급으로 추정되나
`PaymentsToAcquireBusinessesNetOfCashAcquired` 태그가 비어있어 확정은
못했다. LNTH IPR&D 사례와 같은 계열의 태그 오분류 의심이나, `capex_years`
경로는 opt-in(`capex_classification`)에서만 호출되고 fcf0는 2025년 정상
capex($3.0M)만 쓰므로 **계산에는 전혀 영향 없음** - 원자료 그대로 사용.

### 부수 발견 2 — 오가닉성장 가이던스(5~7%)가 trailing CAGR(20.5%/24.1%)
보다 훨씬 낮다 - GEN/BRO/TCOM 계열이나 override 기준 미달

Q2 2026 오가닉 +6.7%(총성장 +7.2%와 근접), FY2026 가이던스 5~7%. ROP가
확립한 override 기준(다년 실현실적 필요)에 못 미쳐(1개 분기+1개년
가이던스뿐) `realistic_growth_override`는 쓰지 않고 괴리만
falsification_conditions에 병기했다.

### 배선

`watchlist.json`에 RYAN 추가(38→39, ROP-SE 사이). 여섯 번째 "알려진
예외" 세트 확장: `test_monitor_state.py`(n_ledgers 38→39),
`test_provenance.py`(`KNOWN_PROVENANCE_RECORDED_LEDGERS`에 RYAN 추가),
`test_sbc_harvest.py`(`KNOWN_POST_SNAPSHOT_LEDGERS`에 RYAN 추가) - 이번엔
screener 거짓탈락(BSX/MEDP 유형)에는 해당하지 않아 `test_screener.py`는
무변경.

baseline 39종목으로 재동결(fingerprint `52bf1b90…`→`5c5af7e4…`). 테스트
1,081개 전부 통과. `ENGINE_VERSION` 무변경(v3.80 유지 - engine/ 코드
변경 없음, 데이터 배선만).

## FIX 정식 분석 — AMD/AMAT급 "이미 비쌈" 신호가 실측으로 재확인됨,
규모조건부 상한 대상이 4번째로 확장 (2026-09-02)

큐 다음 순위 FIX(Comfort Systems USA, 데이터센터 MEP 시공업체, tier B,
스크리너 Gap 추정 +18.51%p, 시총 근사 ~$18.64B)를 정식분석했다.

### ⭐ 핵심 발견 — 스크리너 근사치가 AI 데이터센터 붐발 +1,240%(3년) 랠리를
거의 놓쳤다

2022년 이후 데이터센터·첨단기술向 수요 폭발로 매출성장률이 다년 가속됐다
(YoY 2022 +34.7%→2025 +29.5%, 단일연도 단계상승이 아닌 진짜 다년 가속 -
M&A 왜곡과 다른 유형). Q1 2026 매출 +56%YoY·EPS 2배 이상, 백로그 $12.5B
(2026년 예상 연매출과 맞먹음). 주가는 3년간 +1,240%, 2026 YTD만 +116% -
실시간 시총(~$54.67B)이 스크리너 근사치의 2.9배다.

### 결과 — "적정가/경계선"(C등급), Gap +4.22%p, 외부 애널리스트의 밸류에이션
경고("43x 2026 EPS, 업종중앙값 2배 이상, 이미 낙관 선반영")가 정량적으로
재확인됨 - AMD(FCF수익률 0.79%)·AMAT(1.30%)에 이은 세 번째 "펀더멘털은
훌륭한데 이미 비쌈"(4분류 2번) 사례.

Lynch fast_grower 상한(25%)과 v3.67 규모조건부 상한(시총 $54.67B, base_rates
"4500-7000" 구간 - 명목 23.00%로 추가 하향)이 **함께** 바인딩 - 원시 CAGR
(3y 30.03%/5y 26.08%)을 그대로 신뢰하지 않는 것이 정확했음이 base_rates
외부실증으로 뒷받침됨. 모델괴리 10.00%p(경고 임계값의 3배 이상, 강건성점검
flip=True) - single_stage(8.78%)와 two_stage(18.78%)가 크게 갈려
Confidence 74로 반영. SBC 교차검증은 flip 없음(SBC/FCF 2.1%, 낮음).

### ⭐ 부수 — 규모조건부 상한 적용 대상이 v3.67 승인범위(PDD·PGR·SE 3종목)
밖에서 처음 발동

`size_conditioned_growth_cap()`은 opt-in이 아니라 fast_grower 유형+대형
시총이면 상시 평가되는 메커니즘이라, v3.67 이후 신규 분석된 티커가 새로
걸리는 것은 정상 동작이다. `tests/test_pipeline.py`의
`test_approved_three_tickers_reproduce_and_others_unchanged`가 "PDD·PGR·SE
정확히 3종목"만 검증하는 스냅샷 방식이라 FIX 추가로 실패했고,
`APPROVED_SIZE_CAPPED_TICKERS`(회귀 감시, 원 3종목 반드시 유지) +
`KNOWN_ADDITIONAL_SIZE_CAPPED_TICKERS`(신규 등록, FIX 추가)로 이원화해
"승인범위 이탈"과 "정당한 신규 발동"을 구분하도록 고쳤다 - BSX 거짓탈락과
동일한 "알려진 확장" 등록 패턴.

### 배선

`watchlist.json`에 FIX 추가(39→40, DUOL-GEN 사이). 일곱 번째 "알려진
예외" 세트 확장 + 규모조건부 상한 등록 확장: `test_monitor_state.py`
(n_ledgers 39→40), `test_provenance.py`(`KNOWN_PROVENANCE_RECORDED_
LEDGERS`에 FIX 추가), `test_sbc_harvest.py`(`KNOWN_POST_SNAPSHOT_
LEDGERS`에 FIX 추가), `test_pipeline.py`(위 이원화 + FIX 등록).

baseline 40종목으로 재동결(fingerprint `5c5af7e4…`→`c3557660…`). 테스트
1,081개 전부 통과. `ENGINE_VERSION` 무변경(v3.80 유지 - engine/ 코드
변경 없음, 데이터 배선만).

## CHDN 제외 — FRAMEWORK_MISMATCH(COVID 저점 기저효과 + M&A 단계상승
이중고, BKNG와 GEN의 결합형) (2026-09-02)

큐 다음 순위 CHDN(Churchill Downs, 경마·카지노, tier A, 스크리너 Gap
추정 +18.04%p)을 조사했다. SEC XBRL 매출 시계열(2016~2025, 2011~2015는
0으로 잡히는 데이터 결측 확인)에서 두 가지 왜곡이 동시에 발견됐다:

1. **COVID 저점 기저효과(BKNG 유형)** - 5y CAGR의 기본 기준연도(years[-6]
   =2020)가 COVID로 매출이 -20.7%YoY 급감한 저점이다. 기준연도를 코로나
   이전 고점(2019)으로 바꾸면 5y(6년으로 재계산) CAGR이 22.65%->14.05%로
   8.6%p 줄어든다 - BKNG(v3.21)가 확립한 정확히 같은 함정.
2. **M&A 단계상승이 override로도 못 피해진다(GEN/ROP 유형의 강화판)** -
   2022-11 Peninsula Pacific Entertainment 인수($2.75B, 버지니아·뉴욕·
   아이오와 카지노 자산)가 종결되며 2023년 매출이 +36.0%YoY 급증했다.
   문제는 이 왜곡이 **3y CAGR 창(2022->2025)의 시작연도 자체**에 걸려
   있어(`rev_cagr_3y`는 `years[-4]`를 항상 쓰므로 `cagr_base_year_
   override`로 못 피한다) override 하나로는 두 왜곡 중 하나만 해소된다.

WebSearch(2026-09-02)로 확인: CHDN은 2026년에도 United Tote 인수를
진행하는 등 **상시적 볼트온 M&A 전략**을 쓰고 있어 회사 자체 가이던스도
동일점포(same-store)/오가닉 성장 지표를 별도 공시하지 않는다(Q2 2026
연결매출 +4.9%YoY만 확인 - trailing CAGR 17~22%대와 크게 괴리되나 이를
분해할 공시 자료가 없음). GEN/BRO/ROP가 요구했던 "세그먼트 공시 대조"
자체가 불가능한 상태라 정량모델에 넣지 않고 FRAMEWORK_MISMATCH로
분류했다 - ledger를 만들지 않았고 watchlist·테스트·baseline 어느 것도
건드리지 않았다(LNTH/EQT/CDE와 동일한 축약 경로).

## VICI·COP·DINO·EOG 제외 — 4종목 일괄 (2026-09-02)

**VICI(VICI Properties) - REIT 구조적 부적합(신규 유형).** 카지노
부동산을 트리플넷리스로 임대하는 REIT다. `OperatingIncomeLoss` 태그가
FY2020 이후 회사 스스로 공시를 중단(REIT 특유의 손익계산서 표시방식
변경 - PGR/ACGL/SIGI가 겪은 보험업 패턴과 유사하나 대체 프록시가
검증된 적이 없다). 더 근본적으로 **REIT capex가 거의 0(연 $1~8M, OCF
$2.5B 대비)** - 임차인(카지노 운영사)이 유지보수를 부담하는 트리플넷
구조 때문이지 자본효율이 좋아서가 아니며, `FCF=OCF-capex`가 실제 배분
가능 현금을 측정하지 못한다. REIT은 법적으로 과세소득의 90%+를
배당해야 해 성장이 유보FCF 재투자가 아니라 **신규 자본조달(증자·기채)로
이뤄지는 자산군**이라 이 엔진의 핵심 가정(FCF 재투자를 통한 성장) 자체가
안 맞는다. 업계 표준(P/FFO·AFFO·배당수익률)과 이 엔진(FCF-DCF)이
근본적으로 다른 질문에 답한다 - `is_insurer`처럼 REIT 전용 경로를
만들려면 실증사례가 더 필요하다(Simplicity First).

**COP·DINO·EOG - 자본집약 원자재(석유/가스) 이중고, AA/NRG/MP/EQT/CDE와
동일 유형.** SEC XBRL 매출 실측으로 극심한 가격사이클을 확인:
- COP(ConocoPhillips): 2014 $55.5B->2015 $30.9B(-44%), 2020 $18.8B->2021
  $45.8B(+144%).
- DINO(HF Sinclair, 정유): 2020 $11.2B->2022 $38.2B(+242%)->2025 $26.9B(-30%,
  고점대비).
- EOG(EOG Resources): 2008/2014/2019 고점·2009/2015/2020 저점의 전형적
  boom/bust 패턴(예: 2014 $18.0B->2015 $8.8B, -51%).

셋 다 `demand_sensitivity_pct`·`competitor_threat_weights` 같은 경쟁구도
기반 주관 입력이 원자재 가격사이클 앞에서 무의미해지는 업종 - 정량모델에
넣지 않고 FRAMEWORK_MISMATCH로 분류했다. 4종목 전부 ledger를 만들지
않았고 watchlist·테스트·baseline 어느 것도 건드리지 않았다.
